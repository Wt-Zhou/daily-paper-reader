---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 基于可微世界模型的离线强化学习模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 用于离线强化学习的可微世界模型
tldr: 本研究针对离线强化学习在推理阶段缺乏灵活性的问题，提出了一种结合可微世界模型（DWM）与模型预测控制（MPC）的推理时自适应框架。该方法不仅利用预训练策略和学习到的动力学模型，还通过在推理时对想象的轨迹进行端到端梯度计算，实时优化策略参数。在D4RL基准测试中的实验证明，这种推理时优化机制能显著提升MuJoCo和AntMaze任务的性能，为离线RL提供了一种高效的策略改进方案。
selection_source: fresh_fetch
motivation: 传统的离线强化学习方法在训练完成后，推理阶段通常直接应用固定策略而无法根据当前状态进行实时优化。
method: 提出一种可微世界模型（DWM）流水线，利用模型预测控制（MPC）在推理时通过想象回放进行端到端梯度计算，从而动态调整策略参数。
result: 在D4RL连续控制基准测试（包括MuJoCo和AntMaze）中，该方法相比现有的强力离线强化学习基准展现出一致的性能提升。
conclusion: 通过在推理阶段利用环境动力学模型实时优化策略参数，可以有效弥补离线训练的局限性并增强智能体的表现。
---

## 摘要
离线强化学习（RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法通常训练一个离线策略（或价值函数），并在推理阶段直接应用，而不进行进一步的细化。我们引入了一种受模型预测控制（MPC）启发的推理阶段自适应框架，该框架利用预训练策略以及学习到的状态转移和奖励的世界模型。虽然现有的世界模型和扩散规划方法在训练期间利用学习到的动力学生成想象轨迹，或在推理阶段采样候选规划，但它们并未利用推理阶段的信息来实时优化策略参数。相比之下，我们设计了一个可微世界模型（DWM）流水线，能够通过想象展开进行端到端的梯度计算，从而在推理阶段基于 MPC 进行策略优化。我们在 D4RL 连续控制基准（MuJoCo 运动任务和 AntMaze）上评估了我们的算法，结果表明，利用推理阶段的信息来优化策略参数，相比强力的离线强化学习基准方法，能够带来持续的性能提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
离线强化学习（Offline RL）的核心挑战在于如何从静态数据集中学习最优策略，而无法进行环境交互。现有的主流方法（如 CQL, IQL, ReBRAC）通常在训练完成后得到一个固定的策略，推理时仅根据当前状态输出动作，不再进行任何调整。
**研究动机：** 作者受到大语言模型（LLM）推理时计算（Inference-time computation，如思维链、搜索）能提升性能的启发，提出：在离线 RL 中，是否也可以通过推理时的额外计算来优化策略？由于离线数据中状态转移和奖励是局部信息，比长程的 Q 值估计更易准确建模，因此利用“世界模型”在推理时进行策略微调具有巨大潜力。

### 2. 方法论：核心思想与技术细节
论文提出了一种名为 **MPCwDWM** 的框架，其核心是将**模型预测控制（MPC）**与**可微世界模型（DWM）**相结合。

*   **核心思想：** 在推理阶段，智能体不直接执行预训练策略的动作，而是先在“头脑”中利用学习到的世界模型进行多步“想象”回放（Rollout），计算预期的累积奖励，并通过反向传播梯度实时更新策略参数，最后执行优化后的动作。
*   **关键组件：**
    1.  **可微扩散采样器 ($f_\theta$)：** 使用条件扩散模型建模状态转移 $P(s'|s, a)$。通过将扩散过程表示为确定性计算图，使得梯度可以穿透采样过程进行回传。
    2.  **奖励模型 ($r_\xi$)：** 一个通过监督学习训练的回归模型，预测状态-动作对的即时奖励。
    3.  **预训练策略与终端价值 ($Q_\phi$)：** 使用 ReBRAC 算法预训练策略 $\pi_\psi$ 和 Critic $Q_\phi$，其中 $Q_\phi$ 用于估计想象轨迹末端的长期价值。
*   **算法流程：**
    *   在每个真实环境步 $t$，从当前状态 $s_t$ 出发，利用 $f_\theta$ 生成 $M$ 条长度为 $H$ 的想象轨迹。
    *   构建目标函数：$H$ 步内的折扣奖励总和 + 第 $H$ 步的终端 Q 值。
    *   **梯度更新：** 利用链式法则（定理 4.1），计算目标函数对策略参数 $\psi$ 的梯度，进行 $E$ 步梯度上升更新 $\psi$。
    *   执行更新后策略输出的动作 $a_t$。

### 3. 实验设计
*   **数据集/场景：** 使用了标准的 **D4RL** 连续控制基准测试。
    *   **Gym MuJoCo：** 包含 HalfCheetah, Hopper, Walker2d 的 18 个子任务（涵盖 random, medium, expert 等不同数据质量）。
    *   **AntMaze：** 包含 6 个更具挑战性的迷宫任务（umaze, medium, large），侧重于长程规划。
*   **对比基准（Baselines）：**
    *   **传统离线 RL：** TD3+BC, IQL, CQL, SAC-RND, ReBRAC。
    *   **生成式/模型化方法：** Decision Transformer (DT), Trajectory Transformer (TT), MOPO, MOReL, MBOP, Diffuser。

### 4. 资源与算力
*   **算力说明：** 论文**未明确指出**具体的 GPU 型号、数量或具体的训练/推理耗时。
*   **推测：** 由于该方法在推理时需要进行多次扩散模型的反向传播（Backpropagation through time），其推理阶段的计算开销显著高于传统的单次前向传播策略。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 24 个不同的 D4RL 任务。
*   **重复性与客观性：** 每个任务均使用了 **10 个不同的随机种子**进行评估，并报告了均值和标准差。
*   **充分性：** 实验不仅对比了最先进的无模型（Model-free）方法，还对比了多种基于生成模型和模型化（Model-based）的方法，涵盖了从简单运动到复杂规划的任务，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能提升：** MPCwDWM 在 18 个 MuJoCo 任务中的 12 个表现优于强基准，平均得分（85.33）超过了 SAC-RND 和 ReBRAC。
*   **复杂任务优势：** 在 AntMaze 任务中提升尤为显著（平均 85.07 vs ReBRAC 77.62），特别是在最难的 `large-diverse` 任务中，得分从 51 提升至 66。
*   **推理优化的有效性：** 实验证明，即使是已经训练良好的策略，通过推理时的梯度优化，依然能进一步挖掘性能空间。
*   **超越扩散规划：** 该方法显著优于同样基于扩散模型的 Diffuser 算法，证明了“优化策略参数”比单纯的“轨迹采样”更有效。

### 7. 优点
*   **端到端可微性：** 成功将复杂的扩散模型转化为可微组件，实现了穿透动力学模型的策略优化。
*   **推理时自适应：** 弥补了离线 RL 无法在部署时根据具体状态进行自我修正的缺陷。
*   **模块化强：** 可以作为一种通用的“插件”，在任何预训练的离线策略基础上进行二次提升。

### 8. 不足与局限
*   **计算开销：** 推理时的梯度计算和多步扩散采样会导致显著的延迟，可能难以应用于对实时性要求极高的场景（如高速机器人控制）。
*   **模型依赖：** 性能高度依赖于世界模型（扩散采样器和奖励模型）的准确性。如果世界模型在某些区域存在偏差，推理优化可能会导致策略“误入歧途”。
*   **超参数敏感：** 推理时的优化步数 $E$、视野长度 $H$ 和粒子数 $M$ 等参数需要精细调优，以平衡性能与计算成本。

（完）
