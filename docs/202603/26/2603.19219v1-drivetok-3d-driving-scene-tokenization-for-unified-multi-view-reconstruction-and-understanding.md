---
title: "DriveTok: 3D Driving Scene Tokenization for Unified Multi-View Reconstruction and Understanding"
title_zh: DriveTok：用于统一多视角重建与理解的 3D 驾驶场景 Token 化
authors: "Dong Zhuo, Wenzhao Zheng, Sicheng Zuo, Siming Yan, Lu Hou, Jie Zhou, Jiwen Lu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19219v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于世界模型的3D驾驶场景标记化
tldr: 针对自动驾驶中多视图图像分词效率低且视图不一致的问题，本文提出 DriveTok。该方法利用 3D 可变形交叉注意力机制将多视图视觉特征转化为统一的 3D 场景 Token，并通过多任务解码器实现 RGB、深度、语义及 3D 占用预测的联合重建。实验证明 DriveTok 在 nuScenes 数据集上表现优异，集成了语义、几何和纹理信息，为自动驾驶世界模型提供了高效的视觉接口。
selection_source: fresh_fetch
motivation: 现有的 2D 单目分词器在处理高分辨率多视图驾驶场景时存在效率低下和视图间不一致的问题。
method: 提出一种基于 3D 可变形交叉注意力的分词器，通过多头解码器将场景 Token 映射为多维度的重建结果和 3D 语义占用。
result: 在 nuScenes 数据集上的实验表明，DriveTok 在图像重建、语义分割、深度预测和 3D 占用预测等任务中均取得了良好表现。
conclusion: DriveTok 成功实现了集语义、几何和纹理信息于一体的统一 3D 场景分词，为多视图驾驶场景的理解与重建提供了有效方案。
---

## 摘要
随着视觉-语言-动作模型和世界模型在自动驾驶系统中的日益普及，可扩展的图像 Token 化作为视觉模态的接口变得至关重要。然而，大多数现有的 Token 化器是针对单目和 2D 场景设计的，在应用于高分辨率多视角驾驶场景时，会导致效率低下和视角间的不一致性。为了解决这一问题，我们提出了 DriveTok，这是一种用于统一多视角重建与理解的高效 3D 驾驶场景 Token 化器。DriveTok 首先从视觉基础模型中获取语义丰富的视觉特征，然后通过 3D 可变形交叉注意力机制将其转换为场景 Token。在解码方面，我们采用多视角 Transformer 从场景 Token 中重建多视角特征，并使用多个头部来获取 RGB、深度和语义重建结果。我们还在场景 Token 上直接添加了一个 3D 头部，用于 3D 语义占用预测，以获得更好的空间感知能力。通过多个训练目标，DriveTok 学习到了整合语义、几何和纹理信息的统一场景 Token，从而实现高效的多视角 Token 化。在广泛使用的 nuScenes 数据集上进行的广泛实验表明，DriveTok 生成的场景 Token 在图像重建、语义分割、深度预测和 3D 占用预测任务中表现出色。

## Abstract
With the growing adoption of vision-language-action models and world models in autonomous driving systems, scalable image tokenization becomes crucial as the interface for the visual modality. However, most existing tokenizers are designed for monocular and 2D scenes, leading to inefficiency and inter-view inconsistency when applied to high-resolution multi-view driving scenes. To address this, we propose DriveTok, an efficient 3D driving scene tokenizer for unified multi-view reconstruction and understanding. DriveTok first obtains semantically rich visual features from vision foundation models and then transforms them into the scene tokens with 3D deformable cross-attention. For decoding, we employ a multi-view transformer to reconstruct multi-view features from the scene tokens and use multiple heads to obtain RGB, depth, and semantic reconstructions. We also add a 3D head directly on the scene tokens for 3D semantic occupancy prediction for better spatial awareness. With the multiple training objectives, DriveTok learns unified scene tokens that integrate semantic, geometric, and textural information for efficient multi-view tokenization. Extensive experiments on the widely used nuScenes dataset demonstrate that the scene tokens from DriveTok perform well on image reconstruction, semantic segmentation, depth prediction, and 3D occupancy prediction tasks.

---

## 论文详细总结（自动生成）

### 论文总结：DriveTok - 用于统一多视角重建与理解的 3D 驾驶场景 Token 化

#### 1. 核心问题与研究动机
随着自动驾驶从“感知中心”转向“推理中心”（如 VLA 模型和世界模型），如何高效地将传感器输入转化为既包含低级纹理信息（用于重建）又包含高级语义信息（用于理解）的表示成为了关键。
*   **现有问题**：传统的图像分词器（Tokenizer）多为 2D 单目设计，直接应用于多相机系统时，会导致视角间不一致、Token 数量随分辨率和相机数激增（效率低），且缺乏驾驶场景必需的 3D 空间感知能力。
*   **研究目标**：提出一种高效的 3D 场景分词器，将多视角高分辨率输入压缩为固定数量、空间一致且语义丰富的 3D 场景 Token。

#### 2. 方法论
DriveTok 采用编码器-解码器架构，核心思想是将多视角图像特征“提升”到统一的 3D 空间中。
*   **3D 场景编码器**：
    *   使用预训练的视觉基础模型（如 DINOv3）提取特征。
    *   利用 **3D 可变形交叉注意力机制**，根据相机内外参将 2D 特征采样并聚合到固定的 3D 场景网格（Scene Queries）中，生成与分辨率和相机数量无关的场景 Token。
*   **空间感知多视角解码器**：
    *   引入 **可见性引导的场景-视角注意力（Visibility-Guided Attention）**：通过预计算的可见性掩码，确保场景 Token 仅与物理上可见的相机视角 Token 进行交互，减少冗余计算并增强几何约束。
    *   使用 **Plücker 射线嵌入** 为视角 Token 提供相机感知的几何先验。
*   **多任务联合训练**：
    *   **图像重建**：预测 RGB 图像（结合 L1、LPIPS 和 GAN 损失）。
    *   **深度预测**：利用 MoGe-2 生成的稠密伪标签进行监督。
    *   **语义预测**：使用稀疏的 LiDARSeg 标签进行监督。
    *   **3D 占用预测**：直接从场景 Token 预测 3D 语义占用（Occupancy）。
    *   **语义正则化**：在潜空间对场景 Token 进行语义对齐，防止结构崩溃。

#### 3. 实验设计
*   **数据集**：在主流的 **nuScenes** 自动驾驶数据集上进行验证（包含 6 个环视相机）。
*   **对比任务与 Benchmark**：
    *   **图像重建**：对比 VQGAN、ViT-VQGAN、FlowMo、BEV-VAE。
    *   **深度预测**：对比单目（UniDepth, Metric3D）和多视角（SurroundDepth, SelfOcc）方法。
    *   **3D 占用预测**：对比 BEVFormer、TPVFormer、OccFormer、GaussianFormer 等 SOTA 方法。
*   **评估指标**：PSNR、SSIM（重建）；AbsRel、$\delta < 1.25$（深度）；mIoU、IoU（占用预测）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：训练时长约为 **400k 次迭代**。
*   **优化技术**：启用了 FlashAttention-2，采用 BFloat16 精度训练，以降低显存消耗并加速计算。
*   **推理效率**：场景分词模块耗时约 88ms，总推理耗时约 672ms（包含所有任务头）。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了四大类任务（重建、深度、语义、占用）的定量对比，并提供了大量的定性可视化结果（包括夜间场景、点云重建等）。
*   **消融实验**：针对“可见性引导注意力”的作用、不同任务组合对 Token 质量的影响、以及不同骨干网络（ResNet-101 vs DINOv3）的性能表现进行了详细消融。
*   **充分性评价**：实验设计较为全面，通过冻结骨干网络等公平设置与现有方法对比，验证了其作为“通用视觉接口”的有效性。

#### 6. 主要结论与发现
*   **统一性**：DriveTok 证明了单一的场景 Token 可以同时服务于高质量图像重建和高精度 3D 语义理解。
*   **几何增益**：引入可见性掩码和 3D 空间约束能显著提升 Token 的几何表达能力，避免模型仅通过纹理过拟合。
*   **多任务协同**：联合训练虽然在图像重建指标上略有下降，但显著增强了 Token 的空间感知和语义判别力，使其更适合作为世界模型的输入。

#### 7. 优点（亮点）
*   **架构解耦**：Token 数量固定，不随相机分辨率或数量变化，极大地提高了下游大模型的处理效率。
*   **空间一致性**：通过 3D 空间的统一表示，解决了多视角图像在重叠区域的特征矛盾问题。
*   **语义丰富**：结合了视觉基础模型的强语义和多任务监督的几何信息，生成的 Token 具有极高的信息密度。

#### 8. 不足与局限
*   **计算开销**：尽管推理高效，但训练过程涉及多任务解码和 3D 变换，对算力要求较高。
*   **伪标签依赖**：深度预测高度依赖于 MoGe-2 等模型生成的伪标签，其上限受限于伪标签的质量。
*   **网格分辨率限制**：固定的 3D 网格分辨率（如 128x128）可能在处理极远距离或极细小物体时存在信息丢失。
*   **时序缺失**：目前主要关注单帧场景的 Token 化，尚未显式引入长时序建模。

（完）
