---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v2"
tags: ["query:ad-ei"]
score: 9.0
evidence: 为自动驾驶分割可行的车辆轨迹
tldr: 本研究提出GHOST，一种利用大规模行车记录仪视频进行自监督学习的方法，旨在从单目图像中分割出可行的车辆行驶轨迹。通过SfM技术恢复相机轨迹并投影至地面生成伪标签，训练深度网络预测路径建议。该方法无需人工标注，能隐式捕捉道路布局和拓扑结构，在NuScenes等数据集上表现优异，并具备良好的跨平台迁移能力。
selection_source: fresh_fetch
motivation: 针对复杂城市环境下自动驾驶路径规划依赖人工标注且难以泛化的问题，探索利用海量无标注视频进行自监督学习。
method: 利用单目SfM恢复车辆运动轨迹并投影至地面生成空间掩码，以此作为伪标签训练深度分割网络，实现从单图预测多路径假设。
result: 在NuScenes数据集上实现了可靠的轨迹预测，并成功通过微调迁移至电动滑板车平台，证明了模型的泛化性。
conclusion: 大规模自我运动蒸馏能有效提取结构化路径信息，使模型在无需显式建模道路标识的情况下实现鲁棒的轨迹假设估计。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下的自动驾驶中，从单目图像中分割出可行的车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时能够根据单张 RGB 图像预测以运动为条件的路径建议，而无需对道路或车道线进行显式建模。该模型在多样化、无约束的网络数据上进行训练，隐式地捕捉了场景布局、车道拓扑和路口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步通过轻量级微调展示了其向电动滑板车平台的迁移能力。我们的结果表明，大规模本车运动蒸馏产生了超出演示轨迹的结构化且可泛化的路径建议，从而实现了通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GHOST** 的自监督学习框架，旨在通过海量无标注的行车记录仪视频，学习如何从单张图像中预测车辆的可行行驶轨迹。

以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的自动驾驶路径规划和可行驶区域分割高度依赖昂贵的人工标注、高精地图（HD Maps）或复杂的传感器校准。
*   **研究动机**：互联网上存在海量的行车记录仪视频，每一段视频都隐含了人类驾驶员在特定场景下的可行路径选择。论文试图通过“本车运动蒸馏”（Ego-motion Distillation）技术，将这些原始视频转化为自动驾驶的监督信号，使模型能够理解场景语义、车道拓扑和可能的运动假设，而无需任何手动标注。

### 2. 论文提出的方法论
该框架包含三个主要阶段：
*   **SfM 轨迹恢复**：使用 COLMAP（一种运动恢复结构工具）处理原始单目视频，提取相机的 6 自由度（6-DoF）位姿和稀疏 3D 点云。为了处理大规模数据，视频被切分为 20 分钟的片段进行并行处理。
*   **地面投影与掩码生成（关键技术）**：
    *   **高度估计**：由于单目 SfM 具有尺度不确定性，研究者利用 Theil-Sen 稳健回归算法，根据识别出的地面点云估计相机相对于地面的高度（$H_{cam}$）。
    *   **轨迹投影**：将未来 5 秒的相机轨迹投影到地面，并根据相机高度赋予一定的物理宽度，生成空间轨迹掩码（Ground Truth Mask）。
*   **分割模型训练**：
    *   **核心思想**：将轨迹预测转化为二值图像分割问题。使用带有 ResNet34 编码器的 U-Net 或 SegFormer 架构。
    *   **加权非对称损失函数**：为了让模型预测出“所有可能”的路径（而不仅仅是视频中走过的那一条），设计了一种非对称损失。该损失加大了对“漏报”（False Negative）的惩罚（约 9 倍于误报），鼓励模型覆盖所有潜在的可行驶区域，从而产生多路径假设。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：从 YouTube 收集的 30 小时多样化行车记录仪视频，涵盖 14 个城市、多种天气（雨、雪、晴）和光照条件，最终生成 65 万帧标注数据。
    *   **评估**：使用 **NuScenes**（OpenLane-V2 子集）进行零样本/跨域评估。
    *   **迁移实验**：在加州大学圣地亚哥分校（UCSD）校园内采集的 40 分钟电动滑板车视频（2.1 万帧）。
*   **Benchmark 与对比**：
    *   使用 **Soft IoU**（Ruzicka 相似度）作为主要评价指标。
    *   对比了 ResNet34 和 SegFormer 两种骨干网络。
    *   在滑板车实验中，对比了“从头训练”与“基于 GHOST 预训练后微调”的效果。

### 4. 资源与算力
*   **算力说明**：文中提到使用了 GPU 加速的 Ceres Solver 进行 SfM 优化。
*   **具体细节**：论文**未明确列出**具体的 GPU 型号（如 A100 或 RTX 4090）及具体的训练总时长。但提到了预训练进行了 25 个 Epoch，微调进行了 10 个 Epoch，且 SfM 过程采用了高度并行化的处理方式。

### 5. 实验数量与充分性
*   **实验规模**：使用了 65 万帧的大规模数据集进行训练，这在自监督轨迹学习领域属于较大规模。
*   **充分性**：
    *   **跨域验证**：在 NuScenes 上进行测试（训练集不含波士顿和新加坡数据），验证了模型的泛化能力。
    *   **多模态验证**：专门针对多车道场景进行了评估，证明模型不会坍缩到单一路径。
    *   **平台迁移**：从汽车迁移到电动滑板车，证明了方法在不同动力学特性车辆上的适用性。
*   **客观性**：实验考虑了左行/右行交通的差异，并讨论了高精地图作为基准时的局限性，表现出较为客观的科研态度。

### 6. 论文的主要结论与发现
*   **自监督可行性**：证明了仅利用单目视频和 SfM 轨迹即可训练出高性能的路径预测模型。
*   **多假设覆盖**：通过非对称损失，模型能够预测出超出演示轨迹之外的其他可行路径（如路口的其他分支）。
*   **强泛化性**：模型能够适应不同的相机安装位置、镜头畸变以及完全不同的地理环境。
*   **迁移潜力**：预训练模型在小规模特定领域数据（如滑板车）上微调后，效果显著优于从头训练。

### 7. 优点
*   **高可扩展性**：无需人工标注，数据获取成本极低，理论上可以利用互联网上无限的视频资源。
*   **鲁棒性**：SfM 流程能够处理带有水印、严重畸变和光照剧烈变化的“野外”数据。
*   **地图无关性**：不依赖高精地图，能够直接从视觉特征中推断道路拓扑和交通规则（如避开逆行）。

### 8. 不足与局限
*   **几何约束限制**：在车辆纯直线行驶时，SfM 会因为视差不足（Low Parallax）导致重建失败，造成部分数据浪费。
*   **平面假设**：高度估计依赖于局部地面平坦的假设，在剧烈的坡度变化（如山路或立交桥）处可能产生误差。
*   **动态障碍物**：目前的伪标签生成未显式剔除动态物体（如前方车辆），虽然模型学会了绕障，但训练信号中仍包含被遮挡区域的噪声。
*   **评估闭环**：目前主要是在离线数据集上进行分割评估，缺乏在真实闭环仿真或实车控制中的表现数据。

（完）
