---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测到的运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 城市环境下自动驾驶车辆轨迹的自监督分割方法
tldr: 本研究提出GHOST，一种自监督的单目图像车辆轨迹分割方法。通过利用大规模行车记录仪视频，将SfM恢复的相机轨迹投影至地面作为隐式监督信号，无需人工标注即可训练深度网络。该模型能从单张RGB图像预测符合场景布局和车道拓扑的可行路径。实验证明其在NuScenes等数据集上具有良好的泛化能力，并能通过微调迁移至电动滑板车等不同平台，为自动驾驶路径规划提供了高效的自监督方案。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶中复杂城市环境下，无需人工标注即可从单目图像中预测可行行驶轨迹的问题。
method: 利用大规模视频中的自我运动数据，通过SfM恢复轨迹并投影至地面生成空间掩码，以此训练深度分割网络预测路径提案。
result: 在NuScenes数据集上实现了可靠的轨迹预测，并成功通过少量微调迁移至电动滑板车平台，展现了极强的泛化性。
conclusion: 大规模自我运动蒸馏能使模型隐式掌握场景结构，实现无需显式道路建模的高效轨迹假设估计。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于在复杂城市环境下的自动驾驶中，从单目图像中分割出可行的车辆轨迹。利用大规模行车记录仪视频，我们将记录的本车运动视为隐式监督，并通过单目运动恢复结构（SfM）恢复相机轨迹，将其投影到地面上，从而在无需人工标注的情况下生成行驶区域的空间掩码。这些自动生成的标签被用于训练一个深度分割网络，该网络在运行时仅凭单张 RGB 图像即可预测受运动约束的路径提议，而无需对道路或车道线进行显式建模。该模型在多样化且不受限的互联网数据上进行训练，能够隐式地捕捉场景布局、车道拓扑和交叉口结构，并能泛化到不同的相机配置。我们在 NuScenes 数据集上评估了我们的方法，展示了可靠的轨迹预测能力，并进一步通过轻量级微调展示了其向电动滑板车平台的迁移能力。我们的结果表明，大规模本车运动蒸馏可以产生超出演示轨迹的结构化且具有泛化能力的路径提议，从而实现通过图像分割进行轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：GHOST

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖昂贵的人工语义标注、高精地图（HD Maps）或校准传感器套件的情况下，让自动驾驶系统学习理解城市环境中的可行行驶路径？
*   **研究动机**：互联网（如 YouTube）拥有海量的行车记录仪视频，这些视频隐含了人类驾驶员在复杂几何、交通规则和社会约束下的真实行为。论文旨在通过“自我运动蒸馏”（Ego-motion Distillation），将这些无标注视频转化为训练信号，学习一种能够预测多种可行轨迹假设的视觉模型。

#### 2. 方法论
GHOST 框架主要分为三个阶段：
*   **SfM 流水线**：使用 COLMAP 对原始单目视频进行处理，提取稀疏 3D 点云和 6 自由度（6-DoF）相机位姿。为了处理大规模数据，视频被切分为 20 分钟的片段进行并行化处理。
*   **后处理与掩码生成**：
    *   **高度估计**：由于单目 SfM 具有尺度不确定性，研究者利用地面点云投影和 **Theil-Sen 稳健回归算法**来估计相机相对于地面的高度 ($H_{cam}$)。
    *   **轨迹投影**：将相机轨迹沿重力方向投影到地面，并根据相机高度赋予一定的横向宽度（$1.5 \times H_{cam}$），生成空间轨迹掩码作为伪标签（GT Mask）。
*   **分割模型训练**：
    *   使用 UNet 架构（ResNet34 或 SegFormer 编码器）。
    *   **核心创新：加权非对称损失函数**。公式为 $L(x, y) = -(y - \epsilon) \log \sigma(x) + C$。该损失函数加大了对“漏报”（False Negative）的惩罚，减少了对“误报”（False Positive）的惩罚。这鼓励模型不仅预测演示过的单一路径，还能预测出场景中所有潜在的可行路径（多假设预测）。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：从 YouTube 收集的 30 小时行车记录仪视频，涵盖 14 个城市、多种天气和光照条件，最终生成 650,000 帧标注数据。
    *   **评估**：使用 **NuScenes**（OpenLaneV2 Subset B）作为基准。该数据集包含波士顿和新加坡的场景（模型未见过），并提供高精地图用于验证。
    *   **迁移实验**：在电动滑板车（GoPro 采集）上进行微调实验。
*   **对比方法与指标**：对比了 ResNet34 和 SegFormer 两种骨干网络；使用 **Soft IoU**（Ruzicka 相似度）作为主要评估指标，以衡量概率掩码的重叠度。

#### 4. 资源与算力
*   **算力说明**：文中提到使用了 GPU 加速的 Ceres Solver 进行 SfM 优化，并采用了高度并行化的处理流程。
*   **训练细节**：预训练阶段在 650k 帧数据上训练了 25 个 epoch，学习率为 $3 \times 10^{-4}$；滑板车微调阶段训练了 10 个 epoch。
*   **具体硬件**：论文**未明确指出**具体的 GPU 型号（如 A100 或 RTX 4090）及具体数量。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从大规模互联网数据预训练到跨城市（NuScenes）、跨驾驶习惯（左舵 vs 右舵）、跨平台（汽车 vs 滑板车）的验证。
*   **充分性评价**：实验设计较为充分。通过 NuScenes 的零样本（Zero-shot）泛化测试证明了模型的鲁棒性；通过滑板车实验证明了方法的通用性。定性分析部分展示了模型在交叉口、分叉路口避让车辆的能力，证明了非对称损失函数的有效性。

#### 6. 主要结论与发现
*   **自监督可行性**：通过 SfM 轨迹投影生成的伪标签足以训练出高性能的路径预测模型，无需任何人工语义标注。
*   **多假设能力**：非对称损失函数成功使模型学会了预测“分布”而非单一轨迹，模型能识别出未被演示但物理上可行的替代路径。
*   **跨域泛化**：模型在未见过的城市和完全不同的车辆平台（滑板车）上表现良好，显示出极强的视觉特征提取和空间推理能力。

#### 7. 优点
*   **高可扩展性**：利用几乎无限的互联网视频资源，摆脱了对昂贵标注数据的依赖。
*   **架构简洁**：将复杂的轨迹预测问题简化为图像分割问题，易于部署和集成。
*   **鲁棒性强**：能够处理带有水印、严重畸变、极端天气和非典型相机安装位置的“野外”数据。
*   **多模态输出**：通过损失函数设计实现了多路径假设预测，更符合真实驾驶逻辑。

#### 8. 不足与局限
*   **几何约束限制**：SfM 在车辆直线行驶（低视差）时容易失效，导致约 35% 的原始数据因无法恢复准确轨迹而被丢弃。
*   **平面假设**：高度估计依赖于局部地面平坦的假设，在剧烈坡度变化（如山路或立交桥）时可能产生误差。
*   **动态障碍物处理**：虽然模型能隐式学会避障，但目前的伪标签生成过程未显式考虑动态物体，可能导致训练信号中包含被暂时遮挡的“不可行”路径。
*   **评估噪声**：使用静态高精地图作为基准无法完全反映动态环境下的真实可行性，导致定量评分可能被低估。

（完）
