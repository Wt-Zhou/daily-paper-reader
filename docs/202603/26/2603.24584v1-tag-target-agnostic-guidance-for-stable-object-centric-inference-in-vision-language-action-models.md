---
title: "TAG: Target-Agnostic Guidance for Stable Object-Centric Inference in Vision-Language-Action Models"
title_zh: TAG：视觉-语言-动作模型中稳定以对象为中心推理的目标无关引导
authors: "Jiaying Zhou, Zhihao Zhan, Ruifeng Zhai, Qinhan Lyu, Hao Liu, Keze Wang, Liang Lin, Guangrun Wang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24584v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 机器人操作中VLA策略的推理时引导机制
tldr: 针对视觉-语言-动作（VLA）模型在杂乱场景中容易出现目标定位偏差的问题，本文提出了TAG（目标无关引导）机制。TAG受分类器自由引导（CFG）启发，通过对比原始观测与物体擦除后的观测，利用残差信号增强决策中的物体特征，从而在不改变模型架构的前提下，显著提升了机器人在复杂环境下的操作鲁棒性。
selection_source: fresh_fetch
motivation: VLA模型在杂乱环境中常因实例级定位失败导致抓取偏移或误抓，而非运动规划本身不可行。
method: 提出TAG机制，通过对比原始图像与物体擦除图像的策略预测差异，生成残差信号来引导策略更关注目标物体。
result: 在LIBERO和VLABench等基准测试中，TAG有效减少了“擦肩而过”和误抓现象，显著增强了模型在杂乱场景下的鲁棒性。
conclusion: TAG是一种简单且无需修改架构的推理增强方法，能有效缓解VLA模型中的外观偏见并提升目标定位精度。
---

## 摘要
视觉-语言-动作（VLA）策略在将语言指令和视觉观测映射到机器人动作方面取得了显著进展，但在包含干扰项的杂乱场景中，其可靠性会有所下降。通过分析失败案例，我们发现许多错误并非源于不可行的运动，而是源于实例级的定位（grounding）失败：策略通常会生成一个看似合理的抓取轨迹，但落点略微偏离目标，甚至落在错误的对象实例上。为了解决这一问题，我们提出了 TAG（目标无关引导），这是一种简单的推理时引导机制，能够显式地减少 VLA 策略中由干扰项和外观引起的偏差。受无分类器引导（CFG）的启发，TAG 对比了原始观测和对象擦除观测下的策略预测，并将其差异作为残差引导信号，从而在决策过程中增强对象证据的影响。TAG 不需要修改策略架构，并且可以以极小的训练和推理改动集成到现有的 VLA 策略中。我们在包括 LIBERO、LIBERO-Plus 和 VLABench 在内的标准操作基准测试上评估了 TAG，结果表明它一致地提高了在杂乱环境下的鲁棒性，并减少了“差一点命中”和“错误对象”执行的情况。

## Abstract
Vision--Language--Action (VLA) policies have shown strong progress in mapping language instructions and visual observations to robotic actions, yet their reliability degrades in cluttered scenes with distractors. By analyzing failure cases, we find that many errors do not arise from infeasible motions, but from instance-level grounding failures: the policy often produces a plausible grasp trajectory that lands slightly off-target or even on the wrong object instance. To address this issue, we propose TAG (Target-Agnostic Guidance), a simple inference-time guidance mechanism that explicitly reduces distractor- and appearance-induced bias in VLA policies. Inspired by classifier-free guidance (CFG), TAG contrasts policy predictions under the original observation and an object-erased observation, and uses their difference as a residual steering signal that strengthens the influence of object evidence in the decision process. TAG does not require modifying the policy architecture and can be integrated with existing VLA policies with minimal training and inference changes. We evaluate TAG on standard manipulation benchmarks, including LIBERO, LIBERO-Plus, and VLABench, where it consistently improves robustness under clutter and reduces near-miss and wrong-object executions.

---

## 论文详细总结（自动生成）

### 论文总结：TAG (Target-Agnostic Guidance)

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型在机器人操作中表现出色，但在**杂乱场景（Cluttered Scenes）**中容易失效。研究发现，失败的主要原因并非动作不可行，而是**实例级定位（Instance-level Grounding）失败**：机器人生成的轨迹看似合理，但往往会偏离目标几厘米，或者抓错附近的干扰物体。
*   **动机**：现有的VLA模型通常隐式地学习如何平衡目标、上下文和背景，但在面对视觉扰动（如光照、背景纹理变化）时鲁棒性较差。
*   **目标**：通过显式地抑制背景和干扰项引起的偏差，增强模型对目标物体的关注度。

#### 2. 方法论：核心思想与关键技术
TAG 借鉴了扩散模型中的**无分类器引导（Classifier-Free Guidance, CFG）**思想，提出了一种推理时的残差引导机制。
*   **核心思想**：对比模型在“原始观测（包含目标）”和“目标无关观测（反事实基准）”下的预测差异。这个差异被视为“目标物体”对决策的边际贡献，通过放大这个信号来纠正动作。
*   **关键技术细节**：
    *   **TAG 公式**：$v_{TAG} = v_{\theta}(x, I_{uncond}) + w \cdot [v_{\theta}(x, I_{cond}) - v_{\theta}(x, I_{uncond})]$。其中 $w$ 是引导尺度，$I_{uncond}$ 是目标被擦除后的图像。
    *   **反事实合成流水线（Counterfactual Synthesis Pipeline）**：
        1.  **语义解析**：利用 Qwen3-VL 将指令转化为物体描述。
        2.  **定位与分割**：使用 Grounding DINO 和 SAM 2 生成物体的时空掩码。
        3.  **擦除与修复**：利用 MiniMaxRemover 模型擦除目标并修复背景。
    *   **训练-推理不对称设计**：训练时以 10% 的概率随机将目标擦除（$I_{erase}$）以进行校准；推理时则使用更稳定的背景参考图（$I_{bg}$）或纯黑图（$I_{black}$）作为基准。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：标准机器人操作基准。
    *   **LIBERO-Plus**：包含相机位姿、光照、背景等 7 种扰动的鲁棒性测试集。
    *   **VLABench (Track 1)**：包含精细物体选择（如扑克牌、麻将）的复杂杂乱场景。
    *   **真实世界实验**：在 Franka Research 3 机械臂上进行抓取实验。
*   **对比方法**：
    *   基础模型：$\pi_0$ 和 $\pi_{0.5}$。
    *   其他 SOTA 模型：OpenVLA, SpatialVLA, CoT-VLA, GR00T-N1, InternVLA-M1 等。

#### 4. 资源与算力
*   **硬件**：使用**单张 NVIDIA RTX PRO 6000 GPU** 进行训练和测试。
*   **训练参数**：
    *   总步数：30,000 steps。
    *   Batch Size：LIBERO 为 32，VLABench 为 24。
    *   优化器：AdamW，配合余弦退火学习率（峰值 2.5e-5）和 EMA（0.999）。
*   **评价**：相比于动辄使用数百张显卡的 VLA 预训练，该方法在微调阶段对算力的需求非常亲民。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流仿真基准和真实世界迁移实验。
*   **消融实验**：非常充分，分别探讨了：
    *   训练阶段的去条件策略（纯黑 vs 背景 vs 擦除）。
    *   推理阶段的去条件方法（实时掩码 vs 静态背景 vs 纯黑）。
    *   引导尺度 $w$ 的影响（从 1 到 15 的性能曲线）。
*   **公平性**：在相同的基准模型（$\pi_0$ 系列）上进行对比，并提供了详细的成功率（SR）和过程分（PS）提升数据。

#### 6. 主要结论与发现
*   **性能提升**：在 LIBERO-Long 任务中，TAG 将 $\pi_{0.5}$ 的成功率从 89.6% 提升至 **97.0%**；在 VLABench 的扑克/麻将选择任务中，成功率翻倍。
*   **鲁棒性增强**：在 LIBERO-Plus 的视觉扰动下，TAG 表现出极强的韧性，尤其是在相机视角变化和传感器噪声方面。
*   **注意力集中**：可视化表明，TAG 有效地将模型的注意力从干扰项转移到了目标物体上。
*   **稳定性关键**：推理时使用**静态背景**比实时擦除效果更好，因为实时擦除会引入时空不一致的伪影。

#### 7. 优点
*   **即插即用**：不需要修改 VLA 模型的底层架构，适用于任何基于扩散或流匹配（Flow-matching）的策略。
*   **自动化流水线**：提出了一套闭环的视觉数据合成方案，能够自动生成高质量的反事实训练数据。
*   **显著减少误触**：针对性地解决了 VLA 模型在杂乱环境下的“顽疾”，即定位不准的问题。

#### 8. 不足与局限
*   **推理开销**：由于需要分别计算条件和无条件分支，推理时的前向传播次数翻倍，可能影响实时性。
*   **依赖感知质量**：如果第一阶段的物体检测（Grounding DINO）或分割（SAM 2）失败，生成的引导信号可能会产生负面影响。
*   **静态背景假设**：推理时依赖于初始帧提取的静态背景，在背景剧烈变化的动态场景中可能受限。

（完）
