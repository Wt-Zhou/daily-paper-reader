---
title: "3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models"
title_zh: 3D-Mix for VLA：一种用于将基于 VGGT 的 3D 信息集成到视觉-语言-动作模型中的即插即用模块
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Haishan Liu, Changti Wu, Hang Yuan, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24393v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 将3D信息集成到VLA模型中
tldr: 针对视觉-语言-动作（VLA）模型因过度依赖2D预训练而导致的3D空间感知能力不足问题，本文通过系统性研究确定了最优的3D特征融合策略，并提出了3D-Mix插件式模块。该模块采用语义调节的门控融合机制，将VGGT提供的3D几何信息无缝集成至多种VLA架构中，在不改变原模型核心组件的前提下，显著提升了机器人在复杂环境下的操作性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型由于主要在2D图像数据上训练，缺乏机器人精细操作所需的3D空间智能和几何感知能力。
method: 提出一种名为3D-Mix的即插即用模块，通过语义调节的门控融合机制自适应地平衡2D语义与3D几何特征。
result: "在SIMPLER和LIBERO基准测试中，3D-Mix在9种模型变体上均取得一致增益，在分布外（OOD）测试中平均提升了7.0%。"
conclusion: 3D-Mix为提升VLA模型的空间智能提供了一种原则性的集成方法，证明了3D几何信息对增强机器人控制鲁棒性的重要性。
---

## 摘要
视觉-语言-动作（VLA）模型利用多模态大语言模型（MLLM）进行机器人控制，但最近的研究表明，由于主要在 2D 数据上进行训练，MLLM 表现出有限的空间智能，导致其在操作任务中的 3D 感知能力不足。虽然最近的方法引入了如 VGGT 等专门的 3D 视觉模型来增强空间理解，但它们采用了多种集成机制且缺乏系统性研究，使得最优融合策略尚不明确。我们在标准化基准上进行了一项全面的初步研究，比较了九种 VGGT 集成方案，并发现语义条件门控融合（semantic-conditioned gated fusion）在所有评估的九种融合方案中表现最强，该方案能根据任务上下文自适应地平衡 2D 语义特征和 3D 几何特征。我们提出了 3D-Mix，这是一个即插即用模块，可以集成到多种 VLA 架构（如 GR00T 风格和 π 风格）中，而无需修改现有的 MLLM 或动作专家组件。在 SIMPLER 和 LIBERO 基准上针对六个 MLLM 系列（九个模型变体，参数量为 2B-8B）进行的实验表明，3D-Mix 带来了持续的性能提升，在所有九个 GR00T 风格变体中，域外（OOD）SIMPLER 基准的平均提升达到 +7.0%，为增强 VLA 系统的空间智能建立了一种原则性方法。

## Abstract
Vision-Language-Action (VLA) models leverage Multimodal Large Language Models (MLLMs) for robotic control, but recent studies reveal that MLLMs exhibit limited spatial intelligence due to training predominantly on 2D data, resulting in inadequate 3D perception for manipulation tasks. While recent approaches incorporate specialized 3D vision models such as VGGT to enhance spatial understanding, they employ diverse integration mechanisms without systematic investigation, leaving the optimal fusion strategy unclear. We conduct a comprehensive pilot study comparing nine VGGT integration schemes on standardized benchmarks and find that semantic-conditioned gated fusion, which adaptively balances 2D semantic and 3D geometric features based on task context, achieved the strongest performance among all nine evaluated fusion schemes in our pilot study. We present 3D-Mix, a plug-and-play module that integrates into diverse VLA architectures (GR00T-style and $π$-style) without modifying existing MLLM or action expert components. Experiments across six MLLM series (nine model variants, 2B--8B parameters) on SIMPLER and LIBERO show that 3D-Mix delivers consistent performance gains, averaging +7.0% on the out-of-domain (OOD) SIMPLER benchmark across all nine GR00T-style variants, establishing a principled approach for enhancing spatial intelligence in VLA systems.

---

## 论文详细总结（自动生成）

### 论文总结：3D-Mix for VLA

#### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型主要依赖于在海量 2D 图像-文本数据上预训练的多模态大语言模型（MLLM）。然而，2D 预训练导致 MLLM 缺乏显式的 3D 几何感知能力，在需要精确空间推理、深度感知和物体定位的机器人操纵任务中表现受限。
*   **核心问题**：虽然已有研究尝试引入专门的 3D 视觉模型（如 VGGT）来增强空间智能，但如何将 3D 几何特征与 2D 语义特征进行最优融合仍缺乏系统性研究。本文旨在探索并确定一种通用的、即插即用的 3D 信息集成方案。

#### 2. 论文提出的方法论
*   **初步研究（Pilot Study）**：作者首先对比了 9 种不同的融合方案（包括早期融合、动作专家融合、空间强制对齐、视觉融合等），最终确定“**语义调节的门控融合（Gated Fusion）**”效果最好。
*   **3D-Mix 核心思想**：提出一种基于语义条件的自适应门控机制。它不使用固定的融合比例，而是根据任务上下文动态平衡语义和几何信息。
*   **关键技术流程**：
    1.  **特征提取与投影**：使用冻结的 VGGT 提取几何特征，并通过线性层投影到 MLLM 的隐藏空间。
    2.  **语义全局上下文提取**：对 MLLM 的隐藏状态进行平均池化，获取全局语义摘要。
    3.  **自适应门控计算**：将全局语义与局部几何特征拼接，通过一个小型网络（含 Sigmoid 激活）生成位置相关的门控权重 $g$。
    4.  **加权融合**：利用门控权重对语义和几何特征进行加权混合，生成富含 3D 信息的增强序列。
*   **架构集成**：
    *   **GR00T 风格**：在 MLLM 最后一层输出后插入 3D-Mix，作为动作专家的输入。
    *   **$\pi$ 风格**：在 MLLM 的多个层级并行插入 3D-Mix，实现层级化的语义-动作对齐。

#### 3. 实验设计
*   **数据集与场景**：
    *   **SIMPLER**：一个 Real-to-Sim 的基准测试，用于评估模型在分布外（OOD）环境下的泛化能力（如物体配置和视觉变化）。
    *   **LIBERO**：包含四个任务套件（空间、物体、目标、长程任务），用于评估域内多任务学习能力。
*   **对比方法**：
    *   **基准模型**：涵盖 6 个 MLLM 系列（Qwen2.5-VL, Qwen3-VL, RoboBrain 2.0/2.5, MimoEmbodied, RynnBrain），参数量从 2B 到 8B。
    *   **融合方案对比**：在初步研究中对比了 AE-Fusion、Early Fusion、Spatial Forcing 等 9 种策略。

#### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   训练步数为 60,000 步。
    *   使用 DeepSpeed ZeRO-2 优化。
    *   全局 Batch Size 为 128。
    *   MLLM 学习率设为 $10^{-5}$，动作专家及融合模块设为 $10^{-4}$。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 **9 种模型变体**上验证了 3D-Mix 的有效性。
    *   涵盖了**两种主流 VLA 架构**（模块化 GR00T 风格和紧耦合 $\pi$ 风格）。
    *   进行了 **3 组核心消融实验**：VGGT 冻结 vs. 微调、3D 信息敏感性测试（置零/加噪）、$\pi$ 风格下的稀疏层融合深度。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型、跨架构、跨任务（域内 vs. 域外）的广泛验证，证明了该模块的通用性和鲁棒性。

#### 6. 论文的主要结论与发现
*   **最优策略**：语义调节的门控融合是集成 3D 信息的最佳方式，因为它能根据任务需求自适应地调用几何信息。
*   **性能提升**：3D-Mix 在所有测试的模型上均有提升。在最具挑战性的 SIMPLER OOD 基准上，GR00T 风格模型平均提升了 **7.0%**，部分模型（如 RynnBrain-8B）提升高达 **12.51%**。
*   **3D 信息的真实性**：消融实验证明，性能提升确实源于 VGGT 提供的 3D 几何信息，而非单纯增加了参数量或特征维度。

#### 7. 优点
*   **即插即用**：无需修改 MLLM 或动作专家的内部结构，易于集成到现有系统中。
*   **通用性强**：适配多种参数规模（2B-8B）和多种架构风格。
*   **系统性强**：对 9 种融合方案进行了详尽的对比，为后续研究提供了原则性的指导。
*   **轻量化**：门控机制简单高效，且在 $\pi$ 风格下支持稀疏融合以节省显存。

#### 8. 不足与局限
*   **VGGT 依赖**：性能高度依赖于预训练 VGGT 模型的质量；虽然实验显示冻结 VGGT 效果良好，但可能限制了针对特定极端任务的进一步优化空间。
*   **推理开销**：虽然是插件式，但在 $\pi$ 风格下进行层级融合会增加一定的显存占用和计算延迟。
*   **仿真局限**：主要实验在 SIMPLER 仿真环境中完成，虽然该环境具有较强的 Real-to-Sim 代表性，但仍需更多真实物理机器人的部署验证。

（完）
