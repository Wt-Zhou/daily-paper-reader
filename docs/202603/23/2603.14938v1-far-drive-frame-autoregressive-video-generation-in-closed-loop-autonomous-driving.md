---
title: "FAR-Drive: Frame-AutoRegressive Video Generation in Closed-Loop Autonomous Driving"
title_zh: FAR-Drive：闭环自动驾驶中的帧级自回归视频生成
authors: "Yaoru Li, Federico Landi, Marco Godi, Xin Jin, Ruiju Fu, Yufei Ma, Muyang Sun, Heyu Si, Qi Guo"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14938v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 使用生成式视频模型的自动驾驶闭环模拟器
tldr: 针对自动驾驶模拟中缺乏闭环交互和长时一致性的问题，本文提出 FAR-Drive 框架。该框架采用多视图扩散 Transformer 和细粒度结构控制，通过自适应参考时界调节和混合强制自回归训练策略，解决了长时生成中的退化问题。实验证明，该方法在 nuScenes 数据集上达到了 SOTA 性能，并实现了单 GPU 亚秒级推理延迟，为闭环自动驾驶仿真提供了高效可靠的方案。
selection_source: fresh_fetch
motivation: 现有的生成式视频模型多为开环运行，难以支持自动驾驶中智能体动作与环境演化之间的细粒度闭环交互。
method: 提出一种基于多视图扩散 Transformer 的自回归框架，结合两阶段训练策略（自适应参考时界与混合强制训练）及系统级推理优化。
result: 在 nuScenes 数据集上实现了最先进的闭环仿真性能，并在单张 GPU 上保持了亚秒级的低延迟推理。
conclusion: FAR-Drive 成功解决了自动驾驶模拟中的长时一致性、自回归退化和推理效率问题，是构建高性能闭环仿真器的有效途径。
---

## 摘要
尽管自动驾驶取得了快速进展，但由于缺乏可扩展且具有交互性的模拟环境，驾驶系统的可靠训练和评估仍受到根本性限制。近期的生成式视频模型实现了卓越的视觉保真度，但大多数模型在开环设置下运行，无法支持智能体动作与环境演化之间细粒度的帧级交互。构建基于学习的自动驾驶闭环模拟器面临三大挑战：保持长时程的时间和跨视图一致性、缓解迭代自调节下的自回归退化，以及满足低延迟推理约束。在这项工作中，我们提出了 FAR-Drive，这是一个用于自动驾驶的帧级自回归视频生成框架。我们引入了一种具有细粒度结构化控制的多视图扩散 Transformer，实现了几何一致的多摄像头生成。为了解决长时程一致性和迭代退化问题，我们设计了一种由自适应参考时界调节和混合强制自回归训练组成的阶段式训练策略，逐步提高了自调节下的模型一致性和鲁棒性。为了满足低延迟交互需求，我们进一步集成了系统级效率优化以加速推理。在 nuScenes 数据集上的实验表明，我们的方法在现有的闭环自动驾驶模拟方法中达到了最先进的性能，同时在单块 GPU 上保持了亚秒级的延迟。

## Abstract
Despite rapid progress in autonomous driving, reliable training and evaluation of driving systems remain fundamentally constrained by the lack of scalable and interactive simulation environments. Recent generative video models achieve remarkable visual fidelity, yet most operate in open-loop settings and fail to support fine-grained frame-level interaction between agent actions and environment evolution. Building a learning-based closed-loop simulator for autonomous driving poses three major challenges: maintaining long-horizon temporal and cross-view consistency, mitigating autoregressive degradation under iterative self-conditioning, and satisfying low-latency inference constraints. In this work, we propose FAR-Drive, a frame-level autoregressive video generation framework for autonomous driving. We introduce a multi-view diffusion transformer with fine-grained structured control, enabling geometrically consistent multi-camera generation. To address long-horizon consistency and iterative degradation, we design a two-stage training strategy consisting of adaptive reference horizon conditioning and blend-forcing autoregressive training, which progressively improves consistency and robustness under self-conditioning. To meet low-latency interaction requirements, we further integrate system-level efficiency optimizations for inference acceleration. Experiments on the nuScenes dataset demonstrate that our method achieves state-of-the-art performance among existing closed-loop autonomous driving simulation approaches, while maintaining sub-second latency on a single GPU.

---

## 论文详细总结（自动生成）

### FAR-Drive：闭环自动驾驶中的帧级自回归视频生成总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的自动驾驶视频生成模型大多处于“开环”状态，即生成的视频序列是预先确定的，无法根据驾驶智能体的实时动作进行动态反馈。
*   **研究动机**：构建“闭环”模拟器面临三大挑战：
    1.  **长时一致性**：在长时间步的生成中保持多视图几何一致性和时间连贯性。
    2.  **自回归退化**：由于模型不断基于自身生成的（带有误差的）帧进行预测，误差会随时间累积导致画面崩溃（曝光偏差）。
    3.  **低延迟需求**：闭环交互要求模拟器能够实时或近实时地响应动作，而扩散模型通常推理缓慢。

#### 2. 方法论
FAR-Drive 提出了一个统一的帧级自回归框架，核心技术包括：
*   **多视图 MMDiT 架构**：基于 Stable Diffusion 3 的多模态扩散 Transformer（MMDiT），引入跨视图注意力机制（Cross-View Attention）以确保六个摄像头视图的几何对齐。
*   **双 DiT 控制分支**：采用类似 ControlNet 的结构，一个主干网络负责生成，一个控制分支负责注入结构化信号（如 3D 框投影、BEV 语义图、相机参数和文本描述）。
*   **两阶段训练策略**：
    1.  **自适应参考时界调节 (ARHC)**：在训练中动态改变参考帧的数量（0 到 15 帧），使模型学会利用不同长度的历史信息来增强时间动力学推理。
    2.  **混合强制自回归训练 (Blend-Forcing)**：在训练阶段，将模型生成的潜在表示（Latent）与真实值（GT）按比例 $\alpha$ 进行混合（$\tilde{X}_i = \alpha \hat{X}_i + (1 - \alpha)X_i$），逐步增加 $\alpha$ 以模拟推理时的分布偏移，从而提高模型对自身误差的鲁棒性。
*   **推理优化**：采用 1x32x32 的高压缩比 VAE、KV Cache 缓存历史帧特征、以及在扩散步骤间缓存控制信号特征，显著降低延迟。

#### 3. 实验设计
*   **数据集**：使用 **nuScenes** 自动驾驶数据集（1000 个场景，包含 6 视图摄像头、3D 标注和 BEV 地图）。
*   **Benchmark 与对比方法**：
    *   对比了 MagicDrive (V1/V2/3D)、DreamForge、Drive-WM、GenAD 等主流驾驶视频生成模型。
    *   评估指标包括：视觉质量（FID、FVD）、感知一致性（物体检测 mAP、地图分割 mIoU）。
*   **场景设置**：涵盖了单步生成（Single-shot）和严格的帧级自回归生成（AR Unit = 1）。

#### 4. 资源与算力
*   **模型规模**：总参数量约为 **3.7B**（主干 2.5B + 控制分支 0.8B + 辅助模块）。
*   **算力设备**：文中提到了在 **NPU** 和 **NVIDIA H200 GPU** 上进行推理延迟测试。
*   **训练时长**：训练分为三个阶段：预训练（10k 迭代）、因果微调（20k 迭代）、混合强制训练（7k 迭代）。具体的 GPU 数量和总训练小时数未在文中明确给出，但提到 Batch Size 为 64。

#### 5. 实验数量与充分性
*   **实验规模**：进行了主干对比实验、长时生成消融实验（最长滚动生成 229 帧）、推理效率消融实验。
*   **充分性**：实验设计较为全面，特别是针对“自回归退化”这一核心痛点，展示了模型在超出训练长度（训练仅 8-16 帧，推理达 229 帧）时的稳定性。对比实验涵盖了从 2023 年到 2025 年的多个 SOTA 模型，具有较强的说服力。

#### 6. 主要结论与发现
*   **SOTA 性能**：在 nuScenes 验证集上，FAR-Drive 在 FVD、mAP 和 mIoU 等关键指标上均优于现有方法。
*   **长时稳定性**：通过 Blend-Forcing 训练，模型在长达 200 多帧的自回归生成中依然能保持结构完整，而未经过此训练的模型在 32 帧后就开始出现模糊和结构崩溃。
*   **高效推理**：通过系统级优化，在单块 GPU 上实现了**亚秒级（<1s）**的单帧生成延迟，满足了交互式仿真的基本需求。

#### 7. 优点
*   **细粒度交互**：支持严格的帧级自回归，比以往“一次生成一簇帧”的方法更符合真实物理世界的连续反馈。
*   **训练策略创新**：Blend-Forcing 巧妙地解决了自回归模型常见的曝光偏差问题，显著提升了外推能力。
*   **工程优化完备**：不仅关注模型架构，还通过 KV Cache 等手段解决了扩散模型在视频生成中的效率瓶颈。

#### 8. 不足与局限
*   **数据集依赖**：目前仅在 nuScenes 上验证，对于更复杂、长尾的交通场景（如 Corner Cases）的泛化能力尚待观察。
*   **实时性差距**：虽然达到了亚秒级延迟，但距离真正的实时仿真（如 10FPS 以上）仍有优化空间。
*   **闭环策略评估**：论文主要关注视频生成的质量和一致性，尚未深入探讨在生成的模拟器中训练出的驾驶策略（Policy）在现实世界中的迁移效果。

（完）
