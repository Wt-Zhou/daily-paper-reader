---
title: "X-World: Controllable Ego-Centric Multi-Camera World Models for Scalable End-to-End Driving"
title_zh: X-World：面向可扩展端到端驾驶的可控自我中心多摄像头世界模型
authors: "Chaoda Zheng, Sean Li, Jinhao Deng, Zhennan Wang, Shijia Chen, Liqiang Xiao, Ziheng Chi, Hongbin Lin, Kangjie Chen, Boyang Wang, Yu Zhang, Xianming Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19979v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于端到端驾驶的动作条件多摄像头生成式世界模型
tldr: X-World是一个受动作驱动的多摄像头生成式世界模型，旨在解决自动驾驶端到端评估中实车测试成本高、场景覆盖有限的问题。它能根据历史视频和未来动作序列生成具有跨视角几何一致性和时间连贯性的多视角视频，并支持对交通参与者、道路元素及天气环境的精细控制，为可扩展且可重复的自动驾驶评估提供了仿真基础。
selection_source: fresh_fetch
motivation: 现有的自动驾驶评估依赖高成本且难以复现的实车测试，亟需一种能够根据驾驶动作生成真实、可控且长时稳定的未来观测的模拟器。
method: 提出一种多视角潜空间视频生成器，通过动作序列、场景元素及文本提示作为控制信号，显式增强跨视角几何一致性与时间连贯性。
result: 实验证明 X-World 在多视角一致性、长时生成稳定性以及对动作和场景控制信号的遵循度方面均表现优异。
conclusion: X-World 为端到端自动驾驶提供了一个高质量、可控且可扩展的仿真平台，支持视频风格迁移与可重复的场景推演。
---

## 摘要
在自动驾驶的端到端时代，可扩展且可靠的评估变得日益关键，其中视觉-语言-动作（VLA）策略直接将原始传感器流映射为驾驶动作。然而，目前的评估流程仍然严重依赖真实世界的道路测试，这种方式成本高昂、场景覆盖范围有限且难以复现。这些挑战促使人们开发一种真实世界模拟器，它能够在给定动作下生成逼真的未来观测结果，同时在长时程内保持可控性和稳定性。我们提出了 X-World，这是一种以动作为条件的生成式多摄像头世界模型，可直接在视频空间中模拟未来观测。给定同步的多视图摄像头历史记录和未来动作序列，X-World 会生成遵循指令动作的未来多摄像头视频流。为了确保场景推演的可复现性和可编辑性，X-World 进一步支持对动态交通代理和静态道路元素的可选控制，并保留了用于外观级控制（如天气和时间）的文本提示接口。除了世界模拟，X-World 还能通过以外观提示为条件，在保留底层动作和场景动力学的同时实现视频风格迁移。X-World 的核心是一个多视图潜空间视频生成器，旨在显式地促进多种控制信号下的跨视图几何一致性和时间连贯性。实验表明，X-World 实现了高质量的多视图视频生成，具有：(i) 跨摄像头的强视图一致性，(ii) 长时程推演中稳定的时间动力学，以及 (iii) 具有严格动作遵循和对可选场景控制忠实遵循的高可控性。这些特性使 X-World 成为可扩展且可复现评估的实用基础。

## Abstract
Scalable and reliable evaluation is increasingly critical in the end-to-end era of autonomous driving, where vision--language--action (VLA) policies directly map raw sensor streams to driving actions. Yet, current evaluation pipelines still rely heavily on real-world road testing, which is costly, biased toward limited scenario coverage, and difficult to reproduce. These challenges motivate a real-world simulator that can generate realistic future observations under proposed actions, while remaining controllable and stable over long horizons. We present X-World, an action-conditioned multi-camera generative world model that simulates future observations directly in video space. Given synchronized multi-view camera history and a future action sequence, X-World generates future multi-camera video streams that follow the commanded actions. To ensure reproducible and editable scene rollouts, X-World further supports optional controls over dynamic traffic agents and static road elements, and retains a text-prompt interface for appearance-level control (e.g., weather and time of day). Beyond world simulation, X-World also enables video style transfer by conditioning on appearance prompts while preserving the underlying action and scene dynamics. At the core of X-World is a multi-view latent video generator designed to explicitly encourage cross-view geometric consistency and temporal coherence under diverse control signals. Experiments show that X-World achieves high-quality multi-view video generation with (i) strong view consistency across cameras, (ii) stable temporal dynamics over long rollouts, and (iii) high controllability with strict action following and faithful adherence to optional scene controls. These properties make X-World a practical foundation for scalable and reproducible evaluation.

---

## 论文详细总结（自动生成）

### X-World: 面向可扩展端到端驾驶的可控自我中心多摄像头世界模型论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在端到端自动驾驶（尤其是 VLA 模型）时代，如何构建一个可扩展、可靠且可复现的评估与训练基础设施。
*   **研究动机**：
    *   **实车测试局限性**：现有的实车测试成本高昂、场景覆盖有限（难以遇到长尾安全关键事件）、且实验结果难以在相同条件下复现。
    *   **仿真器需求**：端到端策略对分布偏移敏感，需要一个能根据驾驶动作生成真实未来传感器观测（视频）的闭环模拟器。
    *   **训练增强**：在线强化学习（RL）需要大量反事实（Counterfactual）经验，这在现实中是不安全且不可行的。
*   **整体含义**：X-World 旨在通过生成式 AI 技术，将视频生成模型转化为一个可交互、可控的“真实世界模拟器”，为自动驾驶提供闭环评估和在线训练环境。

#### 2. 论文提出的方法论
*   **核心思想**：构建一个以动作为条件的生成式多摄像头视频世界模型，支持对动态代理、静态道路元素和全局外观的精细控制。
*   **关键技术细节**：
    *   **架构基础**：基于 WAN 2.2 (5B 参数) 的 DiT 架构，使用 3D 因果 VAE 进行高倍率压缩（空间 16x，时间 4x）。
    *   **视图-时间自注意力（View-temporal Self-attention）**：在多摄像头和多时间步的 Token 之间交替执行注意力机制，确保跨视角的几何一致性和时间连贯性。
    *   **解耦交叉注意力**：为不同模态（动作、动态代理、静态元素、文本）分配独立的交叉注意力分支，减少干扰，提高控制精度。
    *   **两阶段训练流程**：
        *   **阶段 I（双向训练）**：基于 Rectified Flow 目标，学习准确的动作遵循和场景控制能力。
        *   **阶段 II（因果微调）**：将模型转换为分块因果（Chunk-wise Causal）结构，采用自强制（Self-forcing）和分布匹配蒸馏（DMD）损失，实现低延迟、长时程的流式生成。
    *   **推理优化**：引入滚动 KV 缓存（Rolling KV Cache），支持固定内存占用的长视频（如 24 秒以上）稳定生成。

#### 3. 实验设计
*   **数据集**：使用小鹏汽车（XPeng）大规模高精度真实驾驶序列。
    *   **传感器配置**：7 路同步摄像头（覆盖 360 度），12 FPS。
    *   **标注**：利用 VLM 生成结构化文本描述，利用高精度感知模型提取动态轨迹和静态元素（车道线等）。
*   **实验场景/任务**：
    *   **动作控制**：验证模型在转向、换道、加减速指令下的视频生成准确性。
    *   **场景编辑**：在相同初始场景下，插入行人/骑行者或修改道路布局，观察模型生成反事实场景的能力。
    *   **长时生成**：测试 24 秒长时程推演的稳定性。
    *   **风格迁移**：通过文本提示改变天气（晴天转雨天）或地域（中国转欧洲）。
*   **对比/基准**：论文主要展示了定性结果和应用案例，强调其在闭环仿真中的反应能力。

#### 4. 资源与算力
*   **模型规模**：基于 50 亿参数（5B）的 DiT 模型。
*   **算力说明**：文中提到投入了“大量的计算资源”用于数据标注和模型迭代训练，但**未明确给出具体的 GPU 型号、数量或总训练时长**。

#### 5. 实验数量与充分性
*   **实验组数**：展示了包括动作控制、动态/静态元素控制、长时生成、风格迁移在内的多组定性实验。
*   **充分性与客观性**：
    *   实验涵盖了自动驾驶模拟的核心维度（一致性、可控性、稳定性）。
    *   展示了复杂的闭环案例（如绕过违停车辆、避让突然出现的骑行者），证明了其实用价值。
    *   **不足之处**：缺乏与现有世界模型（如 GAIA-1, Drive-WM 等）在公开指标（如 FVD, PSNR, LPIPS）上的定量对比表格。

#### 6. 论文的主要结论与发现
*   **高保真模拟**：X-World 能够生成具有强跨视角一致性和物理真实感的驾驶视频。
*   **强可控性**：模型能严格遵循驾驶动作指令，并支持对交通参与者和道路结构的精细编辑。
*   **闭环价值**：该模型可作为 VLA 2.0 策略的评估引擎，支持反事实测试，有效识别策略的失效模式。
*   **流式交互**：通过阶段 II 的优化，模型支持实时流式输出，满足在线强化学习的需求。

#### 7. 优点
*   **流式生成架构**：不同于传统的离线视频生成，其因果设计更符合实时仿真的需求。
*   **精细化控制**：通过解耦注意力机制，实现了对动态和静态元素的极高控制精度，优于仅靠文本控制的模型。
*   **长时稳定性**：滚动 KV 缓存和自强制训练解决了生成式模型常见的长时漂移问题。
*   **应用导向**：直接服务于端到端驾驶策略的闭环开发，具有极强的工程落地意义。

#### 8. 不足与局限
*   **定量评估缺失**：论文侧重于视觉效果和应用展示，缺乏标准化的定量 Benchmark 对比。
*   **感知依赖**：训练数据的质量高度依赖于预先存在的感知模型（用于提取标签），感知误差可能会传播到生成模型中。
*   **算力门槛**：5B 规模的视频生成模型推理成本依然较高，对于大规模实时 RL 训练可能存在吞吐量瓶颈。
*   **隐私处理**：文中提到对路牌、车牌进行了模糊处理，这在一定程度上影响了展示视频的原始细节。

（完）
