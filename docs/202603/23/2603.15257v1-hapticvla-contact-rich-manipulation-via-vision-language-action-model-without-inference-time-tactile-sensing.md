---
title: "HapticVLA: Contact-Rich Manipulation via Vision-Language-Action Model without Inference-Time Tactile Sensing"
title_zh: HapticVLA：无需推理时触觉感知的视觉-语言-动作模型实现丰富接触操作
authors: "Konstantin Gubernatorov, Mikhail Sannikov, Ilya Mikhalchuk, Egor Kuznetsov, Makar Artemov, Ogunwoye Faith Ouwatobi, Marcelino Fernando, Artem Asanov, Ziang Guo, Dzmitry Tsetserukou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15257v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 无需推理时触觉感知的HapticVLA触觉感知操纵模型
tldr: "HapticVLA 提出了一种无需推理时触觉传感器的视觉-语言-动作模型，用于处理接触丰富的操作任务。该方法通过安全感知奖励加权流匹配（SA-RWFM）训练专家模型，并利用触觉蒸馏（TD）将触觉感知能力转移到常规 VLA 中。这使得机器人仅凭视觉和状态即可生成具备触觉意识的动作，在降低硬件成本的同时，在真实实验中取得了 86.7% 的成功率，优于带有直接触觉反馈的基准模型。"
selection_source: fresh_fetch
motivation: 触觉传感器成本高且难以在不同机器人平台间复用，限制了具备触觉感知能力的 VLA 模型的广泛应用。
method: 采用两阶段法：先通过 SA-RWFM 训练包含触觉奖励的专家模型，再通过触觉蒸馏将该能力转移给仅依赖视觉和状态输入的 VLA 学生模型。
result: "在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，表现优于包括推理时具备直接触觉反馈在内的多种基准模型。"
conclusion: 该研究证明了触觉感知能力可以离线学习并蒸馏至视觉模型中，从而在无需额外触觉硬件的情况下实现安全且精准的接触密集型操作。
---

## 摘要
触觉感知是视觉-语言-动作（VLA）架构的一项关键能力，因为它能够在丰富接触的任务中实现灵巧且安全的操作。然而，对专用触觉硬件的依赖增加了成本，并降低了在不同机器人平台上的可复现性。我们认为，触觉感知操作可以离线学习，并在推理阶段无需直接触觉反馈的情况下进行部署。为此，我们提出了 HapticVLA，它包含两个紧密耦合的阶段：安全感知奖励加权流匹配（SA-RWFM）和触觉蒸馏（TD）。SA-RWFM 训练了一个流匹配动作专家，该专家结合了预计算的安全感知触觉奖励，旨在惩罚过大的抓取力和次优的抓取轨迹。TD 进一步将这种触觉感知能力迁移到常规 VLA 中：我们从 SA-RWFM 教师模型中蒸馏出一个紧凑的触觉 token，并训练一个学生 VLA 从视觉和状态模态中预测该 token，从而在推理时无需车载触觉传感器即可实现触觉感知动作生成。这种设计在 VLA 中保留了丰富接触的触觉感知推理能力，同时在部署时消除了对车载触觉传感器的需求。在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，始终优于基准 VLA，包括那些在推理期间提供直接触觉反馈的版本。

## Abstract
Tactile sensing is a crucial capability for Vision-Language-Action (VLA) architectures, as it enables dexterous and safe manipulation in contact-rich tasks. However, reliance on dedicated tactile hardware increases cost and reduces reproducibility across robotic platforms. We argue that tactile-aware manipulation can be learned offline and deployed without direct haptic feedback at inference. To this end, we present HapticVLA, which proceeds in two tightly coupled stages: Safety-Aware Reward-Weighted Flow Matching (SA-RWFM) and Tactile Distillation (TD). SA-RWFM trains a flow-matching action expert that incorporates precomputed, safety-aware tactile rewards penalizing excessive grasping force and suboptimal grasping trajectories. TD further transfers this tactile-aware capability into a conventional VLA: we distill a compact tactile token from the SA-RWFM teacher and train a student VLA to predict that token from vision and state modalities, enabling tactile-aware action generation at inference without requiring on-board tactile sensors. This design preserves contact-rich tactile-aware reasoning within VLA while removing the need for on-board tactile sensors during deployment. On real-world experiments, HapticVLA achieves a mean success rate of 86.7%, consistently outperforming baseline VLAs - including versions provided with direct tactile feedback during inference.

---

## 论文详细总结（自动生成）

以下是对论文《HapticVLA: Contact-Rich Manipulation via Vision-Language-Action Model without Inference-Time Tactile Sensing》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的视觉-语言-动作（VLA）模型主要依赖视觉和本体感知，缺乏对力或触觉的显式感知，导致在处理易碎、柔软或需要精密力度控制的物体（如鸡蛋、华夫饼）时容易失败。
*   **核心挑战**：虽然引入触觉传感器可以解决上述问题，但专用触觉硬件成本高昂、难以在不同机器人平台间复用，且增加了部署的复杂性。
*   **核心思想**：提出 **HapticVLA** 框架，主张“触觉感知能力可以离线学习，但在推理时无需传感器”。通过在训练阶段利用触觉数据引导模型学习，随后将其蒸馏至仅依赖视觉和状态输入的模型中。

### 2. 论文提出的方法论
该方法分为三个阶段，核心技术包括 **SA-RWFM** 和 **TD**：

*   **阶段一：离线触觉奖励计算**：
    *   利用 10x10 的触觉传感器阵列获取压力分布图。
    *   定义安全奖励函数，惩罚过大抓取力、压力集中、左右指尖受力不对称以及物体滑动（Slip）。
    *   计算每一步的奖励 $r_t$ 和整条轨迹的风险分数，生成最终的片段奖励 $R_{episode}$。
*   **阶段二：安全感知奖励加权流匹配（SA-RWFM）**：
    *   **核心思想**：在流匹配（Flow Matching）训练中，根据奖励值对训练样本进行加权。高奖励（成功且安全）的样本权重更大，低奖励（导致物体损坏）的样本权重小。
    *   **锚点正则化（Anchor Regularization）**：为了防止生成式策略在加权训练时发生模式崩溃，引入正则化项使参数保持在初始模仿学习解附近。
*   **阶段三：触觉蒸馏（Tactile Distillation, TD）**：
    *   **教师-学生架构**：将训练好的 SA-RWFM 专家作为教师，生成包含触觉意识的动作预测。
    *   **混合目标训练**：学生模型（仅输入视觉和本体状态）通过混合目标进行训练，目标动作是地面真值（GT）与教师预测动作的加权融合（比例各占 50%）。这使得学生模型在没有触觉输入的情况下，也能模仿出具备“力度分寸”的动作。

### 3. 实验设计
*   **硬件平台**：双臂 LeRobot SO-101 机器人，右臂配备高密度触觉传感器阵列（100个触点/指尖）。
*   **实验场景**：
    1.  **果酱罐取放**：处理易变形的塑料罐。
    2.  **华夫饼取放**：极易碎，需要精确的力度和位置。
    3.  **鸡蛋取放**：最高难度，涉及打开包装盒并精准抓取易碎鸡蛋。
*   **数据集**：
    *   实机数据：310 个片段（包含成功和故意损坏物体的失败案例）。
    *   仿真数据：在 Isaac Sim 中生成 1000 个片段以增强鲁棒性。
*   **Benchmark（对比方法）**：
    *   **SmolVLA**（基础模型）、**X-VLA**、**VLA-0**。
    *   对比了带有和不带有触觉反馈的变体。

### 4. 资源与算力
*   **推理设备**：使用 **NVIDIA Jetson Orin NX (16GB)** 边缘计算节点进行实机部署。
*   **训练细节**：文中未明确给出具体的 GPU 训练时长或使用的显卡数量（如 A100/H100 的具体消耗），但提到模型基于轻量级的 SmolVLA (0.45B 参数)，适合在边缘端进行微调和部署。

### 5. 实验数量与充分性
*   **实验规模**：每个任务对每个模型进行了 **20 次独立测试**，总计进行了大量的实机验证。
*   **消融实验**：针对“触觉蒸馏（TD）”和“异步推理”进行了专门的消融研究，验证了 TD 对提升成功率的关键作用。
*   **客观性**：所有基准模型均在相同的数据集上进行了微调，确保了对比的公平性。实验涵盖了从易变形到极易碎的不同梯度任务，具有较强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：HapticVLA 在三项任务中的平均成功率达到 **86.7%**，远超基础模型 SmolVLA（45%）和其他大型 VLA 模型（X-VLA 和 VLA-0 在此类高精度任务中表现极差）。
*   **蒸馏有效性**：即使在推理时完全移除触觉传感器，经过蒸馏的学生模型表现甚至优于某些在推理时拥有直接触觉反馈的模型。
*   **安全性**：SA-RWFM 能够有效利用失败数据，使模型学会避开会导致物体损坏的高压区域。

### 7. 优点
*   **低成本部署**：解决了 VLA 模型对昂贵触觉硬件的依赖，使普通机器人也能执行复杂的接触密集型任务。
*   **安全意识**：通过奖励加权机制，显式地将“安全性”（不损坏物体）引入到动作生成中，而不仅仅是模仿动作轨迹。
*   **端到端优化**：将触觉知识转化为视觉-动作的隐式推理，保持了 VLA 架构的简洁性。

### 8. 不足与局限
*   **数据依赖**：虽然使用了仿真数据，但实机数据量（310个片段）相对较小，对于更复杂、多变的环境可能存在泛化风险。
*   **教师模型限制**：学生模型的上限受限于 SA-RWFM 教师模型的质量，如果教师模型在某些极端情况下的触觉推理有误，学生模型也会继承这些偏差。
*   **任务范围**：目前仅验证了取放类任务，对于更复杂的动态接触任务（如擦拭、插入、工具使用）尚需进一步验证。

（完）
