---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v2"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于机器人策略学习的以动作为中心的世界-动作模型
tldr: 针对现有世界-动作模型（WAM）推理开销大且视觉与动作表示过度耦合的问题，本文提出GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果设计解耦动作预测与视频生成，使得推理时可选择性生成视频，显著提升了效率。在多项机器人任务中，该模型在保持高成功率的同时，推理速度比领先基准快9倍，展现了高效且鲁棒的策略学习能力。
selection_source: fresh_fetch
motivation: 现有世界-动作模型因视觉与动作推理深度耦合，导致推理延迟高且动作预测精度易受视频生成质量影响。
method: 设计了一种以动作为中心的架构，利用因果约束确保动作预测不依赖未来视频标记，并结合大规模机器人数据集进行预训练。
result: "实验显示该模型推理速度比Motus快9倍，成功率提升7%，在RoboTwin 2.0基准测试中性能提升了95%。"
conclusion: GigaWorld-Policy通过解耦视觉动力学与动作解码，成功实现了高效且高性能的机器人策略部署。
---

## 摘要
从预训练视频生成主干网络初始化的世界-动作模型 (World-Action Models, WAM) 在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模通常会使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习 2D 像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练制定为两个耦合组件：模型在当前观测的条件下预测未来动作序列，并同时在预测动作和相同观测的条件下生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束鼓励产生符合物理规律的动作。通过防止未来视频 token 影响动作 token 的因果设计，显式的未来视频生成在推理时是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化、大规模的机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其适配为机器人策略学习的主干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

### GigaWorld-Policy：高效的以动作为中心的世界-动作模型论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前机器人领域中**世界-动作模型（World-Action Models, WAM）**存在的两大瓶颈提出改进：
*   **推理延迟高**：现有的 WAM 通常需要通过迭代采样生成未来视频流来推导动作，导致推理速度极慢，难以满足实时闭环控制的需求。
*   **动作与视频过度耦合**：动作预测往往过度依赖显式的视频生成质量，视频预测中的微小像素误差会随时间累积，导致长程控制失效。
*   **监督稀疏性**：传统的视觉-语言-动作（VLA）模型仅依赖稀疏的动作标签，缺乏对物理世界动态规律的深度理解。

**GigaWorld-Policy** 的核心意义在于提出了一种“以动作为中心”的架构，通过因果解耦设计，使模型在训练时利用视频生成作为强监督信号，而在推理时可以跳过视频生成直接输出动作，兼顾了世界模型的物理理解能力与 VLA 模型的高效性。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：将策略学习建模为两个耦合任务：(1) 根据当前观测预测未来动作序列；(2) 根据当前观测和预测的动作生成未来视觉演化。
*   **关键技术细节**：
    *   **因果自注意力掩码（Causal Self-Attention Mask）**：这是模型实现高效推理的关键。通过设计特定的掩码，确保动作 token 只能观察到当前的图像和状态，而不能“偷看”未来的视频 token。这使得推理时可以完全不实例化视频 token，直接解码动作。
    *   **统一的 Diffusion Transformer (DiT)**：基于 5B 参数的 Wan-2.2 主干网络，将多视角图像、文本指令、机器人状态、动作和未来视频帧全部 token 化并输入同一个 Transformer 堆栈。
    *   **流匹配（Flow Matching）目标函数**：采用流匹配算法同时优化动作预测损失（$\mathcal{L}_{action}$）和视频生成损失（$\mathcal{L}_{video}$），利用视频生成作为正则项来约束动作的物理合理性。
    *   **三阶段课程学习**：
        1.  **通用视频预训练**：从大规模互联网视频模型初始化。
        2.  **具身数据预训练**：在约 10,000 小时的机器人视频和人类第一视角视频（如 Ego4D）上训练，注入物理先验。
        3.  **目标机器人后训练**：在特定机器人的轨迹数据上进行微调，对齐指令与动作。

#### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：使用 **RoboTwin 2.0** 基准测试，涵盖 50 多个复杂的双臂/单臂操纵任务，并引入了强随机化（背景、光照、物体干扰）。
    *   **真实世界**：在 AgileX PiPER 6-DoF 机械臂上测试了四个任务：清理桌面、扫描二维码、叠碗、扫垃圾。
*   **对比方法（Baselines）**：
    *   **VLA 模型**：$\pi$ 0.5、GigaBrain-0、X-VLA。
    *   **WAM 模型**：Motus（目前的 SOTA）、Cosmos-Policy。

#### 4. 资源与算力
*   **模型规模**：5B 参数。
*   **训练算力**：预训练阶段消耗了约 **6000 GPU 小时**（未明确说明具体 GPU 型号，但通常为 A100 或 H100 集群），全局 Batch Size 为 256。
*   **推理算力**：推理性能测试在 **NVIDIA A100 GPU** 上完成。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖 50+ 任务，每个任务 100 次测试，数据量充足。
    *   真实世界实验每个任务 20 次尝试，总计 80 次测试。
    *   **消融实验**：针对预训练配置、未来帧预测数量（$\Delta$ 间隔）、因果掩码设计、数据效率（10% 到 100% 数据量对比）进行了详细测试。
*   **客观性评价**：实验设计较为客观，对比了不同范式的最新模型，并同时报告了成功率和推理延迟（Latency），这在机器人论文中是非常关键的闭环评价指标。

#### 6. 主要结论与发现
*   **高效性**：GigaWorld-Policy 的推理速度比 Motus 快 **9 倍**（单次推理仅需 0.36s），达到了接近实时的闭环控制水平。
*   **性能领先**：在真实世界中，成功率比 Motus 高出 7%；在仿真环境 RoboTwin 2.0 中，性能比 $\pi$ 0.5 提升了 95%。
*   **数据效率**：由于引入了强大的视频生成预训练，该模型仅需 10% 的训练数据即可达到传统 VLA 模型使用 100% 数据时的性能。
*   **视频生成的辅助作用**：消融实验证明，即使推理时不生成视频，训练时加入视频预测任务也能显著提升动作预测的准确性（成功率从 0.65 提升至 0.83）。

#### 7. 优点（亮点）
*   **解耦设计**：巧妙地解决了世界模型“好用但太慢”的矛盾，通过因果掩码实现了视频生成的“训练时必需，推理时可选”。
*   **多视角一致性**：通过将三视角图像拼接为复合图像并使用共享 Transformer 处理，保证了空间理解的一致性。
*   **强大的预训练范式**：充分利用了互联网视频、人类演示视频和机器人视频的多层次数据，构建了极强的物理直觉。

#### 8. 不足与局限
*   **推理延迟仍有优化空间**：虽然比其他 WAM 快，但 360ms 的延迟对于极高频（如 100Hz 以上）的动态控制任务仍显不足。
*   **任务类型局限**：实验主要集中在桌面操纵任务，尚未在移动导航、大范围动态避障或更复杂的灵巧手操作中进行验证。
*   **硬件依赖**：5B 参数的模型虽然在云端 A100 上运行尚可，但在嵌入式机器人端侧设备（如 Jetson Orin）上的部署表现未提及。

（完）
