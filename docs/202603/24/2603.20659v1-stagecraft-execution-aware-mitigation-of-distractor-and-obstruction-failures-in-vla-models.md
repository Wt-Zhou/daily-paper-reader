---
title: "StageCraft: Execution Aware Mitigation of Distractor and Obstruction Failures in VLA Models"
title_zh: StageCraft：视觉-语言-动作（VLA）模型中干扰物与障碍物失败的执行感知缓解
authors: "Kartikay Milind Pangaonkar, Prabin Rath, Omkar Patil, Nakul Gopalan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20659v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 利用互联网规模的VLM减轻VLA模型的失效
tldr: "视觉语言动作模型（VLA）在面对干扰物和物理障碍时容易失效。本文提出 StageCraft，一种无需训练的即插即用方法。它利用大语言模型（VLM）的推理能力，通过分析少量策略执行视频，识别并移除初始环境中的干扰因素，从而优化执行环境。实验表明，该方法在多种真实任务中将 VLA 性能提升了 40%，显著增强了模型在复杂场景下的鲁棒性。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型在遇到未见过的干扰物或物理障碍时容易执行失败，且现有的微调方法难以完全解决这一泛化难题。
method: 提出一种名为 StageCraft 的无需训练的方法，利用 VLM 的上下文推理能力，通过分析执行视频来识别并清理环境中可能导致失败的障碍物。
result: "在真实世界的三个任务领域中，StageCraft 使最先进的 VLA 模型性能绝对提升了 40%，并在仿真实验中展现出良好的可扩展性。"
conclusion: StageCraft 证明了利用互联网规模预训练的 VLM 进行环境干预是提升机器人策略鲁棒性的一种高效且通用的途径。
---

## 摘要
在文本和图像数据上进行的大规模预训练，结合多样化的机器人演示，已帮助视觉-语言-动作模型（VLA）泛化到新的任务、物体和场景。然而，当机器人工作空间中存在干扰物和物理障碍等执行时阻碍时，这些模型仍然容易发生失败。现有的策略改进方法通过微调基础 VLA 来提高泛化能力，但在未见过的干扰物设置中仍然难以应对。为了解决这一问题，我们研究了是否可以利用互联网规模预训练的大型视觉语言模型（VLM）来对这些阻碍进行推理并缓解策略失败。为此，我们提出了 StageCraft，这是一种无需训练的方法，通过使用基于 VLM 的上下文推理来操纵环境的初始状态，从而提高预训练 VLA 策略的性能。StageCraft 将策略展开视频和成功标签作为输入，并利用 VLM 的推理能力来推断初始状态中哪些物体需要被操纵，以避免预期的执行失败。StageCraft 是一个可扩展的即插即用模块，不会对底层策略引入额外的约束，且仅需要少量的策略展开即可工作。我们评估了结合 StageCraft 的最先进 VLA 模型的性能，结果显示在涉及多种干扰物和障碍物的三个真实世界任务领域中，性能绝对提升了 40%。我们在 RLBench 中的仿真实验表明，StageCraft 会根据底层策略的强度定制其干预程度，并随着上下文样本的增加而提高性能。StageCraft 的实际效果视频可在 https://stagecraft-decorator.github.io/stagecraft/ 找到。

## Abstract
Large scale pre-training on text and image data along with diverse robot demonstrations has helped Vision Language Action models (VLAs) to generalize to novel tasks, objects and scenes. However, these models are still susceptible to failure in the presence of execution-time impediments such as distractors and physical obstructions in the robot's workspace. Existing policy improvement methods finetune base VLAs to improve generalization, yet they still struggle in unseen distractor settings. To address this problem, we investigate whether internet-scale pretraining of large vision-language models (VLMs) can be leveraged to reason about these impediments and mitigate policy failures. To this end, we propose StageCraft, a training-free approach to improve pretrained VLA policy performance by manipulating the environment's initial state using VLM-based in-context reasoning. StageCraft takes policy rollout videos and success labels as input and leverages VLM's reasoning ability to infer which objects in the initial state need to be manipulated to avoid anticipated execution failures. StageCraft is an extensible plug-and-play module that does not introduce additional constraints on the underlying policy, and only requires a few policy rollouts to work. We evaluate performance of state-of-the-art VLA models with StageCraft and show an absolute 40% performance improvement across three real world task domains involving diverse distractors and obstructions. Our simulation experiments in RLBench empirically show that StageCraft tailors its extent of intervention based on the strength of the underlying policy and improves its performance with more in-context samples. Videos of StageCraft in effect can be found at https://stagecraft-decorator.github.io/stagecraft/ .

---

## 论文详细总结（自动生成）

以下是对论文《StageCraft: Execution Aware Mitigation of Distractor and Obstruction Failures in VLA Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在泛化性上表现出色，但在面对执行时的**干扰物（Distractors）**和**物理障碍（Obstructions）**时非常脆弱。
*   **研究动机**：现有的策略改进方法（如微调或强化学习）成本高昂，且容易导致“灾难性遗忘”。作者观察到人类在执行任务前会清理环境以降低难度，因此提出：是否可以利用互联网规模预训练的视觉语言模型（VLM）的推理能力，在策略执行前通过“操纵环境初始状态”来规避失败？
*   **整体含义**：StageCraft 是一种无需训练的即插即用模块，它通过观察少量的失败/成功案例，自动识别并移除环境中阻碍 VLA 运行的物体，从而提升现有模型的鲁棒性。

### 2. 方法论
StageCraft 的核心思想是**“执行感知的环境干预”**，具体流程如下：
*   **物体集合创建（Object-Set Creation）**：收集少量（如10次）策略在含有不同干扰物环境下的执行视频及成功/失败标签。利用 VLM 对这些视频进行推理，识别出物体组合与成功率之间的关系。
*   **物体集合转换（Object-Set Transition）**：当面对一个新场景时，VLM 会对比当前物体与历史成功案例。它会寻找一个能保证最高成功率的最大物体子集，并确定需要移除的“多余”物体。
*   **VLM 上下文推理**：使用 Chain-of-Thought（CoT）提示词，让 VLM（如 Gemini 3.1 Pro）执行上述逻辑。VLM 输出需要移除的物体描述。
*   **环境修改执行**：
    1.  使用 **SAM3** 模型根据描述检测物体边界框。
    2.  通过相机内外参映射到 3D 空间。
    3.  调用预定义的**动作原语（Action Primitives）**（如抓取和放置），将干扰物移入回收筐。
    4.  最后，在清理后的环境中运行原始 VLA 策略。

### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：使用 Franka FR3 机器人，设置了三个任务：叠杯子（Stack Cups）、摆盘（Setup Plate）、球入碗（Block in Bowl）。引入了 8 种 VLA 训练集中未见过的干扰物。
    *   **仿真环境**：在 RLBench 中进行“抓取红杯子”任务，设置了不同数量干扰物的变体（Zero, One, Two, Three）。
*   **对比基准（Benchmark）**：
    *   **Base**：无干扰物的原始环境。
    *   **Distractor**：有干扰物但无干预的原始 VLA 执行。
    *   **StageCraft**：有干扰物且经过 StageCraft 干预后的执行。
*   **对比方法**：评估了两种最先进的 VLA 模型：**Pi0.5** 和 **SmolVLA**。

### 4. 资源与算力
*   **模型使用**：使用了 Gemini-3.1-Pro、Gemini-2.5-Pro 和 GPT-5.2-Pro（注：文中提及的型号可能为未来版本或特定 API 版本）作为推理后端。
*   **VLA 训练**：在 LeRobot 框架下对 Pi0.5 和 SmolVLA 进行了全参数微调，每个任务使用 60 个演示样本。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长，但提到使用了预训练权重并进行了下游任务微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   真实世界中，每个任务使用 10 个上下文样本，并在 15 个新场景下进行评估。
    *   仿真实验中，每个设置运行了 100 次评估。
*   **消融实验**：对比了不同 VLM 模型的推理能力、上下文样本数量（1 vs 20）的影响，以及“结构化物体集策略”与“直接提示 VLM”的效果对比。
*   **充分性评价**：实验设计较为全面，涵盖了真实机器人和仿真环境，并针对不同强度的基础策略（$\pi_{weak}$ 和 $\pi_{strong}$）进行了测试，证明了方法的普适性。

### 6. 主要结论与发现
*   **性能提升**：在三个真实任务中，StageCraft 使 VLA 的成功率平均**绝对提升了 40%**。
*   **自适应干预**：StageCraft 会根据底层策略的强弱自动调整干预程度。策略越弱，移除的物体越多；策略越强，干预越少（体现了最小化干预原则）。
*   **样本效率**：随着上下文样本增加，VLM 对策略鲁棒性的估计更准确，干预效果更好。
*   **策略必要性**：简单的“盲目移除所有干扰物”是低效的，而 StageCraft 的物体集转换策略比直接询问 VLM 更稳定、更准确。

### 7. 优点
*   **无需训练（Training-free）**：作为即插即用模块，不需要重新训练复杂的 VLA 模型。
*   **执行感知**：不是盲目清理环境，而是基于策略的实际表现进行有针对性的清理。
*   **高泛化性**：利用了互联网规模 VLM 的视觉推理能力，能够处理训练集中从未见过的奇特干扰物。

### 8. 不足与局限
*   **VLM 窗口限制**：由于视频帧转化为 Token 数量巨大，增加上下文样本量会受到 VLM 上下文长度限制。
*   **物体定义依赖**：该方法假设干扰物可以被组织成离散的物体集合，对于非离散障碍（如液体污渍、光照变化）可能不适用。
*   **推理延迟**：依赖云端 VLM API 和额外的环境清理步骤，会增加任务的总执行时间。
*   **硬件依赖**：需要机器人具备执行“抓取-放置”原语的基本能力，如果干扰物本身无法被机器人搬动，则方法失效。

（完）
