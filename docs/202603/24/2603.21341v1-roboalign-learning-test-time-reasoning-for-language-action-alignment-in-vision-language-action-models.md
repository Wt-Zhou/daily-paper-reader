---
title: "RoboAlign: Learning Test-Time Reasoning for Language-Action Alignment in Vision-Language-Action Models"
title_zh: RoboAlign：在视觉-语言-动作模型中学习用于语言-动作对齐的测试时推理
authors: "Dongyoung Kim, Sumin Park, Woomin Song, Seungku Kim, Taeyoung Kim, Huiwon Jang, Jinwoo Shin, Jaehyung Kim, Younggyo Seo"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21341v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 在VLA模型中学习测试时推理以实现语言-动作对齐
tldr: RoboAlign是一个旨在提升视觉-语言-动作模型（VLA）具身推理能力的训练框架。针对现有MLLM在翻译多模态理解为底层动作时表现不稳的问题，该方法通过零样本自然语言推理采样动作标记，并利用强化学习（RL）优化推理过程，从而对齐语言与动作空间。实验表明，RoboAlign在LIBERO、CALVIN及真实环境基准测试中，仅需极少量数据即可显著超越传统监督微调基准。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在具身推理和底层动作转化上表现不稳定，且传统的监督微调往往收益有限甚至产生负面影响。
method: 提出RoboAlign框架，通过零样本自然语言推理采样动作，并结合强化学习对推理过程进行精细化对齐以提高动作准确性。
result: "在LIBERO、CALVIN和真实场景中，RoboAlign相比SFT基准分别实现了17.5%、18.9%和106.6%的显著性能提升。"
conclusion: RoboAlign成功弥合了语言与底层动作间的模态鸿沟，证明了通过强化学习优化推理过程能有效增强VLA模型的执行能力。
---

## 摘要
提升多模态大语言模型（MLLMs）中的具身推理能力，对于在其基础上构建视觉-语言-动作模型（VLAs）以将多模态理解转化为底层动作至关重要。因此，最近的研究探索了通过视觉问答类型的监督来增强 MLLMs 的具身推理能力。然而，据报道这些方法会导致 VLA 性能不稳定，通常只能产生微小甚至负面的收益。在本文中，我们提出了一种更系统的 MLLM 训练框架 RoboAlign，能够可靠地提升 VLA 的性能。我们的核心思想是通过零样本自然语言推理采样动作标记，并利用强化学习（RL）优化该推理过程，从而提高动作准确性。因此，RoboAlign 弥合了 MLLMs 中语言与底层动作之间的模态鸿沟，并促进了从 MLLM 到 VLA 的知识迁移。为了验证 RoboAlign 的有效性，我们在 MLLM 骨干网络上添加了一个基于扩散的动作头来训练 VLAs，并在主要的机器人基准测试中对其进行了评估。值得注意的是，通过在有监督微调（SFT）后使用不到 1% 的数据进行基于强化学习的对齐，RoboAlign 在 LIBERO、CALVIN 和真实世界环境中相比 SFT 基准分别实现了 17.5%、18.9% 和 106.6% 的性能提升。

## Abstract
Improving embodied reasoning in multimodal-large-language models (MLLMs) is essential for building vision-language-action models (VLAs) on top of them to readily translate multimodal understanding into low-level actions. Accordingly, recent work has explored enhancing embodied reasoning in MLLMs through supervision of vision-question-answering type. However, these approaches have been reported to result in unstable VLA performance, often yielding only marginal or even negative gains. In this paper, we propose a more systematic MLLM training framework RoboAlign that reliably improves VLA performance. Our key idea is to sample action tokens via zero-shot natural language reasoning and refines this reasoning using reinforcement learning (RL) to improve action accuracy. As a result, RoboAlign bridges the modality gap between language and low-level actions in MLLMs, and facilitate knowledge transfer from MLLM to VLA. To validate the effectiveness of RoboAlign, we train VLAs by adding a diffusion-based action head on top of an MLLM backbone and evaluate them on major robotics benchmarks. Remarkably, by performing RL-based alignment after SFT using less than 1\% of the data, RoboAlign achieves performance improvements of 17.5\%, 18.9\%, and 106.6\% over SFT baselines on LIBERO, CALVIN, and real-world environments, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：RoboAlign —— 学习用于视觉-语言-动作模型中语言-动作对齐的测试时推理

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了如何有效地将多模态大语言模型（MLLM）转化为机器人控制的视觉-语言-动作模型（VLA）。
*   **研究动机**：尽管 MLLM 在常识推理和视觉理解方面表现出色，但直接将其应用于底层动作生成时存在“模态鸿沟”。现有的研究试图通过视觉问答（VQA）等具身推理任务来增强 MLLM，但这种语言层面的推理提升往往无法转化为实际动作性能的增长，甚至可能导致性能下降。
*   **核心问题**：如何建立一种系统化的训练框架，使 MLLM 的推理能力能够直接服务于底层动作的精确执行。

#### 2. 方法论
RoboAlign 提出了一个两阶段的训练框架，旨在通过强化学习（RL）将推理过程与底层动作对齐：
*   **核心思想**：让模型在输出动作前先进行自然语言推理（Chain-of-Thought），并利用动作执行的准确性作为奖励信号来优化推理路径。
*   **关键技术细节**：
    *   **动作标记化（FAST Tokenization）**：将连续的机器人动作（位置、姿态、夹持器状态）通过离散余弦变换（DCT）和字节对编码（BPE）转化为 MLLM 可处理的离散 Token。
    *   **第一阶段：机器人感知 SFT**：使用混合数据集（包括通用 VQA、自定义的 RoboAlign VQA、具身推理数据及 FAST 动作数据）进行监督微调，使模型具备初步的零样本推理和动作生成能力。
    *   **第二阶段：基于 RL 的对齐（GRPO）**：采用组相对策略优化（Group Relative Policy Optimization, GRPO）。模型针对指令生成多个包含 `<think>` 推理过程和 `<answer>` 动作 Token 的响应。
    *   **奖励函数**：奖励由格式奖励（是否符合推理标签格式）和**动作准确性奖励**（生成的动作 Token 与真实轨迹的前缀匹配度）共同组成。
*   **VLA 适配**：在训练好的 MLLM 骨干网络（冻结）之上，添加一个基于扩散（Diffusion）的动作头，利用 MLLM 的隐藏层表示来预测连续动作。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：包含空间、对象、目标和长程（Long-horizon）四个类别的仿真操作任务。
    *   **CALVIN**：评估模型在未见环境（ABC→D）中的零样本泛化能力和连续任务执行能力。
    *   **真实世界机器人**：使用 Franka Research 3 机械臂进行四种不同的物体抓取与放置任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Qwen2.5-VL-7B-Instruct。
    *   **对比方法**：包括 Diffusion Policy、Octo、OpenVLA 等知名 VLA 模型，以及专门针对具身推理优化的 MLLM（如 RoboBrain 2.0, Cosmos-Reason1）。
    *   **消融实验**：对比了仅 SFT、语言基 RL（高层动作）、视觉基 RL（2D 轨迹）以及不同 MLLM 骨干（Qwen3-VL）。

#### 4. 资源与算力
*   **算力设备**：
    *   **MLLM 训练**：使用 8 台 NVIDIA H200 GPU。
    *   **VLA 训练**：使用 2 台 NVIDIA A100 GPU。
*   **训练时长**：
    *   MLLM 的 SFT 阶段约 30 小时，RL 阶段约 1 小时。
    *   VLA 动作头训练：每 10K 步约 1 小时（LIBERO 需 60K 步，CALVIN 需 100K 步）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流仿真基准（数千次测试）和真实机器人（96 次测试/任务）上进行了评估。
*   **充分性与公平性**：
    *   实验涵盖了从 7B 到 8B 不同参数规模的骨干网络，验证了方法的通用性。
    *   通过 KNN 表示分析深入探讨了模型内部特征的变化，证明了 RL 确实增强了特征的判别力。
    *   对比了多种对齐策略，实验设计较为全面，对照组设置合理（如冻结骨干网络以公平比较表示能力）。

#### 6. 主要结论与发现
*   **性能飞跃**：RoboAlign 在 LIBERO 上提升了 17.5%，CALVIN 上提升了 18.9%，真实世界任务中提升了 106.6%。
*   **推理与动作的解耦与重合**：单纯提升语言推理（如 RoboBrain 2.0）不一定能提升 VLA 性能，但通过动作准确性引导的 RL 推理能显著增强模型对底层状态的感知。
*   **长程任务优势**：RoboAlign 在复杂、长步骤任务中表现尤为突出，证明了测试时推理（Test-time reasoning）对处理复杂逻辑的有效性。
*   **通用性保持**：在增强具身能力的同时，模型在通用视觉理解基准（如 MMStar）上的性能并未退化。

#### 7. 优点
*   **高效对齐**：仅需不到 1% 的额外 RL 数据即可获得显著的性能增益。
*   **弥合鸿沟**：成功将 LLM 的“思考”过程与机器人的“执行”动作通过 Token 匹配奖励联系起来。
*   **开源兼容性**：框架可适配多种 MLLM 骨干和动作头架构，具有很强的实用价值。

#### 8. 不足与局限
*   **数据依赖**：虽然 RL 阶段数据量小，但第一阶段 SFT 仍依赖高质量的具身推理和动作数据集。
*   **实时性挑战**：测试时生成长推理链（`<think>` 标签内容）会增加推理延迟，这对于需要高频控制（如 100Hz）的机器人任务可能存在挑战（文中未详细讨论推理耗时对实时控制的影响）。
*   **安全风险**：论文提到如果训练数据包含不安全轨迹，模型可能会通过 RL 强化这些不安全行为。

（完）
