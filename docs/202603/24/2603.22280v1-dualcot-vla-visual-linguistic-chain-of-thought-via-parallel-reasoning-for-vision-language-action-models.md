---
title: "DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models"
title_zh: DualCoT-VLA：基于并行推理的视觉-语言思维链视觉-语言-动作模型
authors: "Zhide Zhong, Junfeng Li, Junjie He, Haodong Yan, Xin Gong, Guanyi Zhao, Yingjie Cai, Jiantao Gao, Xu Yan, Bingbing Liu, Yingcong Chen, Liuqing Yang, Haoang Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22280v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: VLA模型的视觉语言思维链
tldr: 针对视觉-语言-动作（VLA）模型在复杂任务规划和精细空间感知方面的不足，本文提出 DualCoT-VLA。该方法创新性地结合了用于高层规划的语言思维链和用于底层空间理解的视觉思维链，并通过引入可学习查询标记的并行推理机制，取代了传统的逐步自回归解码，在显著降低推理延迟的同时，有效提升了模型在多步复杂任务中的操作精度和逻辑能力。
selection_source: fresh_fetch
motivation: 现有 VLA 模型难以同时捕捉底层视觉细节与高层逻辑规划，且自回归思维链推理存在高延迟和误差累积的缺陷。
method: 提出一种双重思维链框架，整合视觉与语言 CoT，并利用并行推理机制将自回归过程转化为单步前向推理。
result: 在 LIBERO、RoboCasa GR1 基准测试及真实世界机器人实验中，该方法均取得了当前最先进的性能表现。
conclusion: DualCoT-VLA 通过并行双模态思维链有效增强了机器人的感知与规划效率，为高效、精准的具身智能提供了新方案。
---

## 摘要
视觉-语言-动作（VLA）模型将视觉观测和语言指令直接映射为机器人动作。虽然标准 VLA 模型在简单任务中表现出色，但在需要逻辑规划的复杂多步任务以及需要细粒度空间感知的精确操作中往往表现不佳。最近的研究引入了思维链（CoT）推理，赋予 VLA 模型“先思后行”的能力。然而，目前的基于 CoT 的 VLA 模型面临两个关键局限：1) 由于依赖孤立的单模态 CoT，无法同时捕捉底层视觉细节和高层逻辑规划；2) 逐步自回归解码导致的高推理延迟和复合误差。为了解决这些局限，我们提出了 DualCoT-VLA，这是一种具有并行推理机制的 VLA 模型视觉-语言 CoT 方法。为了实现全面的多模态推理，我们的方法集成了用于底层空间理解的视觉 CoT 和用于高层任务规划的语言 CoT。此外，为了克服延迟瓶颈，我们引入了一种并行 CoT 机制，该机制结合了两组可学习的查询标记（query tokens），将自回归推理转变为单步前向推理。广泛的实验表明，我们的 DualCoT-VLA 在 LIBERO 和 RoboCasa GR1 基准测试以及真实世界平台中均达到了最先进的性能。

## Abstract
Vision-Language-Action (VLA) models map visual observations and language instructions directly to robotic actions. While effective for simple tasks, standard VLA models often struggle with complex, multi-step tasks requiring logical planning, as well as precise manipulations demanding fine-grained spatial perception. Recent efforts have incorporated Chain-of-Thought (CoT) reasoning to endow VLA models with a ``thinking before acting'' capability. However, current CoT-based VLA models face two critical limitations: 1) an inability to simultaneously capture low-level visual details and high-level logical planning due to their reliance on isolated, single-modal CoT; 2) high inference latency with compounding errors caused by step-by-step autoregressive decoding. To address these limitations, we propose DualCoT-VLA, a visual-linguistic CoT method for VLA models with a parallel reasoning mechanism. To achieve comprehensive multi-modal reasoning, our method integrates a visual CoT for low-level spatial understanding and a linguistic CoT for high-level task planning. Furthermore, to overcome the latency bottleneck, we introduce a parallel CoT mechanism that incorporates two sets of learnable query tokens, shifting autoregressive reasoning to single-step forward reasoning. Extensive experiments demonstrate that our DualCoT-VLA achieves state-of-the-art performance on the LIBERO and RoboCasa GR1 benchmarks, as well as in real-world platforms.

---

## 论文详细总结（自动生成）

以下是对论文《DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视觉-语言-动作（VLA）模型在处理需要长程逻辑规划和精细空间感知的复杂任务时表现不佳。
*   **研究背景**：现有的思维链（CoT）VLA模型存在两大局限：
    1.  **模态孤立**：现有的CoT通常仅限于单一模态（纯文本或纯视觉），导致模型无法同时兼顾高层逻辑规划与底层空间细节。
    2.  **推理延迟高**：依赖自回归（AR）逐个生成Token，导致严重的推理延迟和误差累积，难以满足机器人高频控制的需求。

### 2. 方法论
DualCoT-VLA 提出了一种在连续潜空间中进行的**并行视觉-语言思维链**推理机制：
*   **核心思想**：通过引入两组可学习的查询标记（Query Tokens），在单次前向传播中同时提取视觉空间先验和语言逻辑规划，并将其注入动作头。
*   **关键技术细节**：
    *   **并行隐式CoT**：在输入序列中加入 `Q_vis`（16个视觉查询标记）和 `Q_lin`（4个语言查询标记）。VLM主干网络一次性输出这些标记的隐藏状态。
    *   **视觉CoT流（几何蒸馏）**：利用冻结的 **Depth Anything 3 (DA3)** 作为教师模型。通过交叉注意力机制将 `Q_vis` 的状态还原为高分辨率特征图，并使用 MSE 损失进行对齐，使模型掌握底层3D空间结构。
    *   **语言CoT流（步骤级监督）**：利用冻结的小型语言模型（如 **Qwen3-0.6B**）作为解码器。将 `Q_lin` 的状态作为前缀，监督其生成显式的任务规划文本（状态跟踪、空间定位、动作制定），从而让模型内化高层逻辑。
    *   **动作生成**：采用基于 **Flow-Matching** 的 **Diffusion Transformer (DiT)** 动作头，将推理增强后的隐藏状态作为上下文输入，预测连续动作。
    *   **推理优化**：训练完成后丢弃辅助教师模块，仅保留 VLM 和动作头，实现单步推理。

### 3. 实验设计
*   **实验场景与基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件，评估长程操作能力。
    *   **RoboCasa GR1**：涉及29自由度灵巧手的复杂桌面任务，评估精细感知能力。
    *   **真实世界实验**：基于 AgileX Cobot 双臂机器人，设计了简单（放面包）、中等（放方块）、困难（收集水果）三种任务。
*   **对比方法**：
    *   **非CoT模型**：OpenVLA, $\pi_0$, PD-VLA, GR00T系列。
    *   **自回归CoT模型**：CoT-VLA, ThinkAct, DeepThinkVLA, LaRA-VLA等。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H100 GPU** 进行训练。
*   **模型规模**：VLM主干采用 **Qwen3-VL-4B**，辅助语言解码器为 **Qwen3-0.6B**。
*   **训练细节**：LIBERO 学习率为 2.5e-5，Batch Size 为 48；RoboCasa 学习率为 3e-5，Batch Size 为 256。文中未明确给出总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   LIBERO 每个任务套件评估 500 个回合。
    *   RoboCasa 每个任务评估 50 个回合（共24个任务）。
    *   真实世界每个任务进行 25 次试验。
*   **充分性评价**：实验设计非常充分且客观。不仅涵盖了不同自由度的机器人（7自由度到29自由度），还包含了消融实验（验证视觉/语言CoT的各自贡献）和推理延迟分析，对比了当前最先进的多种基准模型。

### 6. 主要结论与发现
*   **性能领先**：在 LIBERO 上达到 98.8% 的平均成功率，在 RoboCasa 上达到 55.1%，均显著优于现有 SOTA 模型。
*   **推理效率**：相比自回归 CoT 模型（延迟约 3178ms），DualCoT-VLA 的总推理延迟仅为 **83.2ms**，仅比非 CoT 模型增加约 7ms，完全满足实时控制需求。
*   **模态互补**：消融实验证明，视觉 CoT 对空间任务提升显著，语言 CoT 对长程逻辑任务至关重要，两者结合效果最优。
*   **隐式推理有效性**：通过可视化探针证明，隐式标记确实捕捉到了深度几何信息和逻辑规划步骤。

### 7. 优点
*   **高效性**：通过并行推理机制解决了 CoT 模型长期存在的推理速度瓶颈。
*   **全面性**：首次在 VLA 中系统性地结合了底层几何蒸馏与高层逻辑监督。
*   **鲁棒性**：隐式推理避免了显式 Token 生成中的幻觉累积和复合误差。
*   **易部署**：推理时无需辅助模型，架构简洁。

### 8. 不足与局限
*   **数据依赖**：语言 CoT 的训练依赖高质量的文本标注（虽然文中使用了 Qwen3-VL-32B 自动生成，但标注质量仍会限制上限）。
*   **查询标记数量固定**：视觉（16个）和语言（4个）标记的数量是经验设定的，对于极度复杂的任务，固定数量的标记可能存在信息瓶颈。
*   **泛化边界**：虽然在多个基准上表现优异，但对于完全未见过的开放域极端环境，其几何蒸馏的泛化能力仍有待进一步验证。

（完）
