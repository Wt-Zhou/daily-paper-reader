---
title: Do World Action Models Generalize Better than VLAs? A Robustness Study
title_zh: 世界动作模型是否比 VLA 具有更好的泛化能力？一项鲁棒性研究
authors: "Zhanguang Zhang, Zhiyuan Li, Behnam Rahmati, Rui Heng Yang, Yintao Ma, Amir Rasouli, Sajjad Pakdamansavoji, Yangzheng Wu, Lingfeng Zhang, Tongtong Cao, Feng Wen, Xingyue Quan, Yingxue Zhang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22078v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 世界动作模型与VLA的鲁棒性对比研究
tldr: 本研究对比了视觉-语言-动作模型（VLA）与新兴的世界动作模型（WAM）在机器人任务中的泛化性。VLA 虽在多任务中表现出色，但受限于训练数据，对未知场景和环境扰动较为敏感；而 WAM 利用大规模视频预训练的时空先验预测未来状态，展现出更强的鲁棒性。通过在 LIBERO-Plus 等基准上的测试，研究发现 WAM 在应对视觉和语言扰动时具有显著优势，为构建更具泛化能力的机器人策略提供了重要参考。
selection_source: fresh_fetch
motivation: 探讨基于视频预训练的世界动作模型（WAMs）是否比传统的视觉-语言-动作模型（VLAs）在面对未知场景和环境扰动时具有更强的泛化能力。
method: 在 LIBERO-Plus 和 RoboTwin 2.0-Plus 基准测试中，对比评估了多种最先进的 VLA 策略与 WAMs 在各类视觉和语言扰动下的性能表现。
result: 实验显示 WAMs（如 LingBot-VA 和 Cosmos-Policy）表现出极强的鲁棒性，而 VLAs 虽能通过大规模数据训练达到类似效果，但其泛化效率通常较低。
conclusion: 视频预训练赋予的显式动态预测能力和时空先验是提升机器人策略鲁棒性的关键，且视频先验的集成方式对模型性能至关重要。
---

## 摘要
现实世界中的机器人动作规划具有挑战性，因为它不仅需要理解环境的当前状态，还需要预测环境将如何随动作而演变。视觉-语言-动作（VLA）模型通过利用动作专家数据，将大规模视觉-语言模型重新用于机器人动作生成，在各种机器人任务中取得了显著成功。然而，它们的性能仍受限于训练数据的范围，在未见场景中表现出有限的泛化能力，且容易受到各种上下文扰动的影响。最近，世界模型作为 VLA 的替代方案被重新审视。这些被称为世界动作模型（WAMs）的模型建立在世界模型之上，后者在大型视频数据集上进行训练以预测未来状态。经过少量调整，它们的潜在表示即可解码为机器人动作。有观点认为，它们显式的动态预测能力，结合从互联网规模视频预训练中获得的时空先验，使 WAMs 能够比 VLAs 更有效地进行泛化。在本文中，我们对当前最先进的 VLA 策略和最近发布的 WAMs 进行了对比研究。我们在各种视觉和语言扰动下，评估了它们在 LIBERO-Plus 和 RoboTwin 2.0-Plus 基准测试中的性能。结果表明，WAMs 表现出强大的鲁棒性，其中 LingBot-VA 在 RoboTwin 2.0-Plus 上达到了 74.2% 的成功率，Cosmos-Policy 在 LIBERO-Plus 上达到了 82.2%。虽然像 $π_{0.5}$ 这样的 VLA 在某些任务上可以达到相当的鲁棒性，但它们通常需要使用多样化的机器人数据集和多样的学习目标进行广泛训练。部分结合了基于视频的动态学习的混合方法表现出中等的鲁棒性，突显了视频先验集成方式的重要性。

## Abstract
Robot action planning in the real world is challenging as it requires not only understanding the current state of the environment but also predicting how it will evolve in response to actions. Vision-language-action (VLA), which repurpose large-scale vision-language models for robot action generation using action experts, have achieved notable success across a variety of robotic tasks. Nevertheless, their performance remains constrained by the scope of their training data, exhibiting limited generalization to unseen scenarios and vulnerability to diverse contextual perturbations. More recently, world models have been revisited as an alternative to VLAs. These models, referred to as world action models (WAMs), are built upon world models that are trained on large corpora of video data to predict future states. With minor adaptations, their latent representation can be decoded into robot actions. It has been suggested that their explicit dynamic prediction capacity, combined with spatiotemporal priors acquired from web-scale video pretraining, enables WAMs to generalize more effectively than VLAs. In this paper, we conduct a comparative study of prominent state-of-the-art VLA policies and recently released WAMs. We evaluate their performance on the LIBERO-Plus and RoboTwin 2.0-Plus benchmarks under various visual and language perturbations. Our results show that WAMs achieve strong robustness, with LingBot-VA reaching 74.2% success rate on RoboTwin 2.0-Plus and Cosmos-Policy achieving 82.2% on LIBERO-Plus. While VLAs such as $π_{0.5}$ can achieve comparable robustness on certain tasks, they typically require extensive training with diverse robotic datasets and varied learning objectives. Hybrid approaches that partially incorporate video-based dynamic learning exhibit intermediate robustness, highlighting the importance of how video priors are integrated.

---

## 论文详细总结（自动生成）

这是一份关于论文《Do World Action Models Generalize Better than VLAs? A Robustness Study》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管视觉-语言-动作模型（VLA）在机器人任务中取得了成功，但它们在面对未见场景时泛化能力有限，且对环境扰动（如光照、噪声、遮挡）非常敏感。
*   **核心问题**：新兴的“世界动作模型”（World Action Models, WAMs）——即基于大规模视频预训练、具备显式未来状态预测能力的模型——是否比传统的 VLA 具有更好的鲁棒性和泛化能力？
*   **背景**：VLA 通常将视觉和语言映射到动作，而 WAM 则利用视频生成模型作为骨干，通过预测环境如何随动作演变（时空先验）来辅助决策。

### 2. 论文提出的方法论
*   **核心思想**：对比研究两类架构。
    *   **VLA 架构**：基于视觉语言模型（VLM），通过自回归 token 预测或扩散过程直接输出动作。
    *   **WAM 架构**：基于视频扩散/流匹配模型（如 Cosmos, Wan2.2），将机器人状态和动作编码为潜在帧，通过预测未来视频帧（$p(h_{t+1}|h_t)$）或联合去噪状态与动作（$p(h_{t+1}, a_t|h_t)$）来生成控制指令。
*   **关键技术细节**：
    *   **时空先验集成**：WAM 利用互联网规模的视频数据预训练，获取物理世界的动态规律。
    *   **动作解码**：WAM 通常采用轻量级修改，将视频模型的潜在表示解码为机器人关节或末端执行器位姿。
    *   **混合方法**：如 VLA-JEPA 或 MOTUS，在 VLA 基础上增加未来状态预测作为辅助任务。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **LIBERO-Plus**：针对单臂操作的鲁棒性基准。
    *   **RoboTwin 2.0-Plus**：论文自研的增强版基准，基于双臂 Aloha-Agilex 平台，引入了 7 类、21 个子维度的扰动。
*   **扰动类型**：相机视角（Camera）、机器人初始状态（Robot）、语言指令（Language）、光照（Light）、背景（Background）、传感器噪声（Noise）、物体布局（Layout）。
*   **对比方法**：
    *   **VLA 类**：$\pi_0, \pi_{0.5}$, OpenVLA, X-VLA, UniVLA 等。
    *   **WAM 类**：Cosmos-Policy, GE-Act, LingBot-VA。
    *   **混合类**：MOTUS, VLA-JEPA。

### 4. 资源与算力
*   **训练细节**：文中提到 $\pi_{0.5}$ 在 RoboTwin 2.0 数据集上使用 JAX 框架微调了 60k 个梯度步（Batch Size 64）。
*   **算力限制说明**：论文明确指出 **DreamZero**（基于 14B 参数的 Wan2.1）由于算力需求极高且推理预热时间超过 15 分钟，无法进行大规模基准测试评估。
*   **未明确信息**：文中未详细列出训练所有模型所使用的 GPU 具体型号（如 A100/H100）的总数及总训练时长，但提供了详细的推理延迟（Runtime）对比。

### 5. 实验数量与充分性
*   **实验规模**：在 RoboTwin 2.0-Plus 上涵盖了 50 个协作任务，每个配置运行 50 个 episode。在 LIBERO-Plus 上评估了超过 10 种主流模型。
*   **充分性评价**：实验设计非常系统，涵盖了从单臂到双臂、从简单到复杂的多种扰动组合。
*   **客观性与公平性**：作者通过在相同硬件上重新运行开源模型（如 $\pi_{0.5}$ 的 JAX 版本）并统一动作空间配置，确保了对比的公平性。

### 6. 主要结论与发现
*   **WAM 的优势**：WAM（如 Cosmos-Policy 和 LingBot-VA）在**视觉扰动**（噪声、光照、布局）下表现出极强的鲁棒性，这归功于视频预训练提供的物理动态先验。
*   **VLA 的表现**：顶尖 VLA（如 $\pi_{0.5}$）虽然能通过海量机器人数据达到类似的鲁棒性，但其训练效率较低，且对视觉变化的适应性通常弱于 WAM。
*   **共同弱点**：两类模型在面对**相机视角**和**机器人初始位姿**的大幅变化时仍然表现脆弱，说明视频先验对几何空间变化的补偿有限。
*   **推理开销**：WAM 的推理速度显著慢于 VLA。例如，WAM 的单步推理延迟通常是 $\pi_{0.5}$ 的 **4.8 倍到 83 倍**，这限制了其在实时任务中的部署。

### 7. 优点
*   **系统性对比**：首次针对 WAM 和 VLA 进行了深入的鲁棒性量化对比，填补了领域空白。
*   **基准贡献**：提出的 RoboTwin 2.0-Plus 为双臂机器人的鲁棒性研究提供了高质量的工具。
*   **洞察深刻**：揭示了视频预训练如何简化策略训练过程（只需学习动作映射，无需从头学习物理动态）。

### 8. 不足与局限
*   **推理效率**：WAM 的高延迟是其实际应用的最大障碍，论文虽提到了优化技术，但尚未完全解决实时性问题。
*   **几何泛化不足**：实验显示 WAM 依然难以处理大幅度的空间几何变换。
*   **模型覆盖**：部分超大规模模型（如 DreamZero）因资源限制未能包含在定量对比中，可能导致对 WAM 潜力评估的不完全。

（完）
