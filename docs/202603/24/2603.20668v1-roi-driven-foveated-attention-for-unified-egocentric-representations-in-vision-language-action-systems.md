---
title: ROI-Driven Foveated Attention for Unified Egocentric Representations in Vision-Language-Action Systems
title_zh: ROI驱动的凹式注意力：用于视觉-语言-动作系统中的统一第一人称视角表征
authors: "Xinhai Sun, Xiang Shi, Menglin Zou, Wenlong Huang"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20668v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 视觉-语言-动作系统的ROI驱动表示
tldr: 针对具身智能中视觉-语言-动作（VLA）模型面临的数据采集成本高、跨机器人对齐难及互联网数据迁移差等挑战，本文提出了一种基于感兴趣区域（ROI）驱动的工程工作流。该方法通过正向运动学将末端执行器位姿投影至外部相机，生成以手部为中心的ROI，在保留全局上下文的同时提升了关键接触区域的信息密度。该流程实现了跨异构机器人的表示对齐与视点归一化，为可扩展的数据采集和跨具身学习提供了实用的抽象方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型受限于高昂的数据采集成本、跨机器人对齐困难以及互联网视觉数据向机器人控制迁移效率低下的问题。
method: 提出一种基于ROI驱动的工作流，利用正向运动学将末端位姿投影到外部相机图像中，提取高分辨率的手部中心区域作为统一的具身表示。
result: 该方法在无需腕部相机的情况下，实现了保留局部细节与全局上下文的平衡，并构建了包含校准、同步及元数据管理的完整可复现流水线。
conclusion: ROI驱动的自我中心表示为跨异构机器人的数据复用和大规模具身智能学习提供了一种高效且标准化的数据抽象方式。
---

## 摘要
具身智能系统的发展日益受到物理交互数据的可用性和结构的限制。尽管视觉-语言-动作（VLA）模型近期取得了进展，但当前的流程仍面临数据采集成本高、跨具身对齐受限以及从互联网规模的视觉数据到机器人控制的迁移效果差等问题。我们提出了一种由感兴趣区域（ROI）驱动的工程工作流，引入了一种基于几何的第一人称视角数据表征。通过正向运动学（FK）将末端执行器姿态投影到单个外部摄像头中，我们在无需腕部摄像头或多视图系统的情况下，推导出与运动对齐的以手为中心的 ROI。与直接对全帧进行下采样不同，ROI 在调整大小之前从原始图像中裁剪，从而在保留全局上下文的同时，为接触关键区域保留了高局部信息密度。我们展示了一个可复现的流程，涵盖了校准、同步、ROI 生成、确定性边界处理和元数据治理。由此产生的表征实现了具身对齐和视角归一化，从而支持在异构机器人之间进行数据复用。我们认为，第一人称视角 ROI 是可扩展采集和跨具身学习的一种实用数据抽象，桥接了互联网规模的感知与特定机器人的控制。

## Abstract
The development of embodied AI systems is increasingly constrained by the availability and structure of physical interaction data. Despite recent advances in vision-language-action (VLA) models, current pipelines suffer from high data collection cost, limited cross-embodiment alignment, and poor transfer from internet-scale visual data to robot control.   We propose a region-of-interest (ROI) driven engineering workflow that introduces an egocentric, geometry-grounded data representation. By projecting end-effector poses via forward kinematics (FK) into a single external camera, we derive movement-aligned hand-centric ROIs without requiring wrist-mounted cameras or multi-view systems. Unlike directly downsampling the full frame, ROI is cropped from the original image before resizing, preserving high local information density for contact-critical regions while retaining global context.   We present a reproducible pipeline covering calibration, synchronization, ROI generation, deterministic boundary handling, and metadata governance. The resulting representation is embodiment-aligned and viewpoint-normalized, enabling data reuse across heterogeneous robots. We argue that egocentric ROI serves as a practical data abstraction for scalable collection and cross-embodiment learning, bridging internet-scale perception and robot-specific control.

---

## 论文详细总结（自动生成）

### 论文总结：ROI驱动的凹式注意力用于VLA系统中的统一第一人称表征

#### 1. 核心问题与整体含义（研究动机和背景）
该论文针对具身智能（Embodied AI）领域中视觉-语言-动作（VLA）模型面临的三大痛点：**高昂的数据采集成本**、**跨机器人（Cross-embodiment）对齐困难**以及**互联网规模视觉数据向机器人控制迁移效率低下**。
传统的解决方案通常依赖多视图相机或腕部相机（Eye-in-hand），但这增加了硬件维护、同步和校准的复杂性。论文提出了一种工程化的工作流，通过将机器人的正向运动学（FK）投影到单一外部相机，生成以手部为中心的“感兴趣区域”（ROI）。这种方法旨在模仿人类视觉的“凹式注意力”（Foveated Attention），在不增加硬件负担的前提下，提供高分辨率的局部交互细节，从而实现标准化的数据抽象。

#### 2. 核心方法论
论文提出了一套确定性的“FK-to-ROI”生成流水线，核心步骤如下：
*   **坐标投影**：利用机器人关节编码器数据，通过正向运动学（FK）计算末端执行器在基座坐标系下的位姿，再利用已校准的外参将其变换至相机坐标系，最后通过针孔相机模型投影至图像像素坐标。
*   **具身感知偏移（Embodiment-aware Offset）**：为了避免ROI中心仅停留在手腕处，引入了一个偏移量 $\beta$，使ROI中心向夹持器内部（接触区域）偏移，确保捕捉到关键的交互细节。
*   **确定性边界处理**：当末端执行器移出画面或靠近边缘时，采用零填充（Zero Padding）保持ROI尺寸固定（如 $256 \times 256$），确保模型输入的连续性。
*   **结构化注意力强制（Attention Forcing）**：在模型训练中，将ROI区域作为独立的Token序列与全局图像Token拼接。由于ROI在Token空间中占比更高且分辨率更高，这在不改变Transformer架构的情况下，诱导模型产生对交互区域的注意力偏置。
*   **元数据治理**：定义了一套包含校准版本、投影置信度、填充比例等字段的元数据架构，确保数据在不同机器人平台间的可复现性和可审计性。

#### 3. 实验设计
需要注意的是，**本论文主要是一篇工程方法论和协议（Protocol）论文**，而非单纯的实验结果报告。
*   **对比策略**：论文在理论和工程层面对比了四种策略：单一外部相机、腕部相机、多视图设置以及本文提出的“单一相机+FK投影ROI”。
*   **评估协议**：提出了两种评估路径：
    *   **路径A（数据集改造）**：对现有包含校准和状态日志的数据集进行离线ROI生成，对比“仅全局视图”与“全局+ROI”的训练效率。
    *   **路径B（仿真/回放）**：在仿真环境中验证投影逻辑和边界填充在跨具身部署时的鲁棒性。
*   **衡量指标**：除了控制成功率，还提出了工程指标（部署时间、传感器带宽）、几何指标（ROI覆盖率、抖动）和学习指标（数据效率、动作稳定性）。

#### 4. 资源与算力
论文中**未明确说明**具体的GPU型号、数量或训练时长。
*   原因在于该文侧重于数据工程工作流和系统架构的定义。
*   文中提到了其团队之前的相关工作 `SaiVLA-0`，暗示该方法已集成到其现有的VLA训练管线中，但未在本篇论文中列出具体的算力消耗数值。

#### 5. 实验数量与充分性
*   **实验组数**：论文更多地展示了算法逻辑的推导、元数据Schema的定义以及定性的对比分析（如表I）。
*   **充分性与客观性**：从工程实现的角度看，论文对校准漂移、时间同步、遮挡等实际部署中的“坑”进行了详尽的讨论，并给出了质量门控（Quality Gates）建议。然而，由于缺乏大规模Benchmark的量化对比数据，其在提升控制精度方面的实际增益仍需通过后续实验进一步验证。

#### 6. 主要结论与发现
*   **ROI作为数据抽象**：FK投影的ROI提供了一种视角归一化、具身对齐的表征，能够桥接互联网感知数据与特定机器人的控制。
*   **信息密度优势**：通过计算证明，ROI分支比全局下采样图像在局部细节上的采样密度高出约5倍。
*   **降低运维负担**：该方法消除了腕部相机的布线和频繁校准需求，将硬件复杂性转嫁给可版本化的软件变换。
*   **注意力偏置**：通过Token拼接实现的结构化偏置，无需辅助损失函数即可强化模型对接触区域的感知。

#### 7. 优点
*   **工程落地性强**：详细定义了元数据字段和质量校验流程，对工业界构建可扩展的机器人数据集具有直接指导意义。
*   **低成本高效率**：利用现有的FK数据和单相机实现多相机/腕部相机的效果，极大地降低了数据采集的门槛。
*   **确定性与可复现性**：强调“确定性生成”而非“学习到的注意力”，保证了数据处理环节的透明度和可追溯性。

#### 8. 不足与局限
*   **校准依赖性**：ROI的质量高度依赖于相机外参和FK的准确性，一旦发生物理碰撞导致相机微移，ROI可能会偏离目标。
*   **遮挡问题**：在某些视角下，机械臂自身会遮挡交互区域，单相机方案无法像多视图那样通过视角切换解决遮挡。
*   **缺乏量化结果**：论文未提供在标准基准（如Open X-Embodiment）上的具体性能提升数值，目前更多停留在架构和协议层面。
*   **上下文丢失风险**：过度关注ROI（Foveation）可能会导致模型忽略全局环境中的长程约束或多目标关系。

（完）
