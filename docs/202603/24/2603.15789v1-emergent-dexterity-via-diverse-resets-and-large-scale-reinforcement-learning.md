---
title: Emergent Dexterity via Diverse Resets and Large-Scale Reinforcement Learning
title_zh: 通过多样化重置和大规模强化学习实现的涌现灵巧性
authors: "Patrick Yin, Tyler Westenbroek, Zhengyu Zhang, Joshua Tran, Ignacio Dagnino, Eeshani Shilamkar, Numfor Mbiziwo-Tiapo, Simran Bagaria, Xinlei Liu, Galen Mullins, Andrey Kolobov, Abhishek Gupta"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15789v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 用于灵巧操作任务的强化学习
tldr: 本研究针对当前机器人强化学习在复杂灵巧操作中依赖人工设计、难以扩展的问题，提出了 OmniReset 框架。该方法摒弃了复杂的课程学习和人类演示，核心在于利用大规模并行仿真，通过程序化生成的多元化重置状态，让机器人系统地探索各种交互场景。OmniReset 仅需单一奖励函数和固定超参数，即可在长程、多接触任务中表现出卓越的鲁棒性，并能通过增加计算量持续提升性能，最终实现的视觉运动策略可成功进行零样本真机迁移。
selection_source: fresh_fetch
motivation: 传统的强化学习在处理长程、多接触的灵巧操作任务时，往往需要大量的人工工程且难以随计算规模有效扩展。
method: 引入 OmniReset 框架，通过程序化生成多样化的模拟器重置点，系统性地让强化学习算法接触到各种机器人与物体的交互状态。
result: 在没有人类演示和课程学习的情况下，该方法在多种复杂任务上超越了现有基准，并实现了高成功率的零样本真机迁移。
conclusion: 实验证明，通过多样化重置将计算能力直接转化为行为覆盖，是实现鲁棒且可扩展的灵巧操作策略的关键。
---

## 摘要
大规模并行物理模拟中的强化学习推动了机器人从模拟到现实（sim-to-real）学习的重大进展。然而，当前的方法仍然脆弱且具有任务特定性，依赖于大量的针对每个任务的工程设计，如奖励函数、课程学习和演示。即使经过这些工程设计，它们在长时程、接触丰富的操纵任务中也经常失败，并且无法有效地随计算量扩展，因为当训练反复访问状态空间中相同的狭窄区域时，性能会迅速饱和。我们介绍了 OmniReset，这是一个简单且可扩展

## Abstract
Reinforcement learning in massively parallel physics simulations has driven major progress in sim-to-real robot learning. However, current approaches remain brittle and task-specific, relying on extensive per-task engineering to design rewards, curricula, and demonstrations. Even with this engineering, they often fail on long-horizon, contact-rich manipulation tasks and do not meaningfully scale with compute, as performance quickly saturates when training revisits the same narrow regions of state space. We introduce \Method, a simple and scalable framework that enables on-policy reinforcement learning to robustly solve a broad class of dexterous manipulation tasks using a single reward function, fixed algorithm hyperparameters, no curricula, and no human demonstrations. Our key insight is that long-horizon exploration can be dramatically simplified by using simulator resets to systematically expose the RL algorithm to the diverse set of robot-object interactions which underlie dexterous manipulation. \Method\ programmatically generates such resets with minimal human input, converting additional compute directly into broader behavioral coverage and continued performance gains. We show that \Method\ gracefully scales to long-horizon dexterous manipulation tasks beyond the capabilities of existing approaches and is able to learn robust policies over significantly wider ranges of initial conditions than baselines. Finally, we distill \Method \ into visuomotor policies which display robust retrying behavior and substantially higher success rates than baselines when transferred to the real world zero-shot. Project webpage: https://omnireset.github.io

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **OmniReset** 的框架，旨在通过大规模并行仿真和多样化的重置策略，解决机器人灵巧操作任务中强化学习（RL）难以扩展和依赖人工工程的问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大规模并行仿真推动了机器人学习的进展，但现有的强化学习方法在处理长程（long-horizon）、接触密集（contact-rich）的灵巧操作任务时仍然非常脆弱。
*   **核心瓶颈**：目前的 RL 往往依赖于针对特定任务的奖励函数设计、复杂的课程学习（Curricula）或人类演示。此外，简单的增加计算量往往会导致性能在狭窄的状态空间内迅速饱和，无法产生真正的鲁棒性。
*   **整体含义**：论文提出，通过系统性地利用模拟器重置（Resets）来覆盖机器人与物体交互的各种模式，可以将计算能力直接转化为行为覆盖范围，从而让复杂的灵巧行为（如翻转、对准、拧动）在无需人类引导的情况下自然涌现。

### 2. 方法论
*   **核心思想**：**OmniReset** 不再试图通过复杂的算法来解决探索难题，而是通过程序化地生成多样化的初始状态（重置点），强制 RL 算法接触到操作任务中的关键交互阶段。
*   **关键技术细节（四种重置分布）**：
    1.  **到达重置（Reaching Resets）**：物体在桌面上随机分布，机械臂在工作空间内随机初始化，训练基础的接近能力。
    2.  **近物重置（Near-Object Resets）**：机械臂初始化在预先计算的抓取点附近，涵盖了从非抓取交互到启动抓取的各种状态。
    3.  **稳定抓取重置（Stable Grasp Resets）**：物体被初始化在空中，机械臂已处于稳定抓取状态，专注于搬运和空中调整。
    4.  **近目标重置（Near-Goal Resets）**：物体初始化在目标位置附近（如插销已部分进入孔中），专注于解决最困难的接触密集阶段（如插入、拧紧）。
*   **算法流程**：
    *   使用 **PPO** 算法进行大规模并行训练。
    *   采用**异步 Actor-Critic 架构**：Actor 仅观察历史状态，Critic 观察特权信息（Privileged information）。
    *   使用**广义状态依赖探索噪声（gSDE）**：允许策略在不同状态下学习时间相关的探索策略。
    *   **任务无关的奖励函数**：所有任务共用一个简单的奖励公式（成功奖励 + 距离惩罚 + 接近奖励 + 平滑惩罚），无需针对特定任务调优。

### 3. 实验设计
*   **实验场景**：包含了 6 种复杂的灵巧操作任务：
    *   **桌腿拧入（Leg Twisting）**：拾取桌腿并将其拧入孔中。
    *   **抽屉插入（Drawer Insertion）**：翻转并推入抽屉。
    *   **插销插入（Peg Insertion）**、**方块堆叠（Cube Stacking）**、**墙边对齐（Wall Slide）**、**纸杯蛋糕放置（Cupcake Placement）**。
*   **Benchmark 与对比方法**：
    *   **BC-PPO**：结合行为克隆损失的 PPO。
    *   **DeepMimic**：基于演示的奖励增强方法。
    *   **Demo Curriculum**：基于演示的成功加权自动课程学习。
*   **实验变体**：分为“简单（Easy）”和“困难（Hard）”两种初始条件分布，后者具有极大的随机性。

### 4. 资源与算力
*   **算力规模**：论文强调了“大规模并行”，在消融实验中展示了使用 **4,096 到 65,536 个并行环境**进行训练的效果。
*   **硬件/时长**：文中提到使用了 **IsaacLab** 仿真框架。虽然未明确给出具体的 GPU 型号和总训练小时数，但从并行规模（6.5万个环境）来看，通常需要多块高性能 GPU（如 A100 或 H100）支持。

### 5. 实验数量与充分性
*   **实验组数**：针对 6 个任务分别进行了 Hard/Easy 版本的对比实验，并进行了多项消融实验（包括并行环境数量、抓取采样范围、重置分布的影响）。
*   **充分性与客观性**：实验设计较为充分，不仅对比了成功率，还通过扰动实验测试了策略的**鲁棒性**。此外，还进行了**真机迁移实验**，通过蒸馏（Distillation）将状态策略转为视觉运动策略（Visuomotor Policy），验证了实用性。

### 6. 主要结论与发现
*   **规模效应**：随着并行环境数量的增加，OmniReset 的性能持续提升，而基准方法在复杂任务上往往无法收敛。
*   **涌现行为**：在没有演示的情况下，机器人学会了复杂的策略，例如在拧桌腿前先利用桌面调整抓取姿态，或在抽屉插入失败后自动重试。
*   **鲁棒性**：相比于依赖演示的方法，OmniReset 训练出的策略在面对初始位置大幅偏移或外部力扰动时表现出极强的韧性。
*   **零样本迁移**：蒸馏后的视觉策略在真实世界中实现了 25% 的零样本成功率（对比 Diffusion Policy 的 4%），并展现出明显的纠错重试行为。

### 7. 优点
*   **极简性**：摒弃了复杂的探索奖励和人工设计的课程，回归到“数据覆盖 + 大规模计算”的本质。
*   **通用性**：一套超参数和奖励函数适用于多种截然不同的任务，降低了机器人学习的门槛。
*   **鲁棒性强**：由于训练覆盖了极广的状态空间，策略不会因为轻微的分布偏移而崩溃。

### 8. 不足与局限
*   **单任务限制**：目前的研究主要集中在单任务、单物体的学习上，尚未扩展到大规模多任务并行学习。
*   **依赖仿真信息**：生成重置点需要物体的几何信息和基本的抓取采样，对于完全未知的物体可能需要额外的预处理。
*   **真机成功率仍有提升空间**：虽然优于基准，但 25% 的真机零样本成功率说明模拟与现实之间（Sim-to-Real）的鸿沟在极高精度任务中依然存在。

（完）
