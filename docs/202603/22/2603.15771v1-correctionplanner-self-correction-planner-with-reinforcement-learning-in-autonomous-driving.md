---
title: "CorrectionPlanner: Self-Correction Planner with Reinforcement Learning in Autonomous Driving"
title_zh: CorrectionPlanner：自动驾驶中结合强化学习的自我修正规划器
authors: "Yihong Guo, Dongqiangzi Ye, Sijia Chen, Anqi Liu, Xianming Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15771v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 自动驾驶中结合强化学习的自回归规划器
tldr: 针对自动驾驶中学习型规划器缺乏自我纠错能力的问题，本文提出CorrectionPlanner。该规划器采用自回归方式，通过“提议-评估-纠错”循环生成运动轨迹。利用碰撞评论器识别潜在风险，并将不安全动作记录为纠错轨迹以引导生成安全动作。结合模仿学习与强化学习训练，显著提升了规划的安全性和性能。
selection_source: fresh_fetch
motivation: 现有的学习型自动驾驶规划器在生成不安全动作后缺乏显式的自我纠错机制，导致安全性难以保障。
method: 提出一种基于运动Token的自回归规划器，通过碰撞评论器评估动作，并在检测到风险时利用纠错轨迹引导模型重新生成安全动作。
result: "在Waymax基准测试中碰撞率降低了20%以上，并在nuPlan挑战赛中取得了最先进的规划得分。"
conclusion: 引入自我纠错机制和强化学习训练能有效增强自动驾驶规划器的安全性，为复杂场景下的可靠决策提供了新思路。
---

## 摘要
自动驾驶需要安全的规划，但大多数基于学习的规划器缺乏显式的自我修正能力：一旦提出了不安全的动作，就没有机制对其进行修正。因此，我们提出了 CorrectionPlanner，这是一种具有自我修正能力的自回归规划器，它将规划建模为“提议、评估和修正”循环中的运动标记（motion-token）生成。在每个规划步骤中，策略会提议一个动作（即运动标记），而一个经过训练的碰撞评论器（collision critic）会预测该动作是否会在短时间内引发碰撞。如果评论器预测到碰撞，我们将历史不安全运动标记序列保留为自我修正轨迹（self-correction trace），并以此为条件生成下一个运动标记，重复此过程直到提议出安全的运动标记或满足安全标准。这种由所有不安全运动标记组成的自我修正轨迹，代表了规划器在运动标记空间中的修正过程，类似于语言模型中的推理轨迹（reasoning trace）。我们首先使用模仿学习训练规划器，随后利用预训练世界模型的展开（rollouts）进行基于模型的强化学习，该世界模型能够真实地模拟智能体的反应行为。闭环评估表明，CorrectionPlanner 在 Waymax 上的碰撞率降低了 20% 以上，并在 nuPlan 上实现了最先进的规划得分。

## Abstract
Autonomous driving requires safe planning, but most learning-based planners lack explicit self-correction ability: once an unsafe action is proposed, there is no mechanism to correct it. Thus, we propose CorrectionPlanner, an autoregressive planner with self-correction that models planning as motion-token generation within a propose, evaluate, and correct loop. At each planning step, the policy proposes an action, namely a motion token, and a learned collision critic predicts whether it will induce a collision within a short horizon. If the critic predicts a collision, we retain the sequence of historical unsafe motion tokens as a self-correction trace, generate the next motion token conditioned on it, and repeat this process until a safe motion token is proposed or the safety criterion is met. This self-correction trace, consisting of all unsafe motion tokens, represents the planner's correction process in motion-token space, analogous to a reasoning trace in language models. We train the planner with imitation learning followed by model-based reinforcement learning using rollouts from a pretrained world model that realistically models agents' reactive behaviors. Closed-loop evaluations show that CorrectionPlanner reduces collision rate by over 20% on Waymax and achieves state-of-the-art planning scores on nuPlan.

---

## 论文详细总结（自动生成）

以下是对论文《CorrectionPlanner: Self-Correction Planner with Reinforcement Learning in Autonomous Driving》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的基于学习的自动驾驶规划器（Learning-based Planners）大多缺乏显式的“自我修正”能力。一旦模型生成了一个不安全的动作，系统通常会直接执行，而没有内部机制在执行前对其进行评估和调整。
*   **研究背景**：受大语言模型（LLM）中“推理轨迹”（Reasoning Trace）和自我反思机制的启发，作者希望在运动规划领域引入类似的逻辑。即在动作执行前，通过一个内部循环不断优化提议，直到动作满足安全标准。

### 2. 方法论
*   **核心思想**：将规划建模为“提议-评估-修正”（Propose-Evaluate-Correct）的自回归过程。
*   **关键技术细节**：
    *   **运动标记化（Motion Tokenization）**：将连续轨迹离散化为1024个Token的词表，每个Token代表0.5秒的位移和航向变化。
    *   **自我修正循环**：
        1.  **提议**：策略网络生成一个候选运动Token。
        2.  **评估**：一个学习到的“碰撞评论器”（Collision Critic）预测该Token在短期内（如2.5秒）是否会导致碰撞。
        3.  **修正**：若预测有风险，该Token不被执行，而是作为“纠错轨迹”（Correction Trace）保留。策略网络根据历史信息和纠错轨迹重新生成Token，直到安全或达到最大修正次数。
    *   **两阶段训练方案**：
        1.  **模仿学习（IL）**：在专家数据上进行Token预测训练，并初步引入纠错损失。
        2.  **基于模型的强化学习（RL）**：利用预训练的反应式世界模型（World Model）进行闭环仿真。使用 REINFORCE 算法配合 KL 散度正则化，优化策略以最大化包含安全和进度的奖励函数。

### 3. 实验设计
*   **数据集与场景**：
    *   **WOMD (Waymo Open Motion Dataset)**：使用 Waymax 模拟器进行闭环评估。
    *   **nuPlan**：在 Val14、Test-Hard 和 Test-Random 等标准分割集上评估。
*   **Benchmark 与对比方法**：
    *   对比了包括 **SMART**（基础世界模型）、**DiffusionPlanner**（基于扩散模型）、**PlanTF**、**PLUTO**、**PlanT** 等多种最先进的（SOTA）学习型规划器。
    *   设置了反应式（Reactive）和非反应式（Non-Reactive）两种仿真模式。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **8 台 NVIDIA H800 GPU**。
*   **训练细节**：
    *   模仿学习阶段训练 16 个 Epoch。
    *   强化学习阶段训练 3 个 Epoch（针对约 10 万个困难样本进行优化）。
    *   推理延迟：在 H800 上，最佳配置下的单条轨迹推理时间约为 0.434 秒，相比无纠错模型（0.357秒）增加较小。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了两个主流的大规模自动驾驶数据集，进行了大量的闭环仿真。
*   **消融实验**：非常充分。作者对比了：
    *   有无 RL、有无纠错机制的性能差异。
    *   纠错机制与传统的“拒绝采样”（Rejection Sampling）和“候选筛选”（Candidate Selection）的对比。
    *   不同碰撞分类阈值和最大纠错长度对安全与进度的影响。
    *   **零样本泛化实验**：在 nuPlan 上训练并在 Waymax 上测试，验证了模型的鲁棒性。
*   **客观性**：通过多指标（碰撞率、进度、得分）综合评价，展示了安全与效率之间的权衡，结果较为客观。

### 6. 主要结论与发现
*   **安全性显著提升**：在 Waymax 模拟器中，CorrectionPlanner 将碰撞率降低了 **20% 以上**。
*   **SOTA 性能**：在 nuPlan 榜单上取得了领先的规划得分，尤其在反应式仿真环境下表现优异。
*   **纠错轨迹的有效性**：实验证明，将不安全动作作为上下文（Context）输入模型，比简单的重新采样更能引导模型避开危险区域。
*   **安全-进度权衡**：增加纠错频率和长度会显著降低碰撞，但可能导致车辆行驶过于保守，从而轻微影响进度（Progression）。

### 7. 优点
*   **机制创新**：首次在运动Token空间引入类似 LLM 推理轨迹的显式纠错机制，而非简单的后处理滤波。
*   **闭环训练**：结合反应式世界模型进行 RL 微调，解决了模仿学习无法处理“反事实”场景的问题。
*   **泛化能力**：展示了良好的跨数据集零样本迁移能力。
*   **实用性**：在显著提升安全性的同时，保持了可接受的推理延迟。

### 8. 不足与局限
*   **保守性风险**：在极端追求低碰撞率时，模型可能会表现出过度避让或行驶缓慢（进度指标下降）。
*   **评论器依赖**：系统的安全性高度依赖于“碰撞评论器”的准确性，若评论器误报或漏报，纠错机制将失效。
*   **指标覆盖**：目前主要针对碰撞进行纠错，尚未涵盖舒适度（Comfort）或交通规则违背（如压线）等其他维度的显式纠错。
*   **应用限制**：自回归生成和多次纠错循环在算力受限的嵌入式车载平台上可能仍面临实时性挑战。

（完）
