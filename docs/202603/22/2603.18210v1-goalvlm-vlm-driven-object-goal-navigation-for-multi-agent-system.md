---
title: "GoalVLM: VLM-driven Object Goal Navigation for Multi-Agent System"
title_zh: GoalVLM：面向多智能体系统的 VLM 驱动物体目标导航
authors: "MoniJesu James, Amir Atef Habel, Aleksey Fedoseev, Dzmitry Tsetserokou"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18210v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: VLM驱动的多智能体零样本目标导航
tldr: GoalVLM 是一个面向多智能体系统的零样本、开放词汇目标导航框架。它将视觉语言模型（VLM）集成到决策循环中，利用 SAM3 进行文本提示的目标检测，并通过 SpaceOM 进行空间推理。智能体构建 BEV 语义地图，利用约束引导的推理层评估探索前沿，从而在无需重新训练的情况下实现对自由文本目标的理解与定位。在 GOAT-Bench 上的实验证明了其在复杂序列导航任务中的有效性。
selection_source: fresh_fetch
motivation: 传统的多智能体目标导航受限于闭集词汇和固定类别，难以泛化到测试时的未知目标。
method: 集成 VLM、SAM3 和 SpaceOM，通过 BEV 语义地图构建、深度投影目标定位及结构化提示链推理来实现开放词汇导航。
result: "在 GOAT-Bench 验证集上，双智能体配置实现了 55.8% 的子任务成功率和 18.3% 的 SPL，性能比肩现有最先进方法。"
conclusion: 该研究证明了利用 VLM 的常识先验和零样本推理能力可以有效解决多智能体系统中的开放词汇导航挑战。
---

## 摘要
物体目标导航传统上局限于具有闭集物体词汇表的地面机器人。现有的多智能体方法依赖于与固定类别集绑定的预计算概率图，这阻碍了在测试时对新目标的泛化。我们提出了 GoalVLM，这是一个用于零样本、开放词汇物体导航的协作式多智能体框架。GoalVLM 将视觉语言模型 (VLM) 直接集成到决策循环中，利用 SAM3 进行文本提示的检测与分割，并使用 SpaceOM 进行空间推理，使智能体能够解释自由形式的语言目标，并无需重新训练即可通过零样本语义先验对边界 (frontiers) 进行评分。每个智能体通过深度投影的体素泼溅 (voxel splatting) 构建鸟瞰图 (BEV) 语义地图，同时目标投影仪 (Goal Projector) 通过校准后的深度将检测结果反向投影到地图中，以实现可靠的目标定位。约束引导的推理层通过结构化提示链（场景描述、房间类型分类、感知门控、多边界排序）评估边界，将常识先验注入探索过程。我们在 GOAT-Bench val_unseen（包含 360 个多子任务回合、1032 个连续物体目标子任务、HM3D 场景）上评估了 GoalVLM，其中每个回合需要导航到由 5-7 个开放词汇目标组成的链条。具有 N=2 个智能体的 GoalVLM 实现了 55.8% 的子任务成功率 (SR) 和 18.3% 的 SPL，在无需特定任务训练的情况下，与现有最先进方法相比具有竞争力。消融实验证实了 VLM 引导的边界推理和深度投影目标定位的贡献。

## Abstract
Object-goal navigation has traditionally been limited to ground robots with closed-set object vocabularies. Existing multi-agent approaches depend on precomputed probabilistic graphs tied to fixed category sets, precluding generalization to novel goals at test time.   We present GoalVLM, a cooperative multi-agent framework for zero-shot, open-vocabulary object navigation. GoalVLM integrates a Vision-Language Model (VLM) directly into the decision loop, SAM3 for text-prompted detection and segmentation, and SpaceOM for spatial reasoning, enabling agents to interpret free-form language goals and score frontiers via zero-shot semantic priors without retraining. Each agent builds a BEV semantic map from depth-projected voxel splatting, while a Goal Projector back-projects detections through calibrated depth into the map for reliable goal localization. A constraint-guided reasoning layer evaluates frontiers through a structured prompt chain (scene captioning, room-type classification, perception gating, multi-frontier ranking), injecting commonsense priors into exploration.   We evaluate GoalVLM on GOAT-Bench val_unseen (360 multi-subtask episodes, 1032 sequential object-goal subtasks, HM3D scenes), where each episode requires navigating to a chain of 5-7 open-vocabulary targets. GoalVLM with N=2 agents achieves 55.8% subtask SR and 18.3% SPL, competitive with state-of-the-art methods while requiring no task-specific training. Ablation studies confirm the contributions of VLM-guided frontier reasoning and depth-projected goal localization.

---

## 论文详细总结（自动生成）

以下是对论文《GoalVLM: VLM-driven Object Goal Navigation for Multi-Agent System》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的物体目标导航（ObjectNav）通常局限于单机器人、闭集词汇（固定类别）以及预定义的概率图。在面对开放词汇（如自由文本描述的目标）和多智能体协作时，现有方法难以在不重新训练的情况下实现高效的泛化与探索。
*   **研究背景**：随着视觉语言模型（VLM）的发展，机器人具备了理解复杂语义的能力。本文旨在构建一个去中心化的多智能体框架，利用 VLM 的常识推理能力，在未知环境中通过协作快速定位一系列开放词汇的目标。

### 2. 方法论
GoalVLM 采用全去中心化的多智能体架构，核心组件包括：
*   **零样本感知流水线**：
    *   **SAM3**：作为感知后端，接收文本提示进行目标的零样本检测与分割。
    *   **Goal Projector**：通过校准的深度信息将 2D 掩码反向投影到 3D 空间，并在鸟瞰图（BEV）语义地图中精确定位。
*   **空间推理与边界评分**：
    *   **SpaceOM (VLM)**：利用 VLM 的常识（如“微波炉通常在厨房”）对探索前沿（Frontiers）进行评分。
    *   **约束引导推理层**：通过结构化提示链（场景描述、房间分类、多前沿排序）引导智能体选择最有潜力的探索区域。
*   **语义地图构建与融合**：
    *   智能体通过深度投影和体素泼溅（Voxel Splatting）构建局部 BEV 地图。
    *   **共享信念协议**：智能体之间通过最大池化（Max-pooling）融合全局语义地图，并采用顺序贪婪协议分配前沿任务，避免重复探索。
*   **导航控制**：使用快速行进法（FMM）在语义占用栅格上规划路径，并通过贝叶斯价值图（Bayesian Value Map）结合 UCB 策略平衡探索与利用。

### 3. 实验设计
*   **数据集与场景**：在 **GOAT-Bench**（基于 HM3D 场景）的 `val_unseen` 拆分集上进行评估。
*   **Benchmark 任务**：包含 360 个多子任务回合，共 1032 个连续的物体目标子任务。每个回合要求智能体依次寻找 5-7 个开放词汇目标。
*   **对比方法**：
    *   **强化学习类**：SenseAct-NN。
    *   **零样本/模块化类**：CLIP on Wheels, VLMNav, DyNaVLM, 3D-Mem, Modular GOAT。
    *   **端到端训练类**：AstraNav-Memory（基于 3B 参数的 Qwen2.5-VL）。

### 4. 资源与算力
*   **仿真环境**：论文未详细列出仿真端使用的具体 GPU 型号和训练时长（由于是零样本方法，无需特定任务的训练）。
*   **物理平台**：在初步的现实世界验证中，使用了两台定制四旋翼无人机，机载计算单元为 **Orange Pi 5**，通过 WiFi 运行 ZMQ 通信桥接。

### 5. 实验数量与充分性
*   **实验规模**：完成了 357 个完整回合的测试，涵盖 1032 个子任务，样本量充足。
*   **消融实验**：针对“有无 VLM 推理”、“单智能体 vs 双智能体”、“相机投影校正”进行了对比实验。
*   **客观性**：实验遵循 GOAT-Bench 标准协议，使用了 SR（成功率）、SPL（路径长度加权成功率）和 DTG（距目标距离）等标准指标，对比涵盖了从经典模块化方法到最新大模型端到端方法的多个维度，评价较为客观。

### 6. 主要结论与发现
*   **性能表现**：双智能体 GoalVLM 实现了 **55.8% 的子任务成功率 (SR)**，远超官方基线 Modular GOAT (29.4%)，且在无需训练的情况下接近端到端训练模型 AstraNav-Memory (62.7%)。
*   **协作增益**：从单智能体增加到双智能体，SR 从 38.6% 提升至 56.2%（约 **1.5 倍提升**），证明了协作探索的有效性。
*   **稳定性**：在连续子任务中，SR 随任务顺序增加仅有轻微下降，表明基于地图的方法能有效抑制误差累积。

### 7. 优点与亮点
*   **零样本泛化**：无需针对特定环境或物体类别进行训练，具备极强的开放词汇处理能力。
*   **常识注入**：通过 VLM 将人类常识（房间与物体的关联）转化为导航决策中的先验概率，提高了探索效率。
*   **去中心化协作**：通过简单的地图融合和前沿分配协议实现了高效的多机协同，具有良好的可扩展性。
*   **实机潜力**：在物理无人机平台上验证了感知流水线的迁移能力。

### 8. 不足与局限
*   **路径效率较低**：SPL 仅为 18.3%，远低于端到端训练模型（56.9%）。这反映了模块化前沿探索方法在路径规划上存在冗余，不够直接。
*   **感知瓶颈**：SAM3 在处理透明物体（玻璃）、反射物体（镜子）和微小物体（书本、照片）时容易失效。
*   **地图维度限制**：采用 2D BEV 地图，无法有效处理多层建筑或垂直堆叠的物体。
*   **级联故障风险**：虽然整体稳定，但在 48.2% 的部分成功案例中，初始定位错误仍可能导致后续子任务失败。

（完）
