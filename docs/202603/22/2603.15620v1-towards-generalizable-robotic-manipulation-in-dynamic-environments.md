---
title: Towards Generalizable Robotic Manipulation in Dynamic Environments
title_zh: 迈向动态环境下的泛化机器人操控
authors: "Heng Fang, Shangru Li, Shuhan Wang, Xuanyang Xi, Dingkang Liang, Xiang Bai"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15620v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 使用VLA模型进行通用动态操作的基准测试
tldr: 针对视觉语言动作（VLA）模型在动态环境下操作能力不足的问题，本文提出了大规模动态操作数据集和基准测试 DOMINO，包含35个任务和超过11万条专家轨迹。同时，提出了一种感知动态的VLA架构PUMA，通过整合历史光流和世界查询来预测未来状态。实验证明PUMA在动态任务上表现优异，且动态数据训练能提升模型在静态任务中的泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型因缺乏动态数据集且依赖单帧观察，难以在包含移动目标的动态环境中进行有效的时空推理。
method: 引入了大规模动态操作数据集DOMINO，并提出PUMA架构，通过结合历史光流和世界查询来隐式预测物体未来状态。
result: "PUMA在动态任务上实现了6.3%的绝对成功率提升，刷新了现有基准的最优性能。"
conclusion: 在动态数据上进行训练能够培养出更鲁棒的时空表征，从而有效提升模型在动态及静态任务中的泛化性能。
---

## 摘要
视觉-语言-动作（VLA）模型在静态操控中表现出色，但在包含移动目标的动态环境中却面临挑战。这种性能差距主要源于动态操控数据集的匮乏，以及主流 VLA 模型对单帧观测的依赖，这限制了它们的时空推理能力。为解决这一问题，我们推出了 DOMINO，这是一个用于泛化动态操控的大规模数据集和基准测试，包含 35 个具有层级复杂度的任务、超过 11 万条专家轨迹以及一个多维评估套件。通过全面的实验，我们系统地评估了现有 VLA 模型在动态任务上的表现，探索了增强动态感知能力的有效训练策略，并验证了动态数据的泛化性。此外，我们提出了 PUMA，一种具有动态感知能力的 VLA 架构。通过整合以场景为中心的历史光流和专门的世界查询（world queries）来隐式预测以物体为中心的未来状态，PUMA 将历史感知能力与短时预测相结合。结果表明，PUMA 达到了最先进的性能，在成功率上比基准模型提高了 6.3% 的绝对值。此外，我们证明了在动态数据上进行训练可以培养出鲁棒的时空表示，并能迁移到静态任务中。所有代码和数据均可在 https://github.com/H-EmbodVis/DOMINO 获取。

## Abstract
Vision-Language-Action (VLA) models excel in static manipulation but struggle in dynamic environments with moving targets. This performance gap primarily stems from a scarcity of dynamic manipulation datasets and the reliance of mainstream VLAs on single-frame observations, restricting their spatiotemporal reasoning capabilities. To address this, we introduce DOMINO, a large-scale dataset and benchmark for generalizable dynamic manipulation, featuring 35 tasks with hierarchical complexities, over 110K expert trajectories, and a multi-dimensional evaluation suite. Through comprehensive experiments, we systematically evaluate existing VLAs on dynamic tasks, explore effective training strategies for dynamic awareness, and validate the generalizability of dynamic data. Furthermore, we propose PUMA, a dynamics-aware VLA architecture. By integrating scene-centric historical optical flow and specialized world queries to implicitly forecast object-centric future states, PUMA couples history-aware perception with short-horizon prediction. Results demonstrate that PUMA achieves state-of-the-art performance, yielding a 6.3% absolute improvement in success rate over baselines. Moreover, we show that training on dynamic data fosters robust spatiotemporal representations that transfer to static tasks. All code and data are available at https://github.com/H-EmbodVis/DOMINO.

---

## 论文详细总结（自动生成）

### 迈向动态环境下的泛化机器人操控：DOMINO 与 PUMA 总结

#### 1. 论文的核心问题与整体含义
当前视觉-语言-动作（VLA）模型在静态环境下的机器人操控已取得显著进展，但在面对移动目标或连续环境变化的**动态操控**任务时表现极差。核心瓶颈在于：
*   **数据匮乏**：现有数据集（如 Open X-Embodiment）多为静态任务，缺乏大规模、高质量的动态操控演示。
*   **架构缺陷**：主流 VLA 模型依赖单帧观测，缺乏处理时空演变和预测未来运动的能力。
本文旨在通过构建大规模动态操控基准 **DOMINO** 和提出动态感知架构 **PUMA**，填补这一空白。

#### 2. 论文提出的方法论
论文提出了 **PUMA (Predictive Unified Manipulation Architecture)**，其核心思想是将历史感知与未来预测相结合：
*   **场景中心的历史动态编码**：不直接堆叠原始历史帧，而是计算历史帧间的**光流图（Optical Flow）**。光流提供了显式的运动状态，降低了策略学习动态模式的难度。
*   **物体中心的未来状态预测**：引入 **World Queries（世界查询）**。在训练阶段，利用预训练的 DINO、GroundingDINO 和 SAM2 提取目标物体的未来特征，并以此作为辅助监督信号。
*   **双重查询机制**：模型同时输出动作块（Action Chunk）和预测的未来特征。通过最小化预测特征与真实 DINO 特征之间的余弦距离（辅助损失），迫使模型隐式建模物体的物理动态。
*   **数据生成管线**：开发了“两阶段时空同步方法”，通过“时间试运行”记录执行时间，再通过“运动反向计算”确定初始位置，从而在仿真中自动生成高质量专家轨迹。

#### 3. 实验设计
*   **数据集/基准 (DOMINO)**：
    *   包含 **35 个动态任务**，涵盖 5 种机器人形态（如 Aloha, Franka, UR5 等）。
    *   **11.7 万条专家轨迹**，分为动态拦截和动态追踪两类。
    *   **三级难度**：Level 1（匀速）、Level 2（高阶多项式曲线）、Level 3（随机突变）。
    *   **评估指标**：成功率 (SR) 和操控评分 (MS，考虑路径完成度及碰撞惩罚)。
*   **对比方法**：包括 ACT、OpenVLA、OpenVLA-OFT、RDT-1B、$\pi_0$、$\pi_{0.5}$、InternVLA-M1 等 10 余种 SOTA 模型。

#### 4. 资源与算力
*   **训练算力**：所有 VLA 模型均在 **NVIDIA A100 GPU** 上训练。
*   **数据生成与评估**：在 **NVIDIA RTX GPU** 上进行。
*   **训练细节**：采用 AdamW 优化器，基础学习率 $10^{-5}$，训练 100 个 epoch（约 10 万步），每台设备 batch size 为 16。

#### 5. 实验数量与充分性
*   **实验规模**：在 35 个任务上进行了闭环评估，每个任务测试 100 个回合，总计进行了数千次闭环测试。
*   **消融实验**：系统对比了历史光流 vs. 原始帧、有无辅助预测任务、预测步数（N=2 vs. N=4）的影响。
*   **泛化性验证**：进行了“动态训练-静态测试”的零样本迁移实验，以及“静态+动态”的协同训练实验。
*   **评价**：实验设计全面，涵盖了从单任务到多任务、从仿真到泛化能力的多个维度，对比基准统一（均在 DOMINO 上微调），具有较高的客观性和公平性。

#### 6. 论文的主要结论与发现
*   **性能提升**：PUMA 在动态任务上达到了 17.2% 的平均成功率，比最强基准（OpenVLA-OFT）提升了 **6.3%**。
*   **动态数据的价值**：在动态数据上训练的模型在静态任务中表现出极强的**零样本迁移能力**，甚至在某些任务上超过了纯静态训练的模型。这表明动态数据能有效减轻模型对静态位置偏差的过拟合。
*   **架构必要性**：单纯增加动态数据而不改变架构（如单帧 VLA）提升有限，显式的时空建模（光流+预测）是解决动态问题的关键。
*   **难度瓶颈**：Level 3（随机突变）任务对所有现有模型仍是巨大挑战，成功率普遍极低。

#### 7. 优点
*   **基准贡献**：填补了具身智能领域缺乏大规模动态操控数据集的空白，任务设计具有层级性。
*   **方法创新**：PUMA 巧妙地利用辅助损失在不增加推理开销的前提下，增强了模型的预测能力。
*   **发现深刻**：揭示了动态训练对提升静态任务鲁棒性的积极作用，为未来的数据收集提供了新思路。

#### 8. 不足与局限
*   **仿真与现实差距 (Sim-to-Real)**：论文实验完全在仿真环境（SAPIEN/RoboTwin）中进行，尚未在真实物理机器人上验证其有效性。
*   **绝对成功率尚低**：尽管 PUMA 优于基准，但在复杂动态任务（Level 2/3）下的绝对成功率仍有待大幅提升，距离实际应用尚远。
*   **计算开销**：虽然推理时不使用未来帧，但历史光流的实时计算和多帧处理仍增加了系统的延迟要求。

（完）
