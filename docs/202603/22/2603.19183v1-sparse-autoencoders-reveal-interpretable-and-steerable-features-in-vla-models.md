---
title: Sparse Autoencoders Reveal Interpretable and Steerable Features in VLA Models
title_zh: 稀疏自编码器揭示了 VLA 模型中具有可解释性且可引导的特征
authors: "Aiden Swann, Lachlain McGranahan, Hugo Buurmeijer, Monroe Kennedy, Mac Schwager"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19183v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 机器人操纵中VLA模型的机械可解释性
tldr: 本研究利用机械可解释性技术，通过在视觉-语言-动作（VLA）模型的隐藏层激活上训练稀疏自编码器（SAEs），揭示了其内部表征。研究发现，虽然大部分特征对应于特定演示的记忆，但仍存在可解释且可操控的通用运动基元。通过在LIBERO基准上的干预实验，证明了这些特征能因果性地影响机器人行为。研究还指出，大规模多样化数据集和知识隔离技术有助于提升模型的泛化特征，为理解VLA的泛化机制提供了首个机械性证据。
selection_source: fresh_fetch
motivation: 旨在通过机械可解释性方法，探究VLA模型在机器人操控任务中泛化性能不稳定的内在机制。
method: 在VLA模型隐藏层训练稀疏自编码器（SAEs）以提取特征字典，并提出量化指标区分通用运动基元与特定任务的记忆特征。
result: 实验证实SAE特征可因果性地引导机器人行为，且发现大规模多样化数据集能促进通用特征形成，而小规模微调则会加剧模型的记忆化倾向。
conclusion: 本研究首次为VLA模型具备跨任务泛化特征提供了机械性证据，并揭示了数据集规模和训练策略对提升模型泛化能力的关键作用。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人操控的一种极具前景的方法。然而，它们的泛化能力并不一致：虽然这些模型在某些设置下表现出色，但微调后的变体往往在面对新物体、新场景和新指令时失效。我们应用机械可解释性（mechanistic interpretability）技术来更好地理解 VLA 模型的内部工作机制。为了探测内部表示，我们在 VLA 的隐藏层激活上训练了稀疏自编码器（SAE）。SAE 学习一个稀疏字典，其特征作为模型计算的紧凑且可解释的基。我们发现，提取的大多数 SAE 特征对应于特定训练演示中的记忆序列。然而，一些特征对应于可解释、通用且可引导的运动原语（motion primitives）和语义属性，为 VLA 的泛化能力提供了令人期待的见解。我们提出了一种指标，根据特征是代表可泛化的可迁移原语还是特定情节的记忆来对其进行分类。我们通过在 LIBERO 基准测试上的引导实验验证了这些发现。我们展示了单个 SAE 特征会因果性地影响机器人行为。引导通用特征会诱发出与其语义含义一致的行为，并可应用于跨任务和跨场景。这项工作提供了首个机械论证据，证明 VLA 能够学习跨任务和场景的可泛化特征。我们观察到，在小型机器人数据集上进行有监督微调会不成比例地放大记忆效应。相比之下，在更大、更多样化的数据集（如 DROID）上训练或使用知识隔离（knowledge insulation）会促进更通用特征的产生。我们提供了一个开源代码库和用户友好的界面，用于激活收集、SAE 训练和特征引导。我们的项目主页位于 http://drvla.github.io

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising approach for general-purpose robot manipulation. However, their generalization is inconsistent: while these models can perform impressively in some settings, fine-tuned variants often fail on novel objects, scenes, and instructions. We apply mechanistic interpretability techniques to better understand the inner workings of VLA models. To probe internal representations, we train Sparse Autoencoders (SAEs) on hidden layer activations of the VLA. SAEs learn a sparse dictionary whose features act as a compact, interpretable basis for the model's computation. We find that the large majority of extracted SAE features correspond to memorized sequences from specific training demonstrations. However, some features correspond to interpretable, general, and steerable motion primitives and semantic properties, offering a promising glimpse toward VLA generalizability. We propose a metric to categorize features according to whether they represent generalizable transferable primitives or episode-specific memorization. We validate these findings through steering experiments on the LIBERO benchmark. We show that individual SAE features causally influence robot behavior. Steering general features induces behaviors consistent with their semantic meaning and can be applied across tasks and scenes. This work provides the first mechanistic evidence that VLAs can learn generalizable features across tasks and scenes. We observe that supervised fine-tuning on small robotics datasets disproportionately amplifies memorization. In contrast, training on larger, more diverse datasets (e.g., DROID) or using knowledge insulation promotes more general features. We provide an open-source codebase and user-friendly interface for activation collection, SAE training, and feature steering. Our project page is located at http://drvla.github.io

---

## 论文详细总结（自动生成）

以下是对论文《Sparse Autoencoders Reveal Interpretable and Steerable Features in VLA Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在机器人操控中表现出色，但其泛化性极不稳定，在微调后往往会失效。研究者试图探究：VLA 模型内部究竟是学习了通用的运动原语（Motion Primitives），还是仅仅死记硬背（Memorization）了训练集中的特定动作序列？
*   **研究背景**：目前的 VLA 模型（如 OpenVLA, $\pi_0$）在面对新场景或指令时容易崩溃。由于缺乏对模型内部机制的理解，开发者难以诊断模型是由于视觉过拟合还是逻辑失效导致的失败。

### 2. 方法论
*   **核心思想**：利用**机械可解释性（Mechanistic Interpretability）**技术，通过**稀疏自编码器（SAE）**将 VLA 模型隐藏层的稠密激活值分解为数千个稀疏的、具有语义含义的“特征”。
*   **关键技术细节**：
    *   **SAE 架构**：采用 TopK 架构配合 AuxK 辅助损失函数，以解决“死神经元”问题并精确控制特征稀疏度。
    *   **特征提取**：在 $\pi_{0.5}$ 模型的 PaliGemma 层（视觉语言主干）和 Action Expert 层（动作解码器）提取残差流激活值。
    *   **泛化性量化指标**：提出四个自动化指标来区分特征：
        1.  **情节覆盖率（Episode Coverage）**：特征在多少比例的训练情节中激活。
        2.  **平均起始计数（Mean Onset Count）**：特征在单个情节中“爆发式”开启的次数。
        3.  **平均激活强度（Mean Activation Magnitude）**：特征激活的峰值强度。
        4.  **相对运行长度（Relative Run Length）**：特征在情节中持续激活的时间比例。
    *   **特征引导（Steering）**：在推理阶段，将特定特征的解码向量按比例加回模型激活值中，从而因果性地改变机器人的行为。

### 3. 实验设计
*   **模型对象**：$\pi_{0.5}$（基于 PaliGemma）和 OpenVLA（基于 Llama 2）。
*   **数据集与场景**：
    *   **LIBERO**：模拟环境下的桌面操纵基准（40 个任务，约 1700 个情节）。
    *   **DROID**：真实世界的大规模机器人操纵数据集（选取 2000 个情节子集，涵盖 1545 个任务）。
*   **对比维度**：对比了不同层（早期 vs 晚期）、不同训练策略（标准微调 vs 知识隔离 KI）、以及不同数据集规模对特征泛化性的影响。

### 4. 资源与算力
*   **算力说明**：论文提到使用了斯坦福大学的 **Marlowe 集群**进行实验。
*   **具体参数**：SAE 训练采用 Adam 优化器，Batch Size 为 4096，训练 100 个 Epoch。
*   **缺失信息**：文中未明确给出具体的 GPU 型号（如 A100/H100）数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 120 个随机抽样的特征进行了人工标注以验证可解释性（准确率约 79%）。
    *   在 $\pi_{0.5}$ 的 8 个层和 OpenVLA 的 5 个层上训练了 SAE。
    *   进行了多种子（Multi-seed）消融实验，证明特征提取的稳定性。
    *   在 LIBERO 环境中进行了闭环引导实验，验证特征的因果影响。
*   **充分性评价**：实验设计较为全面，涵盖了模拟与现实数据、不同架构的模型以及因果干预验证，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **记忆 vs 泛化**：VLA 模型中绝大多数特征（>90%）属于对特定情节的**记忆**，但确实存在少部分**通用特征**（如“抓取”、“运输”、“任务完成”）。
*   **数据集影响**：在更多样化的数据集（如 DROID）上训练会显著增加通用特征的比例；而小型数据集上的微调会加剧记忆化。
*   **知识隔离（KI）的作用**：证明了 KI 技术能有效减缓微调过程中的特征退化，保留更多通用特征。
*   **可引导性**：通过干预 SAE 特征，可以精确控制机器人行为（例如，增强“预抓取对齐”特征会让机器人悬停在物体上方而不执行抓取）。

### 7. 优点
*   **首创性**：首次为 VLA 模型具备跨任务泛化特征提供了机械论层面的证据。
*   **工具化**：提供了开源工具包 **Dr. VLA**，降低了机器人模型可解释性研究的门槛。
*   **量化评估**：不仅停留在定性观察，还提出了自动化的特征分类指标，使大规模分析成为可能。

### 8. 不足与局限
*   **硬件验证缺失**：所有的引导实验均在仿真环境（LIBERO）中完成，尚未在真实机器人硬件上验证引导效果。
*   **Token 处理限制**：为了节省算力和存储，主要采用了均值池化（Mean-pooled）后的 Token 激活值，可能丢失了细粒度的空间信息。
*   **引导的不完美性**：部分特征在引导时表现出叠加效应（Superposition），导致行为改变不够纯粹或预测性受限。

（完）
