---
title: "$π$, But Make It Fly: Physics-Guided Transfer of VLA Models to Aerial Manipulation"
title_zh: $π$，但让它飞起来：物理引导的 VLA 模型向空中操作的迁移
authors: "Johnathan Tucker, Denis Liu, Aiden Swann, Allen Ren, Javier Yu, Jiankai Sun, Brandon Kim, Lachlain McGranahan, Quan Vuong, Mac Schwager"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25038v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 将VLA模型迁移至空中操纵任务
tldr: 本研究提出AirVLA，旨在将预训练的视觉-语言-动作（VLA）模型从固定基座机械臂迁移至空中操作平台。针对空中机器人动力学复杂且数据稀缺的挑战，该系统引入了载荷感知引导机制以在推理时注入物理约束，并利用高斯泼溅技术合成导航数据。通过460次真实世界实验，AirVLA证明了物理启发引导和合成数据能有效弥合动力学差异，显著提升了空中取放及长程复合任务的成功率。
selection_source: fresh_fetch
motivation: 旨在解决预训练VLA模型在固定基座机械臂与高动态、欠驱动的空中机器人之间存在的动力学失配及数据匮乏问题。
method: 引入了在推理阶段注入物理约束的载荷感知引导机制，并结合高斯泼溅技术生成大规模合成导航训练数据。
result: "载荷感知引导将取放任务成功率从23%提升至50%，合成数据助力导航任务达到100%成功率，长程复合任务成功率为62%。"
conclusion: 证明了通过适当的物理启发引导和数据增强，预训练的操纵VLA模型能够有效迁移并胜任复杂的空中操作与导航任务。
---

## 摘要
视觉-语言-动作（VLA）模型（如 $π_0$）在多种固定基座机械臂上展现出了卓越的泛化能力。然而，由于固定基座机械臂的准静态动力学与飞行过程中欠驱动、高动态的特性之间存在根本性的不匹配，将这些基础模型迁移到空中平台仍然是一个开放性的挑战。在这项工作中，我们介绍了 AirVLA，这是一个研究预训练的操作 VLA 模型向空中取放任务迁移能力的系统。我们发现，虽然视觉表示可以有效迁移，但飞行所需的特定控制动力学却无法直接迁移。为了在不重新训练基础模型的情况下弥合这种“动力学差距”，我们引入了一种负载感知引导（Payload-Aware Guidance）机制，将负载约束直接注入策略的流匹配（flow-matching）采样过程中。为了克服数据稀缺问题，我们进一步利用高斯泼溅（Gaussian Splatting）流水线来合成导航训练数据。我们通过累计 460 次真实世界实验评估了我们的方法，结果表明这种合成数据是性能提升的关键，使导航任务的成功率从仅通过遥操作数据微调的 81% 提升到了 100%。我们的推理时干预机制——负载感知引导，将真实世界取放任务的成功率从 23% 提高到了 50%。最后，我们在长时程复合任务上评估了该模型，实现了 62% 的总成功率。这些结果表明，通过适当的数据增强和物理启发式引导，预训练的操作 VLA 模型可以迁移到空中操作和导航，以及这些任务的组合中。

## Abstract
Vision-Language-Action (VLA) models such as $π_0$ have demonstrated remarkable generalization across diverse fixed-base manipulators. However, transferring these foundation models to aerial platforms remains an open challenge due to the fundamental mismatch between the quasi-static dynamics of fixed-base arms and the underactuated, highly dynamic nature of flight. In this work, we introduce AirVLA, a system that investigates the transferability of manipulation-pretrained VLAs to aerial pick-and-place tasks. We find that while visual representations transfer effectively, the specific control dynamics required for flight do not. To bridge this "dynamics gap" without retraining the foundation model, we introduce a Payload-Aware Guidance mechanism that injects payload constraints directly into the policy's flow-matching sampling process. To overcome data scarcity, we further utilize a Gaussian Splatting pipeline to synthesize navigation training data. We evaluate our method through a cumulative 460 real-world experiments which demonstrate that this synthetic data is a key enabler of performance, unlocking 100% success in navigation tasks where directly fine-tuning on teleoperation data alone attains 81% success. Our inference-time intervention, Payload-Aware Guidance, increases real-world pick-and-place task success from 23% to 50%. Finally, we evaluate the model on a long-horizon compositional task, achieving a 62% overall success rate. These results suggest that pre-trained manipulation VLAs, with appropriate data augmentation and physics-informed guidance, can transfer to aerial manipulation and navigation, as well as the composition of these tasks.

---

## 论文详细总结（自动生成）

### AirVLA：物理引导的 VLA 模型向空中操作迁移研究总结

#### 1. 核心问题与研究动机
*   **核心问题**：探讨预训练的视觉-语言-动作（VLA）基础模型（如 $\pi_0$）能否从传统的固定基座机械臂迁移到高动态、欠驱动的空中操作平台（无人机）。
*   **研究动机**：VLA 模型在地面机器人上表现优异，但空中机器人面临三大挑战：
    1.  **动力学失配**：地面机器人的准静态动力学与无人机的 6 自由度欠驱动飞行控制存在巨大差异。
    2.  **视觉扰动**：机载摄像头存在剧烈的自我运动、视角变化和运动模糊。
    3.  **负载干扰**：接触物体会改变无人机质量并产生气动干扰，打破准静态假设。

#### 2. 方法论与关键技术
*   **核心思想**：在不重新训练基础模型的前提下，通过“推理时引导”弥合动力学差距，并通过“合成数据增强”解决空中操作数据稀缺问题。
*   **关键技术细节**：
    *   **负载感知引导（Payload-Aware Guidance）**：在 $\pi_0$ 的流匹配（Flow-matching）采样过程中注入物理约束。通过计算一个负载感知损失函数的梯度，修正采样速度场，补偿抓取物体后产生的垂直下沉（Sag）。
    *   **实时分块执行（Real-Time Chunking, RTC）**：引入异步推理机制，通过平滑掩码处理动作序列，确保动作切换的连续性，减少飞行中的抖动。
    *   **高斯泼溅（3DGS）数据流水线**：利用 3DGS 重建环境，结合无人机动力学模型合成大量“纠偏”导航轨迹。通过 Segment Anything (SAM) 提取机械臂视觉特征并进行合成，生成具有物理真实感的训练样本。
    *   **策略架构**：基于 $\pi_0$ 模型，输入多视图 RGB 图像、本体感受数据和自然语言指令，输出相对末端执行器的位姿命令。

#### 3. 实验设计
*   **实验场景**：
    1.  **企鹅抓取（取放任务）**：测试操纵技能的迁移。
    2.  **门洞导航**：测试避障与空间定位。
    3.  **复合任务（导航后抓取）**：测试零样本（Zero-shot）任务组合能力。
*   **对比基准（Benchmarks）**：
    *   **$\pi_0$ Naive**：直接微调后的原始模型。
    *   **$\pi_0$ + RTC**：加入实时分块机制。
    *   **ACT 与 Diffusion Policy**：传统的模仿学习基准方法。
*   **数据集**：手动收集了 270 个真实世界遥操作演示样本，并利用 3DGS 合成了 50 个纠偏导航示例。

#### 4. 资源与算力
*   **训练细节**：微调过程进行了 30,000 个梯度步，使用 AdamW 优化器，全局 Batch Size 为 32，动作时界（Horizon）$H=50$。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。但提到推理频率为 10Hz，通过机载 VOXL 2 计算机与外部服务器通信实现。

#### 5. 实验数量与充分性
*   **实验规模**：累计进行了 **460 次真实世界飞行实验**，这在机器人领域属于较大规模的实机测试。
*   **充分性评价**：实验涵盖了消融实验（验证 RTC 和引导机制的作用）、分布外（OOD）测试（更换未见过的物体、改变门洞位置）。实验设计较为客观，通过成功率和失败模式分类（如坠毁、未对准等）进行了深入分析，验证了各组件的必要性。

#### 6. 主要结论与发现
*   **视觉表示可迁移，动力学不可直接迁移**：预训练 VLA 的视觉特征能很好地适应无人机视角，但必须通过物理引导才能处理飞行控制。
*   **物理引导至关重要**：负载感知引导将取放任务成功率从 23% 提升至 50%。
*   **合成数据是关键**：3DGS 合成数据使导航任务成功率从 81% 提升至 100%。
*   **具备零样本组合能力**：模型能根据语言指令将导航和抓取原子动作组合，在复合任务中达到 62% 的成功率。

#### 7. 优点与亮点
*   **首创性**：首次系统性地将预训练 VLA 模型迁移至空中操作平台。
*   **推理时干预**：无需重训庞大的基础模型，通过采样引导（Guidance）即可注入物理先验，具有很高的灵活性。
*   **纠偏数据合成**：利用 3DGS 生成“失败恢复”轨迹，有效解决了真实数据中缺乏错误修正样本的问题。

#### 8. 不足与局限
*   **定位依赖**：目前仍依赖外部动捕系统（MoCap）进行定位，尚未实现完全基于机载视觉（VIO/SLAM）的闭环。
*   **泛化性有限**：在分布外（OOD）导航任务中表现较弱（成功率仅 40%），说明模型对环境几何变化的适应性仍有待提高。
*   **数据规模**：虽然使用了合成数据，但总样本量（约 300 个）相对于基础模型微调而言仍显单薄，可能导致模型在某些复杂场景下过拟合。

（完）
