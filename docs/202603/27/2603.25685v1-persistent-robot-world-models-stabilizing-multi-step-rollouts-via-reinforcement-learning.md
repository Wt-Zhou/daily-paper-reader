---
title: "Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning"
title_zh: 持久性机器人世界模型：通过强化学习稳定多步展开
authors: "Jai Bardhan, Patrik Drozdik, Josef Sivic, Vladimir Petrik"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25685v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 通过强化学习稳定机器人世界模型中的多步展开
tldr: 针对机器人世界模型在长程自回归预测中存在的误差累积和视觉质量退化问题，本文提出了一种基于强化学习（RL）的后训练方案。该方法通过在模型自身的预测序列上进行训练，并结合对比RL目标函数、多视图视觉保真度奖励以及多候选序列对比协议，显著提升了多步预测的稳定性。在DROID数据集上的实验表明，该方法在各项指标上均达到SOTA，并获得了极高的人类偏好率。
selection_source: fresh_fetch
motivation: 传统的机器人世界模型在长程自回归预测时会因误差累积导致视觉质量迅速崩溃。
method: 提出一种RL后训练方案，利用对比学习目标和多视图视觉保真度奖励，让模型在自身的预测序列上进行优化。
result: "在DROID数据集上刷新了SOTA，LPIPS降低了14%，SSIM提升了9.1%，且在人类盲测中获得80%的偏好率。"
conclusion: 通过RL微调自回归预测过程，可以有效解决世界模型的长期预测不稳定性，为复杂机器人任务模拟提供了更可靠的工具。
---

## 摘要
动作条件化的机器人世界模型在给定机器人动作序列的情况下生成被操纵场景的未来视频帧，为模拟传统物理引擎难以建模的任务提供了一种极具前景的替代方案。然而，这些模型针对短期预测进行了优化，在自回归部署时会失效：每个预测的片段都会作为下一个片段的上下文反馈，导致误差累积，视觉质量迅速下降。我们通过以下贡献解决了这一问题。首先，我们引入了一种强化学习（RL）后训练方案，该方案在世界模型自身的自回归展开（rollouts）上而非真实历史数据上进行训练。我们通过将近期针对扩散模型的对比强化学习目标适配到我们的设置中来实现这一点，并证明其收敛性保证可以完全保留。其次，我们设计了一种训练协议，从同一个展开状态生成并比较多个候选的可变长度未来，从而强化高保真度预测并抑制低保真度预测。第三，我们开发了高效的多视图视觉保真度奖励，结合了跨摄像机视图的互补感知指标，并在片段级别进行聚合，以获得密集、低方差的训练信号。第四，我们展示了我们的方法在 DROID 数据集上的展开保真度达到了新的最先进水平，在所有指标上都优于最强的基准模型（例如，外部摄像机的 LPIPS 降低了 14%，手腕摄像机的 SSIM 提高了 9.1%），在成对比较中胜率为 98%，并在盲测人类研究中获得了 80% 的偏好率。

## Abstract
Action-conditioned robot world models generate future video frames of the manipulated scene given a robot action sequence, offering a promising alternative for simulating tasks that are difficult to model with traditional physics engines. However, these models are optimized for short-term prediction and break down when deployed autoregressively: each predicted clip feeds back as context for the next, causing errors to compound and visual quality to rapidly degrade. We address this through the following contributions. First, we introduce a reinforcement learning (RL) post-training scheme that trains the world model on its own autoregressive rollouts rather than on ground-truth histories. We achieve this by adapting a recent contrastive RL objective for diffusion models to our setting and show that its convergence guarantees carry over exactly. Second, we design a training protocol that generates and compares multiple candidate variable-length futures from the same rollout state, reinforcing higher-fidelity predictions over lower-fidelity ones. Third, we develop efficient, multi-view visual fidelity rewards that combine complementary perceptual metrics across camera views and are aggregated at the clip level for dense, low-variance training signal. Fourth, we show that our approach establishes a new state-of-the-art for rollout fidelity on the DROID dataset, outperforming the strongest baseline on all metrics (e.g., LPIPS reduced by 14% on external cameras, SSIM improved by 9.1% on the wrist camera), winning 98% of paired comparisons, and achieving an 80% preference rate in a blind human study.

---

## 论文详细总结（自动生成）

以下是对论文《Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：动作条件化的视频扩散世界模型（World Models, WM）在机器人领域极具潜力，可作为物理引擎的替代方案来模拟复杂任务。然而，现有模型在**长程自回归展开（Autoregressive Rollouts）**时表现极差。
*   **核心问题**：**曝光偏差（Exposure Bias）**。模型在训练时使用的是真实的（Ground-truth）历史帧，但在实际部署时，它必须基于自己生成的、带有微小误差的预测帧进行后续预测。这些误差会随时间迅速累积，导致生成的物体“溶解”、机器人末端执行器漂移以及场景崩溃。
*   **整体含义**：本文旨在通过强化学习（RL）后训练，使世界模型能够“看到”并纠正自己的错误，从而实现持久、稳定的长程视频生成。

### 2. 方法论
*   **核心思想**：将世界模型的微调建模为一个在线强化学习问题。模型不再仅仅学习预测下一帧，而是学习在自回归循环中生成高保真度的序列。
*   **关键技术细节**：
    *   **对比 RL 目标函数**：适配了 DiffusionNFT 方法，将其从原始的噪声预测（$\epsilon$-prediction）推导并应用到机器人 WM 常用的清洁图像预测（$x_0$-prediction）参数化中。该方法通过比较一组候选样本的奖励，避免了昂贵的通过整个去噪链的反向传播。
    *   **训练协议（PersistWorld）**：
        1.  **共享前缀生成**：从真实初始状态开始，自回归生成一段可变长度（0-9步）的前缀，模拟不同程度的累积误差。
        2.  **分支候选序列**：从该前缀状态出发，独立采样 $K=16$ 个候选后续片段。
        3.  **评分与排名**：使用多视图视觉奖励对候选片段进行评分，并进行组内归一化（z-score），转化为相对优势信号。
        4.  **对比更新**：利用奖励权重 $r$ 线性插值正向和负向分支，强化高奖励预测，排斥低奖励预测。
*   **公式逻辑**：构造隐式的正向预测 $\hat{x}^+_0$ 和负向预测 $\hat{x}^-_0$，通过最小化奖励加权的去噪损失 $L(\theta) = \mathbb{E} [r \|\hat{x}^+_0 - x_0\|^2 + (1-r) \|\hat{x}^-_0 - x_0\|^2]$ 来更新模型。

### 3. 实验设计
*   **数据集**：**DROID**（大规模真实机器人操纵数据集，包含双外部视角和单手腕视角）。
*   **Benchmark（基准）**：对比了 **WPE**、**IRASim** 和 **Ctrl-World**（本文的 Baseline）。
*   **评估指标**：
    *   **视觉质量**：SSIM、PSNR、LPIPS。
    *   **任务相关性**：使用掩码（Mask）隔离物体和机器人手臂，专门评估关键区域的保真度。
    *   **人类研究**：2AFC（双选强制选择）盲测，评估真实感和一致性。
    *   **策略评估**：在 WM 中运行 VLA 策略，计算任务进度率与真实环境的相关性。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练时长**：训练了 **3 天**（共 8,000 个步长）。
*   **参数设置**：Batch size 为 64，组大小 $K=16$，使用 LoRA 适配器进行高效微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在验证集上进行了 14 步（约 11 秒）的长程展开测试。
    *   进行了详细的**消融实验**（共 9 组），涵盖学习率、奖励函数组合、前缀长度策略、组大小、KL 正则化等。
    *   人类偏好研究包含 217 次成对比较。
*   **充分性与公平性**：实验设计非常充分且客观。通过掩码评估证明了性能提升并非来自背景重建，而是来自对物体交互的准确建模。对比实验均在相同的数据分割和评估协议下进行。

### 6. 主要结论与发现
*   **性能刷新**：在 DROID 数据集上达到了新的 SOTA。外部相机 LPIPS 降低 14%，手腕相机 SSIM 提升 9.1%。
*   **稳定性提升**：在 11 秒的长程预测中，Baseline 模型通常在几秒内崩溃，而本文方法能保持物体的结构完整性。
*   **人类偏好**：在盲测中，人类观察者在 80% 的情况下更倾向于本文模型生成的视频。
*   **策略评估一致性**：该模型作为模拟器时，与真实世界策略表现的相关性更高（Pearson $r=0.822$）。

### 7. 优点
*   **理论严谨**：为 $x_0$ 预测模型重新推导了对比 RL 目标的收敛保证。
*   **训练高效**：通过分支采样和对比损失，解决了扩散模型 RL 训练中计算量巨大的难题。
*   **多视图融合**：设计了跨视角的片段级奖励，提供了密集且低方差的训练信号。
*   **任务聚焦**：通过掩码评估和手腕相机指标的显著提升，证明了其在机器人操纵任务中的实用价值。

### 8. 不足与局限
*   **计算开销**：虽然避免了全路径反向传播，但训练时每个步骤需要采样 16 个候选序列，推理开销依然较高。
*   **物理约束缺失**：目前的奖励主要基于视觉保真度（LPIPS/SSIM），尚未显式引入物理定律（如重力、碰撞）或几何约束。
*   **泛化范围**：主要在 DROID 数据集上验证，对于跨机器人形态或极端未见场景的泛化能力仍有待进一步探索。

（完）
