---
title: "LaMP: Learning Vision-Language-Action Policies with 3D Scene Flow as Latent Motion Prior"
title_zh: LaMP：以 3D 场景流作为潜在运动先验的学习型视觉-语言-动作策略
authors: "Xinkai Wang, Chenyi Wang, Yifu Xu, Mingzhe Ye, Fu-Cheng Zhang, Jialin Tian, Xinyu Zhan, Lifeng Zhu, Cewu Lu, Lixin Yang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25399v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 以3D场景流作为潜在运动先验的视觉-语言-动作策略
tldr: LaMP是一个双专家视觉-语言-动作（VLA）框架，旨在通过引入3D场景流作为潜在运动先验来增强机器人操作。针对现有模型仅依赖2D特征导致3D物理交互学习困难的问题，LaMP通过门控交叉注意力机制将运动专家（生成3D场景流）与动作专家（预测策略）对齐。实验表明，LaMP在多个仿真基准和真实场景中表现优异，显著提升了任务成功率和对分布外（OOD）扰动的鲁棒性。
selection_source: fresh_fetch
motivation: 现有VLA模型直接从2D视觉特征回归动作，难以隐式学习复杂的3D物理交互，导致在陌生空间动态下性能下降。
method: 提出双专家架构，利用运动专家生成单步去噪的3D场景流隐藏状态，并通过门控交叉注意力引导动作专家进行策略预测。
result: "在LIBERO、SimplerEnv等基准测试中刷新了最高平均成功率记录，且在LIBERO-Plus OOD测试中比最强基准提升了9.7%。"
conclusion: 将3D场景流作为运动先验能有效增强VLA模型的空间感知能力和泛化性能，是提升机器人复杂操作能力的有效途径。
---

## 摘要
我们介绍了 LaMP，这是一个双专家视觉-语言-动作（VLA）框架，它将稠密 3D 场景流嵌入为机器人操作的潜在运动先验。现有的 VLA 模型直接从 2D 语义视觉特征回归动作，迫使它们隐式地学习复杂的 3D 物理交互。这种隐式学习策略在不熟悉的空间动力学环境下性能会下降。LaMP 通过门控交叉注意力机制，将流匹配（flow-matching）运动专家与策略预测动作专家进行对齐，从而解决了这一局限性。具体而言，运动专家生成单步部分去噪的 3D 场景流，其隐藏状态在无需进行完整多步重建的情况下为动作专家提供条件。我们在 LIBERO、LIBERO-Plus 和 SimplerEnv-WidowX 仿真基准测试以及真实世界实验中对 LaMP 进行了评估。LaMP 在 LIBERO、LIBERO-Plus 和 SimplerEnv-WidowX 基准测试中始终优于所评估的 VLA 基线模型，在相同的训练预算下实现了报告的最高平均成功率。在 LIBERO-Plus 的分布外（OOD）扰动测试中，LaMP 表现出更强的鲁棒性，比最强的先前基线模型平均提升了 9.7%。我们的项目主页位于 https://summerwxk.github.io/lamp-project-page/。

## Abstract
We introduce \textbf{LaMP}, a dual-expert Vision-Language-Action framework that embeds dense 3D scene flow as a latent motion prior for robotic manipulation. Existing VLA models regress actions directly from 2D semantic visual features, forcing them to learn complex 3D physical interactions implicitly. This implicit learning strategy degrades under unfamiliar spatial dynamics. LaMP addresses this limitation by aligning a flow-matching \emph{Motion Expert} with a policy-predicting \emph{Action Expert} through gated cross-attention. Specifically, the Motion Expert generates a one-step partially denoised 3D scene flow, and its hidden states condition the Action Expert without full multi-step reconstruction. We evaluate LaMP on the LIBERO, LIBERO-Plus, and SimplerEnv-WidowX simulation benchmarks as well as real-world experiments. LaMP consistently outperforms evaluated VLA baselines across LIBERO, LIBERO-Plus, and SimplerEnv-WidowX benchmarks, achieving the highest reported average success rates under the same training budgets. On LIBERO-Plus OOD perturbations, LaMP shows improved robustness with an average 9.7% gain over the strongest prior baseline. Our project page is available at https://summerwxk.github.io/lamp-project-page/.

---

## 论文详细总结（自动生成）

### LaMP: 以 3D 场景流作为潜在运动先验的学习型视觉-语言-动作策略

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 OpenVLA、$\pi_0$ 等）通常直接将 2D 图像特征映射到低级控制动作。这种做法存在一个根本性的表征失配：**VLM 特征主要是 2D 语义导向的，而机器人操纵需要对 3D 物理动力学有显式的推理能力**。这导致模型在面对需要精确深度控制（如插孔、堆叠）或视角/布局发生变化的陌生空间动态时，表现得非常脆弱。
论文的核心动机是：能否引入**稠密 3D 场景流（3D Scene Flow）**作为一种潜在的运动先验，显式地为 VLA 策略提供物理几何引导，从而弥补 2D 语义与 3D 动作之间的鸿沟。

#### 2. 方法论：核心思想与关键技术
LaMP 提出了一个**双专家（Dual-Expert）框架**，将感知与运动预测解耦：

*   **核心思想**：通过联合建模动作和运动的分布 $P(A, M | z)$，将其分解为运动专家（预测 3D 场景流）和动作专家（基于运动先验预测动作）。
*   **运动专家（Motion Expert）**：
    *   采用基于 Transformer 的流匹配（Flow Matching）架构。
    *   预测稠密 3D 场景流：在图像平面采样 $20 \times 20$ 的网格，预测未来 $T=32$ 步的 3D 位移 $(\Delta u, \Delta v, \Delta d)$。
    *   **单步去噪特征（One-step denoising）**：为了兼顾效率和性能，推理时并不进行完整的多步去噪重建，而是仅执行一步去噪并提取其隐藏状态（Hidden States）作为运动先验。
*   **门控运动引导（Gated Motion Guidance）**：
    *   使用**门控交叉注意力（Gated Cross-Attention）**机制将运动专家的特征注入到 VLM 的最后一层特征中。
    *   门控参数初始为零，允许模型自适应地学习何时以及注入多少几何信息，防止预训练 VLM 的语义特征发生表征崩溃。
*   **动作专家（Action Expert）**：
    *   接收经过运动引导的增强特征，通过流匹配生成连续的机器人动作序列。
*   **训练流程**：分为两阶段。第一阶段在 1.6M 的大规模多具身数据集上预训练运动专家；第二阶段冻结运动专家和 VLM，仅训练门控模块和动作专家。

#### 3. 实验设计
论文在仿真和真实世界中进行了广泛评估：
*   **仿真基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估语言条件下的操纵和长程规划。
    *   **LIBERO-Plus**：评估在相机视角、光照、背景、机器人动力学等 7 个维度的分布外（OOD）鲁棒性。
    *   **SimplerEnv-WidowX**：评估从真实数据（BridgeV2）到仿真环境的迁移能力（Real-to-Sim）。
*   **对比方法**：包括通用 VLA（OpenVLA, $\pi_0, \pi_{0.5}$, GR00T）、潜在动作 VLA（UniVLA）、视频/预测型 VLA（mimic-video, F1）以及 2D 流引导模型（FlowVLA, TraceVLA）。
*   **真实世界场景**：使用 Flexiv Rizon 4 机械臂，测试了叠杯子（硬体）、折毛巾（柔性体）和做面包（长程任务）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **16 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   运动专家预训练：在 1.6M 数据上训练 30 个 epoch。
    *   动作专家训练：在 LIBERO 和 SimplerEnv 上分别训练约 15k 步。
    *   推理效率：由于采用了单步去噪策略，模型支持在线实时部署。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **覆盖面广**：涵盖了从基础操纵到长程规划，从仿真到真实世界，从同分布到异分布（OOD）的全面测试。
*   **消融实验深入**：对比了“无运动先验”、“2D 流先验 vs 3D 流先验”以及“不同特征融合策略（Add, Concat, Gated）”，验证了 3D 几何信息和门控机制的必要性。
*   **客观性**：在相同的训练预算和数据规模下与当前最先进的（SOTA）模型进行对比，确保了比较的公平性。

#### 6. 主要结论与发现
*   **性能领先**：LaMP 在 LIBERO（98.3% 成功率）和 SimplerEnv（79.2% 成功率）上均刷新了记录，显著优于 $\pi_{0.5}$ 和 FlowVLA。
*   **长程任务优势**：在 LIBERO-Long 任务中，LaMP 的表现远超基线，证明显式运动预测能有效减少长程任务中的误差累积。
*   **卓越的鲁棒性**：在 LIBERO-Plus 的 OOD 测试中，LaMP 比最强基线提升了 9.7%，尤其在相机视角和机器人动力学变化时表现稳健。
*   **3D 的重要性**：实验证明 3D 场景流（含深度信息）在处理接触密集型任务（如叠块）时比 2D 光流更有效。

#### 7. 优点
*   **物理接地（Physical Grounding）**：通过 3D 场景流将几何直觉引入语义模型，解决了 VLA 模型“空间盲”的问题。
*   **高效集成**：单步去噪特征提取和门控交叉注意力设计，既保留了 VLM 的强大语义能力，又避免了生成完整视频或流序列的高昂计算开销。
*   **具身无关性**：3D 场景流是在相机坐标系下定义的，具有很强的跨机器人泛化潜力。

#### 8. 不足与局限
*   **分辨率限制**：目前的运动专家运行在固定的 $20 \times 20$ 空间网格上，对于需要极高精度局部操作的任务（如穿针引线）可能分辨率不足。
*   **时间窗口固定**：预测视野固定为 32 步，对于极长时程的规划可能仍存在局限。
*   **数据依赖**：运动专家的监督信号依赖于 TraceForge 自动生成的伪标签，其质量上限受限于底层视觉算法（如深度估计和点追踪）的准确性。

（完）
