---
title: "Latent-WAM: Latent World Action Modeling for End-to-End Autonomous Driving"
title_zh: Latent-WAM：面向端到端自动驾驶的潜空间世界动作建模
authors: "Linbo Wang, Yupeng Zheng, Qiang Chen, Shiwei Li, Yichen Zhang, Zebin Xing, Qichao Zhang, Xiang Li, Deheng Qian, Pengxuan Yang, Yihang Dong, Ce Hao, Xiaoqing Ye, Junyu han, Yifeng Pan, Dongbin Zhao"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24581v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 端到端自动驾驶的潜空间世界动作建模
tldr: Latent-WAM 是一种高效的端到端自动驾驶框架，旨在解决现有世界模型在空间理解和动态建模方面的不足。它通过空间感知压缩世界编码器（SCWE）提取几何知识并将多视图图像压缩为场景标记，结合动态潜世界模型（DLWM）利用因果 Transformer 进行自回归预测。该模型仅需 104M 参数和较少训练数据，便在 NAVSIM v2 和 HUGSIM 基准测试中刷新了 SOTA 记录，显著提升了轨迹规划性能。
selection_source: fresh_fetch
motivation: 现有的基于世界模型的规划器在受限的数据和计算预算下，存在表示压缩不足、空间理解有限以及时间动态利用不充分的问题。
method: 提出了由空间感知压缩世界编码器（SCWE）和动态潜世界模型（DLWM）组成的框架，通过几何知识蒸馏和因果 Transformer 实现高效的场景表征与未来状态预测。
result: 在 NAVSIM v2 上达到 89.3 EPDMS，在 HUGSIM 上达到 28.9 HD-Score，以更小的模型规模和更少的数据量超越了先前的最优方法。
conclusion: Latent-WAM 证明了通过紧凑的潜空间表征和有效的动态建模，可以在较低的计算成本下实现卓越的端到端自动驾驶规划性能。
---

## 摘要
我们介绍了 Latent-WAM，这是一个高效的端到端自动驾驶框架，通过具有空间感知和动力学信息的潜空间世界表示实现了强大的轨迹规划。现有的基于世界模型的规划器面临表示压缩不足、空间理解有限以及时间动力学利用不充分的问题，导致在受限的数据和计算预算下规划效果不佳。Latent-WAM 通过两个核心模块解决了这些局限性：空间感知压缩世界编码器 (SCWE)，它从基础模型中提取几何知识，并通过可学习查询将多视图图像压缩为紧凑的场景标记 (tokens)；以及动态潜空间世界模型 (DLWM)，它采用因果 Transformer，在历史视觉和运动表示的约束下，自回归地预测未来的世界状态。在 NAVSIM v2 和 HUGSIM 上的广泛实验证明了新的最先进 (SOTA) 结果：在 NAVSIM v2 上达到 89.3 EPDMS，在 HUGSIM 上达到 28.9 HD-Score，在训练数据显著减少且模型参数仅为 104M 的情况下，比之前最好的无感知方法高出 3.2 EPDMS。

## Abstract
We introduce Latent-WAM, an efficient end-to-end autonomous driving framework that achieves strong trajectory planning through spatially-aware and dynamics-informed latent world representations. Existing world-model-based planners suffer from inadequately compressed representations, limited spatial understanding, and underutilized temporal dynamics, resulting in sub-optimal planning under constrained data and compute budgets. Latent-WAM addresses these limitations with two core modules: a Spatial-Aware Compressive World Encoder (SCWE) that distills geometric knowledge from a foundation model and compresses multi-view images into compact scene tokens via learnable queries, and a Dynamic Latent World Model (DLWM) that employs a causal Transformer to autoregressively predict future world status conditioned on historical visual and motion representations. Extensive experiments on NAVSIM v2 and HUGSIM demonstrate new state-of-the-art results: 89.3 EPDMS on NAVSIM v2 and 28.9 HD-Score on HUGSIM, surpassing the best prior perception-free method by 3.2 EPDMS with significantly less training data and a compact 104M-parameter model.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Latent-WAM** 的高效端到端自动驾驶框架，旨在通过空间感知和动力学信息的潜空间世界表示来提升轨迹规划性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的基于世界模型的端到端自动驾驶方案虽然减少了对感知标签的依赖，但存在三大痛点：
*   **表示压缩不足：** 潜空间表示依然过于沉重，导致计算开销大。
*   **空间理解有限：** 往往缺乏 3D 空间意识，或需依赖外部深度模型，增加了推理延迟。
*   **时间动态利用不充分：** 多数方法仅进行简单的单步预测（T 到 T+1），未能充分挖掘历史和未来的长时序动态。
**Latent-WAM** 的提出是为了在有限的数据和计算预算下，通过高度压缩且具备空间/动态感知能力的潜空间表征，实现更精准的驾驶规划。

### 2. 方法论：核心思想与关键技术
Latent-WAM 由三个核心模块组成：
*   **空间感知压缩世界编码器 (SCWE)：**
    *   **场景压缩：** 使用 DINOv2 作为视觉骨干，通过一组可学习的查询（Queries）将多视图图像压缩为极少量的场景标记（Tokens），大幅降低计算量。
    *   **几何对齐（知识蒸馏）：** 引入几何基础模型（WorldMirror）作为教师模型，通过余弦相似度损失将密集的几何特征蒸馏到视觉编码器中，使模型在不增加推理成本的前提下获得 3D 空间感知能力。
*   **动态潜世界模型 (DLWM)：**
    *   **因果 Transformer：** 采用自回归方式预测未来世界状态。它将场景标记与自我状态（速度、加速度、指令）结合，构建统一的“场景-自我”世界状态。
    *   **3D-RoPE：** 引入三维旋转位置编码，在注意力机制中显式注入时间、相机视角和标记位置信息，增强时空追踪能力。
    *   **教师强制掩码：** 训练时采用帧级注意力掩码，确保预测的因果性并提高训练效率。
*   **轨迹解码器：** 一个轻量级的 Transformer 解码器，根据当前世界状态和驾驶指令，从 K 个候选路径中选择最优轨迹。

### 3. 实验设计
*   **数据集：** 
    *   **NAVSIM v2：** 基于 nuPlan 的大规模真实场景数据集（103k 训练场景），使用 EPDMS（扩展预测驾驶模型得分）作为核心指标。
    *   **HUGSIM：** 一个光照真实的闭环模拟器，用于测试模型的零样本（Zero-shot）泛化能力。
*   **对比方法：** 包括感知驱动方法（如 UniAD, VAD, TransFuser）和无感知/世界模型方法（如 World4Drive, Epona, DriveVLA-W0）。

### 4. 资源与算力
*   **硬件：** 使用了 **32 台 NVIDIA A100 GPU**。
*   **训练时长：** 训练 100 个 epoch，耗时约 **2 天**。
*   **模型规模：** 推理时参数量仅为 **104M**，在同类模型中属于非常轻量级的水平。

### 5. 实验数量与充分性
论文进行了详尽的实验验证：
*   **主实验：** 在 NAVSIM v2 和 HUGSIM 两个基准上均取得了 SOTA（最先进）结果。
*   **消融实验：** 至少进行了 7 组消融研究，涵盖了组件有效性、几何注入方式（蒸馏 vs 拼接）、视觉骨干规模、训练策略（全微调 vs LoRA）以及预测时间步长等。
*   **定性分析：** 提供了注意力图可视化，证明了模型在几何蒸馏后能更聚焦于车道线和可行驶区域。
实验设计客观、公平，通过零样本测试验证了模型的鲁棒性。

### 6. 主要结论与发现
*   **性能领先：** 在 NAVSIM v2 上达到 89.3 EPDMS，超越了之前最好的无感知方法 3.2 分。
*   **数据效率极高：** 相比于需要数千小时数据的模型，Latent-WAM 在较小的数据规模下表现更优。
*   **几何蒸馏的关键性：** 实验证明，将几何知识蒸馏到骨干网络中比直接拼接特征更有效，能显著提升规划精度。
*   **闭环泛化：** 在未见过的 HUGSIM 场景中，模型在道路完成率（RC）上排名第一，证明了其强大的泛化能力。

### 7. 优点（亮点）
*   **高效压缩：** 通过 Query 机制将沉重的视觉特征压缩为紧凑 Token，极大地优化了世界模型的运行效率。
*   **空间感知：** 巧妙地利用基础模型的几何知识进行离线蒸馏，使轻量级模型具备了复杂的 3D 理解力。
*   **端到端一致性：** 统一了视觉预测和运动预测，使世界模型生成的表征直接服务于规划任务。

### 8. 不足与局限
*   **安全指标微调：** 虽然整体得分最高，但在某些纯安全性指标（如碰撞率 NC）上，仍略逊于部分依赖显式感知标注的方法。
*   **长时序依赖：** 尽管引入了长时序预测，但在极端复杂的动态交通流中，自回归预测可能仍存在累积误差的风险。
*   **应用限制：** 目前主要在离线数据集和特定模拟器上验证，实际实车部署中的实时性与不确定性处理仍需进一步探讨。

（完）
