---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种面向视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v2"
tags: ["query:ad-ei"]
score: 10.0
evidence: 针对VLA模型的异步强化学习与世界模型
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难等挑战，本文提出了AcceRL框架。该框架采用完全异步且解耦的设计，通过物理隔离训练、推理和采样过程消除同步瓶颈，并首次将可训练的世界模型集成到分布式异步流水线中以生成虚拟经验。实验证明，AcceRL在LIBERO基准上达到了SOTA水平，展现出超线性的吞吐量扩展能力和极高的采样效率。
selection_source: fresh_fetch
motivation: 解决大规模VLA模型在强化学习训练过程中存在的计算效率低下和数据获取困难的问题。
method: 提出一种完全异步且解耦的分布式框架，通过物理隔离核心环节并集成可插拔的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，并实现了超线性的吞吐量增长和卓越的硬件利用率。
conclusion: AcceRL为大规模VLA模型的高效训练提供了强有力的分布式异步强化学习与世界模型集成方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样（rollouts）过程来消除同步障碍。至关重要的是，AcceRL 是首个将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中以生成虚拟经验的框架。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展和高效的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。代码已在 https://github.com/distanceLu/AcceRL 公开。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO~\cite{liu2023libero} benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks. Code is publicly available at https://github.com/distanceLu/AcceRL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AcceRL** 的分布式异步强化学习框架，专门针对大规模视觉-语言-动作（VLA）模型设计。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：大规模 VLA 模型（如 7B 参数量级）在具身智能领域展现了巨大潜力，但利用强化学习（RL）对其进行微调面临严峻挑战。
*   **核心痛点**：
    *   **同步瓶颈**：传统 RL 框架采用“收集-更新”的同步模式，导致高性能 GPU 在等待缓慢的物理模拟器时处于闲置状态（长尾效应）。
    *   **数据饥渴**：物理模拟器运行速度慢，而大模型训练需要海量交互数据。
    *   **硬件限制**：VLA 模型显存占用极高，传统的推理与训练共存模式易导致显存溢出（OOM）。

### 2. 方法论与关键技术
AcceRL 提出了一个**完全解耦且异步**的架构，核心思想是将训练、推理和环境交互在物理上隔离。
*   **双层异步机制**：
    *   **宏观异步**：训练器（Trainer）与采样器（Rollout Worker）解耦。训练器不断从重放池采样更新，采样器独立生成轨迹，互不阻塞。
    *   **微观异步**：环境交互与模型推理解耦。采用“推理即服务”（Inference-as-a-Service）模式，通过动态窗口机制实现高效的 GPU 批处理推理。
*   **集成可训练世界模型（World Model）**：
    *   **核心创新**：首个在分布式异步流水线中集成可插拔世界模型的框架。
    *   **“想象中学习”**：利用基于扩散模型（Diffusion-based）的世界模型生成高保真虚拟经验，绕过物理模拟器的速度限制，将样本效率提升了 200 倍。
*   **算法优化细节**：
    *   **词表瘦身（Vocabulary Slimming）**：裁剪语言模型头，仅保留动作相关的 Token，降低显存占用并提升吞吐量。
    *   **GIPO 算法**：采用高斯重要性采样策略优化，通过软约束替代 PPO 的硬截断，解决异步架构中的策略滞后（Policy Lag）问题。
    *   **Token 级优化**：在 Token 级别计算损失，避免动作块（Action Chunk）过长导致的数值不稳定。

### 3. 实验设计
*   **基准测试**：使用 **LIBERO** 机器人操作基准，涵盖 LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务）四个子集。
*   **对比方法**：
    *   **模仿学习基准**：OpenVLA-OFT。
    *   **SOTA RL 框架**：RLinf-VLA、SimpleVLA-RL。
*   **实验类型**：吞吐量扩展性测试、任务成功率对比、世界模型效率分析、消融实验（针对价值重计算和 GIPO 算法）。

### 4. 资源与算力
*   **硬件型号**：实验使用了 **NVIDIA H200 GPU**。
*   **算力规模**：
    *   训练器扩展性测试涵盖了 1 到 7 个 GPU。
    *   采样器扩展性测试最高达到 64 个 Rollout Worker。
*   **训练细节**：利用 DeepSpeed ZeRO-2 优化器状态分区，实现了超线性的吞吐量增长（在 7 个 H200 上达到 104.22 SPS）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括在 4 个复杂任务集上的性能评估，以及针对系统吞吐量的详细压力测试。
*   **充分性评价**：实验设计较为充分。通过消融实验验证了“价值重计算”和“GIPO”对稳定异步训练的必要性；通过对比世界模型开启前后的学习曲线，客观展示了样本效率的巨大提升。实验覆盖了从系统效率到算法效果的全过程。

### 6. 主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有任务类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中表现出极强的鲁棒性（成功率 99.1%）。
*   **超线性扩展**：得益于 ZeRO 优化和完全解耦架构，系统吞吐量随 GPU 数量增加呈超线性增长，硬件利用率保持在 94% 以上。
*   **效率飞跃**：引入世界模型后，仅需 10,000 次真实环境交互即可完成策略微调，相比纯模型驱动的方法实现了数量级的效率提升。

### 7. 优点与亮点
*   **架构创新**：彻底解决了分布式 RL 中的“长尾效应”和 GPU 空转问题。
*   **即插即用**：世界模型模块化设计，可根据需求灵活开启，支持“在想象中进化”。
*   **工程落地性强**：词表裁剪和推理池化等设计非常贴合大规模 VLA 模型在有限显存下的部署需求。

### 8. 不足与局限
*   **模型覆盖**：目前主要针对 VLA 模型的动作输出进行优化，尚未支持超大规模语言模型（LLM）的全量对齐（Alignment）训练。
*   **世界模型开销**：虽然世界模型减少了环境交互，但训练和运行高保真扩散模型本身也需要额外的计算资源。
*   **应用限制**：实验主要在模拟器（MuJoCo）中完成，在真实物理世界中的泛化能力和实时性表现仍需进一步验证。

（完）
