---
title: Modernising Reinforcement Learning-Based Navigation for Embodied Semantic Scene Graph Generation
title_zh: 现代化基于强化学习的导航，用于具身语义场景图生成
authors: "Roman Kueble, Marco Hueller, Mrunmai Phatak, Rainer Lienhart, Joerg Haehner"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25415v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 具身智能体的语义世界模型与强化学习导航
tldr: 本研究针对具身语义场景图（SSG）生成中的导航效率问题，提出了一种现代化的强化学习导航组件。通过改进策略优化算法、重新设计离散动作空间（从原子动作转向分解式多头策略）以及引入深度图碰撞监督，该方法在有限动作预算内实现了更优的信息获取与导航成本平衡。实验表明，该方法显著提升了场景图的完整性与执行安全性，为具身智能体的环境理解提供了高效方案。
selection_source: fresh_fetch
motivation: 在有限动作预算下，如何通过高效的导航策略获取观测数据以构建高质量的语义场景图是具身智能面临的核心挑战。
method: 改进了强化学习优化算法，并对比了细粒度离散动作集与分解式多头策略在导航决策中的表现。
result: "仅更换优化算法就使场景图完整性提升了21%，而结合细粒度分解动作表示则实现了最佳的完整性与效率权衡。"
conclusion: 现代化的策略优化与分解式动作空间设计能显著增强具身智能体在复杂环境中的语义感知与导航能力。
---

## 摘要
语义世界模型使具身智能体能够超越纯几何表示，对物体、关系和空间上下文进行推理。在有机计算中，此类模型是在不确定性和资源限制下实现目标驱动自适应的关键推动因素。核心挑战是在有限的动作预算内获取能最大化模型质量和下游效用的观测结果。语义场景图（SSG）为此提供了一种结构化且紧凑的表示。然而，在有限的动作时界内构建它们需要探索策略，在信息增益与导航成本之间进行权衡，并决定何时额外的动作会产生收益递减。本研究为具身语义场景图生成提出了一个模块化导航组件，并通过替换策略优化方法和重新审视离散动作公式来使其决策过程现代化。我们研究了紧凑且更细粒度的、更大的离散运动集，并比较了原子动作上的单头策略与动作分量上的分解多头策略。我们评估了课程学习和可选的基于深度的碰撞监督，并评估了 SSG 的完整性、执行安全性和导航行为。结果表明，在相同的奖励塑造下，仅更换优化算法就能使 SSG 的完整性相对于基准提高 21%。深度主要影响执行安全性（无碰撞运动），而完整性基本保持不变。将现代优化与更细粒度的分解动作表示相结合，可以实现最强的整体完整性与效率之间的权衡。

## Abstract
Semantic world models enable embodied agents to reason about objects, relations, and spatial context beyond purely geometric representations. In Organic Computing, such models are a key enabler for objective-driven self-adaptation under uncertainty and resource constraints. The core challenge is to acquire observations maximising model quality and downstream usefulness within a limited action budget.   Semantic scene graphs (SSGs) provide a structured and compact representation for this purpose. However, constructing them within a finite action horizon requires exploration strategies that trade off information gain against navigation cost and decide when additional actions yield diminishing returns.   This work presents a modular navigation component for Embodied Semantic Scene Graph Generation and modernises its decision-making by replacing the policy-optimisation method and revisiting the discrete action formulation. We study compact and finer-grained, larger discrete motion sets and compare a single-head policy over atomic actions with a factorised multi-head policy over action components. We evaluate curriculum learning and optional depth-based collision supervision, and assess SSG completeness, execution safety, and navigation behaviour.   Results show that replacing the optimisation algorithm alone improves SSG completeness by 21\% relative to the baseline under identical reward shaping. Depth mainly affects execution safety (collision-free motion), while completeness remains largely unchanged. Combining modern optimisation with a finer-grained, factorised action representation yields the strongest overall completeness--efficiency trade-off.

---

## 论文详细总结（自动生成）

# 论文总结：现代化基于强化学习的导航，用于具身语义场景图生成

### 1. 核心问题与整体含义
*   **研究背景**：具身智能体（Embodied Agents）在未知环境中需要构建语义场景图（SSG）来理解物体、属性及其空间关系。这不仅是几何建图，更是语义理解。
*   **核心挑战**：在有限的动作预算（Action Budget）内，智能体必须决定如何导航以获取最有价值的观测，从而最大化场景图的完整性。
*   **研究动机**：现有的具身语义场景图生成（ESSG）基准方法（如 Li et al., 2022）存在算法不稳定（使用 REINFORCE）、动作空间受限（离散度低）以及导航效率低下的问题。本论文旨在通过现代化的强化学习技术提升导航组件的性能。

### 2. 方法论
*   **核心思想**：将导航视为一个自优化的模块，通过改进策略优化算法和动作空间设计，在固定步数内实现更高效的环境探索。
*   **关键技术细节**：
    *   **算法升级**：使用 **PPO（近端策略优化）** 替换传统的 REINFORCE 算法，引入优势估计（GAE）和价值函数裁剪，提升训练稳定性。
    *   **动作空间重构**：
        *   **单头原子动作（SH）**：智能体从预定义的动作 ID 中选择（如旋转 45° + 移动 0.3m）。
        *   **分解式多头策略（MH）**：将动作分解为“旋转角度”、“移动长度”和“停止决策”三个独立的预测头。这允许更细粒度的控制（如 15° 旋转分辨率，0.1m-2.0m 的步长范围）。
    *   **多模态状态表示**：融合 RGB 图像、深度图（可选）、局部场景图（LSSG）、全局场景图（GSSG）以及**停滞检测嵌入**（通过监测 GSSG 嵌入的变化来判断是否陷入重复探索）。
    *   **奖励塑造**：结合了基于势能的密集奖励（节点/边缘召回率、视角多样性）和基于事件的稀疏奖励（碰撞惩罚、新物体发现奖励、合理的停止奖励）。

### 3. 实验设计
*   **模拟环境**：使用 **AI2-THOR** 交互式室内模拟器。
*   **数据集/场景**：使用 FloorPlans 1-27 进行训练，FloorPlans 28-30 作为持留测试集。
*   **Benchmark**：以 Li et al. (2022) 的 ESSG 框架为主要对比基准。
*   **对比方法**：
    *   算法对比：REINFORCE vs. PPO。
    *   动作空间对比：SH16（低分辨率） vs. SH504（高分辨率） vs. MH504（分解式高分辨率）。
    *   消融实验：有无深度图输入、有无课程学习（CL）、有无模仿学习（IL）预训练。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号（如 NVIDIA A100 或 RTX 系列）及具体的训练总时长。
*   **训练规模**：提到使用了 **Optuna** 进行大规模超参数搜索；训练过程包含 1000 个迭代块（Blocks），每个块包含 32 个并行环境的采样，每批次处理 32 条轨迹。

### 5. 实验数量与充分性
*   **实验组数**：设计了从 S0 到 S9 以及 Baseline 共 11 种主要配置场景。
*   **充分性**：
    *   每种配置均使用 **5 个随机种子**进行重复实验，并给出了 95% 置信区间。
    *   进行了详尽的消融实验，涵盖了算法、动作表示、输入模态和训练策略。
    *   **客观性**：通过贝叶斯优化（Optuna）为不同场景独立寻找最优超参数，确保了对比的公平性，避免了因超参数未对齐导致的偏差。

### 6. 主要结论与发现
*   **算法是关键**：在相同奖励机制下，仅将 REINFORCE 替换为 PPO，场景图完整性（Node Recall）就提升了 **21%**。
*   **动作空间的影响**：高分辨率动作空间显著提升了探索上限。分解式多头策略（MH）在处理大规模动作空间时比原子动作（SH）更稳健，学习速度更快，且能生成更合理的空间分布轨迹。
*   **深度信息的作用**：深度图输入和碰撞辅助头主要提升了**执行安全性**（减少碰撞），但对场景图的最终完整性提升并不显著。
*   **课程学习（CL）**：在 MH 模式下，CL 能够显著提升训练初期的安全性，但在固定预算下可能会略微减慢收敛速度。

### 7. 优点
*   **模块化解耦**：将导航作为独立组件进行现代化改造，其结论对具身 AI 的其他任务（如物体导航、视觉寻路）具有参考价值。
*   **动作空间洞察**：深入探讨了离散动作空间的分辨率与参数化方式对探索效率的影响。
*   **鲁棒性设计**：引入停滞检测和显式停止机制，使智能体具备了“知道何时完成任务”的能力，更符合有机计算（Organic Computing）的自适应原则。

### 8. 不足与局限
*   **感知理想化**：实验中的场景图构建基于模拟器元数据（Oracle），即假设感知和数据关联是完美的，这与现实中存在噪声的视觉感知系统有差距。
*   **环境规模限制**：实验仅在相对较小的室内场景（FloorPlans）中进行，在更复杂、多楼层或室外环境中的泛化能力尚待验证。
*   **变量耦合**：高分辨率动作空间在增加精细度的同时也增加了最大步长上限，实验未能完全隔离“步长增加”与“分辨率增加”各自带来的贡献。

（完）
