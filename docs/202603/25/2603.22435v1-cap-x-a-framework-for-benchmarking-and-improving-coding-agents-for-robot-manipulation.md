---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：一个用于基准测试和提升机器人操纵编程智能体的框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 评估代码即策略作为机器人操纵中VLA方法补充的效果
tldr: 本研究针对机器人操作中的“代码即策略”（CaP）方法，提出了CaP-X开源框架。该框架包含交互式环境CaP-Gym和评估基准CaP-Bench，系统分析了前沿模型在不同抽象层级下的表现。研究发现，虽然模型性能依赖人工设计的先验，但通过多轮交互、反馈修正等测试时计算手段可显著增强鲁棒性。据此，作者开发了无需训练的CaP-Agent0和结合强化学习的CaP-RL，在仿真和真实机器人任务中均达到了人类水平的可靠性。
selection_source: fresh_fetch
motivation: 旨在系统性地研究和提升“代码即策略”代理在机器人操作任务中作为自主控制器的有效性与鲁棒性。
method: 提出了包含交互环境、评估基准、无训练代理框架及强化学习优化方案的综合性CaP-X框架。
result: 揭示了模型性能对人工抽象的依赖，并证明通过增加测试时计算（如反馈和集成推理）能有效弥补低级原语操作的性能差距。
conclusion: CaP-X为推动具备高可靠性和跨环境迁移能力的机器人编码代理研究提供了一个原则性的开源平台。
---

## 摘要
“Code-as-Policy”探讨了可执行代码如何补充数据密集型的视觉-语言-动作（VLA）方法，但其作为具身操纵自主控制器的有效性仍未得到充分探索。我们提出了 CaP-X，这是一个用于系统研究机器人操纵中 Code-as-Policy 智能体的开源框架。其核心是 CaP-Gym，一个智能体通过合成并执行组合了感知与控制原语的程序来控制机器人的交互式环境。在此基础上，CaP-Bench 在不同的抽象级别、交互程度和感知落地方面评估了前沿的语言和视觉-语言模型。通过对 12 个模型的评估，CaP-Bench 揭示了一个一致的趋势：性能随人工设计的抽象而提升，但随着这些先验知识的移除而下降，暴露了对设计者脚手架的依赖。同时，我们观察到这种差距可以通过扩展智能体测试时计算（通过多轮交互、结构化执行反馈、视觉差异化、自动技能合成和集成推理）来弥补，即使智能体在底层原语上运行，也能显著提高鲁棒性。这些发现使我们推导出 CaP-Agent0，这是一个无需训练的框架，在仿真和真实具身环境下的多项操纵任务中达到了人类水平的可靠性。我们进一步引入了 CaP-RL，表明具有可验证奖励的强化学习可以提高成功率，并能以极小的差距实现从仿真到现实（sim2real）的迁移。总之，CaP-X 为推进具身编程智能体提供了一个规范的开源平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

### CaP-X：机器人操纵编程智能体基准测试与提升框架总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了“代码即策略”（Code-as-Policy, CaP）在机器人操纵中的有效性。
*   **研究动机**：传统的机器人控制（如TAMP）依赖人工设计，难以扩展；而端到端的视觉-语言-动作（VLA）模型虽然强大，但缺乏可解释性且依赖海量数据。CaP利用大语言模型（LLM）生成可执行代码来控制机器人，但现有方法高度依赖人工预设的高级原语（如`stack_objects()`），掩盖了智能体在底层逻辑上的真实能力。
*   **核心问题**：当移除人工设计的“脚手架”时，编程智能体的表现如何？能否通过增加测试时计算（Test-time Computation）来弥补底层原语带来的性能损失？

#### 2. 方法论：核心思想与关键技术
论文提出了 **CaP-X** 框架，包含四个核心组件：
*   **CaP-Gym**：一个分层控制框架，将物理模拟器（如Robosuite, LIBERO, BEHAVIOR）与Python代码执行器（REPL）绑定，使智能体能通过编写代码调用感知和控制原语。
*   **CaP-Bench**：一个系统化的基准测试，从三个维度评估模型：
    *   **抽象层级**：从高级宏指令到原子级底层原语。
    *   **时间交互**：对比单轮生成与多轮交互（错误恢复）。
    *   **感知落地**：评估不同视觉反馈模式（如原始图像 vs. 文本描述）。
*   **CaP-Agent0**：一个无需训练的智能体框架，包含：
    *   **视觉差异模块（VDM）**：将视觉观察转化为结构化文本描述，对比前后状态。
    *   **自动合成技能库**：从成功案例中提取通用的底层逻辑函数。
    *   **并行推理与集成**：多模型并行生成候选代码并由中心智能体合成。
*   **CaP-RL**：利用可验证奖励（RLVR）和GRPO算法对代码生成模型进行在线强化学习，优化其编程策略。

#### 3. 实验设计
*   **数据集/场景**：集成了187个任务，涵盖桌面操纵、双臂协作和移动操纵。核心分析集中在7个代表性任务（如方块堆叠、溢出擦拭、销钉插入等）。
*   **Benchmark**：CaP-Bench，分为S1-S4（单轮）和M1-M4（多轮）共8个评估等级。
*   **对比方法**：
    *   **模型对比**：评估了12个前沿模型（包括GPT-5.2、Gemini-3-Pro、Claude 4.5、DeepSeek-V3.1、Qwen-235B等）。
    *   **基准对比**：对比了人类专家编写的代码。
    *   **VLA对比**：在LIBERO-PRO任务中对比了OpenVLA、$\pi_0$ 和 $\pi_{0.5}$。

#### 4. 资源与算力
*   **算力说明**：文中未详细列出推理阶段使用的具体GPU型号和数量。
*   **训练细节**：在CaP-RL部分，提到对Qwen2.5-Coder-7B进行了50次迭代的强化学习训练。由于CaP-Agent0是无需训练的（Training-free），其主要开销在于调用API的推理成本。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务/等级通常进行100次试验（Trials），总计评估了12个模型在多个维度下的表现，实验数据量大。
*   **充分性**：包含了消融实验（针对CaP-Agent0的各个组件）、仿真与现实世界的对比、以及与当前最强VLA模型的直接竞争。
*   **公平性**：所有模型在相同的感知和控制原语下进行测试，并与人类专家基准进行对比，评估标准客观。

#### 6. 主要结论与发现
*   **抽象依赖性**：模型性能随原语抽象层级的降低而显著下降，证明当前CaP方法高度依赖人工设计的API。
*   **测试时计算的价值**：多轮交互、结构化反馈（VDM）和集成推理能显著提升鲁棒性，使智能体在底层原语上的表现接近甚至超过高级原语的单轮表现。
*   **文本反馈优于图像**：直接将原始图像输入VLM进行代码生成的效果，不如先将图像转化为文本描述（VDM）再输入模型。
*   **CaP-Agent0的优越性**：在无需任务特定训练的情况下，在多项任务上达到了人类水平，且在指令扰动下比VLA模型更具鲁棒性。

#### 7. 优点
*   **系统性强**：首次深入探讨了API抽象层级对编程智能体的影响，打破了以往CaP研究中“高级原语即性能”的幻觉。
*   **开源贡献**：提供了统一的CaP-Gym环境，支持多种模拟器，方便后续研究。
*   **实用性**：CaP-Agent0无需训练即可部署，且CaP-RL展示了极小的Sim-to-Real差距。

#### 8. 不足与局限
*   **接触密集型任务挑战**：对于需要高频视觉伺服和连续反馈的任务（如精确插入、倾倒），编程智能体仍显笨拙，不如端到端模型流畅。
*   **推理延迟**：多轮交互和多模型集成虽然提高了成功率，但也带来了显著的推理延迟，限制了实时性要求极高的应用。
*   **依赖感知模块**：VDM和感知原语的准确性直接决定了代码生成的质量，感知层面的微小误差可能导致整个程序的逻辑崩溃。

（完）
