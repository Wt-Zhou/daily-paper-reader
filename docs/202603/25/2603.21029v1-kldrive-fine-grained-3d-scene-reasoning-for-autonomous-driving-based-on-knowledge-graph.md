---
title: "KLDrive: Fine-Grained 3D Scene Reasoning for Autonomous Driving based on Knowledge Graph"
title_zh: KLDrive：基于知识图谱的自动驾驶细粒度三维场景推理
authors: "Ye Tian, Jingyi Zhang, Zihao Wang, Xiaoyuan Ren, Xiaofan Yu, Onat Gungor, Tajana Rosing"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21029v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 知识图谱增强的大语言模型用于自动驾驶推理
tldr: 针对自动驾驶中3D场景推理存在的幻觉和事实不可靠问题，本文提出KLDrive框架。该框架首次将知识图谱与大语言模型结合，通过能量模型构建可靠的场景知识图谱，并利用LLM代理在显式结构约束下进行事实驱动的推理。KLDrive在NuScenes-QA和GVQA基准上均取得领先性能，显著提升了计数等复杂推理任务的准确性，且无需大规模任务特定微调。
selection_source: fresh_fetch
motivation: 现有的自动驾驶大模型方法在处理细粒度3D场景推理时，常面临感知事实不可靠、推理过程不透明及严重的幻觉问题。
method: 提出一种由能量驱动的场景事实构建模块和基于显式结构约束的LLM推理代理组成的知识图谱增强框架。
result: 在NuScenes-QA和GVQA数据集上刷新了SOTA记录，特别是在最具挑战性的计数任务上比最强基准提升了46.01个百分点。
conclusion: 通过将可靠的结构化知识图谱与受限动作空间的LLM推理相结合，能有效减少模型幻觉并提升自动驾驶场景下的细粒度推理能力。
---

## 摘要
自动驾驶需要对细粒度的三维场景事实进行可靠的推理。基于多模态驾驶观测的细粒度问答为评估这一能力提供了一种自然的方式，然而现有的感知流水线和面向驾驶的大语言模型（LLM）方法仍面临场景事实不可靠、幻觉、推理不透明以及对特定任务训练的严重依赖等问题。我们提出了 KLDrive，这是首个用于自动驾驶细粒度问答的知识图谱增强型 LLM 推理框架。KLDrive 通过设计两个紧密耦合的组件来解决这一问题：一个基于能量的场景事实构建模块，用于将多源证据整合为可靠的场景知识图谱；以及一个 LLM 智能体，在显式结构约束下，在受限的动作空间内执行基于事实的推理。通过将结构化提示与少样本上下文示例相结合，该框架无需繁重的特定任务微调即可适应多样化的推理任务。在两个大规模自动驾驶问答基准测试上的实验表明，KLDrive 优于现有的最先进方法，在 NuScenes-QA 上实现了 65.04% 的最佳总准确率，在 GVQA 上实现了 42.45 的最佳 SPICE 分数。在最具挑战性的事实推理任务——计数上，它比最强的基准模型提高了 46.01 个百分点，证明了幻觉的显著减少，以及将可靠的场景事实构建与显式推理相结合的优势。

## Abstract
Autonomous driving requires reliable reasoning over fine-grained 3D scene facts. Fine-grained question answering over multi-modal driving observations provides a natural way to evaluate this capability, yet existing perception pipelines and driving-oriented large language model (LLM) methods still suffer from unreliable scene facts, hallucinations, opaque reasoning, and heavy reliance on task-specific training. We present KLDrive, the first knowledge-graph-augmented LLM reasoning framework for fine-grained question answering in autonomous driving. KLDrive addresses this problem through designing two tightly coupled components: an energy-based scene fact construction module that consolidates multi-source evidence into a reliable scene knowledge graph, and an LLM agent that performs fact-grounded reasoning over a constrained action space under explicit structural constraints. By combining structured prompting with few-shot in-context exemplars, the framework adapts to diverse reasoning tasks without heavy task-specific fine-tuning. Experiments on two large-scale autonomous-driving QA benchmarks show that KLDrive outperforms prior state-of-the-art methods, achieving the best overall accuracy of 65.04% on NuScenes-QA and the best SPICE score of 42.45 on GVQA. On counting, the most challenging factual reasoning task, it improves over the strongest baseline by 46.01 percentage points, demonstrating substantially reduced hallucinations and the benefit of coupling reliable scene fact construction with explicit reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《KLDrive: Fine-Grained 3D Scene Reasoning for Autonomous Driving based on Knowledge Graph》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：自动驾驶系统不仅需要识别物体，还需要对复杂的3D场景事实（如物体身份、运动状态、空间关系）进行细粒度推理。
*   **核心挑战**：
    1.  **感知事实不可靠**：受环境噪声、遮挡影响，单一传感器或检测器常出现漏检、误报或不一致。
    2.  **推理幻觉与不透明**：现有的端到端多模态大模型（VLM）在处理细粒度空间和事实查询时，推理过程像“黑盒”，且容易产生严重的幻觉（如数错车辆数量）。
    3.  **任务适配成本高**：现有的驾驶专用模型通常需要针对特定任务进行大规模的微调。
*   **整体含义**：本文提出了 **KLDrive**，这是首个结合知识图谱（KG）与大语言模型（LLM）的自动驾驶推理框架，旨在通过构建可靠的结构化场景事实，实现可解释、低幻觉的细粒度场景问答。

### 2. 论文提出的方法论
KLDrive 由两个紧密耦合的阶段组成：

*   **阶段一：基于能量的场景知识图谱生成**
    *   **多源证据检测**：同时利用相机（RayDN）、激光雷达（FocalFormer3D）和融合检测器（IS-Fusion）获取候选目标。
    *   **证据池化与恢复**：通过跨源池化整合不同检测器的输出，并利用时间序列上的相邻帧进行插值，恢复因遮挡或运动模糊丢失的物体。
    *   **能量模型（EBM）优化**：定义了一个能量函数 $E$，综合考虑候选物体的可保留性（置信度）、成对交互（去重）、属性一致性（运动状态合理性）和支持度（时间/上下文支持）。通过最小化能量函数，从噪声候选集中筛选出最可靠的场景实体，构建 3D 场景 KG。
*   **阶段二：LLM 驱动的编排与推理**
    *   **Plan-Execute-Observe 循环**：LLM 不直接生成答案，而是作为语义规划器，在受限的动作空间内调用工具。
    *   **受限动作空间**：定义了一套场景查询代数（如 `Resolve` 筛选物体、`RelSelect` 空间关系选择、`Count` 计数等）。
    *   **上下文学习（ICL）**：通过结构化提示词和少量示例（Few-shot），引导冻结的 LLM（如 Qwen3-7B）生成可执行的符号程序，确保推理过程严格基于 KG 事实。

### 3. 实验设计
*   **数据集**：
    1.  **NuScenes-QA**：包含 39,704 个细粒度事实推理问题，涵盖存在性、计数、对象查询、状态查询和比较五大类。
    2.  **GVQA**：包含 104,030 个驾驶理解任务，答案为句子形式。
*   **Benchmark 与对比方法**：
    *   **感知基准**：RayDN、FocalFormer3D、IS-Fusion（外接 QA 头）。
    *   **通用 VLM**：LLaVA-v1.6-Mistral-7B、InternVL-3.5-8B、Qwen3Vision-8B。
    *   **驾驶专用模型**：DriveLM、LiDAR-LLM、MAPLM、CREMA（均经过特定任务微调）。

### 4. 资源与算力
*   **硬件环境**：
    *   **工作站**：AMD Ryzen Threadripper 9985WX CPU，768 GB RAM，NVIDIA RTX A6000 GPU。
    *   **边缘设备**：NVIDIA Jetson Orin NX (16 GB RAM)，用于测试推理延迟。
*   **模型配置**：LLM 后端使用 Qwen3-7B，保持参数冻结，仅通过 ICL 运行。感知前端在相应数据集上进行了预训练。
*   **训练时长**：文中未明确给出感知前端的具体训练总时长，但强调了推理阶段无需对 LLM 进行微调。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流大规模基准上进行了全面测试，涵盖了数万个样本。
*   **消融实验**：
    *   验证了 **KG 生成模块** 的有效性（对比了无 EBM 优化的情况）。
    *   验证了 **LLM Agent 模块** 的必要性（对比了 LLM 直接生成答案的情况）。
    *   分析了不同推理难度（H0 零步推理 vs H1 一步推理）下的表现。
*   **客观性与公平性**：对比了当前最先进的（SOTA）微调模型和通用大模型，并提供了基于真值（Ground Truth）的 KLDrive-GT 版本作为性能上限参考，实验设计较为严谨、客观。

### 6. 论文的主要结论与发现
*   **性能领先**：KLDrive 在 NuScenes-QA 上达到 65.04% 的准确率，在 GVQA 上 SPICE 分数达到 42.45，均刷新了记录。
*   **显著抑制幻觉**：在最难的**计数任务**上，KLDrive 比最强基准（MAPLM）高出 **46.01 个百分点**，证明了符号化推理在处理数量事实上的绝对优势。
*   **推理透明度**：通过 Plan-Execute-Observe 循环，系统生成的每一个答案都可以追溯到具体的 KG 节点和工具调用链。
*   **感知质量是瓶颈**：KLDrive-GT（使用真值 KG）的准确率高达 84.49%，说明进一步提升感知精度是未来提升推理性能的关键。

### 7. 优点
*   **架构创新**：首次将 EBM 驱动的可靠 KG 构建与受限动作空间的 LLM Agent 结合，解决了自动驾驶推理中的“幻觉”顽疾。
*   **可解释性**：推理过程是符号化的、可审计的，这对于安全至上的自动驾驶领域至关重要。
*   **高效适配**：无需昂贵的特定任务微调，仅靠 Few-shot 即可适配多种问答任务。
*   **鲁棒性**：通过多源证据融合和能量模型优化，有效缓解了单一传感器失效带来的感知错误。

### 8. 不足与局限
*   **实时性限制**：目前定位为“离线分析”设置。在 RTX A6000 上平均延迟为 1.26s，在边缘设备 Orin 上延迟高达 57s，无法直接用于毫秒级的在线实时决策。
*   **感知依赖**：虽然 EBM 能优化结果，但如果所有传感器都发生系统性漏检，KG 仍无法补全事实。
*   **空间关系词库有限**：目前仅支持 6 个主要的方位词（如 front, back_left 等），对于更复杂的空间描述（如“在两车之间稍微偏左”）可能覆盖不足。

（完）
