---
title: "Describe-Then-Act: Proactive Agent Steering via Distilled Language-Action World Models"
title_zh: 先描述后行动：通过蒸馏语言-动作世界模型实现主动式智能体引导
authors: "Massimiliano Pappa, Luca Romani, Valentino Sacco, Alessio Palma, Stéphane Lathuilière, Fabio Galasso, Xavier Alameda-Pineda, Indro Spinelli"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23149v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 用于主动智能体引导的蒸馏语言-动作世界模型
tldr: 针对安全关键型智能体在执行动作前需预判后果但视觉模拟延迟高的问题，本文提出DILLO框架。该方法挑战了视觉处理在故障预防中的必要性，通过跨模态蒸馏将视觉语言模型（VLM）的知识迁移到轻量级大语言模型（LLM）中，实现从“模拟后行动”到“描述后行动”的转变。DILLO仅需文本推理即可预测动作语义结果，推理速度提升14倍，并在MetaWorld和LIBERO任务中显著提升了任务成功率。
selection_source: fresh_fetch
motivation: 现有的基于视觉模拟的世界模型推理延迟过高，难以满足实时安全预判的需求。
method: 提出DILLO框架，利用VLM作为教师标注轨迹，训练一个以潜变量为条件的LLM学生模型来预测语义结果，实现纯文本推理。
result: 在MetaWorld和LIBERO基准测试中，DILLO实现了14倍的加速，并将平均任务成功率提升了9.3个百分点。
conclusion: 证明了潜状态与动作已包含足够的预测信息，通过语义描述而非视觉模拟可更高效地实现智能体的预防性引导。
---

## 摘要
部署安全关键型智能体需要在执行动作之前预判其后果。虽然世界模型为这种主动预见提供了一种范式，但目前依赖视觉模拟的方法会产生极高的延迟，每步通常超过数秒。在这项工作中，我们挑战了视觉处理对于预防失败是必要的这一假设。我们证明，训练好的策略的潜状态结合其计划动作，已经编码了足够的信息来预判动作结果，从而使视觉模拟在预防失败方面变得冗余。为此，我们引入了 DILLO（蒸馏语言-动作世界模型），这是一个快速引导层，将范式从“先模拟后行动”转变为“先描述后行动”。DILLO 通过跨模态蒸馏进行训练，其中特权视觉语言模型教师对离线轨迹进行标注，而以潜状态为条件的大语言模型学生则学习预测语义结果。这创建了一条纯文本推理路径，完全绕过了沉重的视觉生成，比基准方法实现了 14 倍的加速。在 MetaWorld 和 LIBERO 上的实验表明，DILLO 能够生成下一状态的高保真描述，并能引导策略，使各任务的平均回合成功率提高了 9.3 个百分点，最高提升达 15 个百分点。

## Abstract
Deploying safety-critical agents requires anticipating the consequences of actions before they are executed. While world models offer a paradigm for this proactive foresight, current approaches relying on visual simulation incur prohibitive latencies, often exceeding several seconds per step. In this work, we challenge the assumption that visual processing is necessary for failure prevention. We show that a trained policy's latent state, combined with its planned actions, already encodes sufficient information to anticipate action outcomes, making visual simulation redundant for failure prevention. To this end, we introduce DILLO (DIstiLLed Language-ActiOn World Model), a fast steering layer that shifts the paradigm from "simulate-then-act" to "describe-then-act." DILLO is trained via cross-modal distillation, where a privileged Vision Language Model teacher annotates offline trajectories and a latent-conditioned Large Language Model student learns to predict semantic outcomes. This creates a text-only inference path, bypassing heavy visual generation entirely, achieving a 14x speedup over baselines. Experiments on MetaWorld and LIBERO demonstrate that DILLO produces high-fidelity descriptions of the next state and is able to steer the policy, improving episode success rate by up to 15 pp and 9.3 pp on average across tasks.

---

## 论文详细总结（自动生成）

### 论文总结：Describe-Then-Act (DILLO)

#### 1. 核心问题与整体含义（研究动机和背景）
在部署安全关键型智能体（如机器人）时，预判动作后果至关重要。现有的“世界模型”通常采用“视觉模拟”方案，即预测未来的图像或视频。然而，这种方案存在**高延迟**（每步处理常需数秒）的问题，难以满足实时控制需求。
本文提出了**“潜状态充足性假设”（Latent Sufficiency Hypothesis）**：认为策略网络的内部潜状态（Latent State）及其计划动作已经编码了预判结果所需的关键信息（如物体几何、距离、接触动力学），因此**视觉模拟在预防失败方面是冗余的**。论文旨在将范式从“模拟后行动”转变为“描述后行动”，通过轻量级的语言模型实现实时的、可解释的主动引导。

#### 2. 方法论：核心思想与关键技术
论文引入了 **DILLO (DIstiLLed Language-Acti On World Model)** 框架，其核心流程如下：
*   **跨模态知识蒸馏**：
    *   **教师模型 (Teacher VLM)**：拥有“特权信息”，能够访问模拟器状态（如6DoF位姿、视觉历史、任务成功信号），对离线轨迹进行语义标注，生成自然语言描述（$d$）和二元判定（$c$，Positive/Negative）。
    *   **学生模型 (DILLO)**：以策略网络的潜状态 $z_t$ 和动作块 $a_{t:t+k}$ 为输入，学习预测教师生成的语义结果。
*   **模型架构**：
    *   使用 Gemma-1B/4B 作为语言模型骨干。
    *   通过两个可学习的投影器（Projectors）将连续的潜状态和动作映射到 LLM 的嵌入空间。
    *   **推理路径**：推理时完全不使用原始图像，仅凭潜状态输出文本描述和成功判定。
*   **训练三阶段**：
    1.  **投影器对齐**：冻结 LLM，仅训练投影器。
    2.  **描述蒸馏**：使用 LoRA 微调 LLM，重建教师的自然语言描述。
    3.  **判定优化**：引入交叉熵损失，优化二元判定（Positive/Negative）的准确性。
*   **主动引导（拒绝采样）**：在推理时，策略提出 $N$ 个候选动作块，DILLO 对其进行并行评估，拒绝 Negative 动作，仅执行被判定为 Positive 的动作。

#### 3. 实验设计
*   **数据集/场景**：
    *   **MetaWorld**：单任务强化学习场景（Soccer, Sweep-Into, Drawer-Open）。
    *   **LIBERO**：多任务模仿学习场景（Goal, Object, Spatial, LIBERO-10, LIBERO-90），测试模型的泛化能力。
*   **Benchmark 与对比方法**：
    *   **Zero-Shot (ZS)**：直接给 LLM 当前观察和任务描述。
    *   **Few-Shot (FS)**：提供 3 个上下文示例。
    *   **Few-Shot Captioning (CAP)**：作为“先知”基准，允许模型看到未来的真实图像进行描述。
    *   **Forewarn**：现有的基于视觉模拟的 VLM 引导基准。
*   **评估指标**：Text-to-Observation (T2O) 忠实度、基于 LLM 的语义评分、任务成功率（Success Rate）以及推理延迟。

#### 4. 资源与算力
*   **硬件使用**：
    *   **推理测试**：在消费级显卡 **NVIDIA RTX 3090** 上进行。
    *   **对比基准**：对比的 Forewarn 方法运行在企业级 **RTX A6000 (48GB VRAM)** 上。
    *   **数据采集**：提到了使用 NVIDIA 捐赠的 **DGX Spark** 集群进行大规模数据采集和模拟。
*   **训练时长**：文中未明确给出具体的训练总时长，但强调了其轻量化特性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的单任务到极其复杂的 90 任务（LIBERO-90）套件。
*   **故障样本构建**：为了保证实验充分性，作者采用了“能力谱采样”（从不同熟练度的 checkpoint 采样）和“感知噪声注入”来模拟真实部署中的各种失败模式。
*   **主观验证**：除了自动指标，还进行了 **10 名人类专家（机器人/视觉博士）** 的验证研究，对比了人类判定与模型判定的一致性。
*   **客观性**：实验对比了不同参数规模（1B vs 4B），并设置了 CAP（可见未来图像）这种极强的基准，证明了潜状态预测的优越性，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **潜状态充足性**：实验证明潜状态 $z_t$ 比原始像素更能有效预测未来动态，DILLO 的预测精度甚至在某些任务上超过了能看到未来图像的 CAP 基准。
*   **显著加速**：DILLO 实现了约 **14 倍** 的推理加速（从 3.7s 降至 0.26s），使得在实时控制循环中加入 LLM 预判成为可能。
*   **性能提升**：在 MetaWorld 和 LIBERO 上，DILLO 引导的策略平均成功率提升了 **9.3 个百分点**，判定准确率达到 **91.4%**。
*   **可解释性**：生成的描述能准确反映机器人意图（如“机械臂向左移动准备抓取”），为人类监控提供了直观参考。

#### 7. 优点
*   **范式创新**：挑战了“必须模拟视觉”的传统观念，提出了更高效的“描述后行动”路径。
*   **极低开销**：推理时无需视觉编码器和渲染器，仅需处理紧凑的潜向量，极大地降低了硬件门槛。
*   **鲁棒的故障处理**：通过主动注入噪声和采样不同阶段模型，确保了对边缘案例（Hard Negatives）的识别能力。

#### 8. 不足与局限
*   **架构依赖性**：由于潜状态 $z_t$ 是特定于策略架构的，每当更换底层策略网络（如从 ACT 换成扩散策略）时，DILLO 的投影器需要重新对齐。
*   **特权信息依赖**：训练阶段仍需教师模型访问模拟器的真实位姿数据，在纯现实世界数据（无模拟器）上的蒸馏效果有待验证。
*   **极端多样性挑战**：在 LIBERO-90（90个任务）中，性能提升不明显，说明在极高任务多样性下，单一轻量级模型的建模能力仍存在瓶颈。

（完）
