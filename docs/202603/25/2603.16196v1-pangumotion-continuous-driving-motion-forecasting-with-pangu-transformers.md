---
title: "PanguMotion: Continuous Driving Motion Forecasting with Pangu Transformers"
title_zh: PanguMotion：基于盘古 Transformer 的连续驾驶运动预测
authors: "Quanhao Ren, Yicheng Li, Nan Song"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16196v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 将盘古大模型Transformer块集成到自动驾驶运动预测架构中
tldr: 针对自动驾驶运动预测中忽略场景连续性的问题，本文提出 PanguMotion 框架。该框架创新性地将 Pangu-1B 大语言模型的 Transformer 模块集成到预测架构中作为特征增强组件，并利用 RealMotion 策略将 Argoverse 2 数据重组为连续序列。该研究通过模拟真实驾驶环境的历史上下文关联，显著提升了连续场景下的轨迹预测性能。
selection_source: fresh_fetch
motivation: 现有运动预测方法多将驾驶场景视为独立离散片段，未能充分利用真实驾驶中的时间连续性和历史上下文信息。
method: 将 Pangu-1B 大模型的 Transformer 块集成至预测架构中，并采用 RealMotion 数据重组策略构建连续驾驶序列。
result: 在基于 Argoverse 2 重组的连续场景数据集上验证了该框架在处理复杂时间关联特征方面的有效性。
conclusion: PanguMotion 证明了引入大语言模型架构和连续场景建模能有效增强自动驾驶系统的运动预测能力。
---

## 摘要
运动预测是自动驾驶系统中的核心任务，旨在准确预测周围智能体的未来轨迹，以确保驾驶安全。现有方法通常独立处理离散的驾驶场景，忽略了真实驾驶环境中固有的时间连续性和历史上下文相关性。本文提出了 PanguMotion，这是一种针对连续驾驶场景的运动预测框架，它将盘古-1B（Pangu-1B）大语言模型中的 Transformer 模块作为特征增强模块，集成到自动驾驶运动预测架构中。我们在通过 RealMotion 数据重组策略处理的 Argoverse 2 数据集上进行了实验，将每个独立的场景转换为连续序列，以模拟真实的驾驶场景。

## Abstract
Motion forecasting is a core task in autonomous driving systems, aiming to accurately predict the future trajectories of surrounding agents to ensure driving safety. Existing methods typically process discrete driving scenes independently, neglecting the temporal continuity and historical context correlations inherent in real-world driving environments. This paper proposes PanguMotion, a motion forecasting framework for continuous driving scenarios that integrates Transformer blocks from the Pangu-1B large language model as feature enhancement modules into autonomous driving motion prediction architectures. We conduct experiments on the Argoverse 2 datasets processed by the RealMotion data reorganization strategy, transforming each independent scene into a continuous sequence to mimic real-world driving scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《PanguMotion: Continuous Driving Motion Forecasting with Pangu Transformers》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的自动驾驶运动预测方法通常将驾驶场景视为相互独立的离散片段，忽略了真实驾驶环境中固有的**时间连续性**和**历史上下文关联**。
*   **研究背景**：虽然已有研究（如 RealMotion）开始探索连续场景建模，但预训练大语言模型（LLM）在增强连续驾驶场景特征表示方面的潜力尚未得到充分挖掘。
*   **整体含义**：本文提出 **PanguMotion** 框架，旨在验证 LLM 的 Transformer 块是否可以作为非语言序列任务（如轨迹预测）的通用特征增强器，并解决连续驾驶场景下的预测精度问题。

### 2. 论文提出的方法论
*   **核心思想**：在连续驾驶预测架构中集成预训练的 **Pangu-1B** 大模型 Transformer 块。通过冻结大模型参数，利用其强大的“信息过滤”和特征提取能力来增强场景表示。
*   **关键技术细节**：
    *   **架构简化**：基于 RealMotion 框架，保留了“场景上下文流”（Scene Context Stream），但移除了“智能体轨迹流”（Agent Trajectory Stream），以减少功能冗余。
    *   **特征增强模块**：在编码器之后插入一个**冻结的盘古 Transformer 块**（通常选择第 25 层）。
    *   **维度对齐**：由于盘古模型隐藏层维度（1536）与运动预测特征维度（128）不匹配，在 Pangu 块前后增加了可训练的线性投影层。
    *   **注意力机制调整**：移除了盘古模型原有的自回归掩码（Autoregressive Mask），改用运动预测中的有效令牌掩码（Valid Token Mask），并移除其内部旋转位置编码，依赖基础编码器的位置信息。
*   **算法流程**：输入数据（轨迹和地图）经基础编码器提取特征 $\rightarrow$ 场景上下文流融合历史记忆 $\rightarrow$ 多个 Transformer 块编码 $\rightarrow$ **Pangu 块深度增强** $\rightarrow$ 解码器输出多模态预测轨迹。

### 3. 实验设计
*   **数据集/场景**：使用 **Argoverse 2** 运动预测数据集。
*   **数据处理策略**：采用 **RealMotion 数据重组策略**，将独立场景转换为连续序列，模拟 10Hz 频率下的真实驾驶。
*   **Benchmark 与对比方法**：
    *   主要对比基准是 **RealMotion** 及其变体（RealMotion-I）。
    *   对比了不同 LLM（Pangu vs. LLaMA）的效果。
*   **评价指标**：minADE（最小平均位移误差）、minFDE（最小终点位移误差）、MR（漏检率）等，重点关注 $k=1$（单模态）和 $k=6$（多模态）的表现。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 台华为昇腾（Ascend）910B2 NPU**。
*   **训练细节**：
    *   采用 PyTorch Lightning 框架进行分布式训练。
    *   优化器：AdamW，学习率 0.001。
    *   Batch Size：每张卡 32。
    *   训练时长：共运行 **80 个 Epoch** 以确保收敛。
*   **适配工作**：针对昇腾架构进行了专门的算子适配（如替换了需要 GPU 支持的邻域注意力机制）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验和对比实验，总计约 15-20 组核心对比数据。
*   **实验类型**：
    1.  **主实验**：PanguMotion 与 RealMotion 在完整数据集上的性能对比。
    2.  **模型对比**：Pangu 与 LLaMA 的横向对比。
    3.  **层数消融**：使用 Pangu 不同层数（1层、3层、6层）的效果对比。
    4.  **位置消融**：Pangu 块插入在模型不同深度位置的效果对比。
    5.  **架构消融**：验证移除智能体轨迹流的合理性。
*   **充分性评价**：实验设计较为**客观且充分**，通过控制变量法验证了 Pangu 块在不同配置下的增益，尤其是对单模态预测指标的显著提升。

### 6. 论文的主要结论与发现
*   **LLM 的跨界有效性**：验证了冻结的盘古大模型 Transformer 块能有效增强非语言的时间序列特征。
*   **高层语义更优**：实验发现使用盘古模型的**最后一层（第 25 层）**效果最好，支持了“高层 Transformer 块更适合处理高级语义信息”的假设。
*   **架构冗余性**：引入 Pangu 块后，移除原有的智能体轨迹流反而提升了单模态预测精度（minFDE 1 提升 2.3%），说明 LLM 的增强能力与某些传统精细化模块存在功能重叠。
*   **国产算力适配**：证明了该方案在昇腾 NPU 上的高效运行能力，推理延迟与原模型相当。

### 7. 优点（亮点）
*   **创新性集成**：首次将盘古大模型引入连续驾驶运动预测领域。
*   **性能提升**：在关键的单模态指标（minFDE 1 和 minADE 1）上取得了显著进步（约 11% 的提升）。
*   **实用性强**：提供了完整的昇腾 NPU 适配方案，并开源了代码，具有较高的工程参考价值。
*   **发现新规律**：揭示了 Pangu 在处理空间和时间模式上可能优于 LLaMA 的特性（可能与预训练语料的语言结构有关）。

### 8. 不足与局限
*   **计算开销**：尽管冻结了参数，但 Pangu-1B 块的引入仍增加了计算负担，在资源受限的嵌入式端部署仍具挑战。
*   **场景局限**：主要关注单智能体预测，对复杂的多智能体协同交互建模相对简化。
*   **数据偏差**：盘古模型主要基于中文语料预训练，可能存在潜在的文化或语言偏见，尽管其在非语言任务中表现良好，但这种影响尚未被完全量化。
*   **连续性鸿沟**：虽然使用了重组数据，但与完全真实的闭环连续驾驶环境仍存在一定差距。

（完）
