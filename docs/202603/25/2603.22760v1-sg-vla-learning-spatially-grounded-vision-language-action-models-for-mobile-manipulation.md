---
title: "SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation"
title_zh: SG-VLA：学习用于移动操作的空间感知视觉-语言-动作模型
authors: "Ruisen Tu, Arth Shukla, Sohyun Yoo, Xuanlin Li, Junxi Li, Jianwen Xie, Hao Su, Zhuowen Tu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22760v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于复杂环境中移动操作的空间落地 VLA 模型
tldr: 针对移动操作任务中复杂环境感知和高维动作控制的挑战，本文提出SG-VLA框架。该框架通过引入多视图RGB、深度信息及时间序列增强感知，并利用辅助任务（如位姿估计、分割等）协同训练，强化了模型的空间接地能力。在家庭重排任务中，SG-VLA在抓取、放置及开关门等操作上显著优于传统模仿学习，为通用家务机器人提供了有效的空间表征学习方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理涉及全局布局和精细几何的复杂移动操作任务时，由于缺乏空间感知能力，表现往往不尽如人意。
method: 提出一种结合多模态输入与辅助任务协同训练（如重建位姿、抓取启发、分割掩码等）的框架，以增强模型的空间接地表征。
result: 在包含13维动作空间的家庭重排任务评估中，该方法在抓取、放置、开启和关闭等操作上均取得了显著优于直接模仿学习的性能。
conclusion: 通过辅助任务和多模态学习实现空间接地，是提升VLA模型在通用家务机器人领域应用能力的有效途径。
---

## 摘要
视觉-语言-动作（VLA）模型在机器人控制方面展现出巨大潜力，但在复杂的家庭环境中的表现仍不尽如人意。移动操作需要对全局场景布局、细粒度几何结构以及高维连续动作进行推理，这使得标准的模仿学习难以胜任。我们提出了一种学习空间感知 VLA 模型的框架，通过辅助任务协同训练和多模态输入增强来强化感知与表示。我们的方法解决了控制 13 维动作空间的挑战，其中包括底盘移动、机械臂关节运动和夹持器动作的协同。为了丰富空间理解，该模型整合了多视角 RGB 观测、深度线索和短期时间历史，提供了全局场景结构和局部操作语境的视角。为了提高表示质量，我们协同训练了辅助解码器，从共享的视觉-语言特征中重建可解释的中间信号，包括机器人全局位置、关节配置、抓取可行性（affordances）、目标物体相对位姿以及分割掩码。这些目标提供了稠密监督，促使骨干网络开发出具有空间感知和操作意识的潜表示。通过在家庭重排任务上的广泛评估，我们的方法在抓取、放置、打开和关闭操作中均取得了一致的改进，显著优于直接模仿学习。我们的研究结果表明，通过辅助学习和多模态学习实现空间感知，为将 VLA 模型扩展到通用家用机器人提供了一个强有力的方向。

## Abstract
Vision-Language-Action (VLA) models show promise for robotic control, yet performance in complex household environments remains sub-optimal. Mobile manipulation requires reasoning about global scene layout, fine-grained geometry, and high-dimensional continuous actions, making standard imitation learning insufficient. We introduce a framework for learning spatially-grounded VLA models that strengthens perception and representation through auxiliary task co-training and multi-modal input enhancement. Our method addresses the challenge of controlling a 13-dimensional action space involving coordinated base motion, arm articulation, and gripper actuation. To enrich spatial understanding, the model incorporates multi-view RGB observations, depth cues, and short temporal history, providing perspectives of both global scene structure and local manipulation context. To improve representation quality, we co-train auxiliary decoders that reconstruct interpretable intermediate signals - including global robot position, joint configurations, grasp affordances, target-object relative pose, and segmentation masks - from shared visual-language features. These objectives provide dense supervision that encourages the backbone to develop spatially grounded, manipulation-aware latent representations. Through extensive evaluation on home rearrangement tasks, our approach achieves consistent improvements across picking, placing, opening, and closing operations, substantially outperforming direct imitation learning. Our findings suggest that spatial grounding through auxiliary and multi-modal learning provides a strong direction for scaling VLA models toward general-purpose domestic robots.

---

## 论文详细总结（自动生成）

### SG-VLA：学习用于移动操作的空间感知视觉-语言-动作模型 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在简单的桌面操作中表现良好，但在复杂的**移动操作（Mobile Manipulation）**任务中（如家庭环境）表现不佳。
*   **研究动机**：移动操作要求机器人同时处理全局场景布局、精细物体几何形状以及高维连续动作（13维动作空间）。传统的直接模仿学习（Direct Imitation Learning）提供的监督信号过于稀疏，导致模型难以建立起对环境的空间理解和稳健的控制能力。

#### 2. 方法论
SG-VLA 框架的核心思想是通过**增强感知输入**和**引入辅助任务协同训练**来强化模型的“空间接地”（Spatial Grounding）能力。
*   **多模态输入增强**：
    *   **多视角**：结合头部摄像头（全局视角）和手部摄像头（局部操作视角）的 RGB 图像。
    *   **深度信息**：引入归一化后的深度图，提供显式的几何距离信息。
    *   **时间上下文**：包含过去 4 个时间步的历史观测。
*   **辅助任务解码器**：在共享的 VLM 骨干网络（Qwen2.5-0.5B + DINOv2/SigLIP）之上，设计了多个辅助解码器：
    *   **MLP 解码器**：预测机器人全局位置、抓取成功状态、目标物体位姿。
    *   **Transformer 解码器**：预测 12 维机器人关节配置（qpos），利用自注意力机制捕捉运动学结构。
    *   **CNN 解码器**：重建目标物体的 128x128 二进制分割掩码。
*   **三阶段渐进式训练流程**：
    1.  **解码器适配阶段**：冻结 VLM 到辅助解码器的梯度，让解码器学习解释固定的 VLM 特征，避免随机初始化的噪声干扰预训练权重。
    2.  **联合精炼阶段**：开启全量梯度回传，通过辅助任务损失函数协同优化 VLM 骨干网络。
    3.  **动作头训练阶段**：冻结 VLM，单独训练基于流匹配（Flow Matching）的动作专家头，以生成高精度连续动作。

#### 3. 实验设计
*   **数据集与场景**：使用 **ManiSkill-HAB** 仿真基准测试，包含 TidyHouse、PrepareGroceries 和 SetTable 三个长程家庭重排任务。
*   **任务类型**：涵盖抓取（Pick）、放置（Place）、打开（Open）和关闭（Close）四种基本操作。
*   **Benchmark 与对比方法**：
    *   **基准**：OpenVLA（7B 参数版本）。
    *   **对比**：直接模仿学习（不带辅助任务）、不同输入模态组合（单视角 vs 多视角、有无深度图）、有无渐进式训练策略。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **优化器**：Adam 优化器，恒定学习率为 2e-5。
*   **训练规模**：全局 Batch Size 为 512。分割任务训练 5 个 Epoch，其他任务训练 10 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：数据集包含 4.4 万个情节（Episodes）和 140 万次转换（Transitions）。
*   **消融实验**：
    *   针对**输入模态**做了详尽对比（RGB、深度、历史、多视角）。
    *   针对**辅助任务**做了逐一拆解实验（qpos、抓取标签、全局位置、分割等）。
    *   针对**训练策略**对比了直接协同训练与渐进式训练的效果。
*   **充分性评价**：实验设计较为全面，涵盖了从感知到表征再到动作生成的全链路验证。每个任务运行 30 个情节取平均成功率，具有一定的统计说服力。

#### 6. 主要结论与发现
*   **空间信息至关重要**：引入多视角和深度信息后，模型性能从极低的成功率大幅提升（OpenVLA 从 4% 提升至 32%，Qwen 基座模型提升至 60%）。
*   **辅助任务的增益**：通过辅助任务协同训练，SG-VLA 的平均成功率从 60% 进一步提升至 **73%**。其中，**关节位置（qpos）预测**和**分割掩码重建**对提升操作精度贡献最大。
*   **训练策略的影响**：直接进行多任务训练会导致性能下降，而“先适配、后联合”的渐进式训练是成功的关键。
*   **动作生成偏好**：流匹配动作头在精细抓取任务中表现优异，但在需要大范围移动的开启/关闭任务中，离散动作 Token 反而更稳健。

#### 7. 优点
*   **高效的表征学习**：通过重建中间物理信号（位姿、分割），强制模型学习具有物理意义的特征，而非仅仅是统计关联。
*   **轻量化与高性能**：使用了较小的 1.3B 参数模型，但在特定任务上通过结构优化超越了更大的 7B 模型。
*   **系统性探索**：对移动操作中“感知-推理-执行”的每一个环节（模态、辅助监督、训练时序）都给出了明确的消融分析。

#### 8. 不足与局限
*   **仿真与现实的差距**：实验完全在 ManiSkill-HAB 仿真环境中进行，尚未在真实物理机器人上验证其 Sim-to-Real 的迁移能力。
*   **时间上下文的负面影响**：实验发现加入历史动作反而降低了性能，论文推测是模型未能有效整合时间信息，这反映了模型在处理动态序列方面的局限。
*   **任务多样性限制**：虽然涵盖了家庭常见操作，但对于更复杂的长程逻辑推理（如多步骤因果关系）探讨较少。

（完）
