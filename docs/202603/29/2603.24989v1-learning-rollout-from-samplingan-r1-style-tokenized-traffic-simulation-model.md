---
title: "Learning Rollout from Sampling:An R1-Style Tokenized Traffic Simulation Model"
title_zh: 从采样中学习推演：一种 R1 风格的 Token 化交通仿真模型
authors: "Ziyan Wang, Peng Chen, Ding Li, Chiwei Li, Qichao Zhang, Zhongpu Xia, Guizhen Yu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24989v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 基于强化学习的自动驾驶交通仿真
tldr: 本研究针对自动驾驶评估中的交通流模拟，提出了一种名为R1Sim的令牌化模拟模型。针对现有基于监督微调（SFT）的预测范式在探索潜在运动令牌方面的局限性，R1Sim引入了受R1启发的强化学习机制。通过熵引导的自适应采样和组相对策略优化（GRPO），该模型在不确定性探索与利用之间取得了平衡。实验证明，R1Sim在Waymo Sim Agent基准测试中表现优异，能够生成真实、安全且多样化的多智能体行为。
selection_source: fresh_fetch
motivation: 现有的基于下个令牌预测（NTP）的交通模拟方法受限于监督微调，难以有效探索具有高不确定性但潜在价值的运动令牌。
method: 提出R1Sim模型，结合熵引导的自适应采样机制与组相对策略优化（GRPO），并辅以安全感知奖励设计来优化运动行为。
result: 在Waymo Sim Agent基准测试中，R1Sim达到了与当前最先进方法竞争的水平，显著提升了模拟的真实性和多样性。
conclusion: 通过将熵驱动的探索与强化学习相结合，可以有效提升令牌化交通模拟模型在复杂场景下的泛化能力和安全性。
---

## 摘要
从人类驾驶演示中学习多样化且高保真度的交通仿真，对于自动驾驶评估至关重要。近期在大语言模型（LLM）中广泛采用的下文预测（NTP）范式已被应用于交通仿真，并通过监督微调（SFT）实现了迭代改进。然而，此类方法限制了对潜在有价值运动 Token 的主动探索，尤其是在次优区域。熵模式为实现由运动 Token 不确定性驱动的探索提供了一个极具前景的视角。受此启发，我们提出了一种新型 Token 化交通仿真策略 R1Sim，这是基于运动 Token 熵模式探索强化学习的初步尝试，并系统分析了不同运动 Token 对仿真结果的影响。具体而言，我们引入了一种熵引导的自适应采样机制，重点关注此前被忽视的、具有高不确定性但高潜力的运动 Token。我们在安全感知奖励设计的引导下，进一步利用群体相对策略优化（GRPO）来优化运动行为。总的来说，这些组件通过多样化的高不确定性采样和群体比较估计，实现了探索与利用之间的平衡权衡，从而产生了真实、安全且多样化的多智能体行为。在 Waymo Sim Agent 基准测试上的大量实验表明，R1Sim 与现有最先进方法相比取得了具有竞争力的性能。

## Abstract
Learning diverse and high-fidelity traffic simulations from human driving demonstrations is crucial for autonomous driving evaluation. The recent next-token prediction (NTP) paradigm, widely adopted in large language models (LLMs), has been applied to traffic simulation and achieves iterative improvements via supervised fine-tuning (SFT). However, such methods limit active exploration of potentially valuable motion tokens, particularly in suboptimal regions. Entropy patterns provide a promising perspective for enabling exploration driven by motion token uncertainty. Motivated by this insight, we propose a novel tokenized traffic simulation policy, R1Sim, which represents an initial attempt to explore reinforcement learning based on motion token entropy patterns, and systematically analyzes the impact of different motion tokens on simulation outcomes. Specifically, we introduce an entropy-guided adaptive sampling mechanism that focuses on previously overlooked motion tokens with high uncertainty yet high potential. We further optimize motion behaviors using Group Relative Policy Optimization (GRPO), guided by a safety-aware reward design. Overall, these components enable a balanced exploration-exploitation trade-off through diverse high-uncertainty sampling and group-wise comparative estimation, resulting in realistic, safe, and diverse multi-agent behaviors. Extensive experiments on the Waymo Sim Agent benchmark demonstrate that R1Sim achieves competitive performance compared to state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：R1Sim —— 一种 R1 风格的 Token 化交通仿真模型

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶领域，构建高保真且多样化的交通仿真环境对于算法评估至关重要。当前主流的交通仿真模型（如 SMART、CATK）借鉴了大语言模型（LLM）的**下个令牌预测（NTP）**范式，将车辆轨迹离散化为 Token 进行自回归生成。
然而，现有方法存在两个核心瓶颈：
*   **采样策略僵化**：通常采用固定的 Top-K 采样，容易忽略那些概率较低但物理上合理且具有潜在价值的“遗珠（Hidden Gem）”行为，导致仿真缺乏多样性。
*   **过度依赖监督微调（SFT）**：SFT 强制模型模仿专家数据，但在复杂交互或次优区域，单纯模仿可能导致碰撞或不符合逻辑的行为，缺乏主动探索安全边界的能力。

#### 2. 方法论：核心思想与关键技术
R1Sim 借鉴了 DeepSeek-R1 的强化学习思路，提出了一种平衡“探索”与“利用”的 Token 化仿真框架。
*   **熵引导的自适应采样（Entropy-Guided Adaptive Sampling）**：
    *   **核心思想**：利用 Token 分布的**香农熵（Shannon Entropy）**来衡量场景的不确定性。
    *   **技术细节**：当熵值较高（如十字路口交互）时，动态增大采样范围 $K_t$，允许模型探索更多可能的路径；当熵值较低（如直线跟车）时，减小 $K_t$ 以确保稳定性。
*   **群体相对策略优化（GRPO）**：
    *   **核心思想**：无需传统的 Critic 网络，通过对同一场景生成的多个候选 Rollout（组）进行横向对比来估计优势函数。
    *   **流程**：针对每个场景采样 $N$ 个轨迹，计算组内平均奖励，表现优于平均水平的 Token 会被强化。
*   **安全感知奖励设计（Safety-aware Reward）**：
    *   奖励函数结合了**碰撞惩罚**（基于分离轴定理 SAT 检测）和**真实性奖励**（与专家轨迹的距离）。
    *   采用乘法形式（Safety $\times$ Realism）而非加法，以提供更稳定的梯度引导，强制模型在保证安全的前提下追求真实。

#### 3. 实验设计
*   **数据集与场景**：使用 **Waymo Open Motion Dataset (WOMD)**，包含约 48.7 万个训练场景。
*   **Benchmark**：Waymo Sim Agent 挑战赛标准，涵盖 8 秒的闭环仿真评估。
*   **对比方法**：包括 SOTA 模型如 SMART、CATK、UniMM、BehaviorGPT、GUMP 等。
*   **评估指标**：Realism Meta Metric (RMM)，以及运动学似然、交互似然（碰撞率）、地图一致性（越野率）等子指标。

#### 4. 资源与算力
*   **硬件配置**：使用了 **4 张 NVIDIA RTX 4090 GPU**。
*   **训练细节**：
    *   在预训练模型（SMART/CATK）基础上仅进行 **1 个 Epoch** 的微调。
    *   总 Batch Size 为 16。
    *   微调时冻结了大部分编码器权重，仅更新最后几层，具有极高的算力效率。

#### 5. 实验数量与充分性
*   **实验规模**：在 WOMD 的验证集和测试集上进行了全面测试。
*   **消融实验**：系统对比了固定 Top-K 与自适应采样、SFT 与 GRPO、以及不同奖励函数设计（Outcome vs Process reward）的效果。
*   **场景划分**：专门将数据集划分为“简单（Easy）”和“困难（Hard）”子集，验证了熵引导机制在处理高不确定性场景时的有效性。
*   **客观性**：通过 5 次独立运行取平均值来确保结果的稳定性，实验设计较为严谨、公平。

#### 6. 主要结论与发现
*   **性能提升**：R1Sim 在 Waymo 榜单上达到了顶尖水平，尤其在**安全性（碰撞率降低）**方面显著优于纯 SFT 方法。
*   **熵的价值**：实验证明 Token 熵与场景复杂度高度相关，高熵场景往往对应模型表现较差的挑战性场景，自适应采样能有效弥补这一缺陷。
*   **GRPO 的优越性**：相比标准 PPO，GRPO 在交通仿真任务中更稳定且计算开销更低；相比 SFT，它能通过组内竞争发现更符合人类偏好的安全行为。

#### 7. 优点（亮点）
*   **创新性引入 R1 范式**：首次将 DeepSeek-R1 的 GRPO 和熵驱动探索理念引入 Token 化交通仿真。
*   **高效微调**：仅需 1 个 Epoch 即可实现显著的性能飞跃，证明了强化学习在后训练阶段（Post-training）的巨大潜力。
*   **自适应机制**：解决了固定采样参数在不同复杂度场景下的不适配问题。

#### 8. 不足与局限
*   **推理开销**：虽然训练高效，但为了实现 GRPO 的优势，推理时可能需要生成多个样本进行筛选或评估，增加了实时仿真的计算负担。
*   **奖励函数依赖**：尽管采用了安全感知奖励，但奖励函数的设计仍包含人工定义的超参数（如距离惩罚的衰减系数），可能存在过拟合特定指标的风险。
*   **泛化性验证**：目前主要在 Waymo 数据集上验证，在其他长尾场景或极端罕见事故场景下的表现仍需进一步观察。

（完）
