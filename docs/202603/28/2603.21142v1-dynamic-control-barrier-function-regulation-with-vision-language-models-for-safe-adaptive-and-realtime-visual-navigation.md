---
title: "Dynamic Control Barrier Function Regulation with Vision-Language Models for Safe, Adaptive, and Realtime Visual Navigation"
title_zh: 基于视觉语言模型的动态控制障碍函数调节，用于安全、自适应且实时的视觉导航
authors: "Jeffrey Chen, Rohan Chandra"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21142v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 用于安全自适应视觉导航的视觉语言模型
tldr: 针对机器人在动态环境中的导航安全与效率平衡问题，本文提出AlphaAdj框架。该框架利用视觉语言模型（VLM）分析第一视角RGB图像并评估风险，动态调整控制障碍函数（CBF）的参数，以实时调节避障的保守程度。通过结合几何限幅和陈旧度门控融合策略，解决了VLM推理延迟问题。实验表明，该方法在保证零碰撞的同时，显著提升了导航效率和成功率。
selection_source: fresh_fetch
motivation: 传统的控制障碍函数（CBF）因固定参数难以在复杂动态环境中兼顾避障的安全性与导航的效率。
method: 利用VLM从视觉输入中提取风险标量，动态调节CBF参数，并引入速度感知动态限幅和陈旧度门控策略以应对推理延迟。
result: "在多种静态和动态场景下，AlphaAdj相比固定参数方法提升了高达18.5%的导航效率，并增强了系统的鲁棒性。"
conclusion: 结合大模型视觉理解与经典控制理论，能够有效实现安全、自适应且实时的机器人视觉导航。
---

## 摘要
在动态、非结构化环境中运行的机器人必须在感知受限的情况下平衡安全性和效率。虽然控制障碍函数（CBF）通过安全滤波提供了一种原则性的避障方法，但其行为通常受固定参数控制，这可能导致在温和场景中过于保守，或在危险附近过于激进。我们提出了 AlphaAdj，这是一个视觉到控制的导航框架，它利用第一视角 RGB 输入实时调整 CBF 安全滤波器的保守程度。视觉语言模型（VLM）根据当前摄像机视图生成一个有界的标量风险估计值，我们将其映射以动态更新 CBF 参数，从而调节安全约束的执行强度。为了解决实际应用中的异步推理和显著的 VLM 延迟问题，我们结合了几何速度感知动态上限和陈旧度门控融合策略，并采用轻量化实现方案以降低端到端推理开销。我们在多种环境下的多个静态和动态障碍物场景中评估了 AlphaAdj，并与固定参数和无上限的消融实验进行了对比。结果表明，AlphaAdj 在保持无碰撞导航的同时，相较于固定设置，其效率（在路径长度和到达目标时间方面）提升了高达 18.5%，且相较于无上限基准提升了鲁棒性和成功率。

## Abstract
Robots operating in dynamic, unstructured environments must balance safety and efficiency under potentially limited sensing. While control barrier functions (CBFs) provide principled collision avoidance via safety filtering, their behavior is often governed by fixed parameters that can be overly conservative in benign scenes or overly permissive near hazards. We present AlphaAdj, a vision-to-control navigation framework that uses egocentric RGB input to adapt the conservativeness of a CBF safety filter in real time. A vision-language model(VLM) produces a bounded scalar risk estimate from the current camera view, which we map to dynamically update a CBF parameter that modulates how strongly safety constraints are enforced. To address asynchronous inference and non-trivial VLM latency in practice, we combine a geometric, speed-aware dynamic cap and a staleness-gated fusion policy with lightweight implementation choices that reduce end-to-end inference overhead. We evaluate AlphaAdj across multiple static and dynamic obstacle scenarios in a variety of environments, comparing against fixed-parameter and uncapped ablations. Results show that AlphaAdj maintains collision-free navigation while improving efficiency (in terms of path length and time to goal) by up to 18.5% relative to fixed settings and improving robustness and success rate relative to an uncapped baseline.

---

## 论文详细总结（自动生成）

### 论文总结：AlphaAdj —— 基于视觉语言模型的动态控制障碍函数调节

#### 1. 核心问题与整体含义
论文探讨了机器人在动态、非结构化环境中导航时，如何在**安全性（避障）**与**效率（到达目标的速度）**之间取得平衡。
*   **研究动机**：传统的控制障碍函数（CBF）依赖固定的安全参数 $\alpha$。若 $\alpha$ 过小，机器人在空旷地带也会过于保守（绕远路、减速）；若 $\alpha$ 过大，在复杂或危险区域则容易发生碰撞。
*   **核心挑战**：如何利用视觉语义信息实时调整控制器的保守程度，并解决大模型（VLM）推理高延迟与机器人实时控制需求之间的矛盾。

#### 2. 方法论
AlphaAdj 框架将高层语义感知与底层安全滤波相结合，核心步骤如下：
*   **风险评估（VLM）**：机器人通过第一视角 RGB 图像，异步调用视觉语言模型（LLaVA-1.5 7B）。VLM 根据场景语义输出一个 $[0, 1]$ 之间的风险标量 $r$。
*   **风险到参数的映射**：使用幂律函数将风险值 $r$ 映射为 CBF 参数 $\alpha_{vlm}$。风险越高，$\alpha$ 越小（越保守）。
*   **延迟感知与融合策略**：
    *   **动态限幅（Dynamic Cap）**：基于几何距离和当前速度计算一个安全上限 $\alpha_{cap}$，确保即使 VLM 给出错误建议，机器人也不会超过物理安全边界。
    *   **陈旧度门控（Staleness Gating）**：由于 VLM 推理约有 700ms 延迟，系统会监测数据新鲜度。若 VLM 更新超时，系统自动回退到基于几何感知的保守“硬限幅”模式。
*   **底层控制**：将调节后的 $\alpha$ 带入 CBF-QP（二次规划）安全滤波器，对名义控制指令进行修正，产生最终的执行指令。

#### 3. 实验设计
*   **实验环境**：使用 NVIDIA IsaacSim 仿真平台。
*   **测试场景**：
    1.  **单一正面障碍物**：测试基础避障。
    2.  **杂乱场域**：测试多障碍物间的路径优化。
    3.  **动态交叉障碍物**：测试对移动物体的反应。
    4.  **动态正面障碍物**：测试迎面而来的危险。
*   **对比基准（Benchmark）**：
    *   **固定参数法**：固定最小 $\alpha$（极度保守）和固定最大 $\alpha$（极度激进）。
    *   **无上限消融实验**：仅使用 VLM 输出，不加几何限幅和延迟处理。
    *   **环境对比**：在简单“玩具”环境和真实“仓库”环境中分别验证。

#### 4. 资源与算力
*   **硬件平台**：AMD Ryzen Threadripper PRO 7955WX 处理器，搭配 **NVIDIA RTX 6000 Ada GPU**。
*   **模型优化**：使用了 **LLaVA-1.5 7B** 模型，并进行了 **4-bit 量化** 处理。
*   **推理表现**：通过本地 FastAPI 部署，端到端延迟从 1482ms 优化至 **695ms**。底层控制频率保持在 **60Hz**，VLM 查询频率为 **2Hz**。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 种场景、2 种环境（共 8 组配置）进行了评估。每组配置重复运行 **25 次** 以获取平均指标。
*   **充分性评价**：实验设计较为全面，涵盖了静态与动态障碍物，并对比了不同 $\alpha$ 取值的性能曲线（图 3）。消融实验清晰地证明了“动态限幅”在处理 VLM 延迟时的必要性。实验对比了路径长度、到达时间、最小安全距离等多个维度，结果较为客观。

#### 6. 主要结论与发现
*   **安全性**：AlphaAdj 在所有测试场景中均实现了 **100% 的成功率（零碰撞）**。
*   **效率提升**：相比于固定保守参数的基准，AlphaAdj 将路径长度缩短了约 **18.5%**，到达时间缩短了约 **10.8%**。
*   **鲁棒性**：引入动态限幅和陈旧度检测后，系统能够有效抵御 VLM 推理延迟带来的安全风险，比“无上限”版本更稳定。

#### 7. 优点（亮点）
*   **语义与控制结合**：不只是用 VLM 做路径规划，而是将其引入底层安全约束参数的调节，实现了“语义感知的保守性”。
*   **延迟处理机制**：针对大模型在机器人领域落地的“延迟痛点”，提出了实用的异步融合与几何回退机制。
*   **实时性**：在不停止机器人运动的情况下，实现了高频控制与低频大模型推理的协同。

#### 8. 不足与局限
*   **仿真局限**：所有实验均在 IsaacSim 仿真中完成，尚未在真实物理机器人和复杂光照/传感器噪声环境下验证。
*   **风险评估黑盒化**：VLM 输出的风险值 $r$ 缺乏可解释性，且映射函数中的超参数（如 $\gamma$）仍需手动调优。
*   **场景多样性**：虽然涵盖了动态障碍物，但环境类型（仓库、玩具场景）仍相对有限，未测试极端拥挤或户外复杂地形。

（完）
