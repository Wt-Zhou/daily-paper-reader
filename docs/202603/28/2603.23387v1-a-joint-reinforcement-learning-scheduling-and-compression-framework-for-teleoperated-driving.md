---
title: A Joint Reinforcement Learning Scheduling and Compression Framework for Teleoperated Driving
title_zh: 一种用于远程驾驶的联合强化学习调度与压缩框架
authors: "Giacomo Avanzi, Marco Giordani, Michele Zorzi"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23387v1"
tags: ["query:ad-ei"]
score: 8.0
evidence: 用于远程驾驶和网络优化的强化学习
tldr: 针对远程驾驶对低延迟和高可靠通信的需求，本文提出了一种联合强化学习（RL）调度与压缩框架。该框架通过两个集成的RL智能体，协同优化传感器数据压缩率与无线资源调度，旨在平衡传输效率、数据质量与端到端延迟。通过ns-3全栈仿真验证，该方案在网络拥塞环境下表现优于单一优化模型，且引入的元学习机制能根据实时网络状况动态选择最优的学习策略。
selection_source: fresh_fetch
motivation: 远程驾驶需要极高的通信可靠性和低延迟，而现有的单一优化手段难以在受限或拥塞的网络环境下满足复杂的QoS需求。
method: 提出一种集成双强化学习智能体的框架，协同优化传感器数据压缩与无线资源调度，并利用元学习动态选择集中式或分布式学习策略。
result: 全栈仿真结果证明，该联合框架在降低端到端延迟和维持数据质量方面显著优于仅执行单一优化的独立模型。
conclusion: 联合优化压缩与调度是提升远程驾驶通信性能的关键，结合元学习的动态策略选择能进一步增强系统在复杂网络环境下的鲁棒性。
---

## 摘要
远程驾驶 (TD) 被视为未来第六代 (6G) 网络的一个关键应用。在这种范式中，联网车辆将传感器感知数据传输给远程（软件）驾驶员，后者返回驾驶控制命令，以提高交通效率和道路安全。这一场景要求在车辆与远程驾驶员之间保持可靠且低延迟的通信。为此，一个极具前景的解决方案是预测性服务质量 (PQoS)，它提供了估算可能的服务质量 (QoS) 退化并据此触发及时网络纠正措施的机制。特别是，可以训练强化学习 (RL) 智能体来识别最优的 PQoS 配置。在本文中，我们开发并实现了两个集成的 RL 智能体，它们共同确定：(i) 远程驾驶传感器数据的最优压缩配置，以平衡传输效率与数据质量之间的权衡；(ii) 最优调度配置，通过根据不同优先级分配无线资源来最小化端到端延迟。我们通过全栈 ns-3 仿真证明，我们的集成智能体能够提供比任何仅优化压缩或调度的独立模型更优越的性能，特别是在受限或拥塞的网络中。虽然这些智能体可以使用集中式或分布式学习进行部署，但我们进一步提出了一种新的元学习智能体，它可以根据当前网络状况和应用需求，在两者之间动态选择最合适的策略。

## Abstract
Teleoperated driving (TD) is envisioned as a key application of future sixth generation (6G) networks. In this paradigm, connected vehicles transmit sensor-perception data to a remote (software) driver, which returns driving control commands to enhance traffic efficiency and road safety. This scenario imposes to maintain reliable and low-latency communication between the vehicle and the remote driver. To this aim, a promising solution is Predictive Quality of Service (PQoS), which provides mechanisms to estimate possible Quality of Service (QoS) degradation, and trigger timely network corrective actions accordingly. In particular, Reinforcement Learning (RL) agents can be trained to identify the optimal PQoS configuration. In this paper, we develop and implement two integrated RL agents that jointly determine (i) the optimal compression configuration for TD sensor data to balance the trade-off between transmission efficiency and data quality, and (ii) the optimal scheduling configuration to minimize the end-to-end latency by allocating radio resources according to different priority levels. We prove via full-stack ns-3 simulations that our integrated agents can deliver superior performance than any standalone model that only optimizes either compression or scheduling, especially in constrained or congested networks. While these agents can be deployed using either centralized or decentralized learning, we further propose a new meta-learning agent that dynamically selects the most appropriate strategy between the two based on current network conditions and application requirements.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于远程驾驶的联合强化学习调度与压缩框架

#### 1. 核心问题与研究动机
远程驾驶（Teleoperated Driving, TD）是 6G 网络的核心应用之一，要求极高的可靠性和极低的端到端（E2E）延迟（通常要求 <50ms）。然而，联网车辆产生的海量传感器数据（如 LiDAR 点云）极易导致网络拥塞，且动态变化的无线信道会增加延迟风险。
*   **研究动机**：现有的预测性服务质量（PQoS）方案通常独立优化数据压缩或无线资源调度。本文认为，单一维度的优化难以在极端网络条件下平衡“数据质量”与“传输延迟”。因此，研究如何通过强化学习（RL）实现压缩与调度的**联合优化**，并根据信道状况动态切换学习范式（集中式 vs. 联邦式），是提升 TD 性能的关键。

#### 2. 方法论
论文提出了一个集成双 RL 智能体的框架，并引入元学习机制：
*   **压缩智能体 (CA)**：采用 **Double Deep Q-Learning (DDQL)** 算法。其动作空间是不同的 LiDAR 压缩配置（基于 Google Draco 算法），目标是在满足延迟阈值的前提下，最大化接收数据的平均精度均值（mAP）。
*   **调度智能体 (SA)**：采用 **多智能体近端策略优化 (MAPPO)** 算法。它根据各车辆的信道状态和缓冲区情况分配优先级，进而决定每时隙分配的 OFDM 符号数量。
*   **联合模式**：
    *   **独立模式 (ICS)**：CA 和 SA 同时运行但互不通信。
    *   **协作模式 (CCS)**：通过修改奖励函数，使 CA 在资源受限时主动增加压缩率，SA 在数据质量需求高时分配更多资源。
*   **元学习智能体 (Meta-Learning Agent)**：针对集中式学习（开销大但全局最优）和联邦学习（开销小但局部最优）的权衡，利用 **神经线性汤普森采样 (NLTS)** 或 **DDQL** 动态选择最优的学习范式。

#### 3. 实验设计
*   **仿真环境**：使用 **ns-3** 全栈网络仿真器，集成 **mmWave** 模块（5G NR 协议栈）、**SUMO**（交通流仿真，场景为意大利博洛尼亚市区）和 **GEMV2**（几何信道模型）。
*   **数据集**：使用 **SELMA** 大规模多模态数据集评估点云压缩后的物体检测性能（mAP）。
*   **场景设置**：5 辆联网车辆，28 GHz 载波，50 MHz 带宽。信道分为“好（Good）”和“差（Bad）”两种状态的马尔可夫链。
*   **基准方法 (Benchmarks)**：
    *   仅优化压缩 (C) 或仅优化调度 (S)。
    *   静态压缩配置（C1, C2, C3）。
    *   固定采用集中式或联邦式学习。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件型号（如 GPU/CPU）或训练所需的总时长。
*   **训练规模**：每个模型训练 250 个回合（Episodes），每个回合包含 400 个学习步（Steps），每收到一帧点云执行一次学习。

#### 5. 实验数量与充分性
*   **实验规模**：论文对比了 4 种集成模式（C, S, ICS, CCS）、2 种学习范式（集中式 vs. 联邦式）、2 种延迟阈值（30ms 和 50ms）以及多种元学习算法（MAB, LTS, NLTS, DDQL）。
*   **充分性评价**：实验覆盖了从良好信道到拥塞/恶劣信道的多种场景，并进行了消融分析（如对比独立优化与联合优化）。虽然车辆规模较小（5 辆），但对于验证协议栈层面的联合优化逻辑而言，实验设计较为客观且具有代表性。

#### 6. 主要结论与发现
*   **联合优化更优**：在受限或拥塞网络中，联合优化（ICS/CCS）在降低延迟违规率方面显著优于单一优化手段。
*   **学习范式的权衡**：在信道良好时，集中式学习因能获取全局信息而表现更好；在信道恶劣时，联邦学习因减少了控制面开销和对不稳信道的依赖，其奖励值比集中式高出约 75%。
*   **元学习的有效性**：提出的元学习智能体（尤其是 NLTS 算法）能根据实时 SINR 动态切换策略，在高度动态的信道中比静态策略获得更高的累积奖励。

#### 7. 优点
*   **全栈仿真**：结合了物理层信道、MAC 层调度、应用层压缩和交通流仿真，结果具有较高的真实性。
*   **多维度协同**：突破了单一层级优化的局限，实现了应用层（压缩）与接入层（调度）的跨层联动。
*   **自适应性**：引入元学习解决学习范式的选择问题，增强了系统在非平稳环境下的鲁棒性。

#### 8. 不足与局限
*   **规模受限**：实验仅针对 5 辆车进行，未探讨在大规模高密度车辆环境下的可扩展性和计算开销。
*   **压缩算法单一**：仅使用了 Draco 这一种压缩工具，未考虑基于深度学习的点云压缩方法。
*   **硬件开销未评估**：虽然讨论了联邦学习的优势，但未量化车载终端运行 RL 模型所需的实际计算资源消耗。

（完）
