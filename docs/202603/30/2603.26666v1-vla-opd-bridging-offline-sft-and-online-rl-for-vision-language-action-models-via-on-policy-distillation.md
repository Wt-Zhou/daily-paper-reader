---
title: "VLA-OPD: Bridging Offline SFT and Online RL for Vision-Language-Action Models via On-Policy Distillation"
title_zh: VLA-OPD：通过同策略蒸馏桥接视觉-语言-动作模型的离线监督微调与在线强化学习
authors: "Zhide Zhong, Haodong Yan, Junfeng Li, Junjie He, Tianran Zhang, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26666v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 在机器人操作中连接VLA模型的监督微调与强化学习
tldr: 针对视觉-语言-动作（VLA）模型在部署时面临的离线微调分布偏移和在线强化学习效率低下的问题，本文提出了VLA-OPD框架。该框架通过在线策略蒸馏，利用专家教师对学生自生成轨迹进行密集的Token级监督。采用Reverse-KL目标函数，在纠正策略偏差的同时保留预训练能力，显著提升了模型的鲁棒性与采样效率。
selection_source: fresh_fetch
motivation: 传统的离线SFT存在分布偏移和灾难性遗忘，而在线RL受限于稀疏奖励和低采样效率，难以实现高效的VLA模型后训练。
method: 提出VLA-OPD框架，利用专家教师在学生生成的轨迹上提供密集监督，并结合Reverse-KL目标函数实现稳定的在线策略蒸馏。
result: 在LIBERO和RoboTwin2.0基准测试中，VLA-OPD在采样效率上优于RL，在鲁棒性上优于SFT，并有效缓解了灾难性遗忘。
conclusion: VLA-OPD成功桥接了SFT的效率与RL的鲁棒性，为VLA模型提供了一种稳定且高效的在线对齐与性能提升方案。
---

## 摘要
尽管预训练的视觉-语言-动作（VLA）模型在机器人操控中表现出令人印象深刻的泛化能力，但后期训练对于确保部署期间的可靠性能仍然至关重要。然而，标准的离线监督微调（SFT）面临分布偏移和预训练能力灾难性遗忘的问题，而在线强化学习（RL）则受困于稀疏奖励和低样本效率。在本文中，我们提出了同策略 VLA 蒸馏（VLA-OPD），这是一个桥接了 SFT 的效率与 RL 的鲁棒性的框架。VLA-OPD 不依赖于稀疏的环境奖励，而是利用专家教师在学生自生成的轨迹上提供密集的、标记级（token-level）的监督。这使得模型能够在策略诱导的状态下进行主动纠错，同时通过温和的对齐保留预训练的通用能力。至关重要的是，我们通过逆向 KL（Reverse-KL）目标函数来构建 VLA-OPD。不同于会导致模式覆盖（mode-covering）熵爆炸的标准正向 KL，或导致过早熵崩溃的硬交叉熵（Hard-CE），我们有界的模式寻求（mode-seeking）目标通过过滤教师的认知不确定性并保持动作多样性，确保了稳定的策略学习。在 LIBERO 和 RoboTwin2.0 基准测试上的实验表明，VLA-OPD 相比 RL 显著提高了样本效率，相比 SFT 显著提高了鲁棒性，同时有效缓解了后期训练过程中的灾难性遗忘。

## Abstract
Although pre-trained Vision-Language-Action (VLA) models exhibit impressive generalization in robotic manipulation, post-training remains crucial to ensure reliable performance during deployment. However, standard offline Supervised Fine-Tuning (SFT) suffers from distribution shifts and catastrophic forgetting of pre-trained capabilities, while online Reinforcement Learning (RL) struggles with sparse rewards and poor sample efficiency. In this paper, we propose On-Policy VLA Distillation (VLA-OPD), a framework bridging the efficiency of SFT with the robustness of RL. Instead of relying on sparse environmental rewards, VLA-OPD leverages an expert teacher to provide dense, token-level supervision on the student's self-generated trajectories. This enables active error correction on policy-induced states while preserving pre-trained general capabilities through gentle alignment. Crucially, we formulate VLA-OPD via a Reverse-KL objective. Unlike standard Forward-KL that induces mode-covering entropy explosion, or Hard-CE that causes premature entropy collapse, our bounded mode-seeking objective ensures stable policy learning by filtering out the teacher's epistemic uncertainty while maintaining action diversity. Experiments on LIBERO and RoboTwin2.0 benchmarks demonstrate that VLA-OPD significantly improves sample efficiency over RL and robustness over SFT, while effectively mitigating catastrophic forgetting during post-training.

---

## 论文详细总结（自动生成）

### VLA-OPD：通过同策略蒸馏桥接视觉-语言-动作模型的离线监督微调与在线强化学习

#### 1. 核心问题与研究动机
*   **核心问题**：预训练的视觉-语言-动作（VLA）模型在部署到具体下游任务时，现有的两种主流后训练（Post-training）范式存在固有缺陷：
    *   **离线监督微调（SFT/行为克隆）**：虽然收敛快，但存在“分布偏移”问题（模型在未见过的状态下会累积误差导致失败）和“灾难性遗忘”（损害预训练的通用能力）。
    *   **在线强化学习（RL）**：虽然能通过环境交互提升鲁棒性，但受限于“稀疏奖励”，导致采样效率极低且训练不稳定。
*   **研究动机**：提出一种既能拥有 SFT 的高效率，又能具备 RL 鲁棒性的统一框架，解决 VLA 模型在数据稀缺或复杂任务下的对齐难题。

#### 2. 方法论：核心思想与技术细节
VLA-OPD 的核心思想是**同策略蒸馏（On-Policy Distillation）**，将对齐过程建模为在学生自生成轨迹上的密集监督。其流程分为三个阶段：
*   **阶段 1：同策略采样（Student Sampling）**：学生模型在环境中执行任务并收集轨迹。由于学生模型初始较弱，它会频繁进入“错误状态”，从而显式地暴露分布偏移问题。
*   **阶段 2：教师密集标注（Teacher Labeling）**：对于学生访问的每一个状态，调用一个冻结的专家教师模型（Teacher）提供 Token 级的动作概率分布（Logits）。这提供了比环境奖励更密集的信号。
*   **阶段 3：逆向 KL 优化（Reverse-KL Optimization）**：
    *   **核心公式**：最小化 $D_{KL}(\pi_{\theta} || \pi_{tea})$。
    *   **模式寻求（Mode-seeking）特性**：与传统的正向 KL（会导致熵爆炸）或硬交叉熵（会导致熵崩溃）不同，逆向 KL 具有“零强制”属性。它允许学生模型专注于教师最确定的动作模式，过滤掉教师在分布外状态下的不确定性噪声，从而保持稳定的策略更新和动作多样性。
*   **算法实现**：采用类似 GRPO 的组采样策略（Group Sampling）来估计策略梯度，但将逆向 KL 的负值直接作为密集奖励，无需额外的 Critic 网络。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO 基准测试**：包含 Spatial、Object、Goal、Long 四个子集，用于测试单臂操作。
    *   **RoboTwin2.0 基准测试**：选择 4 个复杂的双臂协同任务（如叠碗、移交方块），测试模型在复杂形态下的表现。
*   **对比方法**：
    *   **教师模型**：SimpleVLA-RL（性能上限）。
    *   **基准模型**：OpenVLA-OFT（学生初始状态）、GRPO（纯在线 RL）、Octo、$\pi_0$、RDT 等。
*   **实验设置**：在 LIBERO 上采用极度稀缺的“1-轨迹（1-traj）”设置，即每个任务仅提供一个演示视频进行初始微调。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **超参数提及**：实验中使用了 Batch Size 为 64（消融实验中为 32），组采样大小 $G$ 通常设为 8。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个不同的机器人操作任务集上进行了性能评估。
    *   进行了针对“灾难性遗忘”的专项分析（Seen vs Unseen 任务权衡）。
    *   针对对齐目标函数（Reverse-KL vs Forward-KL vs Hard-CE）和组采样大小 $G$ 进行了详细的消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了从单臂到双臂、从数据充足到极度稀缺的多种场景。通过熵值分析（Entropy Analysis）深入探讨了优化目标的动力学差异，具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **高效率**：在 LIBERO 任务中，VLA-OPD 仅需 50 步即可达到纯 RL 方法 150 步的效果，实现约 **3 倍的加速**。
*   **高性能**：在仅有 1 条演示轨迹的情况下，VLA-OPD 的成功率（87.4%）接近甚至超过了使用 50 条轨迹训练的基准模型。
*   **缓解遗忘**：相比离线 SFT，VLA-OPD 在提升目标任务性能的同时，能更好地保留模型在未见任务上的预训练能力。
*   **目标函数关键性**：逆向 KL 是保持训练稳定的核心，它能有效避免“熵爆炸”和“过早熵崩溃”。

#### 7. 优点
*   **理论深刻**：深入分析了不同 KL 散度方向对 VLA 模型动作分布的影响，提出了“有界模式寻求”的概念。
*   **架构优雅**：通过密集蒸馏信号替代稀疏奖励，解决了 RL 训练 VLA 模型时的高方差问题，且无需 Critic 网络，节省显存。
*   **泛化性强**：在双臂协同等复杂任务上表现出良好的鲁棒性。

#### 8. 不足与局限
*   **依赖专家教师**：该框架的前提是存在一个高性能的教师模型。虽然文中提到可以利用开源模型或单任务专家，但在某些全新领域，获取高质量教师可能本身就是一个挑战。
*   **仿真与现实差距**：实验主要在仿真环境（LIBERO, RoboTwin）中进行，未展示在真实物理机器人上的部署效果及 Sim-to-Real 的迁移表现。
*   **算力细节缺失**：缺乏具体的算力消耗数据，不便于其他研究者评估复现成本。

（完）
