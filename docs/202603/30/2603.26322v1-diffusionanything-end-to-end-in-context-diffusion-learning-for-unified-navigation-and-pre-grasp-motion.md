---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 用于导航和操作的统一扩散策略，结合了VLA概念
tldr: 针对机器人视觉运动规划中目标指定复杂及模型资源消耗大的问题，本文提出 DiffusionAnything。这是一个统一的图像空间扩散策略，通过多尺度特征调制，仅需5分钟自监督数据即可实现米级导航与厘米级抓取前运动。该模型仅依赖 RGB 输入，具备低显存占用和高推理频率，在未知场景中展现出强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 传统的视觉动作模型计算资源需求高且在未知场景中泛化能力差，难以在端侧设备上实现统一的导航与操作规划。
method: 采用多尺度 FiLM 条件机制、轨迹对齐的深度预测以及基于 AnyTraverse 的自监督注意力机制，构建了一个端到端的扩散策略模型。
result: 模型仅需 2.0 GB 显存即可在 10 Hz 频率下运行，并在多种任务中实现了稳健的零样本泛化。
conclusion: 该研究证明了通过轻量化的扩散模型和自监督学习，可以在单一框架下高效统一不同尺度的机器人运动规划任务。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，通常规划需要明确的目标说明和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源、大量的训练数据，且在陌生场景中无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在单一、轻量级的模型中统一处理机器人**米级（meter-scale）的导航**与**厘米级（centimeter-scale）的抓取前运动规划**。
*   **研究动机**：
    *   **传统架构缺陷**：导航与操作通常被视为独立领域，导致级联架构存在误差累积、高延迟和模式切换僵硬的问题。
    *   **VLA模型局限**：现有的视觉-语言-动作（VLA）大模型（如RT-2、GR00T）虽然泛化性强，但计算成本极高、推理延迟大，且在陌生场景中往往无法实现真正的零样本（Zero-shot）泛化。
    *   **目标**：开发一种仅依赖RGB输入、低计算开销、且能快速适应新任务的统一扩散策略。

### 2. 方法论
*   **核心思想**：提出一种基于上下文感知的图像空间扩散策略，通过多尺度特征调制实现跨任务的知识共享与行为区分。
*   **关键技术细节**：
    1.  **多尺度 FiLM 条件化**：在UNet主干网络中引入特征线性调制（FiLM），根据任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力图对特征进行实时干预。
    2.  **轨迹对齐的深度推理**：不生成全图深度，而是仅预测生成的轨迹路径点上的深度值。这种设计显著降低了计算量，同时保证了避障和精确接近所需的度量信息。
    3.  **自监督注意力预测**：利用 AnyTraverse 框架生成的注意力图作为监督信号，使模型能自主识别可通行区域（导航）或目标物体（抓取），无需昂贵的视觉语言模型（VLM）或深度传感器。
*   **算法流程**：模型接收RGB图像和上下文向量 $c$，通过扩散模型迭代去噪，最终输出包含起始点、目标点及避障轨迹的多通道图像空间预测。

### 3. 实验设计
*   **实验场景**：
    *   **导航**：室内走廊、通往不同桌子的路径。
    *   **抓取前规划**：人形机器人面对放置有多种物体的桌面。
*   **基准模型（Benchmark）**：
    *   **导航**：对比了 **NoMaD**（基于大规模机器人数据集训练的视觉导航策略）。
    *   **操作**：对比了 NVIDIA 的 **GR00T (n1.5/n1.6)** 模型。
*   **评估指标**：成功率（SR）、避障间距（Clearance）、目标距离误差（Dist）。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090 GPU**。
    *   **基础训练**：使用20分钟视频数据，训练50个epoch，耗时约2小时。
    *   **任务微调**：针对新任务仅需5分钟数据，微调10个epoch，耗时约15-20分钟。
*   **推理性能**：在 RTX 4090 上运行，显存占用仅 **2.0 GB**，推理频率达到 **10 Hz**，适合嵌入式端侧部署。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了探索性导航、目标导向导航和抓取前规划三类主要实验。
    *   在导航任务中对比了自主目标选择和给定目标图像两种模式。
    *   在抓取任务中对比了专家演示数据量巨大的 GR00T 模型。
*   **充分性评价**：实验设计较为全面，涵盖了从宏观移动到微观操作的跨度。通过消融实验验证了深度尺度条件化的有效性（抓取精度提升了6倍）。虽然总训练数据量较小（分钟级），但这正是该论文强调的“高效学习”卖点。

### 6. 主要结论与发现
*   **统一性验证**：单一模型能够通过上下文切换，在导航时保持米级避障（100%成功率），在抓取时实现厘米级精度（4.71 cm 平均误差）。
*   **泛化能力**：在完全未见的场景中，该模型表现出比 GR00T 更强的零样本泛化能力，后者在陌生配置下会出现系统性失效。
*   **高效性**：相比于需要数小时电信操作数据的 VLA 模型，该方法仅需极少量的自监督数据即可达到同等甚至更优的避障效果。

### 7. 优点
*   **极低开销**：2GB 显存占用和 10Hz 的速度使其非常适合部署在人形机器人或移动平台上。
*   **数据效率极高**：通过自监督标签生成，摆脱了对大量人工标注或专家演示的依赖。
*   **多尺度融合**：巧妙地解决了导航（粗粒度）与操作（细粒度）在同一网络中的尺度冲突问题。

### 8. 不足与局限
*   **监督依赖**：模型性能高度依赖于 AnyTraverse 生成的伪标签质量，若自监督源出错，模型会继承偏差。
*   **反馈缺失**：目前仅限于运动规划（轨迹生成），尚未整合力反馈或触觉反馈，无法处理实际抓取时的物理交互。
*   **任务范围**：实验主要集中在“接近物体”的预抓取阶段，对于复杂的长程操作任务（如开门、倒水）尚未验证。

（完）
