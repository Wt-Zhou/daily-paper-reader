---
title: "ETA-VLA: Efficient Token Adaptation via Temporal Fusion and Intra-LLM Sparsification for Vision-Language-Action Models"
title_zh: ETA-VLA：通过时间融合与 LLM 内部稀疏化实现的视觉-语言-动作模型高效 Token 适配
authors: "Yiru Wang, Anqing Jiang, Shuo Wang, Yuwen Heng, Zichong Gu, Hao Sun"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25766v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于自动驾驶的VLA模型及时间融合技术
tldr: 针对自动驾驶中视觉-语言-动作（VLA）模型处理多视图历史帧带来的巨大计算开销，本文提出ETA-VLA框架。该框架引入了LLM内部稀疏聚合器（ILSA），通过文本引导和时间一致性动态剪枝冗余视觉标记。实验表明，该方法在显著降低计算量和推理开销的同时，仍能保持与最先进模型相当的驾驶性能。
selection_source: fresh_fetch
motivation: 自动驾驶VLA模型在处理多视图历史帧时，受限于LLM自注意力机制的平方复杂度，面临严重的计算瓶颈。
method: 提出ETA-VLA框架，利用ILSA模块结合文本引导评分和多样性保留策略，在LLM内部动态筛选并剪枝冗余视觉标记。
result: "在NAVSIM v2基准上，该方法在减少32%至61%计算量的同时，仍能保持与基准模型相当的驾驶性能。"
conclusion: ETA-VLA通过高效的标记适配策略，成功平衡了自动驾驶大模型的推理效率与感知精度。
---

## 摘要
将视觉-语言-动作（VLA）模型集成到自动驾驶系统中，为解释复杂场景和执行控制指令提供了一个统一的框架。然而，为了实现准确的时间推理，必须纳入历史多视图帧，这带来了沉重的计算负担，主要源于大语言模型（LLM）中自注意力机制的二次方复杂度。为了缓解这一瓶颈，我们提出了 ETA-VLA，一种用于 VLA 模型的高效 Token 适配框架。ETA-VLA 处理过去 $n$ 帧的多视图图像，并引入了一种新颖的 LLM 内部稀疏聚合器（ILSA）。受人类驾驶员注意力分配的启发，ILSA 在文本查询和时间一致性的引导下，动态地识别并剪枝冗余的视觉 Token。具体而言，我们利用文本引导的评分机制以及保持多样性的稀疏化策略来选择关键 Token 的稀疏子集，从而确保对驾驶场景的全面感知。在 NAVSIM v2 上的广泛实验表明，ETA-VLA 实现了与最先进基准模型相当的驾驶性能，同时将计算 FLOPs 降低了约 32%。值得注意的是，我们的方法剪枝了 85% 的视觉 Token，并将推理 FLOPs 降低了 61%，但在 NAVSIM v2 基准测试中仍保留了 94% 的原始准确率。

## Abstract
The integration of Vision-Language-Action (VLA) models into autonomous driving systems offers a unified framework for interpreting complex scenes and executing control commands. However, the necessity to incorporate historical multi-view frames for accurate temporal reasoning imposes a severe computational burden, primarily driven by the quadratic complexity of self-attention mechanisms in Large Language Models (LLMs). To alleviate this bottleneck, we propose ETA-VLA, an Efficient Token Adaptation framework for VLA models. ETA-VLA processes the past $n$ frames of multi-view images and introduces a novel Intra-LLM Sparse Aggregator (ILSA). Drawing inspiration from human driver attention allocation, ILSA dynamically identifies and prunes redundant visual tokens guided by textual queries and temporal consistency. Specifically, we utilize a text-guided scoring mechanism alongside a diversity-preserving sparsification strategy to select a sparse subset of critical tokens, ensuring comprehensive awareness of the driving scene. Extensive experiments on the NAVSIM v2 demonstrate that ETA-VLA achieves driving performance comparable to state-of-the-art baselines while reducing computational FLOPs by approximately 32\%. Notably, our method prunes 85% of visual tokens and reduces inference FLOPs by 61\%, but still retaining 94% of the original accuracy on the NAVSIM v2 benchmark.

---

## 论文详细总结（自动生成）

以下是对论文《ETA-VLA: Efficient Token Adaptation via Temporal Fusion and Intra-LLM Sparsification for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在自动驾驶领域，视觉-语言-动作（VLA）模型需要处理多视图（Multi-view）和长历史帧（Temporal history）以实现准确的推理。然而，LLM 的自注意力机制具有二次方复杂度 $O(L^2)$，多视图与多帧导致的“Token 膨胀”使得计算开销巨大，难以在车载嵌入式硬件上实时部署。
*   **研究动机**：模仿人类驾驶员的注意力分配机制——即在复杂场景中动态地将认知资源集中在关键区域（如前方道路或转弯侧），而忽略冗余背景，从而在保证安全的前提下提高处理效率。

### 2. 方法论
ETA-VLA 框架由三个核心部分组成：
*   **时间融合模块 (TFM)**：
    *   在进入 LLM 之前，利用可学习的时间嵌入（Time Embedding）和 Transformer 编码器捕捉多帧间的时序依赖。
    *   通过可学习的时间权重对历史特征进行加权聚合，将 $n$ 帧特征压缩为单一的紧凑特征图，从源头上缓解 Token 增长。
*   **LLM 内部稀疏聚合器 (ILSA)**：
    *   **动态文本锚点选择**：根据视觉 Token 与文本指令的相似度，动态筛选出相关的文本 Token 作为“评分者”，避免无关指令噪声。
    *   **无 RoPE 语义评分**：在计算注意力权重时移除旋转位置嵌入（RoPE）和因果掩码，确保视觉 Token 的重要性评分纯粹基于语义相关性，而非位置距离。
    *   **多样性保留稀疏化策略**：采用两阶段筛选。首先进行全局 Top-K 语义剪枝；随后通过计算特征多样性得分，回收各视图中被误删但具有独特空间信息的 Token，防止盲区感知丢失。
*   **动作解码与优化**：
    *   使用轻量级 Transformer Decoder 将 LLM 输出的稀疏隐藏状态转换为连续轨迹。
    *   引入预训练的 Scorer 模块（基于 EPDMS 指标）对输出轨迹进行进一步精细化调整。

### 3. 实验设计
*   **数据集与场景**：使用 **NAVSIM v2** 基准测试，包含 **Navtest**（开环评估）和 **Navhard**（带有扰动的伪闭环评估，难度更高）。
*   **评估指标**：主要指标为 **EPDMS**（扩展预测驾驶员模型得分），综合考量了无过错碰撞（NC）、可行驶区域合规性（DAC）、舒适度（HC）等 9 个子项。
*   **对比方法**：包括 Transfuser、VADv2、HydraMDP++、GTRS、DiffusionDrive 以及最新的 SimScale 等 SOTA（最先进）方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练细节**：
    *   第一阶段：预训练投影层和适配器（1 个 epoch，Batch Size 32）。
    *   第二阶段：全系统联合微调（1 个 epoch，Batch Size 8）。
    *   学习率分别设为 $1 \times 10^{-3}$ 和 $1 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括在 Navtest 和 Navhard 上的性能对比、针对不同 LLM 层级和剪枝率的效率分析，以及消融实验。
*   **充分性与公平性**：
    *   对比了多种剪枝策略（如随机剪枝、HoloV、SparseVLM），证明了 ILSA 的优越性。
    *   在相同剪枝率（35%）下与现有方法进行公平对比。
    *   实验覆盖了从感知到规划的完整闭环逻辑，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：ETA-VLA 在 Navtest 上达到 **85.0 EPDMS**，在 Navhard 上达到 **48.0 EPDMS**，均刷新了 SOTA 纪录，接近人类专家水平（90.3）。
*   **高效减损**：在剪枝 35% Token 的情况下，计算量减少 32%，性能反而略有提升（从 84.8 升至 85.0），说明 ILSA 起到了过滤噪声的作用。
*   **极限压缩**：在剪枝 85% 视觉 Token 时，推理 FLOPs 降低了 61%，仍能保留 94% 的原始精度。
*   **层级敏感性**：实验发现，在 LLM 的中间层（如第 4 层）进行剪枝效果最好，过深或过浅的层级剪枝都会导致关键信息丢失。

### 7. 优点
*   **启发式设计**：成功将人类驾驶员的注意力分配逻辑转化为数学模型（ILSA），兼顾了任务相关性和空间多样性。
*   **解决 RoPE 偏差**：敏锐察觉到 LLM 原生位置编码对视觉 Token 评分的干扰，并提出了有效的 RoPE-free 解决方案。
*   **效率与精度双赢**：不仅降低了计算开销，还通过去除冗余 Token 提升了模型对关键驾驶信息的聚焦能力。

### 8. 不足与局限
*   **层级依赖性**：剪枝策略对 LLM 的层级位置非常敏感，缺乏一种自动确定最优剪枝层级的通用机制。
*   **预训练依赖**：轨迹优化依赖于一个预训练的 Scorer 模块，这增加了系统的复杂度和对特定评估指标的依赖。
*   **实时性验证**：虽然 FLOPs 显著下降，但论文未详细给出在具体车载嵌入式硬件（如 NVIDIA Orin）上的实际推理延迟（Latency）数据。

（完）
