---
title: "KineVLA: Towards Kinematics-Aware Vision-Language-Action Models with Bi-Level Action Decomposition"
title_zh: KineVLA：通过双层动作分解构建运动学感知的视觉-语言-动作模型
authors: "Gaoge Han, Zhengqing Gao, Ziwen Li, Jiaxin Huang, Shaoli Huang, Fakhri Karray, Mingming Gong, Tongliang Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17524v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 感知运动学的VLA模型用于精细机器人操作
tldr: 本研究针对现有视觉-语言-动作（VLA）模型在处理精细运动学指令方面的不足，提出了KineVLA框架。该框架通过双层动作分解和推理Token，将任务目标的不变性与执行轨迹的运动学变异性显式解耦。通过构建包含丰富运动学属性的新型数据集，并在仿真与真实机器人平台上进行实验，KineVLA证明了其在实现精确、可控且具泛化性的机器人操控任务中的卓越性能。
selection_source: fresh_fetch
motivation: 现有的机器人指令往往缺乏对运动学细节（如方向、轨迹和姿态）的精确描述，限制了模型在复杂操控任务中的表现。
method: 提出一种双层动作表示方法，利用推理Token作为中间变量，将语言指令中的运动学要求与具体的动作执行进行对齐。
result: 实验结果显示，KineVLA在LIBERO和真实机器人测试中均优于强基准模型，展现出更高的操控精度和控制力。
conclusion: KineVLA通过显式建模运动学属性，为实现更精细、个性化的视觉-语言-动作交互提供了有效的解决方案。
---

## 摘要
在本文中，我们引入了一项全新的富含运动学信息的视觉-语言-动作（VLA）任务。在该任务中，语言指令在从开始到完成的关键时刻密集地编码了多种运动学属性（如方向、轨迹、朝向和相对位移），这与现有的仅粗略或部分捕捉运动学的动作指令不同，从而支持细粒度和个性化的操作。在这种设定下，任务目标保持不变，而执行轨迹必须适应指令级的运动学规范。为了应对这一挑战，我们提出了 KineVLA，这是一个视觉-语言-动作框架，它通过双层动作表示和双层推理标记（reasoning tokens）将目标层级的不变性与运动学层级的可变性显式解耦，这些标记作为显式的、受监督的中间变量来对齐语言和动作。为了支持这一任务，我们构建了跨越仿真和真实机器人平台的运动学感知 VLA 数据集，其特点是具有指令级的运动学变化和双层标注。在 LIBERO 和 Realman-75 机器人上进行的大量实验表明，KineVLA 在运动学敏感的基准测试中始终优于强大的 VLA 基线模型，实现了更精确、可控且更具泛化性的操作行为。

## Abstract
In this paper, we introduce a novel kinematics-rich vision-language-action (VLA) task, in which language commands densely encode diverse kinematic attributes (such as direction, trajectory, orientation, and relative displacement) from initiation through completion, at key moments, unlike existing action instructions that capture kinematics only coarsely or partially, thereby supporting fine-grained and personalized manipulation. In this setting, where task goals remain invariant while execution trajectories must adapt to instruction-level kinematic specifications. To address this challenge, we propose KineVLA, a vision-language-action framework that explicitly decouples goal-level invariance from kinematics-level variability through a bi-level action representation and bi-level reasoning tokens to serve as explicit, supervised intermediate variables that align language and action. To support this task, we construct the kinematics-aware VLA datasets spanning both simulation and real-world robotic platforms, featuring instruction-level kinematic variations and bi-level annotations. Extensive experiments on LIBERO and a Realman-75 robot demonstrate that KineVLA consistently outperforms strong VLA baselines on kinematics-sensitive benchmarks, achieving more precise, controllable, and generalizable manipulation behaviors.

---

## 论文详细总结（自动生成）

### KineVLA：通过双层动作分解构建运动学感知的视觉-语言-动作模型总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）主要关注“目标导向”的任务，即模型能理解“做什么”（如抓取瓶子），但往往忽略了“如何做”的精细运动学细节（如以什么角度抓取、沿什么轨迹移动、标签朝向何处）。
*   **研究动机**：在现实的人机交互中，语义相同的目标（目标不变性）往往需要完全不同的执行轨迹（运动学变异性）。现有的 VLA 模型在处理这类“富含运动学信息”的任务时表现不佳，无法满足用户对机器人操作的个性化和精确化需求。

#### 2. 方法论
KineVLA 的核心思想是通过**双层架构**将任务目标与运动学细节显式解耦：
*   **双层动作表示（Bi-Level Action Representation）**：
    *   利用**残差向量量化变分自编码器（RVQ-VAE）**将连续动作空间离散化。
    *   **第一层（目标层）**：捕捉低频的全局任务结构和语义目标。
    *   **第二层（运动学层）**：捕捉高频的运动修正、接触细节和精细轨迹。
    *   **训练策略**：采用两阶段训练，先在多任务数据上预训练目标层码本，再在运动学数据集上微调运动学层码本。
*   **双层推理标记（Bi-Level Reasoning Tokens）**：
    *   引入类似思维链（CoT）的生成范式。模型在输出动作 Token 之前，先生成两段推理文本：一段描述粗粒度的任务目标，另一段描述细粒度的运动学参数（如方向、距离、约束）。
*   **互信息正则化（Mutual Information Regularization）**：
    *   为了确保生成的推理文本与实际动作执行一致，通过 **InfoNCE 损失函数**最大化推理文本与动作序列之间的条件互信息 $I(Reasoning; Action | Context)$，减少执行漂移。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO-Goal-Relabeled**：对现有 LIBERO 数据集进行重标注，加入运动学指令。
    *   **Kine-LIBERO（仿真）**：新构建的大规模仿真数据集，包含 26.6 万帧图像，涵盖多样的物体部位交互和运动约束。
    *   **Kine-Realman（真机）**：在 Realman-75 机器人上收集的 4.9 万帧真实数据，涉及 7 类精细操作任务。
*   **基准模型（Benchmarks）**：对比了 **OpenVLA**（基于 Binning 的主流模型）、**$\pi_{0.5}$**（基于扩散的模型）和 **VQ-VLA**（单层向量量化模型）。
*   **评估指标**：除了传统的“目标成功率（Goal Success）”，重点引入了“运动学成功率（Kinematics Success）”，并使用 BLEU 和 BERTScore 评估推理文本的质量。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   RVQ-VAE 训练：使用 **1 张 NVIDIA A100 GPU**。
    *   VLA 模型微调：使用 **4 张 NVIDIA A100 GPU**。
*   **训练细节**：微调过程进行了 5 万个梯度步（50K steps），总 Batch Size 为 64。
*   **推理速度**：KineVLA 的推理延迟约为 96ms，与单层 VQ 模型（84ms）和扩散模型（86ms）处于同一量级，具备实时控制能力。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真到真实世界的迁移，总计约 37.8 万帧数据。
*   **消融实验**：系统地验证了双层表示（Bi-Rep）、双层推理（Bi-Rea）以及互信息优化（MI）的有效性。
*   **充分性评价**：实验设计较为充分，通过“干预实验”（Intervention）证明了推理 Token 对动作生成的因果影响，对比基线涵盖了当前最先进的自回归和扩散类 VLA 模型，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **解耦的必要性**：传统的 VLA 模型在目标达成上表现尚可，但在遵循精细运动学指令方面存在显著缺陷。
*   **性能提升**：KineVLA 在运动学敏感的基准测试中显著优于所有基线模型，尤其是在需要特定姿态或轨迹的任务中。
*   **推理与动作的对齐**：引入双层推理 Token 不仅提高了模型的可解释性，还通过互信息正则化增强了动作执行的物理一致性。

#### 7. 优点
*   **任务定义新颖**：首次明确提出了“目标不变、运动学变异”的任务设定，贴近真实应用需求。
*   **架构设计优雅**：通过双层 RVQ-VAE 巧妙地解决了语义目标与物理细节在同一表示空间中的冲突问题。
*   **实用性强**：在保持推理速度的同时，显著提升了机器人操作的精度和可控性。

#### 8. 不足与局限
*   **场景局限性**：目前的实验主要集中在桌面操作（Tabletop Manipulation），尚未在更复杂的全身操控（Whole-body Manipulation）或动态环境中验证。
*   **坐标系依赖**：运动学指令目前依赖于固定的第三人称相机坐标系（如“向左”、“向后”），对于多视角或移动底座机器人的适应性有待进一步研究。
*   **数据标注成本**：双层推理文本和精细运动学标注需要较高的人工成本，大规模扩展可能面临挑战。

（完）
