---
title: "Action Draft and Verify: A Self-Verifying Framework for Vision-Language-Action Model"
title_zh: 动作草拟与验证：一种用于视觉-语言-动作模型的自验证框架
authors: "Chen Zhao, Zhuoran Wang, Haoyang Li, Shifeng Bao, Guanlin Li, Youhe Feng, Yang Li, Jie Tang, Jing Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18091v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 结合扩散模型与VLM评分的愿景-语言-动作模型自验证框架
tldr: 针对具身智能任务中扩散模型生成动作的高精度与自回归模型泛化性之间的权衡，本文提出ADV框架。该框架利用扩散动作专家生成多个候选动作块，并由视觉语言模型（VLM）通过单次前向传播的困惑度指标进行评分和筛选。实验证明，ADV在保持效率的同时，显著提升了模拟和真实环境下的任务成功率，为VLA模型提供了一种高效的自验证机制。
selection_source: fresh_fetch
motivation: 旨在结合扩散模型的高精度动作生成能力与自回归模型在分布外环境中的强鲁棒性与泛化优势。
method: 提出ADV框架，利用扩散专家起草多个候选动作，并由VLM通过单次推理的困惑度指标进行评分验证。
result: "在相同条件下，该方法在模拟环境和真实世界中的成功率分别比纯扩散基准提升了4.3%和19.7%。"
conclusion: ADV框架通过自验证机制有效融合了两种生成范式的优势，显著增强了VLA模型在复杂环境下的性能。
---

## 摘要
视觉-语言-动作（VLA）模型近期在具身任务中展现出了强大的性能。现代 VLA 通常采用扩散动作专家来高效生成高精度的连续动作块，而自回归生成在底层控制方面可能较慢且精度较低。然而，自回归范式仍能提供互补的先验知识，从而提高在分布外环境中的鲁棒性和泛化能力。为了利用这两种范式，我们提出了“动作草拟与验证”（Action-Draft-and-Verify, ADV）：由扩散动作专家草拟多个候选动作块，并由视觉语言模型（VLM）通过类困惑度指标在单次前向传播中对所有候选动作进行评分，从而选出其中一个。在相同的骨干网络、训练数据和动作块长度下，ADV 在模拟环境中的成功率比基于扩散的基准模型提高了 4.3 个百分点，在真实世界中提高了 19.7 个百分点，且仅增加了单次 VLM 重排序的开销。

## Abstract
Vision-Language-Action (VLA) models have recently demonstrated strong performance across embodied tasks. Modern VLAs commonly employ diffusion action experts to efficiently generate high-precision continuous action chunks, while auto-regressive generation can be slower and less accurate at low-level control. Yet auto-regressive paradigms still provide complementary priors that can improve robustness and generalization in out-of-distribution environments. To leverage both paradigms, we propose Action-Draft-and-Verify (ADV): diffusion action expert drafts multiple candidate action chunks, and the VLM selects one by scoring all candidates in a single forward pass with a perplexity-style metric. Under matched backbones, training data, and action-chunk length, ADV improves success rate by +4.3 points in simulation and +19.7 points in real-world over diffusion-based baseline, with a single-pass VLM reranking overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《Action Draft and Verify: A Self-Verifying Framework for Vision-Language-Action Model》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前的视觉-语言-动作（VLA）模型主要分为自回归（AR）和扩散（Diffusion）两种范式。扩散模型生成动作精度高且平滑，但在面对分布外（OOD）环境时容易失效（如出现抖动、碰撞且缺乏纠错能力）；自回归模型虽然推理慢，但能更好地利用 VLM 的预训练先验，展现出更强的鲁棒性和泛化性。
*   **核心问题**：如何结合扩散模型的高精度生成能力与自回归模型的语义验证能力，以提升机器人在复杂及未知环境中的执行成功率？
*   **整体含义**：本文提出了 **ADV (Action Draft and Verify)** 框架，通过“扩散模型提议候选动作，VLM 进行评分筛选”的机制，实现了一种高效的自验证具身智能架构。

### 2. 方法论
*   **核心思想**：将动作生成视为一个“采样+重排序”的过程。利用扩散专家作为“草拟者”生成多样化候选，利用 VLM 作为“验证者”剔除不合理的动作。
*   **关键技术流程**：
    1.  **草拟阶段 (Draft)**：扩散动作专家（Diffusion Action Expert）基于 VLM 提取的特征，通过注入不同噪声并行生成 $M$ 个候选动作块（Action Chunks）。
    2.  **验证阶段 (Verify)**：
        *   **Textual FAST 标记化**：将连续动作通过离散余弦变换（DCT）和字节对编码（BPE）压缩后，渲染为文本字符串，再由 VLM 的分词器处理。这使得动作表示与 VLM 的预训练文本分布更一致。
        *   **困惑度评分**：VLM 对所有候选动作块进行单次并行前向传播，计算每个动作序列的长度归一化负对数似然（类似 Perplexity 评分）。
        *   **选择执行**：选取评分最低（即 VLM 认为最合理）的动作块作为最终输出。
*   **训练目标**：采用联合训练（Co-training），同时优化自回归损失（$L_{VLM}$）和扩散流匹配损失（$L_{dif}$）。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：LIBERO（包含 Spatial, Object, Goal, Long 四类任务）和 RoboTwin 2.0（包含 Easy 和 Hard/OOD 模式）。
    *   **真实世界**：设计了推方块、清理桌面、取放物体、挂杯子四类任务，并包含专门的“未见过（Unseen）”测试集。
*   **对比方法 (Baselines)**：
    *   **Model-FAST**：纯自回归生成动作的模型。
    *   **Model-Diffusion**：纯扩散专家生成动作的模型。
    *   **现有 SOTA**：如 $\pi_{0.5}$、OpenVLA、Octo、HybridVLA 等。
*   **评估指标**：成功率（Success Rate）、平均成功步数、恢复尝试次数、预抓取碰撞率、推理速度（Hz）。

### 4. 资源与算力
*   **模型规模**：动作专家模型约为 500M 参数（16层 Transformer），VLM 骨干采用 Qwen2.5-VL-3B 和 InternVL3.5-2B。
*   **训练超参**：Batch size 为 120-144，学习率在 $2\times10^{-5}$ 到 $5\times10^{-5}$ 之间。
*   **算力说明**：**论文未明确提及具体的 GPU 型号、数量及总训练时长**。仅给出了推理时的控制频率（如 Qwen2.5-VL-ADV 在 LIBERO 上约为 41.0Hz）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 50 多个模拟任务和 4 类真实世界任务，收集并使用了超过 2000 条真实轨迹。
*   **充分性分析**：
    *   **多维度验证**：不仅对比了成功率，还深入分析了 OOD 环境下的行为模式（如恢复尝试次数）。
    *   **消融实验**：针对 Tokenization 方法（Textual FAST vs 其他）、候选数量 $M$、评分可靠性等做了详细消融。
    *   **公平性**：在相同的骨干网络、训练数据和动作块长度下对比 AR、Diffusion 和 ADV，实验设计客观公平。

### 6. 主要结论与发现
*   **性能飞跃**：ADV 在模拟环境中比纯扩散基准提升 4.3%，在真实世界中提升 **19.7%**。
*   **过滤机制**：VLM 验证器的核心作用是**“失败模式过滤器”**。它不一定能选出绝对完美的动作，但能有效识别并剔除那些会导致碰撞或无效的“糟糕”动作。
*   **OOD 鲁棒性**：在分布外环境中，ADV 显著减少了机械臂的随机抖动和碰撞，并提高了失败后的自我纠错（恢复）能力。
*   **Token 影响**：Textual FAST 证明了将动作对齐到文本空间对于 VLM 评分的准确性至关重要。

### 7. 优点
*   **架构优雅**：无需复杂的启发式融合，通过简单的“采样+评分”逻辑结合了两种范式的长处。
*   **推理高效**：利用 VLM 的并行评分能力，相比逐 Token 生成的自回归模型，推理速度大幅提升（30-50Hz），满足实时控制需求。
*   **泛化性强**：在未见过的指令和物体上表现出显著优于基准模型的鲁棒性。

### 8. 不足与局限
*   **推理开销**：虽然比 AR 快，但由于需要生成 $M$ 个候选并进行一次 VLM 前向计算，其频率仍低于纯扩散模型（约下降 40%）。
*   **候选质量瓶颈**：ADV 的上限受限于扩散专家。如果扩散专家在极端环境下无法生成任何有效的候选动作，VLM 验证也无法挽回失败。
*   **算力细节不透明**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。

（完）
