---
title: "vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models"
title_zh: vla-eval：视觉-语言-动作模型的统一评估框架
authors: "Suhwan Choi, Yunsung Lee, Yubeen Park, Chris Dongjoo Kim, Ranjay Krishna, Dieter Fox, Youngjae Yu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13966v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 视觉-语言-动作模型的评估框架
tldr: 针对视觉-语言-动作（VLA）模型评估中存在的脚本碎片化、依赖冲突和协议不明确等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术解耦了模型推理与基准测试，支持 13 个基准测试和 6 种模型。其并行评估机制将吞吐量提升了 47 倍，并提供了包含 657 条结果的排行榜，显著提升了 VLA 模型评估的效率与可复现性。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型评估依赖于各模型库独立维护的脚本，导致代码重复、环境依赖冲突且实验协议不统一。
method: 提出一种基于 WebSocket 协议和 Docker 容器化的解耦架构，通过统一接口实现模型推理与基准环境的隔离与并行化执行。
result: 该框架实现了 47 倍的评估吞吐量提升，并在复现实验中揭示了影响结果的隐藏归一化统计和不明确的终止语义等关键细节。
conclusion: vla-eval 为 VLA 领域提供了标准化的评估基础设施和综合排行榜，极大地促进了模型性能的公平比较与科研复现。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用由每个模型仓库独立维护的特定基准测试脚本进行评估，这导致了代码重复、依赖冲突以及协议定义不明确等问题。我们提出了 vla-eval，这是一个开源评估框架，它通过基于 WebSocket msgpack 协议和 Docker 环境隔离，实现了模型推理与基准测试执行的解耦。模型只需实现一个 predict() 方法即可完成集成；基准测试通过四方法接口进行集成；完整的交叉评估矩阵将自动运行。完成一次完整的评估仅需两条命令：vla eval serve 和 vla eval run。该框架支持 13 个仿真基准测试和 6 个模型服务器。通过回合分片（episode sharding）和批处理推理实现的并行评估使吞吐量提升了 47 倍，在大约 18 分钟内即可完成 2000 个 LIBERO 回合。利用该基础设施，我们对一个已发表的 VLA 模型在三个基准测试上进行了复现性审计，发现这三个基准测试均能紧密复现已发表的数值，同时也揭示了一些未记录的需求、模糊的终止语义以及可能在无形中扭曲结果的隐藏归一化统计数据。此外，我们还发布了一个 VLA 排行榜，汇总了 17 个基准测试中的 657 项已发表结果。框架、评估配置以及所有复现结果均已公开。

## Abstract
Vision Language Action VLA models are typically evaluated using per benchmark scripts maintained independently by each model repository, leading to duplicated code, dependency conflicts, and underspecified protocols. We present vla eval, an open source evaluation harness that decouples model inference from benchmark execution through a WebSocket msgpack protocol with Docker based environment isolation. Models integrate once by implementing a single predict() method; benchmarks integrate once via a four method interface; the full cross evaluation matrix works automatically. A complete evaluation requires only two commands: vla eval serve and vla eval run. The framework supports 13 simulation benchmarks and six model servers. Parallel evaluation via episode sharding and batch inference achieves a 47x throughput improvement, completing 2000 LIBERO episodes in about 18 minutes. Using this infrastructure, we conduct a reproducibility audit of a published VLA model across three benchmarks, finding that all three closely reproduce published values while uncovering undocumented requirements ambiguous termination semantics and hidden normalization statistics that can silently distort results. We additionally release a VLA leaderboard aggregating 657 published results across 17 benchmarks. Framework, evaluation configs, and all reproduction results are publicly available.

---

## 论文详细总结（自动生成）

### vla-eval：视觉-语言-动作（VLA）模型统一评估框架深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：当前 VLA 模型的评估生态系统高度碎片化。每个模型仓库通常独立维护特定基准测试（Benchmark）的脚本，导致了四大痛点：
    1.  **重复劳动**：不同仓库维护相似但略有差异的评估代码。
    2.  **依赖冲突**：不同基准测试（如 LIBERO 需 Python 3.8，ManiSkill2 需 3.10）对环境要求冲突，难以在单一环境中运行。
    3.  **协议不明确**：论文常忽略随机种子、回合数、归一化统计数据等关键细节，导致难以复现。
    4.  **评估缓慢**：串行评估耗时长（如 LIBERO 需 14 小时），限制了大规模消融实验。
*   **整体含义**：本文仿照语言模型的 `lm-evaluation-harness`，为机器人 VLA 模型设计了一个解耦、隔离且高效的统一评估框架，旨在标准化评估流程并提升科研效率。

#### 2. 方法论
*   **核心思想**：通过 **客户端-服务器架构** 将模型推理与基准测试执行完全解耦。
*   **关键技术细节**：
    *   **通信协议**：使用 WebSocket 配合 msgpack 二进制序列化进行数据传输，支持观察值（obs）、动作（action）及回合状态的实时交换。
    *   **环境隔离**：
        *   **基准测试端**：每个基准测试运行在独立的 Docker 容器中，预装所有依赖和资产（场景、纹理、机器人描述）。
        *   **模型服务端**：利用 PEP 723 内联元数据和 `uv` 工具创建隔离的 Python 环境，解决不同模型间的库版本冲突。
    *   **统一接口**：模型端仅需实现一个 `predict()` 方法；基准测试端通过四个核心方法（reset, step, make_obs, get_step_result）进行适配。
    *   **并行化机制**：采用“回合分片（Episode Sharding）”+“批处理推理（Batch Inference）”的策略。通过公式 $\lambda(N) < 0.8 \cdot \mu(B^*)$ 优化并行度，其中 $\lambda$ 为环境吞吐量，$N$ 为分片数，$\mu$ 为模型吞吐量，$B$ 为批大小。

#### 3. 实验设计
*   **基准测试与模型**：
    *   **支持基准**：集成了 13 个仿真基准，包括 LIBERO、CALVIN、SimplerEnv、ManiSkill2、RoboCasa 等。
    *   **支持模型**：集成了 6 种主流模型，如 OpenVLA、CogACT、π0、GR00T N1 等。
*   **复现性审计**：以 DB-CogACT 模型为对象，在 LIBERO（2000 回合）、CALVIN（1000 链式序列）、SimplerEnv（288 回合）上进行复现实验。
*   **对比实验**：对比了串行评估与并行评估的耗时（Wall-clock time）。
*   **排行榜**：汇总了来自 2685 篇论文的 657 项结果，涵盖 17 个基准测试。

#### 4. 资源与算力
*   **算力设备**：实验中使用了 **NVIDIA H100 GPU** 作为模型服务器。
*   **效率提升**：
    *   **LIBERO**：在 50 个分片和 Batch=16 的配置下，耗时从 14 小时缩短至 **18 分钟**（47 倍加速）。
    *   **CALVIN**：16 个分片下耗时约 33 分钟（16 倍加速）。
    *   **SimplerEnv**：16 个分片下耗时约 8.5 分钟（12 倍加速）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了数千个回合的自动化评估，涵盖了从 6D 到 14D 动作空间的多种任务。
*   **充分性**：通过对已发表模型的复现审计，验证了框架的准确性。实验不仅关注成功率，还深入分析了导致结果偏差的底层原因。
*   **客观性**：使用了固定种子和版本化的 Docker 镜像，确保了实验的可重复性和公平性。

#### 6. 主要结论与发现
*   **性能复现**：框架能以极小的误差（±3% 以内）复现已发表论文的指标。
*   **揭示隐性障碍**：审计发现了两个常被忽视的复现障碍：
    1.  **模糊的终止语义**：如 SimplerEnv 中某些标志位仅代表瞬时成功而非回合结束，提前停止会虚高得分。
    2.  **隐藏的归一化统计**：CALVIN 等基准需要特定的训练集均值/方差进行预处理，若论文未注明则无法正确推理。
*   **领域现状**：排行榜分析显示，81% 的模型仅在单一基准上评估，跨基准评估极度缺乏。

#### 7. 优点
*   **易用性极高**：仅需两条命令即可完成复杂环境的配置与运行。
*   **极速评估**：通过高度并行化显著降低了 VLA 模型的开发迭代周期。
*   **标准化**：通过 Docker 镜像和统一配置文件，消除了“环境配置”这一最大的复现变量。
*   **社区贡献**：提供了详尽的排行榜和开源工具，有助于规范化整个领域的研究。

#### 8. 不足与局限
*   **场景局限**：目前仅针对**仿真环境**，未涵盖真实机器人（Real-robot）评估。
*   **指标单一**：主要关注任务成功率，尚未集成运动质量、效率、安全性等更丰富的评估维度。
*   **数据来源**：排行榜数据主要来自论文提取（由 AI 辅助及人工审核），而非全部通过该框架重新运行验证，可能存在原始论文报告偏差。
*   **模型覆盖**：虽然支持主流模型，但对于一些闭源或架构极其特殊的模型，集成成本可能较高。

（完）
