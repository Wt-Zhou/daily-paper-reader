---
title: "VectorWorld: Efficient Streaming World Model via Diffusion Flow on Vector Graphs"
title_zh: VectorWorld：基于矢量图扩散流的高效流式世界模型
authors: "Chaokang Jiang, Desen Zhou, Jiuming Liu, Kevin Li Sun"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17652v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于自动驾驶策略的流式世界模型
tldr: VectorWorld是一个针对自动驾驶闭环评估的高效流式世界模型。它通过生成以自我为中心的矢量图瓦片，解决了现有模型在初始化不匹配、实时性差和长时域运动学不可行等方面的挑战。该模型结合了运动感知VAE、单步扩散流生成和物理对齐的NPC策略，实现了稳定且实时的1公里以上闭环仿真。
selection_source: fresh_fetch
motivation: 现有生成式世界模型在闭环评估中面临初始化不匹配、采样延迟高以及长时域运动学不一致的问题。
method: 提出VectorWorld，利用运动感知VAE对齐策略输入，并通过单步扩散流和物理对齐的ΔSim策略实现高效、稳定的矢量图生成。
result: 在Waymo和nuPlan数据集上，该模型显著提升了地图保真度和初始化有效性，并支持超过1公里的实时闭环仿真。
conclusion: VectorWorld为自动驾驶提供了一个高效、物理一致且支持长距离仿真的流式矢量世界模型框架。
---

## 摘要
自动驾驶策略的闭环评估需要超越日志回放的交互式仿真。然而，现有的生成式世界模型在闭环中往往性能下降，原因在于：(i) 无历史信息的初始化与策略输入不匹配；(ii) 多步采样延迟超出了实时性预算；以及 (iii) 长时界下运动学不可行性的累积。我们提出了 VectorWorld，这是一种流式世界模型，能够在展开（rollout）过程中增量生成以自车为中心的 $64 \mathrm{m}\times 64\mathrm{m}$ 车道-智能体矢量图瓦片。VectorWorld 通过运动感知门控 VAE 产生与策略兼容的交互状态，从而使初始化与基于历史信息的策略保持一致。它通过无求解器的一步掩码补全实现了实时外扩（outpainting），该补全采用边缘门控关系 DiT，并使用区间条件 MeanFlow 和基于 JVP 的大步长监督进行训练。为了稳定长时界展开，我们引入了 $\Delta$Sim，这是一种符合物理规律的非自车（NPC）策略，具有离散-连续混合动作和可微运动学 Logit 整形。在 Waymo Open Motion 和 nuPlan 数据集上，VectorWorld 提高了地图结构的保真度和初始化的有效性，并支持稳定、实时的 $1\mathrm{km}+$ 闭环展开。

## Abstract
Closed-loop evaluation of autonomous-driving policies requires interactive simulation beyond log replay. However, existing generative world models often degrade in closed loop due to (i) history-free initialization that mismatches policy inputs, (ii) multi-step sampling latency that violates real-time budgets, and (iii) compounding kinematic infeasibility over long horizons. We propose VectorWorld, a streaming world model that incrementally generates ego-centric $64 \mathrm{m}\times 64\mathrm{m}$ lane--agent vector-graph tiles during rollout. VectorWorld aligns initialization with history-conditioned policies by producing a policy-compatible interaction state via a motion-aware gated VAE. It enables real-time outpainting via solver-free one-step masked completion with an edge-gated relational DiT trained with interval-conditioned MeanFlow and JVP-based large-step supervision. To stabilize long-horizon rollouts, we introduce $Δ$Sim, a physics-aligned non-ego (NPC) policy with hybrid discrete--continuous actions and differentiable kinematic logit shaping. On Waymo open motion and nuPlan, VectorWorld improves map-structure fidelity and initialization validity, and supports stable, real-time $1\mathrm{km}+$ closed-loop rollouts (\href{https://github.com/jiangchaokang/VectorWorld}{code}).

---

## 论文详细总结（自动生成）

以下是对论文《VectorWorld: Efficient Streaming World Model via Diffusion Flow on Vector Graphs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机）
现有的生成式自动驾驶世界模型在闭环（Closed-loop）仿真评估中存在三大痛点：
*   **初始化不匹配（冷启动问题）：** 许多模型生成的是 $t=0$ 的静态快照，而实际的驾驶策略通常依赖历史运动信息，导致仿真开始时出现不稳定的瞬态（如加速度跳变）。
*   **采样延迟高：** 基于扩散模型的生成器通常需要多次迭代（Multi-step），难以满足流式外扩（Streaming Outpainting）的实时性要求。
*   **长时界运动学漂移：** 在长距离（公里级）仿真中，微小的运动学违规会不断累积，导致车辆行为最终不可行或发生碰撞。

**VectorWorld** 的核心意义在于通过矢量图（Vector Graph）表示，构建了一个能够实时、增量生成环境，且物理一致的流式世界模型，支持公里级的稳定闭环仿真。

### 2. 方法论
论文提出了一个由三个核心组件构成的系统框架：

*   **运动感知门控 VAE（Motion-aware Gated VAE）：**
    *   **核心思想：** 为每个智能体学习一个紧凑的“交互状态”接口，包含静态状态和运动历史编码。
    *   **技术细节：** 引入门控机制（Gating Mechanism），根据智能体是否运动动态调整特征融合。对于静止车辆抑制历史噪声，对于运动车辆保留动力学特征，从而为下游策略提供“热启动”输入。
*   **边缘门控关系 DiT（Edge-Gated Relational DiT）：**
    *   **核心思想：** 在 Transformer 主干网络中显式建模车道拓扑和智能体间的关系。
    *   **技术细节：** 采用因子化注意力机制，并在注意力分数中加入基于边缘特征的加性偏置（Bias）和乘性门控（Value Gating），以确保生成的车道连接性和智能体-车道对齐的结构一致性。
*   **区间条件 MeanFlow 与 JVP 监督：**
    *   **核心思想：** 实现“无求解器”的一步（One-step）生成。
    *   **算法流程：** 训练模型预测给定时间区间内的平均速度。为了提高单步采样的精度，引入了基于雅可比-向量积（JVP）的一阶修正项，对大步长传输进行校准，使其在单次推理中即可完成高质量的掩码补全。
*   **$\Delta$Sim 物理对齐 NPC 策略：**
    *   **核心思想：** 结合离散动作空间与连续残差修正，并通过可微运动学约束（DKAL 损失）引导策略生成符合物理规律的动作。

### 3. 实验设计
*   **数据集：** 使用了自动驾驶领域主流的 **Waymo Open Motion** 和 **nuPlan** 数据集。
*   **Benchmark 与对比方法：**
    *   **离线生成对比：** 与 SLEDGE (L/XL) 和 ScenDream (B/L) 等先进的矢量化场景生成模型进行对比。
    *   **闭环评估：** 使用规则驱动的 IDM 策略和基于强化学习的 PPO 策略作为探针，评估仿真环境的稳定性。
*   **评估指标：** 涵盖了感知指标（FD）、地图结构指标（路由长度、端点距离 EPD）、安全性指标（碰撞率）以及闭环指标（成功率、路程进度、抖动值 Jerk）。

### 4. 资源与算力
论文明确列出了训练配置：
*   **硬件：** 使用了 **NVIDIA H20 GPU** 集群。
*   **训练时长与规模：**
    *   **VAE 编码器：** 8 张 GPU，训练 60,000 步。
    *   **MeanFlow 生成器：** 8 张 GPU，训练 100,000 步（约 480 GPU 小时）。
    *   **$\Delta$Sim 策略：** 4 张 GPU，训练 10,000 步。
*   **推理效率：** 在单步模式下，生成一个 $64\mathrm{m} \times 64\mathrm{m}$ 的瓦片仅需约 **6ms**。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了大规模的定量评估（数千个场景）和详尽的消融实验。
*   **消融维度：** 针对 VAE 的门控机制、DiT 的关系模块、MeanFlow 的训练目标（JVP 的作用）以及 $\Delta$Sim 的各个组件均做了对比实验。
*   **客观性：** 引入了“跨后端验证”（Cross-backend validation），即在 VectorWorld 中训练的策略在其他仿真器（如日志回放）中也能表现提升，证明了模型并非仅仅拟合了自身的伪影，具有较强的客观性和泛化价值。

### 6. 主要结论与发现
*   **实时性突破：** VectorWorld 是首个能在 10ms 内完成高质量矢量场景生成的模型，支持在线流式仿真。
*   **长距离稳定：** 成功支持了超过 **1 公里** 的稳定闭环展开，而之前的模型往往在几百米内就因漂移导致崩溃。
*   **训练价值：** 在 VectorWorld 仿真环境中重新训练 PPO 策略，可将压力测试下的成功率从 25.0% 提升至 56.0%，证明了其作为训练后端的潜力。

### 7. 优点
*   **高效性：** 巧妙利用 MeanFlow 和 JVP 实现了单步生成，极大地降低了计算开销。
*   **结构保真度：** 边缘门控关系 DiT 有效解决了矢量生成中常见的车道断裂和智能体浮空问题。
*   **系统性对齐：** 从初始化接口到 NPC 行为逻辑，全方位考虑了闭环仿真的需求，而非单纯追求离线生成的视觉效果。

### 8. 不足与局限
*   **地图元素覆盖：** 目前主要关注中心线表示，尚未涵盖复杂的路缘、路标等精细几何元素。
*   **微观分布差异：** 虽然整体结构优异，但在某些微观分布指标（如 nuPlan 上的智能体间距 JSD）上，相比多步迭代的扩散模型仍有细微差距。
*   **极端工况：** $\Delta$Sim 虽然符合物理规律，但在处理极端的、非稳态的避障动作时，其保真度仍有待进一步验证。

（完）
