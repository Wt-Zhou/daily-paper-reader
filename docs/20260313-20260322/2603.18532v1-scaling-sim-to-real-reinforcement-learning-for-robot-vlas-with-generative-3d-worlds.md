---
title: Scaling Sim-to-Real Reinforcement Learning for Robot VLAs with Generative 3D Worlds
title_zh: 利用生成式 3D 世界扩展机器人 VLA 的仿真到现实强化学习
authors: "Andrew Choi, Xinjie Wang, Zhizhong Su, Wei Xu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18532v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 利用生成式3D世界对机器人VLA进行强化学习
tldr: 本研究针对机器人视觉-语言-动作（VLA）模型在真实世界强化学习中泛化性受限的问题，提出利用生成式3D世界模型进行大规模仿真训练。通过结合语言驱动的场景设计器，自动生成数百个包含多样化物体和背景的交互式场景，实现了高效并行的策略学习。该方法不仅显著提升了仿真和真实环境下的任务成功率与执行速度，还通过增加场景多样性增强了模型的零样本泛化能力，为解决Sim-to-Real鸿沟提供了新思路。
selection_source: fresh_fetch
motivation: 真实世界强化学习难以扩展场景多样性，导致预训练模型容易过拟合到特定场景，而手动设计仿真场景成本过高。
method: 利用3D生成模型和语言驱动的场景设计器自动构建大规模多样化仿真环境，并在此基础上对预训练VLA模型进行强化学习微调。
result: "仿真成功率从9.7%提升至79.8%，真实世界成功率从21.7%提升至75%，且任务完成速度显著加快。"
conclusion: 借助生成式3D世界模型提供的大规模多样化数据，可以有效提升VLA模型的性能和零样本泛化能力，实现高效的虚实迁移。
---

## 摘要
通过强化学习 (RL) 训练的大型视觉语言模型 (VLM) 的强劲表现，激发了在机器人领域微调视觉-语言-动作 (VLA) 模型的类似方法。许多近期工作直接在现实世界中微调 VLA，以避免处理仿真到现实 (sim-to-real) 的差距。虽然现实世界中的 RL 规避了仿真到现实的问题，但它本质上限制了所得 VLA 的泛化性，因为在物理世界中扩展场景和物体的多样性极其困难。这导致了一个矛盾的结果：将一个广泛预训练的模型转变为一个过拟合的、特定场景的策略。相比之下，在仿真中训练可以提供多样化的场景，但设计这些场景的成本也很高。在这项工作中，我们展示了通过利用 3D 世界生成模型，可以在不牺牲泛化性且减少人工劳动的情况下对 VLA 进行 RL 微调。利用这些模型结合语言驱动的场景设计器，我们生成了数百个包含独特物体和背景的多样化交互式场景，从而实现了可扩展且高度并行的策略学习。从预训练的模仿基线出发，我们的方法将仿真成功率从 9.7% 提高到 79.8%，同时在任务完成时间上实现了 1.25 倍的加速。我们进一步证明，得益于生成的数字孪生质量以及领域随机化，成功的仿真到现实迁移得以实现，将现实世界的成功率从 21.7% 提高到 75%，并实现了 1.13 倍的加速。最后，我们通过消融实验进一步强调了利用 3D 世界生成模型提供的近乎无限数据的优势，结果表明增加场景多样性能直接提高零样本泛化能力。

## Abstract
The strong performance of large vision-language models (VLMs) trained with reinforcement learning (RL) has motivated similar approaches for fine-tuning vision-language-action (VLA) models in robotics. Many recent works fine-tune VLAs directly in the real world to avoid addressing the sim-to-real gap. While real-world RL circumvents sim-to-real issues, it inherently limits the generality of the resulting VLA, as scaling scene and object diversity in the physical world is prohibitively difficult. This leads to the paradoxical outcome of transforming a broadly pretrained model into an overfitted, scene-specific policy. Training in simulation can instead provide access to diverse scenes, but designing those scenes is also costly. In this work, we show that VLAs can be RL fine-tuned without sacrificing generality and with reduced labor by leveraging 3D world generative models. Using these models together with a language-driven scene designer, we generate hundreds of diverse interactive scenes containing unique objects and backgrounds, enabling scalable and highly parallel policy learning. Starting from a pretrained imitation baseline, our approach increases simulation success from 9.7% to 79.8% while achieving a 1.25$\times$ speedup in task completion time. We further demonstrate successful sim-to-real transfer enabled by the quality of the generated digital twins together with domain randomization, improving real-world success from 21.7% to 75% and achieving a 1.13$\times$ speedup. Finally, we further highlight the benefits of leveraging the effectively unlimited data from 3D world generative models through an ablation study showing that increasing scene diversity directly improves zero-shot generalization.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scaling Sim-to-Real Reinforcement Learning for Robot VLAs with Generative 3D Worlds》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：虽然视觉-语言-动作（VLA）模型在预训练阶段表现出色，但在特定任务微调时面临困境。在现实世界中进行强化学习（RL）虽然能规避“仿真到现实”（Sim-to-Real）的鸿沟，但由于物理世界场景和物体多样性难以扩展，往往导致模型过拟合于特定场景，丧失了预训练带来的泛化性。
*   **核心问题**：如何在不耗费大量人力手动设计仿真环境的前提下，利用仿真环境的大规模并行能力和多样性来提升 VLA 模型的性能和泛化能力？

### 2. 方法论
*   **核心思想**：利用生成式 3D 世界模型自动构建大规模、交互式的训练环境，并结合强化学习对预训练的 VLA 模型进行微调。
*   **关键技术流程**：
    1.  **语言驱动的场景设计器**：利用 GPT-4o 将任务描述（如“把笔放进笔筒”）解析为结构化的场景图（包含背景、目标物体、干扰项及其空间关系）。
    2.  **3D 世界生成后端**：基于 EmbodiedGen 引擎，根据场景图自动生成 3D 资产并布置布局，创建一个物理可交互的数字孪生环境。
    3.  **PPOFlow 算法**：针对流匹配（Flow Matching）架构的 VLA 模型（如 $\pi_0$），提出 PPOFlow。它通过在数值积分过程中注入可学习的噪声，将确定性的流匹配策略转化为高斯策略，从而能够使用 PPO 算法进行强化学习更新。
    4.  **Sim-to-Real 迁移**：通过高质量的数字孪生资产、领域随机化（Domain Randomization）以及带重力补偿的 PD 控制，减小视觉和动力学差距。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：自动生成了 100 个独特的交互式场景（W 集合）。
    *   **Benchmark**：使用 SimplerEnv 中的 Bridge 桌面场景作为分布外（OOD）测试基准。
    *   **真实世界**：设置了 12 个真实场景，涵盖了 OOD 物体（如螺丝刀）和复杂任务（如叠杯子），共进行 240 次实验。
*   **对比方法**：
    *   **$\pi_{pre}$**：仅在 BridgeV2 数据集上进行模仿学习预训练的基线模型。
    *   **手动设计场景 RL**：在少量手动设计的 SimplerEnv 场景上进行 RL 微调的模型。
    *   **不同规模的 $N$**：对比在 1、3、10、25、50、100 个生成场景下训练的效果。

### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA RTX 6000 Ada GPU**。
*   **训练时长**：LoRA 微调 VLM 主干并全量微调动作专家头，耗时约 **5 天**。
*   **生成算力**：场景生成在单张 RTX 4090 上运行，每个场景平均耗时约 46.8 分钟（若使用预构建资产库可缩短至 2 分钟）。
*   **推理部署**：实时推理在单张 RTX 4090 上完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了针对场景数量 $N$ 的详尽消融实验，验证了泛化性随场景多样性增加而提升的趋势。
    *   针对流匹配步数 $K$ 进行了消融，探讨了多模态对 RL 性能的影响。
    *   真实世界测试包含 12 个场景、240 次独立尝试，样本量在同类机器人论文中属于较为充分。
*   **客观性**：通过引入第三方基准（SimplerEnv）和严格的 OOD 测试集，实验设计较为客观，能够反映模型在未见场景下的真实能力。

### 6. 主要结论与发现
*   **性能飞跃**：RL 微调将仿真成功率从 9.7% 提升至 79.8%，真实世界成功率从 21.7% 提升至 75%。
*   **多样性即泛化性**：实验证明，零样本泛化能力与训练场景的数量呈正相关。在 50 个场景下训练的模型比在 1 个场景下训练的模型 OOD 成功率高出 24.8%。
*   **多模态并非必要**：在 RL 微调阶段，将多步流匹配简化为单步高斯策略（$K=1$）不仅没有性能损失，反而使推理速度提升了 2.36 倍。
*   **Sim-to-Real 可行性**：生成式 3D 模型产生的“数字孪生”质量足以支持有效的虚实迁移。

### 7. 优点
*   **高度自动化**：通过 GPT-4o 和生成引擎，极大地降低了构建多样化机器人仿真环境的人工成本。
*   **算法创新**：PPOFlow 成功解决了流匹配模型难以直接应用 PPO 的问题，并实现了显著的推理加速。
*   **闭环验证**：从自动生成环境到强化学习训练，再到真实机器人部署，形成了一套完整的、可扩展的管线。

### 8. 不足与局限
*   **任务类型单一**：目前主要集中在“取放”（Pick-and-Place）任务，尚未涵盖更复杂的长程任务或涉及关节类物体（如开抽屉）的操作。
*   **生成质量限制**：虽然自动化程度高，但仍有约 15% 的生成环境存在物体尺寸不匹配或初始位置不稳定等问题，需要人工干预或进一步优化 QA 流程。
*   **物理模拟简化**：主要依赖刚体物理，对于柔性物体（如布料）或复杂流体的模拟支持不足。

（完）
