---
title: "CarPLAN: Context-Adaptive and Robust Planning with Dynamic Scene Awareness for Autonomous Driving"
title_zh: CarPLAN：具有动态场景感知能力的自动驾驶上下文自适应且鲁棒的规划框架
authors: "Junyong Yun, Jungho Kim, ByungHyun Lee, Dongyoung Lee, Sehwan Choi, Seunghyeop Nam, Kichun Jo, Jun Won Choi"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12607v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 自动驾驶的模仿学习与运动规划
tldr: CarPLAN是一个基于模仿学习的自动驾驶运动规划框架，旨在增强对复杂驾驶环境的理解和自适应决策能力。它引入了位移感知预测编码（DPE）以提升空间意识，并采用基于混合专家模型（MoE）的上下文自适应多专家解码器（CMD），根据场景结构动态选择解码器。在nuPlan和Waymax基准测试中，CarPLAN展现了卓越的闭环仿真性能和跨场景泛化能力。
selection_source: fresh_fetch
motivation: 传统的模仿学习规划器往往过度依赖专家轨迹，难以在复杂多变的现实驾驶场景中实现自适应决策和精准的空间感知。
method: 提出了位移感知预测编码（DPE）来预测与周围元素的相对位移，并结合混合专家框架（CMD）在Transformer层中动态选择最适合当前场景的解码器。
result: 在nuPlan基准测试中取得了最先进的闭环仿真结果，特别是在极具挑战性的Test14-Hard场景中表现稳健，并在Waymax上验证了良好的泛化性。
conclusion: CarPLAN通过显式的空间位移预测和动态专家选择机制，显著提升了自动驾驶系统在复杂动态环境下的规划鲁棒性和上下文适应性。
---

## 摘要
模仿学习 (IL) 因其数据效率和获取真实驾驶数据的便捷性，被广泛应用于自动驾驶的运动规划。为了实现安全且鲁棒的真实世界驾驶，基于 IL 的规划需要捕捉真实数据中固有的复杂驾驶上下文，并实现上下文自适应的决策，而不仅仅是依赖于专家轨迹模仿。在本文中，我们提出了 CarPLAN，这是一种新型的基于 IL 的运动规划框架，它显式地增强了对驾驶上下文的理解，并实现了跨多种交通场景的自适应规划。我们的贡献有两个方面：首先，我们引入了位移感知预测编码 (DPE)，通过预测自动驾驶车辆 (AV) 与周围场景元素之间的未来位移向量来提高模型的空间感知能力，使规划器在生成轨迹时能够考虑相对间距。除了标准的模仿损失外，我们还加入了一个捕获位移预测误差的增强损失项，确保规划决策考虑到与其他智能体的相对距离。其次，为了提高模型处理多样化驾驶上下文的能力，我们提出了上下文自适应多专家解码器 (CMD)，该解码器利用了混合专家 (MoE) 框架。CMD 在每个 Transformer 层根据场景结构动态选择最合适的专家解码器，从而在动态环境中实现自适应且具有上下文感知能力的规划。我们在 nuPlan 基准测试上评估了 CarPLAN，并证明其在所有闭环仿真指标上均达到了最先进的性能。特别是，CarPLAN 在 Test14-Hard 等挑战性场景中表现出鲁棒的性能，验证了其在复杂驾驶条件下的有效性。在 Waymax 基准测试上的额外实验进一步证明了其在不同基准设置下的泛化能力。

## Abstract
Imitation learning (IL) is widely used for motion planning in autonomous driving due to its data efficiency and access to real-world driving data. For safe and robust real-world driving, IL-based planning requires capturing the complex driving contexts inherent in real-world data and enabling context-adaptive decision-making, rather than relying solely on expert trajectory imitation. In this paper, we propose CarPLAN, a novel IL-based motion planning framework that explicitly enhances driving context understanding and enables adaptive planning across diverse traffic scenarios. Our contributions are twofold: We introduce Displacement-Aware Predictive Encoding (DPE) to improve the model's spatial awareness by predicting future displacement vectors between the Autonomous Vehicle (AV) and surrounding scene elements. This allows the planner to account for relational spacing when generating trajectories. In addition to the standard imitation loss, we incorporate an augmented loss term that captures displacement prediction errors, ensuring planning decisions consider relative distances from other agents. To improve the model's ability to handle diverse driving contexts, we propose Context-Adaptive Multi-Expert Decoder (CMD), which leverages the Mixture of Experts (MoE) framework. CMD dynamically selects the most suitable expert decoders based on scene structure at each Transformer layer, enabling adaptive and context-aware planning in dynamic environments. We evaluate CarPLAN on the nuPlan benchmark and demonstrate state-of-the-art performance across all closed-loop simulation metrics. In particular, CarPLAN exhibits robust performance on challenging scenarios such as Test14-Hard, validating its effectiveness in complex driving conditions. Additional experiments on the Waymax benchmark further demonstrate its generalization capability across different benchmark settings.

---

## 论文详细总结（自动生成）

以下是对论文《CarPLAN: Context-Adaptive and Robust Planning with Dynamic Scene Awareness for Autonomous Driving》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的基于模仿学习（IL）的自动驾驶规划器过度依赖于最小化预测轨迹与专家轨迹之间的距离（如 L1 损失），而忽视了对复杂驾驶上下文（Context）的深刻理解。这导致模型在面对碰撞风险或道路边界约束时，可能为了匹配专家轨迹而做出不安全的决策。
*   **研究动机**：
    *   **空间意识不足**：现有模型往往独立预测智能体行为，缺乏对自动驾驶车辆（AV）与周围环境（车辆、行人、车道线）之间演化空间关系的显式建模。
    *   **缺乏自适应性**：单一的策略网络难以应对多样化的驾驶场景（如拥堵、路口、行人横穿），容易向常见场景产生偏见，在罕见或复杂场景下表现不佳。

### 2. 方法论与关键技术
CarPLAN 提出了两个核心模块来增强上下文感知和自适应能力：

*   **位移感知预测编码 (DPE)**：
    *   **核心思想**：在训练阶段引入一个自监督任务，预测 AV 与场景中所有元素（智能体和地图点）在未来时间步的相对位移向量。
    *   **技术细节**：场景编码器生成统一特征后，位移预测器（DispHead）计算 $\hat{D}_{1:T_f}$。这迫使编码器在特征表示中显式捕获空间间距信息。
    *   **优势**：该模块仅在训练时使用，推理时可关闭，不增加实时计算开销。
*   **上下文自适应多专家解码器 (CMD)**：
    *   **核心思想**：引入混合专家（MoE）架构，根据当前场景动态选择最合适的解码器分支。
    *   **关键组件**：
        *   **场景感知路由（Scene-Aware Router）**：利用交叉注意力机制结合轨迹查询（Query）和场景特征，计算各专家的贡献得分。
        *   **共享专家与路由专家**：设置 $N_s$ 个共享专家（捕获通用驾驶模式）和 $N$ 个路由专家（处理特定场景）。最终输出是 Top-K 路由专家与共享专家的加权融合。
*   **损失函数**：总损失 $L_{total} = L_{plan}（模仿损失） + L_{disp}（位移预测损失） + L_{bal}（专家负载均衡损失）$。

### 3. 实验设计
*   **数据集与基准**：
    *   **nuPlan**：使用了 1300 小时的真实驾驶数据，涵盖 Val14、Test14-Hard（极具挑战性场景）和 Test14-Random。
    *   **Waymax**：基于 Waymo 开放数据集的闭环仿真，用于验证跨基准的泛化能力。
*   **评估指标**：开环得分（OLS）、非反应性闭环得分（CLS-NR）、反应性闭环得分（CLS-R）。
*   **对比方法**：包括 PDM-Open/Hybrid、GameFormer、PlanTF、PLUTO、RaSc、BeTopNet、Diffusion-Planner 等最新的 SOTA 模型。

### 4. 资源与算力
*   **论文说明**：文中提到模型包含 4 层场景编码器和 4 层 CMD 解码器，路由专家数量设为 16，选择 Top-2。
*   **算力细节**：**文中未明确给出具体的 GPU 型号、数量及训练总时长**。但在推理效率分析中提到，CarPLAN 在引入 MoE 后仍能保持约 15 FPS 的实时推理速度，FLOPs 为 1.08G。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 nuPlan 的三个子集上进行了全面的闭环仿真对比。
    *   进行了详细的**消融实验**，验证了 DPE、CMD、路由机制、专家数量、共享专家设置等对性能的影响。
    *   在 Waymax 平台上进行了额外的闭环实验，证明了方法的通用性。
*   **充分性评价**：实验设计非常充分且客观。通过 Test14-Hard 场景的测试，有力地证明了模型在处理长尾、复杂场景时的鲁棒性。对比实验涵盖了从规则驱动到扩散模型等多种技术路线，具有很强的说服力。

### 6. 主要结论与发现
*   **性能领先**：CarPLAN 在 nuPlan 闭环仿真中刷新了纪录，Val14 的 CLS-NR 达到 91.4，CLS-R 达到 84.6。
*   **空间意识的作用**：DPE 显著提升了模型在避障（Colli）和道路合规性（DAC）方面的表现。
*   **自适应性的价值**：CMD 能够根据场景（如直行 vs 拥堵）动态切换专家组合，可视化结果证实了路由器的决策与场景特征高度相关。
*   **泛化性**：在 Waymax 上的成功应用证明了该架构不依赖于特定数据集的特性。

### 7. 亮点
*   **MoE 的创新应用**：将大语言模型中成熟的 MoE 概念引入自动驾驶规划，解决了单一策略网络难以兼顾多样化场景的问题。
*   **训练辅助任务设计**：通过预测相对位移（而非绝对位置）来增强空间感知，既符合人类驾驶直觉，又避免了推理负担。
*   **鲁棒性强**：在 Test14-Hard 这种高难度基准上表现优异，证明了其在极端情况下的安全性。

### 8. 不足与局限
*   **感知依赖**：模型假设拥有“完美感知”（Perfect Perception），即输入是准确的物体状态和高精地图。在传感器噪声较大的真实环境中，性能可能会受限。
*   **确定性假设**：目前的位移预测是确定性的。作者在结论中提到，未来应将其扩展为基于动作条件的预测模型，以应对未来空间关系演化的不确定性。
*   **系统优化**：目前的 MoE 实现尚未进行系统级的推理优化（如专家剪枝或并行流执行），仍有进一步提升实时性的空间。

（完）
