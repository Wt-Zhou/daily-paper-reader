---
title: "DreamPlan: Efficient Reinforcement Fine-Tuning of Vision-Language Planners via Video World Models"
title_zh: DreamPlan：通过视频世界模型对视觉语言规划器进行高效强化微调
authors: "Emily Yue-Ting Jia, Weiduo Yuan, Tianheng Shi, Vitor Guizilini, Jiageng Mao, Yue Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16860v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 通过视频世界模型对VLM规划器进行强化学习微调
tldr: DreamPlan 针对视觉语言模型（VLM）在机器人操纵任务中缺乏物理感知的问题，提出了一种利用视频世界模型进行强化微调的框架。该方法首先利用 VLM 收集探索性数据训练视频生成模型以模拟物理规律，随后在虚拟“想象”中通过 ORPO 算法对 VLM 规划器进行微调。这种方法有效结合了语义推理与物理接地，在无需大规模真实交互的情况下显著提升了复杂任务的成功率。
selection_source: fresh_fetch
motivation: 现有的 VLM 规划器缺乏对物理环境的深刻理解，且直接在现实世界中进行强化学习微调成本高昂且不安全。
method: 利用探索性数据训练动作条件视频生成模型作为世界模型，并在该虚拟环境中通过优势比策略优化（ORPO）对 VLM 进行微调。
result: 实验证明 DreamPlan 能够有效将物理和任务特定知识注入 VLM，显著提高了机器人在复杂操纵任务中的成功率。
conclusion: 通过视频世界模型进行虚拟微调是提升 VLM 规划器物理接地能力和任务执行效率的一种高效且安全的可行路径。
---

## 摘要
机器人操纵需要复杂的常识推理，这是大规模视觉语言模型（VLM）天生具备的能力。虽然 VLM 作为零样本规划器展现出潜力，但由于缺乏具身的物理理解，在部署到复杂的现实环境时（特别是针对可变形物体操纵等挑战性任务），往往会导致复合误差和较低的成功率。尽管强化学习（RL）可以使这些规划器适应特定的任务动力学，但通过现实世界的交互直接微调 VLM 的成本极高、不安全且样本效率低下。为了克服这一瓶颈，我们提出了 DreamPlan，这是一个通过视频世界模型对 VLM 规划器进行强化微调的新型框架。DreamPlan 不依赖昂贵的物理展开，而是首先利用零样本 VLM 收集探索性交互数据。我们证明，这些次优数据足以训练一个动作条件视频生成模型，该模型隐式地捕捉了复杂的现实世界物理规律。随后，利用优势比策略优化（ORPO），VLM 规划器完全在该视频世界模型的“想象”中进行微调。通过利用这些虚拟展开，物理和任务特定的知识被高效地注入到 VLM 中。我们的结果表明，DreamPlan 弥合了语义推理与物理具身之间的差距，在无需大规模现实世界数据收集的情况下，显著提高了操纵成功率。项目主页见 https://psi-lab.ai/DreamPlan/。

## Abstract
Robotic manipulation requires sophisticated commonsense reasoning, a capability naturally possessed by large-scale Vision-Language Models (VLMs). While VLMs show promise as zero-shot planners, their lack of grounded physical understanding often leads to compounding errors and low success rates when deployed in complex real-world environments, particularly for challenging tasks like deformable object manipulation. Although Reinforcement Learning (RL) can adapt these planners to specific task dynamics, directly fine-tuning VLMs via real-world interaction is prohibitively expensive, unsafe, and sample-inefficient. To overcome this bottleneck, we introduce DreamPlan, a novel framework for the reinforcement fine-tuning of VLM planners via video world models. Instead of relying on costly physical rollouts, DreamPlan first leverages the zero-shot VLM to collect exploratory interaction data. We demonstrate that this sub-optimal data is sufficient to train an action-conditioned video generation model, which implicitly captures complex real-world physics. Subsequently, the VLM planner is fine-tuned entirely within the "imagination" of this video world model using Odds Ratio Policy Optimization (ORPO). By utilizing these virtual rollouts, physical and task-specific knowledge is efficiently injected into the VLM. Our results indicate that DreamPlan bridges the gap between semantic reasoning and physical grounding, significantly improving manipulation success rates without the need for large-scale real-world data collection. Our project page is https://psi-lab.ai/DreamPlan/.

---

## 论文详细总结（自动生成）

### DreamPlan：通过视频世界模型高效强化微调视觉语言规划器

#### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）虽具备强大的语义推理能力，但在机器人操纵（尤其是可变形物体如布料、绳索）中缺乏**物理接地（Physical Grounding）**。
*   **研究动机**：
    *   零样本（Zero-shot）VLM 规划器生成的动作往往语义合理但物理无效。
    *   直接在现实世界进行强化学习（RL）微调成本极高、不安全且样本效率低。
    *   现有的物理模拟器难以准确建模复杂的非线性可变形动力学。

#### 2. 方法论
DreamPlan 提出了一个三阶段的框架，将物理知识注入 VLM 规划器：
*   **零样本探索与数据收集**：利用预训练 VLM 在目标环境中进行自主探索。即使成功率较低，这些“次优”数据也包含了丰富的动作-结果因果信号。
*   **动作条件视频世界模型（VWM）学习**：
    *   基于 **CogVideoX-5B** 扩散模型，利用 **ControlNet** 架构注入机器人手臂的运动轨迹（渲染为视频）。
    *   模型学习预测物体在特定动作下的变形。为了减少干扰，采用“仅预测物体（Object-Only）”的策略，去除背景和光影复杂性。
*   **基于世界模型的强化微调（RL Fine-tuning）**：
    *   **Best-of-K 采样策略**：为减少扩散模型推理的计算开销，先采样 $K$ 个候选动作，由 VWM 预测结果，再由 GPT-4o 作为评价器选出最优动作。
    *   **优势比策略优化（ORPO）**：利用上述生成的偏好对（正样本 vs 负样本），直接对 VLM（如 Qwen3-VL-8B）进行对比式微调，无需额外的价值函数。

#### 3. 实验设计
*   **实验场景**：三个挑战性的可变形物体操纵任务：绳索拉直（Rope Straightening）、布料折叠（Cloth Folding）、软玩具手臂复位（Toy Arm Repositioning）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Qwen3-VL (4B, 8B, 32B) 和 GPT-4o。
    *   **对比实验**：对比了零样本表现与经过 DreamPlan 微调后的表现，以及与“显式验证（Explicit Verification）”采样方法的效率对比。
*   **评估指标**：平均得分（0-1 分量表，0 为失败，0.5 为有进展，1 为完全成功）。

#### 4. 资源与算力
*   **硬件设备**：双 Franka FR3 机器人手臂，RealSense D435i 摄像头。
*   **计算资源**：本地工作站配备单块 **NVIDIA RTX 5090 (32GB)** GPU。
*   **数据规模**：通过 4 小时的自主交互收集了 **2056 条** 轨迹用于训练世界模型。
*   **推理效率**：微调后的模型单次决策仅需约 **1.12 秒**，而显式验证方法（N=8）需约 2605 秒。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务进行了 10 次随机初始状态的真实机器人测试。
*   **消融实验**：对比了“全场景预测”与“仅物体预测”的性能（PSNR 指标），验证了简化视觉目标的有效性。
*   **充分性评价**：实验覆盖了多种不同动力学特性的物体，对比了不同参数规模的 SOTA 模型。虽然真实机器人测试次数（每项 10 次）受限于硬件成本，但在机器人领域属于标准范畴，且结果显示出显著的统计提升（15%-40% 的成功率增益）。

#### 6. 主要结论与发现
*   **次优数据价值**：即使是零样本 VLM 生成的失败尝试，也足以训练出能够捕捉复杂物理规律的视频世界模型。
*   **物理知识注入**：通过在“想象”中进行微调，VLM 能够内化物理动力学，显著优于参数量更大的零样本模型（如微调后的 8B 模型超过了零样本 32B 模型）。
*   **效率优势**：ORPO 结合 Best-of-K 策略成功解耦了昂贵的视频生成过程与策略优化过程，实现了高效的离线微调。

#### 7. 优点
*   **高效性**：避免了大规模真实世界数据收集，且推理阶段无需调用世界模型。
*   **物理接地**：成功解决了 VLM 在处理非刚性物体时“眼高手低”的问题。
*   **自动化**：数据收集过程由 VLM 自主驱动，降低了人工标注和示教的成本。

#### 8. 不足与局限
*   **任务局限性**：目前主要针对单步或短时域的操纵任务，对于极长序列的复杂任务，世界模型的累积误差可能会影响微调效果。
*   **泛化风险**：世界模型是在特定任务数据上训练的，对于完全未见过的物体类别，可能需要重新收集少量数据进行适配。
*   **评价器依赖**：微调过程中的正负样本筛选仍依赖于 GPT-4o 等外部强模型作为评价器。

（完）
