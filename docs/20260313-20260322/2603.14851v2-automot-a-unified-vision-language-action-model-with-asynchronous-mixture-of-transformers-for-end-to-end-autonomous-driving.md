---
title: "AutoMoT: A Unified Vision-Language-Action Model with Asynchronous Mixture-of-Transformers for End-to-End Autonomous Driving"
title_zh: AutoMoT：一种基于异步 Mixture-of-Transformers 的端到端自动驾驶统一视觉-语言-动作模型
authors: "Wenhui Huang, Songyan Zhang, Qihang Huang, Zhidong Wang, Zhiqi Mao, Collister Chua, Zhan Chen, Long Chen, Chen Lv"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14851v2"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于端到端自动驾驶的统一视觉-语言-动作模型
tldr: AutoMoT是一个统一的视觉-语言-动作（VLA）模型，旨在解决端到端自动驾驶中推理与动作空间不匹配及推理延迟问题。它采用异步Transformer混合架构（MoT），通过联合注意力共享保留预训练VLM的推理能力，并支持不同频率的任务执行。实验证明其在多项基准测试中表现优异，并揭示了预训练VLM在自动驾驶中语义理解与动作规划的界限。
selection_source: fresh_fetch
motivation: 现有端到端自动驾驶系统在整合VLM时面临推理与动作分布不一致、预训练能力利用不足及推理延迟高等挑战。
method: 提出AutoMoT框架，利用具有联合注意力共享的混合Transformer架构，通过异步执行实现高效的快慢推理。
result: 在多个开环和闭环基准测试中达到了与当前最先进方法相当的竞争性性能。
conclusion: 预训练VLM通过语义提示即可实现出色的场景理解，但在决策和轨迹规划等动作级任务上仍需针对性微调。
---

## 摘要
将视觉语言模型（VLM）集成到端到端（E2E）自动驾驶（AD）系统中，在提升场景理解方面展现出巨大潜力。然而，现有的集成策略存在若干局限性：它们要么难以解决推理空间与动作空间之间的分布失配问题，要么未能充分利用预训练 VLM 的通用推理能力，或者在动作策略生成过程中产生显著的推理延迟，从而降低了驾驶性能。为了应对这些挑战，本文提出了 AutoMoT，这是一个将推理和动作生成统一在单个视觉-语言-动作（VLA）模型中的端到端自动驾驶框架。我们的方法利用了具有联合注意力共享的 Mixture-of-Transformer（MoT）架构，在保留预训练 VLM 通用推理能力的同时，通过在不同任务频率下的异步执行实现高效的快慢推理。在多个基准测试的开环和闭环设置下进行的广泛实验表明，AutoMoT 达到了与现有最先进方法相当的性能。我们进一步研究了预训练 VLM 在自动驾驶中的功能边界，探讨了何时需要针对自动驾驶进行定制化微调。结果表明，预训练 VLM 仅通过语义提示即可实现具有竞争力的多任务场景理解性能，而微调对于决策和轨迹规划等动作级任务仍然至关重要。演示视频和定性结果请参阅项目主页。

## Abstract
Integrating vision-language models (VLMs) into end-to-end (E2E) autonomous driving (AD) systems has shown promise in improving scene understanding. However, existing integration strategies suffer from several limitations: they either struggle to resolve distribution misalignment between reasoning and action spaces, underexploit the general reasoning capabilities of pretrained VLMs, or incur substantial inference latency during action policy generation, which degrades driving performance. To address these challenges, we propose \OURS in this work, an end-to-end AD framework that unifies reasoning and action generation within a single vision-language-action (VLA) model. Our approach leverages a mixture-of-transformer (MoT) architecture with joint attention sharing, which preserves the general reasoning capabilities of pre-trained VLMs while enabling efficient fast-slow inference through asynchronous execution at different task frequencies. Extensive experiments on multiple benchmarks, under both open- and closed-loop settings, demonstrate that \OURS achieves competitive performance compared to state-of-the-art methods. We further investigate the functional boundary of pre-trained VLMs in AD, examining when AD-tailored fine-tuning is necessary. Our results show that pre-trained VLMs can achieve competitive multi-task scene understanding performance through semantic prompting alone, while fine-tuning remains essential for action-level tasks such as decision-making and trajectory planning. We refer to \href{https://automot-website.github.io/}{Project Page} for the demonstration videos and qualitative results.

---

## 论文详细总结（自动生成）

以下是对论文《AutoMoT: A Unified Vision-Language-Action Model with Asynchronous Mixture-of-Transformers for End-to-End Autonomous Driving》的结构化深度总结：

### 1. 核心问题与研究背景
*   **研究动机**：虽然将视觉语言模型（VLM）集成到端到端（E2E）自动驾驶中能提升场景理解，但现有方法存在三大痛点：
    1.  **分布失配**：VLM的推理空间与驾驶员的动作空间之间存在分布不一致。
    2.  **能力退化**：对VLM进行特定任务微调往往会破坏其预训练的通用推理能力（灾难性遗忘）。
    3.  **推理延迟**：VLM庞大的参数量导致动作生成延迟高，难以满足实时驾驶需求。
*   **核心目标**：构建一个统一的视觉-语言-动作（VLA）模型，既能保留VLM的通用智能，又能实现低延迟、高精度的动作规划。

### 2. 方法论
*   **核心思想**：提出 **AutoMoT** 框架，采用 **异步混合 Transformer（MoT）** 架构，将场景理解与动作生成解耦但又通过注意力机制统一。
*   **关键技术细节**：
    *   **双专家架构**：
        *   **理解专家（UE）**：采用预训练的 **Qwen3-VL-4B**（冻结状态），负责高层语义推理和思维链（CoT）生成。
        *   **动作专家（AE）**：一个 **1.6B** 参数的轻量化 Transformer（从头训练），负责决策（Meta-actions）和轨迹规划。
    *   **层向联合注意力共享（Layer-wise Joint Attention Sharing）**：AE 在计算时会通过交叉注意力机制共享 UE 的键值对（KV Cache），使动作生成能够感知高层语义特征。
    *   **异步快慢推理**：UE 以低频率运行（慢系统，更新语义），AE 以高频率运行（快系统，实时控制）。AE 复用 UE 最近一次生成的 KV Cache，从而大幅降低推理延迟。
    *   **动作细化器（Action Refiner）**：集成了一个基于扩散模型（Diffusion-based）的规划头，利用混合注意力（MoA）机制对 AE 生成的粗略轨迹进行精细化处理。

### 3. 实验设计
*   **数据集与场景**：
    *   **闭环评估**：CARLA 仿真环境下的 **Bench2Drive** 排行榜。
    *   **开环评估**：**nuScenes** 真实世界数据集。
    *   **决策基准**：自建了 **NuSync**（基于 nuScenes）和 **PDM-Meta**（基于 CARLA）数据集，包含多帧异步元动作标注。
    *   **推理基准**：LingoQA、OmniDrive、CODA-LM 等自动驾驶 VQA 数据集，以及 ScienceQA 等通用 VQA 数据集。
*   **对比方法**：包括 UniAD、VAD、DriveTransformer 等传统端到端方法，以及 Orion、AutoVLA、SimLingo 等最新的 VLM 增强型方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练策略**：采用了全分片数据并行（FSDP）策略。
*   **学习率**：在 $1 \times 10^{-4}$ 到 $2 \times 10^{-5}$ 之间调整。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的实验，包括：
    *   **主实验**：涵盖了开环规划、闭环驾驶、决策准确率、通用 VQA 四个维度。
    *   **消融实验**：验证了预训练 UE 的必要性、决策目标的影响、异步推理与同步推理的性能/延迟对比。
*   **充分性评价**：实验设计非常全面且客观。通过在通用 VQA 数据集上测试微调后的模型，揭示了“微调导致灾难性遗忘”的现象，这为“冻结 VLM 主干”的设计提供了强有力的证据。

### 6. 主要结论与发现
*   **功能边界**：预训练 VLM 仅通过**语义提示（Prompting）**即可实现顶级的场景理解，而**微调**对于动作级任务（决策和规划）是必需的，但对语义理解提升有限且会损害通用性。
*   **性能领先**：AutoMoT 在 Bench2Drive 闭环测试中取得了 SOTA 性能（驾驶得分 87.34），超过了需要大量数据增强的 SimLingo。
*   **效率提升**：异步推理机制将推理延迟从 0.38s 降低至 0.05s（**提速 7.6 倍**），而规划精度损失仅为 1.24%。

### 7. 优点
*   **架构创新**：通过异步 MoT 巧妙解决了 VLM 推理慢与自动驾驶实时性要求之间的矛盾。
*   **知识保留**：坚持冻结 VLM 主干，成功保留了模型在长尾场景下的通用推理和常识判断能力。
*   **统一性**：在一个模型内实现了感知、推理、决策和规划的端到端整合，且任务间具有明确的因果注意力约束。

### 8. 不足与局限
*   **扩散头稳定性**：实验显示，虽然扩散模型规划头（Diffusion Head）具有更高的性能上限，但其随机性导致闭环测试中的方差较大，稳定性不如传统的 MLP 规划头。
*   **数据依赖**：尽管 UE 是冻结的，但 AE 的训练仍高度依赖于高质量的动作标注数据（如自建的 NuSync）。
*   **硬件门槛**：虽然推理速度快，但 4B+1.6B 的模型规模对于嵌入式车载芯片仍有较大部署压力。

（完）
