---
title: Tactile Modality Fusion for Vision-Language-Action Models
title_zh: 视觉-语言-动作模型的触觉模态融合
authors: "Charlotte Morissette, Amin Abyaneh, Wei-Di Chang, Anas Houssaini, David Meger, Hsiu-Chin Lin, Jonathan Tremblay, Gregory Dudek"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14604v1"
tags: ["query:ad-ei"]
score: 9.0
evidence: 视觉-语言-动作模型的触觉模态融合
tldr: 针对视觉语言动作（VLA）模型在接触密集型任务中缺乏力学感知的局限，本文提出 TacFiLM。这是一种轻量级的模态融合方法，通过特征线性调制（FiLM）将预训练的触觉信号融入视觉特征中。实验表明，该方法在插拔任务的成功率、操作速度和力稳定性方面均有显著提升，为 VLA 模型集成触觉信息提供了一种高效且泛化性强的解决方案。
selection_source: fresh_fetch
motivation: 视觉感知难以捕捉接触密集型任务中的力学动态（如摩擦和力），而现有触觉融合方法往往计算开销过大或复杂度过高。
method: 提出 TacFiLM，利用特征线性调制（FiLM）将预训练的触觉表示作为条件，对 VLA 模型的中间视觉特征进行后训练微调融合。
result: 在插拔任务的实验中，该方法在成功率、完成时间、力稳定性和分布外泛化能力上均表现出持续的改进。
conclusion: TacFiLM 证明了轻量级触觉融合是增强 VLA 模型处理复杂接触任务能力的有效途径。
---

## 摘要
我们提出了 TacFiLM，这是一种将视触觉信号集成到视觉-语言-动作（VLA）模型中的轻量级模态融合方法。虽然 VLA 模型的最新进展引入了兼具泛化性和语义基础的机器人策略，但这些模型主要依赖于基于视觉的感知。然而，仅靠视觉无法捕捉富接触操作过程中发生的复杂交互动力学，包括接触力、表面摩擦、顺应性和剪切力。虽然最近将触觉信号集成到 VLA 模型中的尝试通常通过令牌拼接（token concatenation）或大规模预训练增加了复杂性，但行为模型沉重的计算需求使得更轻量级的融合策略成为必要。为了应对这些挑战，TacFiLM 提出了一种训练后微调方法，该方法使用特征级线性调制（FiLM）将中间视觉特征以预训练的触觉表示为条件进行调节。在插入任务上的实验结果表明，在分布内和分布外任务中，成功率、直接插入性能、完成时间和力稳定性均有一致的提升。总之，这些结果证明了我们的方法是将触觉信号集成到 VLA 模型中的有效途径，改善了富接触操作行为。

## Abstract
We propose TacFiLM, a lightweight modality-fusion approach that integrates visual-tactile signals into vision-language-action (VLA) models. While recent advances in VLA models have introduced robot policies that are both generalizable and semantically grounded, these models mainly rely on vision-based perception. Vision alone, however, cannot capture the complex interaction dynamics that occur during contact-rich manipulation, including contact forces, surface friction, compliance, and shear. While recent attempts to integrate tactile signals into VLA models often increase complexity through token concatenation or large-scale pretraining, the heavy computational demands of behavioural models necessitate more lightweight fusion strategies. To address these challenges, TacFiLM outlines a post-training finetuning approach that conditions intermediate visual features on pretrained tactile representations using feature-wise linear modulation (FiLM). Experimental results on insertion tasks demonstrate consistent improvements in success rate, direct insertion performance, completion time, and force stability across both in-distribution and out-of-distribution tasks. Together, these results support our method as an effective approach to integrating tactile signals into VLA models, improving contact-rich manipulation behaviours.

---

## 论文详细总结（自动生成）

以下是对论文《Tactile Modality Fusion for Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA）虽然在语义推理和通用化任务上表现出色，但主要依赖视觉感知。在“接触密集型”（contact-rich）任务（如精密插拔、组装）中，视觉容易受到遮挡且无法感知微米级的调整、摩擦力或接触力。
*   **核心问题**：如何高效地将触觉信号集成到大参数量的 VLA 模型中？现有的方法要么需要昂贵的大规模多模态预训练，要么采用“令牌拼接”（Token Concatenation）方式，这会增加输入序列长度，导致计算开销激增且可能稀释视觉/语言先验知识。

### 2. 论文提出的方法论：TacFiLM
*   **核心思想**：提出一种轻量级的模态融合方法 **TacFiLM**。它不增加语言模型的输入令牌数量，而是通过**特征线性调制（FiLM）**，将预训练的触觉表示作为条件，直接作用于 VLA 模型视觉编码器的中间特征层。
*   **关键技术细节**：
    *   **预训练触觉编码器**：利用现有的自监督触觉模型（如 Sparsh 或 T3）提取触觉图像的嵌入向量（Embedding）。
    *   **FiLM 融合机制**：将触觉嵌入通过一个轻量级 MLP 映射为缩放参数 $\gamma$ 和偏移参数 $\beta$。在视觉主干网络（如 DINOv2 和 SigLIP）的 ViT 块中，在多头自注意力层之前应用公式：$FiLM(F_n | \gamma, \beta) = F_n \odot (1 + \gamma) + \beta$。
    *   **高效微调**：采用 **LoRA（低秩自适应）** 技术对增强后的 VLA 模型进行后训练微调，仅更新极少数参数，从而保留原始模型的视觉-语言先验。

### 3. 实验设计
*   **实验场景**：使用 Franka Emika Panda 机器人手臂，配备 DIGIT 视觉触觉传感器。
*   **任务设置**：
    *   **分布内（ID）任务**：圆形插销（3mm/2mm 间隙）、USB 接口插拔。
    *   **分布外（OOD）任务**：方形插销、五边形插销（不同间隙）、HDMI 接口插拔。
*   **Benchmark 与对比方法**：
    *   **OpenVLA-OFT**：仅视觉的基准模型。
    *   **TactileConcat**：将触觉特征投影为额外令牌并拼接到输入序列的常见融合方法。
*   **评估指标**：成功率（%）、直接插入率（无调整一次成功）、平均最大接触力（N）、平均完成时间（s）。

### 4. 资源与算力
*   **训练细节**：所有部署的方法均训练了 **80,000 步（80k steps）**。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。但提到该方法旨在降低计算需求，且使用了 LoRA 这种显存友好的微调技术。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 **700 次** 真实机器人测试（Rollouts）。
    *   分布内评估：270 次。
    *   分布外泛化评估：225 次。
    *   消融实验（FiLM 位置、相机故障模拟）：210 次。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了不同融合策略，还对不同的预训练触觉编码器（T3, Sparsh-MAE/DINO/IJEPA）进行了分类性能评估，并模拟了光照不足和帧率下降等极端视觉环境，验证了触觉的补偿作用。

### 6. 主要结论与发现
*   **性能提升**：TacFiLM 在所有任务中均优于视觉唯一模型和拼接融合模型。在分布内任务中，成功率最高提升了 30%。
*   **安全性与效率**：TacFiLM 显著降低了操作过程中的最大接触力（仅为基准方法的约 1/3），并缩短了任务完成时间，证明了模型对接触动力学的敏感性。
*   **泛化能力**：在面对未见过的几何形状（如五边形）和更复杂的接口（HDMI）时，TacFiLM 表现出极强的鲁棒性，而基准方法在 HDMI 任务上几乎全部失败。
*   **视觉补偿**：在视觉信号受损（变暗或掉帧）时，TacFiLM 仍能保持 100% 的成功率。

### 7. 优点
*   **轻量高效**：FiLM 机制避免了增加 Transformer 的序列长度，保持了推理速度。
*   **即插即用**：能够利用现有的预训练触觉模型，无需从头开始大规模多模态预训练。
*   **力学感知**：通过实验量化证明了该方法能有效减少物理碰撞力，这对保护机器人和工件至关重要。

### 8. 不足与局限
*   **任务多样性限制**：实验主要集中在插拔类任务（Insertion），尚未在更复杂的双手协作或长程操作任务中验证。
*   **仿真缺失**：由于缺乏精确的视触觉仿真器，所有实验均在真机上完成，这限制了数据规模的进一步扩大。
*   **架构依赖**：目前主要基于 OpenVLA 架构验证，其在其他 VLA 架构（如 $\pi_0$）上的通用性仍有待探索。

（完）
