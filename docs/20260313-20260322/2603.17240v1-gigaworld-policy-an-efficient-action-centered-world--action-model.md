---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于机器人策略学习的以动作为中心的世界动作模型
tldr: 针对现有世界-动作模型（WAM）推理开销大且视觉与动作表示过度耦合的问题，本文提出GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果设计解耦动作预测与视频生成，使得推理时可选择性生成视频，显著提升了效率。在多项机器人任务中，该模型在保持高成功率的同时，推理速度比领先基准快9倍，展现了高效且鲁棒的策略学习能力。
selection_source: fresh_fetch
motivation: 现有世界-动作模型因视觉与动作推理深度耦合，导致推理延迟高且动作预测精度易受视频生成质量影响。
method: 设计了一种以动作为中心的架构，利用因果约束确保动作预测不依赖未来视频帧，并结合大规模机器人数据集进行预训练。
result: "实验显示该模型推理速度比Motus快9倍，成功率提升7%，在RoboTwin 2.0基准测试中性能提升了95%。"
conclusion: GigaWorld-Policy通过解耦视觉动力学与动作解码，成功实现了高效且物理一致的机器人策略部署。
---

## 摘要
从预训练视频生成骨干网络初始化的世界-动作模型（WAM）在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模往往使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习 2D 像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的部分：模型在当前观测的条件下预测未来动作序列，并同时在预测动作和相同观测的条件下生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束鼓励产生符合物理规律的动作。通过防止未来视频 token 影响动作 token 的因果设计，显式的未来视频生成在推理时是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化、大规模的机器人数据集来预训练一个以动作为中心的视频生成模型，随后将其作为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时将任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

### GigaWorld-Policy: 高效的以动作为中心的世界-动作模型论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在机器人领域，视觉-语言-动作（VLA）模型常面临**监督信号稀疏**的问题，即高维的观测数据对应极其稀疏的动作标签。为了解决这一问题，近年来的“世界-动作模型”（World-Action Models, WAM）尝试通过预测未来视频帧来提供稠密的监督信号。
然而，现有的 WAM 存在两大瓶颈：
*   **推理延迟高**：生成未来视频通常需要复杂的迭代采样过程，难以满足实时控制需求。
*   **误差累积与耦合**：动作预测往往过度依赖显式的视频生成质量，视频预测中的微小像素误差可能导致动作解码失败。
**GigaWorld-Policy** 的核心动机是设计一种**以动作为中心**的架构，在训练时利用视频生成作为稠密监督，但在推理时允许“跳过”视频生成，实现高效、鲁棒的实时控制。

#### 2. 方法论：核心思想与关键技术
GigaWorld-Policy 基于 5B 参数的扩散 Transformer（Diffusion Transformer, DiT）构建，其核心思想是**解耦动作预测与视频生成**。

*   **核心架构**：采用统一的序列建模，将当前观测（$T_o$）、机器人状态（$T_s$）、动作序列（$T_a$）和未来视频帧（$T_f$）打包为 Token 序列。
*   **因果掩码设计（Causal Masking）**：这是实现高效推理的关键。通过块状因果掩码确保：
    1.  动作 Token ($T_a$) 只能关注当前观测和状态，不能“偷看”未来视频。
    2.  未来视频 Token ($T_f$) 可以关注动作 Token，从而实现动作条件下的动力学预测。
*   **训练目标（Flow Matching）**：使用流匹配（Flow Matching）算法同时优化动作预测损失（$\mathcal{L}_{action}$）和视频生成辅助损失（$\mathcal{L}_{video}$）。公式表达为：
    $$\mathcal{L}_{all} = \lambda_{video} \mathcal{L}_{video} + \lambda_{action} \mathcal{L}_{action}$$
*   **推理模式**：在部署时，模型运行在“仅动作”模式，直接从当前观测解码动作序列，无需生成视频 Token，从而大幅降低计算开销。
*   **课程预训练**：
    1.  **通用视频初始化**：从大规模互联网视频模型初始化。
    2.  **具身数据预训练**：在约 10,000 小时的机器人操作和人类第一视角视频上进行预训练，注入物理先验。
    3.  **目标任务微调**：在特定机器人的轨迹数据上进行指令对齐。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 RoboTwin 2.0 基准，包含 50 多个代表性操作任务（如调整瓶子、叠碗、开笔记本等），并引入了强随机化（背景、光照、物体干扰）。
    *   **真实世界**：使用 AgileX PiPER 6自由度机械臂，设计了四个复杂任务：清理桌面（含几何约束）、扫描二维码（含工具使用）、叠碗、扫垃圾。
*   **对比基准（Baselines）**：
    *   **VLA 类**：$\pi$ 0.5、GigaBrain-0、X-VLA。
    *   **WAM 类**：Motus、Cosmos-Policy。

#### 4. 资源与算力
*   **模型规模**：5B 参数的 Diffusion Transformer。
*   **算力消耗**：
    *   **预训练**：在公共数据集上进行了 **6000 GPU 小时** 的预训练（未明确具体 GPU 型号，推测为 A100 或 H100 集群）。
    *   **推理测试**：所有推理延迟测试均在 **NVIDIA A100 GPU** 上完成。
*   **训练细节**：全局 Batch Size 为 256，使用 AdamW 优化器，学习率采用余弦退火策略（$1 \times 10^{-4}$ 到 $1 \times 10^{-6}$）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 50 多个任务，每个任务 100 个测试回合，数据量充足。
    *   真实世界实验中，每个任务进行 20 次试验，并采用分级评分制（如抓取成功 0.5，放置成功 0.5），评价较为客观。
*   **消融实验**：论文针对预训练配置、采样间隔 $\Delta$（未来帧数量）、因果掩码的作用、以及数据效率（Data Efficiency）分别做了专项实验。
*   **客观性**：通过对比不同类别的 SOTA 模型，并展示在数据稀缺情况下的性能曲线（仅需 10% 数据即可达到 VLA 基准性能），证明了该方法的优越性。

#### 6. 主要结论与发现
*   **高效性**：GigaWorld-Policy 的推理速度比领先的 WAM 模型 Motus 快 **9 倍**（单步推理仅需 0.36s）。
*   **高性能**：在真实世界中，成功率比 Motus 高出 **7%**；在 RoboTwin 2.0 仿真中，性能比 $\pi$ 0.5 提升了 **95%**。
*   **辅助监督的价值**：消融实验证明，引入未来视频预测作为辅助任务，能显著提升动作预测的准确性（成功率从 0.65 提升至 0.83），即使推理时不生成视频。
*   **数据效率**：得益于强大的视频先验，该模型在极少量演示数据下即可展现出极强的泛化能力。

#### 7. 优点
*   **架构优雅**：通过简单的因果掩码解决了 WAM 推理慢的顽疾，实现了“训练时稠密监督，推理时轻量解码”。
*   **物理一致性**：利用视频生成作为正则项，迫使模型学习符合物理规律的动作分布，减少了“捷径学习”。
*   **多阶段预训练**：成功整合了互联网视频、人类操作视频和机器人数据，构建了强大的具身先验。

#### 8. 不足与局限
*   **3D 几何理解**：模型主要基于 2D 像素动力学，对于需要极高空间精度的 3D 任务，可能仍存在感知局限。
*   **长程任务挑战**：虽然在扫码等任务中表现良好，但对于需要极长时间跨度推理的任务，2D 视频预测的辅助作用可能会随时间步增加而减弱。
*   **硬件依赖**：尽管推理速度大幅提升，但 5B 参数量对于嵌入式边缘计算设备（如机器人端侧芯片）仍有一定压力。

（完）
