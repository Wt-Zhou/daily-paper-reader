---
title: "PerlAD: Towards Enhanced Closed-loop End-to-end Autonomous Driving with Pseudo-simulation-based Reinforcement Learning"
title_zh: PerlAD：基于伪仿真强化学习的增强型闭环端到端自动驾驶
authors: "Yinfeng Gao, Qichao Zhang, Deqing Liu, Zhongpu Xia, Guang Li, Kun Ma, Guang Chen, Hangjun Ye, Long Chen, Da-Wei Ding, Dongbin Zhao"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14908v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于闭环端到端自动驾驶的强化学习
tldr: 针对端到端自动驾驶中模仿学习在闭环执行表现不佳以及强化学习训练成本高的问题，本文提出PerlAD。该方法利用离线数据构建矢量空间的伪仿真环境，通过预测世界模型模拟动态交通流，并采用分层解耦规划器（IL生成横向路径，RL优化纵向速度）。PerlAD在Bench2Drive等基准测试中取得SOTA性能，显著提升了闭环驾驶的效率与安全性。
selection_source: fresh_fetch
motivation: 旨在解决模仿学习在闭环场景下的表现不佳，以及传统强化学习在渲染仿真中存在的计算开销大和渲染鸿沟挑战。
method: 提出基于矢量空间的伪仿真强化学习框架，结合预测世界模型生成反应式轨迹，并采用横纵向解耦的混合规划策略。
result: "在Bench2Drive基准测试中驾驶得分超越前代强化学习方法10.29%，并在DOS遮挡场景测试中表现出极高的可靠性。"
conclusion: PerlAD证明了利用离线数据构建伪仿真环境进行强化学习训练是提升端到端自动驾驶闭环性能的高效且有效的途径。
---

## 摘要
基于模仿学习（IL）的端到端自动驾驶策略在闭环执行中往往面临困难，这是由于不足的开环训练目标与实际驾驶需求之间存在失配。虽然强化学习（RL）通过奖励信号直接优化驾驶目标提供了一种解决方案，但基于渲染的训练环境会引入渲染差距，且由于高昂的计算成本而效率低下。为了克服这些挑战，我们提出了一种用于闭环端到端自动驾驶的新型基于伪仿真的强化学习方法 PerlAD。基于离线数据集，PerlAD 构建了一个在向量空间中运行的伪仿真环境，实现了高效、无需渲染的试错训练。为了弥合静态数据集与动态闭环环境之间的差距，PerlAD 引入了一个预测世界模型，该模型能够根据自车的规划生成反应式智能体轨迹。此外，为了促进高效规划，PerlAD 采用了一个分层解耦规划器，结合了用于横向路径生成的模仿学习和用于纵向速度优化的强化学习。全面的实验结果表明，PerlAD 在 Bench2Drive 基准测试中达到了最先进的性能，在无需昂贵的在线交互的情况下，驾驶得分比之前的端到端强化学习方法高出 10.29%。在 DOS 基准测试上的额外评估进一步证实了其在处理安全至关重要的遮挡场景中的可靠性。

## Abstract
End-to-end autonomous driving policies based on Imitation Learning (IL) often struggle in closed-loop execution due to the misalignment between inadequate open-loop training objectives and real driving requirements. While Reinforcement Learning (RL) offers a solution by directly optimizing driving goals via reward signals, the rendering-based training environments introduce the rendering gap and are inefficient due to high computational costs. To overcome these challenges, we present a novel Pseudo-simulation-based RL method for closed-loop end-to-end autonomous driving, PerlAD. Based on offline datasets, PerlAD constructs a pseudo-simulation that operates in vector space, enabling efficient, rendering-free trial-and-error training. To bridge the gap between static datasets and dynamic closed-loop environments, PerlAD introduces a prediction world model that generates reactive agent trajectories conditioned on the ego vehicle's plan. Furthermore, to facilitate efficient planning, PerlAD utilizes a hierarchical decoupled planner that combines IL for lateral path generation and RL for longitudinal speed optimization. Comprehensive experimental results demonstrate that PerlAD achieves state-of-the-art performance on the Bench2Drive benchmark, surpassing the previous E2E RL method by 10.29% in Driving Score without requiring expensive online interactions. Additional evaluations on the DOS benchmark further confirm its reliability in handling safety-critical occlusion scenarios.

---

## 论文详细总结（自动生成）

### PerlAD：基于伪仿真强化学习的增强型闭环端到端自动驾驶论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的端到端（E2E）自动驾驶主要依赖模仿学习（IL），但在闭环执行中表现不佳。主要原因在于：
    *   **目标失配**：IL 仅最小化几何偏差，而非直接优化安全性和效率等驾驶目标。
    *   **因果混淆**：IL 容易学习到虚假的相关性而非真实的因果关系。
    *   **仿真瓶颈**：虽然强化学习（RL）能解决上述问题，但传统的基于渲染的仿真器（如游戏引擎）存在“渲染鸿沟”且计算开销巨大，导致训练效率极低。
*   **研究动机**：提出一种无需渲染、基于离线数据集构建矢量空间“伪仿真”环境的方法，通过高效的试错训练提升闭环驾驶性能。

#### 2. 方法论：核心思想与关键技术
PerlAD 的核心思想是在矢量空间中利用离线数据进行强化学习训练，其架构包含以下关键组件：
*   **矢量空间伪仿真（Pseudo-Simulation）**：
    *   **自车仿真**：利用自行车运动学模型和 PID 控制器，将规划动作转化为未来轨迹。
    *   **智能体仿真**：通过预测模型生成周围障碍物的轨迹，并进行高频插值以捕捉碰撞等关键事件。
    *   **奖励函数**：$r_{sim} = r_{col} + r_{lk} + r_{prog} + r_{dist}$。包含碰撞惩罚、车道保持、进度奖励以及隐含驾驶正确性的距离奖励（与专家轨迹的 L2 距离）。
*   **预测世界模型（PWM）**：
    *   引入**反应式预测**机制，使周围智能体的轨迹预测以自车的未来规划为条件（Conditioned on Ego Plan），从而模拟真实的闭环交互。
*   **分层解耦规划器（DeP）**：
    *   **横向规划（IL）**：使用模仿学习生成多模态平滑路径，降低连续空间搜索难度。
    *   **纵向规划（RL）**：在离散速度空间内使用强化学习（REINFORCE 算法）优化目标速度，以处理复杂的动态交互。
*   **训练策略**：采用两阶段训练。第一阶段预训练感知模块；第二阶段冻结感知，联合训练规划与预测，并引入“横纵向对齐”策略和“反应式训练仿真”课程学习。

#### 3. 实验设计
*   **数据集与基准**：
    *   **Bench2Drive (B2D)**：主要的闭环评估基准，包含 220 条测试路线。训练使用 B2D-Base 离线数据集（约 23 万帧）。
    *   **DOS (Driving in Occlusion Simulation)**：专门用于评估安全至关重要的遮挡场景（如停车遮挡、突然刹车等）。
*   **对比方法**：
    *   **IL 方法**：UniAD, VAD, SparseDrive, DriveTransformer 等。
    *   **RL 方法**：Raw2Drive（基于渲染仿真的 SOTA RL 方法）。
    *   **其他**：ReasonPlan, DiffAD 等最新端到端模型。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H20 GPU**。
*   **训练时长**：
    *   第一阶段（感知预训练）：12 个 epoch，学习率 4e-4。
    *   第二阶段（规划与预测训练）：18 个 epoch，学习率 2e-4。
*   **优化器**：使用 AdamW，权重衰减为 0.01。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 B2D 的 220 条路线上进行了完整的闭环测试，涵盖了合并、超车、紧急制动等多种能力评估。
    *   在 DOS 的 4 类典型遮挡场景下进行了安全性验证。
    *   **消融实验**：针对训练策略（RL-lon, LLA, RTS）、奖励函数组成部分、反应式场景比例等进行了详细的对比实验（在 Dev10 子集上完成）。
*   **充分性评价**：实验设计较为充分，不仅对比了 DS（驾驶得分）和 SR（成功率），还深入分析了预测模型的反应能力（Counterfactual Evaluation），证明了方法的客观性和有效性。

#### 6. 主要结论与发现
*   **性能领先**：PerlAD 在 Bench2Drive 上取得了 **57.27% 的成功率**和 **78.70 的驾驶得分**，显著超越了之前的 RL 方法 Raw2Drive（DS 提升 10.29%），且无需昂贵的在线交互。
*   **安全性增强**：在 DOS 遮挡场景中，PerlAD 的平均得分（86.83）远高于其他 IL 模型，证明 RL 目标能有效弥补 IL 在危险场景下的局限。
*   **反应式建模的重要性**：实验显示，随着训练中反应式场景比例的增加，闭环驾驶性能稳步提升，验证了 PWM 的必要性。

#### 7. 优点（亮点）
*   **高效性**：通过矢量空间伪仿真避开了高成本的图像渲染，使 RL 训练在离线数据集上变得可行且高效。
*   **解耦规划**：巧妙结合了 IL 在路径生成上的平滑性和 RL 在速度决策上的交互性，解决了端到端模型常见的控制不稳问题。
*   **反应式闭环**：PWM 能够根据自车意图调整环境反馈，弥补了离线数据“静态”的缺陷。

#### 8. 不足与局限
*   **离线数据限制**：伪仿真仍受限于离线数据的分布，无法完全模拟在线环境中的极端长尾场景或完全未见的工况。
*   **对抗性不足**：目前的 PWM 主要模拟常规反应，尚未显式建模极端的对抗性行为（Adversarial Behaviors）。
*   **奖励函数依赖**：虽然引入了 RL，但奖励函数中仍包含部分人工设计的规则和与专家轨迹的距离项，尚未完全摆脱对专家数据的依赖。

（完）
