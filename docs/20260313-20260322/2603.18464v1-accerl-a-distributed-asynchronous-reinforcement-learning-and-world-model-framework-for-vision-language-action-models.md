---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: VLA 模型的强化学习和世界模型框架
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习过程中面临的计算效率低下、同步延迟以及数据获取困难等核心挑战。
method: 提出了一种完全异步且解耦的框架，通过物理隔离核心环节并集成即插即用的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现了吞吐量的超线性扩展，并展现出极高的样本效率和训练稳定性。
conclusion: AcceRL为大规模VLA模型的高效强化学习训练提供了一个高性能、可扩展且具备世界模型增强能力的系统级解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：视觉-语言-动作（VLA）模型的分布式异步强化学习与世界模型框架总结

#### 1. 论文的核心问题与整体含义
*   **研究背景**：随着具身智能的发展，视觉-语言-动作（VLA）模型（如 OpenVLA）在复杂任务中展现了潜力。然而，利用强化学习（RL）对这些数十亿参数的大模型进行微调面临严峻挑战。
*   **核心问题**：
    1.  **同步瓶颈**：传统 RL 框架采用同步机制（收集数据-更新参数），导致高性能 GPU 在等待缓慢的物理模拟器时处于闲置状态（长尾效应）。
    2.  **数据饥渴**：物理模拟器运行速度慢，且大模型训练需要海量交互数据。
    3.  **显存压力**：VLA 模型参数量巨大，传统的并行策略难以在有限显存下实现高效推理与训练的共存。
*   **整体含义**：AcceRL 旨在通过一种完全异步、解耦的架构，并集成可训练的世界模型，解决 VLA 模型在 RL 训练中的效率与样本获取难题。

#### 2. 方法论
*   **核心思想**：实现训练（Training）、推理（Inference）与采样（Rollout）在物理上的完全隔离与异步运行。
*   **关键技术细节**：
    *   **宏观异步（Macro-Asynchrony）**：训练器与采样工人解耦。采样工人持续向分布式重放池发送轨迹，训练器独立采样并利用 ZeRO-2 优化器进行参数更新，更新后的权重通过 NCCL 广播至推理池。
    *   **微观异步（Micro-Asynchrony）**：将环境交互与模型推理分离。采用“推理即服务”（Inference-as-a-Service）模式，通过动态窗口机制实现高效的 GPU 批处理推理。
    *   **世界模型增强（World Model Integration）**：首次在异步框架中集成可训练的世界模型（基于扩散模型的 DIAMOND 架构）。智能体可以在“想象”中进行虚拟交互，绕过物理模拟器的速度限制。
    *   **算法优化**：
        *   **GIPO 算法**：采用高斯重要性采样策略优化，替代 PPO 的硬截断，缓解异步带来的策略滞后问题。
        *   **词表瘦身（Vocabulary Slimming）**：剪裁语言模型头，仅保留动作相关的 Token，降低显存占用并提升吞吐量。
        *   **Token 级优化**：在 Token 级别计算损失，避免动作块（Action Chunk）整体截断导致的梯度丢失。

#### 3. 实验设计
*   **数据集/场景**：使用了 **LIBERO** 机器人操作基准测试，涵盖 LIBERO-Spatial、LIBERO-Object、LIBERO-Goal 和 LIBERO-Long 四个子集。
*   **Benchmark**：对比了当前最先进的方法，包括：
    *   **OpenVLA-OFT**（基于正交微调的监督学习基准）。
    *   **SimpleVLA-RL**（同步 RL 训练方案）。
    *   **RLinf-VLA**（针对 VLA 优化的混合流水线框架）。
*   **评估指标**：任务成功率、系统吞吐量（SPS，每秒样本数）、硬件利用率。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   实验中明确提到了使用 **NVIDIA H200** GPU。
    *   在扩展性测试中，训练器（Trainer）扩展到了 **7 个 GPU**，采样工人（Rollout Workers）扩展到了 **64 个**。
*   **训练时长**：文中未明确给出总训练时长，但强调了在 400 次更新内即可达到性能平台，且在 10,000 步环境交互内实现了显著的性能突破。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的四个大类任务上进行了全面的成功率对比。
    *   进行了详细的系统扩展性实验（1-64 个采样工人，1-7 个训练 GPU）。
    *   **消融实验**：针对“价值重计算（Value Re-computation）”和“GIPO 算法”分别做了对比实验，验证了异步环境下这些组件对稳定性的贡献。
*   **充分性与客观性**：实验设计较为充分，涵盖了从系统吞吐量到算法收敛性的多维度评估。通过对比 SOTA 方法并使用标准基准，展现了结果的客观性。

#### 6. 主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中成功率达到 99.1%，远超监督学习基准（90.7%）。
*   **超线性扩展**：由于 ZeRO-2 优化减少了单卡显存占用并提升了计算强度，训练吞吐量随 GPU 数量增加呈现出超线性增长。
*   **样本效率飞跃**：集成世界模型后，在线样本效率提升了 **200 倍**（20,000%），智能体通过“想象”极大地加速了策略精炼。
*   **系统稳定性**：通过 GIPO 和价值重计算，成功解决了异步架构中常见的策略滞后和数据陈旧问题。

#### 7. 优点
*   **架构创新**：彻底解决了 VLA 模型训练中的 GPU 空转问题，实现了极高的硬件利用率（>94%）。
*   **即插即用**：世界模型模块化设计，可根据需要开启，极大地缓解了物理模拟器的速度瓶颈。
*   **算法鲁棒性**：针对 VLA 的 Token 特性做了专门的优化（词表瘦身、Token 级 PPO），使其更适合大规模生成式模型的 RL 训练。

#### 8. 不足与局限
*   **模型支持范围**：目前主要针对 VLA 模型的动作微调，尚未支持大规模语言模型（LLM）的后训练对齐（如复杂逻辑推理的 RLHF）。
*   **世界模型开销**：虽然减少了物理模拟时间，但训练和运行基于扩散模型的世界模型本身也需要额外的计算资源。
*   **应用限制**：实验主要在模拟环境（MuJoCo）中进行，在真实物理世界中的异步延迟处理和安全性尚未深入探讨。

（完）
