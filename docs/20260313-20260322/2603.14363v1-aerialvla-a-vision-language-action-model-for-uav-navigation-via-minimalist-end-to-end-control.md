---
title: "AerialVLA: A Vision-Language-Action Model for UAV Navigation via Minimalist End-to-End Control"
title_zh: AerialVLA：一种通过极简端到端控制实现无人机导航的视觉-语言-动作模型
authors: "Peng Xu, Zhengnan Deng, Jiayan Deng, Zonghua Gu, Shaohua Wan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14363v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于无人机导航和控制的视觉-语言-动作框架
tldr: AerialVLA是一个针对无人机视觉语言导航（VLN）的极简端到端框架。它通过双视图感知策略减少视觉冗余，并利用模糊定向提示机制摆脱对外部密集引导的依赖。该模型将原始视觉和模糊指令直接映射为连续的3自由度运动及着陆信号，在TravelUAV基准测试中达到SOTA水平，尤其在未知场景下的泛化能力显著优于现有模块化系统。
selection_source: fresh_fetch
motivation: 现有的无人机导航方法过度依赖密集指令引导或辅助检测器，导致语义鸿沟并限制了真正的自主性。
method: 提出一种端到端框架，结合双视图感知、基于机载传感器的模糊定向提示以及统一的连续控制空间。
result: 在TravelUAV基准测试中取得领先性能，且在未知环境中的成功率是现有主流基准的三倍。
conclusion: 极简且以自主性为中心的范式比复杂的模块化系统更能捕捉鲁棒的视觉运动表征。
---

## 摘要
无人机（UAV）的视觉语言导航（VLN）要求在动态 3D 环境中进行复杂的视觉解释和连续控制。现有的分层方法依赖于密集的先验指导或辅助目标检测器，从而产生了语义鸿沟并限制了真正的自主性。我们提出了 AerialVLA，这是一个极简的端到端视觉-语言-动作框架，可将原始视觉观测和模糊语言指令直接映射为连续的物理控制信号。首先，我们引入了一种精简的双视图感知策略，在减少视觉冗余的同时保留了前向导航和精确落地的关键线索，这进一步促进了未来的仿真到现实迁移。为了恢复真正的自主性，我们部署了一种仅源自机载传感器的模糊方向提示机制，完全消除了对密集先验指导的依赖。最后，我们构建了一个统一的控制空间，将连续的 3 自由度（3-DoF）运动学指令与内在降落信号相结合，使智能体无需外部目标检测器即可实现精准降落。在 TravelUAV 基准测试上的大量实验表明，AerialVLA 在已知环境中达到了最先进的性能。此外，它在未知场景中表现出卓越的泛化能力，成功率接近领先基线模型的三倍，验证了以自主为中心的极简范式比复杂的模块化系统能捕获更稳健的视觉-运动表征。

## Abstract
Vision-Language Navigation (VLN) for Unmanned Aerial Vehicles (UAVs) demands complex visual interpretation and continuous control in dynamic 3D environments. Existing hierarchical approaches rely on dense oracle guidance or auxiliary object detectors, creating semantic gaps and limiting genuine autonomy. We propose AerialVLA, a minimalist end-to-end Vision-Language-Action framework mapping raw visual observations and fuzzy linguistic instructions directly to continuous physical control signals. First, we introduce a streamlined dual-view perception strategy that reduces visual redundancy while preserving essential cues for forward navigation and precise grounding, which additionally facilitates future simulation-to-reality transfer. To reclaim genuine autonomy, we deploy a fuzzy directional prompting mechanism derived solely from onboard sensors, completely eliminating the dependency on dense oracle guidance. Ultimately, we formulate a unified control space that integrates continuous 3-Degree-of-Freedom (3-DoF) kinematic commands with an intrinsic landing signal, freeing the agent from external object detectors for precision landing. Extensive experiments on the TravelUAV benchmark demonstrate that AerialVLA achieves state-of-the-art performance in seen environments. Furthermore, it exhibits superior generalization in unseen scenarios by achieving nearly three times the success rate of leading baselines, validating that a minimalist, autonomy-centric paradigm captures more robust visual-motor representations than complex modular systems.

---

## 论文详细总结（自动生成）

以下是对论文《AerialVLA: A Vision-Language-Action Model for UAV Navigation via Minimalist End-to-End Control》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
无人机（UAV）的视觉语言导航（VLN）要求智能体在动态3D环境中理解复杂指令并进行连续物理控制。
*   **研究动机**：现有的无人机导航方法存在“双拐杖”依赖问题：一是依赖**密集先验指导（Oracle Guidance）**，即输入中包含精确的每一步指令（如“右转”），使智能体沦为被动跟随者；二是依赖**外部目标检测器**（如Grounding DINO）来触发降落，导致感知与控制链路断裂。
*   **核心目标**：提出一种极简的端到端视觉-语言-动作（VLA）框架，摆脱对外部检测器和密集指令的依赖，实现真正的自主感知、推理与连续控制。

### 2. 方法论：核心思想与关键技术
AerialVLA 基于 OpenVLA-7B 架构，将视觉观测和模糊指令直接映射为物理控制信号。
*   **极简双视图感知（Minimalist Dual-View Perception）**：
    *   将无人机的前视（Front）和下视（Down）图像进行垂直拼接（Vertical Mosaic），调整为 224x224 分辨率。
    *   使用 SigLIP 和 DINOv2 混合视觉编码器提取语义和空间特征，通过 MLP 投影器接入大语言模型（LLM）空间。
*   **模糊定向提示（Fuzzy Directional Prompting）**：
    *   完全弃用步进式精确指令，仅利用机载传感器（IMU/GPS）获取目标相对方位，并将其映射为模糊语义（如“在你右侧”、“正前方”）。
    *   引入**几何一致性过滤策略**：利用侧向深度图剔除专家数据中因人类反应延迟导致的矛盾样本，确保模型学习到正确的避障逻辑。
*   **数值动作标记化（Numerical Action Tokenization）**：
    *   定义 3 自由度（3-DoF）动作空间：前向位移 $\Delta x$、垂直位移 $\Delta z$ 和偏航角变化 $\Delta \psi$。
    *   将连续动作离散化为 99 个 bin，直接映射到 LLM 词表中的**标准数字 Token**，利用预训练模型已有的数值推理能力。
*   **统一控制与内在降落**：
    *   将导航与降落统一在单一策略中。当模型预测出 `LAND` 标记或接近零的位移时触发降落，无需外部检测器。

### 3. 实验设计
*   **数据集与场景**：使用 **TravelUAV** 基准测试（UAV-Need-Help 任务），包含约 1.2 万条人类驾驶轨迹。
*   **测试集划分**：
    *   **Seen**：已知环境。
    *   **Unseen Object**：未知目标类别。
    *   **Unseen Map**：完全陌生的地图几何结构。
*   **对比方法**：
    *   启发式基线：随机动作、固定动作。
    *   混合 VLM 方法：CMA、TravelUAV-DA（原 SOTA）。
    *   通用基础模型：NavFoM、LongFly（最新的长程导航模型）。
*   **评价指标**：导航误差（NE）、成功率（SR）、Oracle 成功率（OSR）、路径长度加权成功率（SPL）。

### 4. 资源与算力
*   **硬件环境**：使用 **4 张 NVIDIA RTX 4090 (24GB)** GPU。
*   **训练细节**：采用 LoRA 微调（秩 $r=64$），可训练参数占比约 2.98%。
*   **时长与显存**：训练 5 个 epoch 耗时约 **35 小时**。推理时占用约 17GB 显存，总延迟为 0.38s（优于 TravelUAV 的 0.63s）。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同维度的测试集上进行了详尽的定量评估，涵盖了从简单到困难的多种飞行任务。
*   **消融实验**：针对数据清洗（过滤策略）、感知视图数量（2 视图 vs 5 视图）、动作标记化方式（标准数字 vs 自定义 Token）进行了对比实验。
*   **充分性评价**：实验设计客观、公平，通过对比不同复杂度的任务（Easy/Hard）和泛化场景，充分验证了端到端模型在处理未知环境时的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在已知环境中，AerialVLA 的成功率（SR）达到 47.96%，显著超过 LongFly（36.39%）。
*   **卓越的泛化性**：在**未知地图（Unseen Map）**中，AerialVLA 的成功率（37.58%）是现有最强基线（11.27%）的 **3 倍以上**。
*   **极简胜过复杂**：实验证明，减少冗余的视觉输入（如侧视/后视）反而能减少过拟合，提升模型对核心导航线索的关注。
*   **内在降落有效性**：统一的动作空间使模型能自主判断降落时机，解决了模块化系统中感知与动作不匹配的问题。

### 7. 优点
*   **端到端简洁性**：消除了对外部目标检测器和密集指令的依赖，系统架构更轻量、延迟更低。
*   **泛化能力强**：利用 LLM 的开放词汇表能力，能识别并导航至训练集中未见过的目标。
*   **数值推理利用**：巧妙地使用标准数字 Token 替代自定义动作 Token，加速了收敛并提高了控制精度。

### 8. 不足与局限
*   **缺乏全局记忆**：由于采用纯反应式（Reactive）策略，在高度重复的城市结构中可能面临全局回溯困难。
*   **极端场景保守**：在目标被茂密植被严重遮挡的极端 OOD（分布外）场景下，模型倾向于保守悬停，而非执行复杂的多阶段探索。
*   **行为克隆限制**：性能受限于专家演示数据的质量，未来可能需要引入强化学习进行进一步优化。

（完）
