---
title: "AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation"
title_zh: AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型，用于移动操作
authors: "Yusuke Takagi, Motonari Kambara, Daichi Yashima, Koki Seno, Kento Tokura, Komei Sugiura"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15046v1"
tags: ["query:ad-ei"]
score: 10.0
evidence: 用于语言引导的机器人移动操作的轻量级VLA
tldr: "针对语言引导的机器人操作任务，为解决标准Transformer架构在资源受限环境下计算成本高的问题，本文提出了AnoleVLA。该模型采用深层状态空间模型（SSM）处理多模态序列，实现了轻量化和高效推理。实验表明，AnoleVLA在真实场景中的成功率比大型VLA高出21%，且推理速度快约三倍，显著提升了移动操作的效率。"
selection_source: fresh_fetch
motivation: 传统的基于Transformer的视觉-语言-动作模型计算开销巨大，难以在资源受限的移动机器人平台上实时部署。
method: 提出AnoleVLA模型，利用深层状态空间模型（SSM）替代Transformer来高效处理视觉、文本和动作序列。
result: 在真实世界评估中，AnoleVLA的成功率比代表性大型VLA高出21个百分点，且推理速度提升了约三倍。
conclusion: AnoleVLA证明了通过状态空间模型构建轻量化VLA的可行性，为高效、实时的机器人多模态任务提供了新方案。
---

## 摘要
在本研究中，我们解决了语言引导的机器人操作问题，即机器人需要根据视觉观察和自然语言指令操作各种物体。这一任务对于在人类环境中运行的服务机器人至关重要，且要求具备安全性、效率和任务级的通用性。尽管视觉-语言-动作模型（VLA）在该任务中表现出强大的性能，但由于标准 Transformer 骨干网络的计算成本，它们在资源受限环境中的部署仍然具有挑战性。为了克服这一限制，我们提出了 AnoleVLA，这是一种轻量级 VLA，它使用深度状态空间模型来高效处理多模态序列。该模型利用其轻量且快速的序列状态建模来处理视觉和文本输入，从而使机器人能够高效地生成轨迹。我们在仿真和物理实验中评估了所提方法。值得注意的是，在真实世界的评估中，AnoleVLA 在任务成功率上比代表性的大规模 VLA 高出 21 个百分点，同时推理速度快了约三倍。

## Abstract
In this study, we address the problem of language-guided robotic manipulation, where a robot is required to manipulate a wide range of objects based on visual observations and natural language instructions. This task is essential for service robots that operate in human environments, and requires safety, efficiency, and task-level generality. Although Vision-Language-Action models (VLAs) have demonstrated strong performance for this task, their deployment in resource-constrained environments remains challenging because of the computational cost of standard transformer backbones. To overcome this limitation, we propose AnoleVLA, a lightweight VLA that uses a deep state space model to process multimodal sequences efficiently. The model leverages its lightweight and fast sequential state modeling to process visual and textual inputs, which allows the robot to generate trajectories efficiently. We evaluated the proposed method in both simulation and physical experiments. Notably, in real-world evaluations, AnoleVLA outperformed a representative large-scale VLA by 21 points for the task success rate while achieving an inference speed approximately three times faster.

---

## 论文详细总结（自动生成）

### AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文针对**语言引导的机器人操作**任务，旨在解决现有视觉-语言-动作模型（VLA）在资源受限的移动机器人平台上部署困难的问题。
*   **研究动机**：传统的 VLA 模型（如 OpenVLA、RT-2）通常基于大规模 Transformer 架构，其自注意力机制的计算复杂度随序列长度呈平方级增长（$O(L^2)$），导致推理延迟高、显存占用大。
*   **核心目标**：开发一种轻量级、高效率且能生成平滑连续动作轨迹的 VLA 模型，使其能够在计算资源有限的移动机器人上实现实时、通用的物体操作。

#### 2. 论文提出的方法论
核心思想是利用**深度状态空间模型（Deep SSM，具体为 Mamba）**替代传统的 Transformer 骨干网络，并结合两阶段训练策略。
*   **核心架构**：
    *   **输入嵌入模块**：将机器人状态（$s_t$）、状态增量（$\Delta s_t$）、视觉观察（采用 SigLIP 2 编码）和自然语言指令（Tokenize 后）投影到统一的隐空间。
    *   **Mamba 骨干网络**：采用具有线性计算复杂度（$O(L)$）的 Mamba 块处理多模态序列。其选择性循环机制能够高效地在长序列中传递和过滤任务相关信息。
    *   **动作头**：不同于将动作离散化为 Token 的做法，AnoleVLA 直接从骨干网络的最终隐藏状态通过线性投影生成连续的动作块（Action Chunk）。
*   **两阶段训练策略**：
    *   **第一阶段**：使用 **速度损失（Velocity Loss, $L_{vel}$）**，即预测动作与真实轨迹之间的 L1 损失，初步学习轨迹重建。
    *   **第二阶段**：引入 **加速度损失（Acceleration Loss, $L_{acc}$）**，通过监督动作序列的时间差分来优化轨迹的平滑度和一致性。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：使用 Meta-World 基准测试，涵盖 50 种不同的物体操作任务（如开门、推块等）。
    *   **物理实验**：使用丰田 HSR 移动机器人，在模拟家庭环境（WRS2020RS 标准）中执行“移动、抓取、打开、关闭、按压”五类任务。
*   **对比基准（Baselines）**：
    *   **大规模模型**：$\pi_{0.5}$ (3.0B 参数)。
    *   **同规模轻量级模型**：SmolVLA (0.45B)、VLA-Adapter (0.5B)、TinyVLA (0.42B)。

#### 4. 资源与算力
*   **硬件配置**：训练使用单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU 和 Intel Core i9-14900F 处理器。
*   **训练时长**：总训练时长约为 **20 小时**（400k 步，两个阶段各 200k 步）。
*   **推理性能**：推理速度为 **216 ms/chunk**，显存占用低，适合实时部署。

#### 5. 实验数量与充分性
*   **数据规模**：
    *   仿真：2500 个情节（2000 训练，500 验证）。
    *   物理：250 个真实演示情节（每类任务 50 个），通过领导者-跟随者系统采集。
*   **实验充分性**：
    *   涵盖了从简单到极难（Easy to Very Hard）的四个仿真等级。
    *   物理实验中每种任务进行了 20 次测试，总计 100 次真实评估。
    *   进行了**消融实验**，验证了加速度损失对成功率的提升（提升约 4.73%）。
    *   整体实验设计较为客观，对比了不同参数规模和架构的模型，评估指标包括成功率和推理速度。

#### 6. 论文的主要结论与发现
*   **性能卓越**：在 Meta-World 仿真中，AnoleVLA 的平均成功率（67.85%）显著超过了所有对比模型（包括 3B 规模的 $\pi_{0.5}$）。
*   **真实场景优势**：在物理实验中，AnoleVLA 的成功率比 $\pi_{0.5}$ 高出 **21 个百分点**，且推理速度快了约 **3 倍**。
*   **平滑性关键**：引入加速度损失能有效减少机器人动作的抖动，提高接触密集型任务（如开抽屉）的成功率。
*   **样本效率**：在仅有少量真实数据（每任务 50 个情节）的情况下，AnoleVLA 表现出比大规模预训练模型更强的微调适应能力。

#### 7. 优点（亮点）
*   **架构创新**：率先将 Mamba 这种线性复杂度的 SSM 应用于 VLA 领域，解决了 Transformer 的扩展性瓶颈。
*   **高效部署**：模型参数量仅约 0.47B，在消费级 GPU 上即可完成训练和高速推理。
*   **控制质量**：通过两阶段训练和连续动作生成，克服了动作离散化带来的精度损失，实现了更平滑的控制。

#### 8. 不足与局限
*   **空间推理能力有限**：错误分析显示，“位置识别错误”是最主要的失败原因，表明模型在从图像中提取精确空间坐标方面仍有欠缺。
*   **物理实验规模较小**：虽然在真实机器人上进行了验证，但 250 个情节的数据量相对较小，且环境相对固定。
*   **泛化风险**：论文未深入探讨在完全未见过的环境或物体上的零样本（Zero-shot）泛化能力。
*   **应用限制**：目前主要集中在短时域的动作块生成，对于极长时序的复杂逻辑推理任务尚待验证。

（完）
